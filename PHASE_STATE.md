# Phase State (operational)

> Read at the start of every session. Maintained by SYNTHESIZER. KEEP THIS FILE TO ROUGHLY ONE SCREEN: the current frontier, live next steps, and the verified state. The full dated-update history 2026-05-30 through 2026-07-11, plus paragraphs rotated 2026-08-09 (#163/#164/#166-#170 and the older last-verified blocks), 2026-08-20 (#174-#181 and the 2026-08-19 last-verified blocks), 2026-08-21 (#182-#186, the B2c arc's early rungs), and 2026-08-30 (#187-#211, the SP-bank tail through the funding-boundary frame's sessions, plus the #171-#173 arc blocks), is frozen verbatim in [PHASE_STATE_ARCHIVE.md](PHASE_STATE_ARCHIVE.md); the finding-level record is [experiments/LEARNINGS.md](experiments/LEARNINGS.md) (through #213).

## Current state (2026-08-30)

**THE FUNDING-BOUNDARY FRAME IS CLOSED: the closing audit lands CLOSE-AS-ATTACK-LANDED with an independent adversary lens concurring (2026-08-30, latest; LEARNINGS #213, [`funding_boundary_audit_2.md`](docs/03_research/funding_boundary_audit_2.md) + [`_funding_boundary_audit_2_adversary.md`](docs/03_research/_funding_boundary_audit_2_adversary.md)).** Trigger: completion, not an exit (the registered order F3-F1-F2a-F2b is done; any further frame session would be exit 3a's subject). The ledger verifies session by session: every session executed its registered spec, the tripwire ran 1/3, 2/3, reset, 1/3, broken 0/3 and never fired, and the five exits close clean (1 never fired; 2 DISCHARGED at its deadline by the landed theorem-shape; 3a/3b never fired; 4/5 never fired). The #201 decay markers are ABSENT at close, and the registration's own ceiling-as-output trap was avoided as specified (the visibility-floor law is about the class holding the external record, verified in-class by the delta-check's formal re-run). Yield inventory (graded, with the lens's fixes): the visibility-floor law at M2's narrowed scope; the delta-checked certificate class $\mathcal{C}$ (plus the definition-before-attack METHOD yield); the typed funding wall F1a AS SCOPED; the corridor's fifth hardening; the standing instruments (e1af, e1ag, `F2bSkeleton.lean`, the seven-artifact adversary/audit chain); FOUR fetch-confabulation catches now standing discipline. NOT landed, stated plainly: M4-the-lemma (batch bound: VERIFIER lane), F1b's pricing (posed only), W1's computation (open, unassigned: door (f)), the Mueller [SINGLE-SOURCE] thread, the AF kernel tier, the two out-of-class certificate families. **Disposition: the successor is chosen by a DELIBERATION scheduled after the two due lanes (VERIFIER ~09-02, WATCH ~09-08), with the door list at audit Q5 (a)-(h)** (headline door: the exact-identity register the scope sentence names, where the priced compass points). A publication-candidate flag is raised for the frame's package (the next registry slot, P13, IF it survives the gate; gate not yet run). **Frameless-window guard IN FORCE: standing lanes only until the successor registration; no progress grading in the window.** The program's open kernel (M4/R1) is unchanged.

**Frame session F2b executed end to end at the exit-2 deadline: the visibility-floor law lands as the frame's theorem-shape, the grade is the frame's first non-UNMOVED, and the registered session order is COMPLETE (2026-08-28; LEARNINGS #212).** The critical path ran as prescribed: the re-posed definition's bounded delta-check FIRST ([`_f2a_delta_check.md`](docs/03_research/_f2a_delta_check.md), PASS_WITH_EDITS: AF and GLSS I/II land in-class by the formal clause-by-clause re-run; the one real hole was the checker's own prior wording, the link fine-class smuggler, closed by pinning the link class at $o(TL)$ absolute sup-normalized, edits 1-7 applied), then the theorem ([`f2b_visibility_floor.md`](docs/03_research/f2b_visibility_floor.md), post-fix authoritative): granted correlation data buys exactly two things at two prices, and they decouple: the certifiable defect floor is $\Theta(\varepsilon_G TL)$ (sharp two-sided over STANDARD batteries; GLSS recovered at the $\varepsilon$-endpoint) and the location-resolution frontier is $\delta^{\ast} = (1+o(1))\log(TL)/\Theta_G$ (two-sided, thin band; at proven-core support $\delta^{\ast} > 1/2$: AF's "budgets bound HOW MANY, never WHERE" DERIVED as the support-1 endpoint), with no conjectural hypotheses and named classical inputs only. The scope sentence rides verbatim; the no-go stays unminted; the compass now carries a RATE. Instruments: [`e1ag_visibility_curve.py`](experiments/spectral/e1ag_visibility_curve.py) 16/16 full and 15/15 quick with all four pre-registrations SURVIVED (envelope exact $7{\times}10^{-15}$; naive-site blocks 106x/6393x and both rescues measured; frontier $\delta^{\ast}\Theta$ spread 1.000; D-H landmark cell + Beurling refusal), and [`lean/ZetaRH/F2bSkeleton.lean`](lean/ZetaRH/F2bSkeleton.lean) typechecking clean with V-F2b-1..5 PROVED sorry-free and exactly one sorry (V-F2b-6). Same-session ADVERSARY ([`_f2b_adversary.md`](docs/03_research/_f2b_adversary.md)): PASS_WITH_FIXES, ten blocking fixes ALL applied in-session and tagged; derivability: M2 (the two-sided resolution frontier) NEW-COORDINATE AS SCOPED and LANDS, M1 enrichment, M3 corroboration, M4 candidate-not-landed (batch-bound proof next-batch); reproduction bit-exact. **Grade non-UNMOVED (adversary; M2), exit 2 DISCHARGED (outcome (a) as scoped, not the #211(iv)(b) relabeling), tripwire chain broken 0/3, frame sessions 4/6.** The program's open kernel (M4/R1) is unchanged; what changed is that the information wall now carries a priced two-axis exchange law with a machine-checked finite skeleton. Mueller rider: the 1983 ES equivalence is RH-conditional [SINGLE-SOURCE], the pool's channels stay separate ([`mueller_1983_es_equivalence.md`](docs/03_research/reading_notes/mueller_1983_es_equivalence.md)).

**Frontier.** The open kernel is unchanged: M4 (the arithmetic Hodge-index / Rosati polarization; in CCM form the Section-7 uniform determinant-class limit) with R1 (the missing Frobenius / derivation / diagonal operator) the co-equal counting-side target. The S4/R1 coordinate is MEASURED (#162): both literature slots verified empty, the factor-2 family derived to its proven ceilings, the absence localized to the Q-linear independence of $\{\log p\}$. The corridor closure (#163/#164) and the compactness arc (#170/#171) are archived; their live residue is in the triggers below. NEW since the frame (#212/#213): the information wall carries the priced two-axis exchange law (slack buys counting at $\Theta(\varepsilon TL)$; support buys resolution at $1/\Delta$; the purchases decouple), and the missing-ingredient compass (exact-class, uniform-in-cutoff contact with $\zeta$ itself) is where every in-class route stops short, now with the rate attached.

**Next steps (updated 2026-08-30; the frameless window: standing lanes only until the successor registration):**

1. **VERIFIER batch (~2026-09-02, due next).** The queued nuggets (#199 certificate skeleton; #200's Gaussian-tail chain, RS partial summation, interpolation bound; #196 holomorphy-tracelessness; e2ax max-at-zero; e2aw seed facts; the #143 upstream PRs) plus the F2b enrichment (V-F2b-6 the second-difference kernel; the L4 batch-selection bound; the V-F2a-1..4 configuration-side $\beta$-data extractions; `MergeFloorTarget` instances) and the `arcoshReal`/`RHLinalg` upstream candidates.
2. **WATCH sweep (~2026-09-08).** Inherits the Hilberdink Beurling-variance S-item with its named screen, the F3 survey's Section 10 split screens, the ACS trigger, and the Suzuki-crowding pressure re-check (P12's scoop clock); any movement on the class question or the in-class ceiling feeds the deliberation, not a dead exit.
3. **The successor-frame deliberation (after 1-2; the next GRADED session).** Inputs: the door list at [`funding_boundary_audit_2.md`](docs/03_research/funding_boundary_audit_2.md) Q5 (a)-(h), headlined by the exact-identity register (the EF as identity / Euler product / Hadamard factorization: the CCM Section-7 determinant-class limit, #148's clause, M4 from the counting side); run under the #206 pattern (its own same-session adversary; registration under the repaired meta-rule).
4. **Shipping lanes.** P12: the arXiv upload remains with Owen (the portfolio's loudest pending human action) + posting-day courtesy emails. P2 #41132: blocked on the dependency PR; P10 gated behind it. The P13 candidate (the frame's package) awaits its PUBLICATIONS.md gate (novelty pass, the M4 batch bound proven or scoped out, external-format rewrite, Owen's call). P9 MathSciNet pass and the Chebyshev $\psi(x) \ge cx$ upstream candidate stay queued.
5. **Open slivers (homes unchanged).** Cross-machine replication of e1ad/e1v; W1's computation on the actual AF window (door (f)); T2's Section 9 missing-theorem statement (#173 residue); the C1 metric-ingredient question (what supplies $\log p$ as SIZE past the e2al leak); e1ad's $r_{\rm mid}$ meter vs an explicit-formula prediction; the two-sided finite-$M$ Theorem V2 and the e1z VERIFIER targets.

## Last verified state (2026-08-30)

- **Tests:** no code changes this session (2026-08-30, audit only). Standing: e1ag **16/16 full and 15/15 quick** (2026-08-28; all four pre-registrations SURVIVED; npz tracked; adversary reproduction bit-exact at seed 212); full battery **15/15 modules** (2026-08-28); `F2bSkeleton.lean` typechecks standalone (zero errors, exactly one sorry). Prior states carried: e1ae 6/7-as-finding, e1ad 21/21, e1v 24/24 quick, e2bg 10/10, e2bf 8/12-as-finding, Lean library 3770 GREEN (#202).
- **New artifacts, #213 round (the closing audit), UNCOMMITTED pending per-action authorization:** `docs/03_research/funding_boundary_audit_2.md` (lens fixes applied in place); `docs/03_research/_funding_boundary_audit_2_adversary.md`; LEARNINGS #213; this file (with the 2026-08-30 rotation executed into `PHASE_STATE_ARCHIVE.md`); the TODO strikes.
- **Prior rounds:** #212 (F2b) committed and pushed as `e0e0a74` + drift fix `22719e9`; #211 (F2a) at `af19efe`; #210 at `b282be0`; F1+audit at `0f2c2c7`; F3 at `628acae`.
- **Environment:** unchanged from 2026-08-20 (`python-flint` 0.9.0, `cvxpy` 1.7.5, `mpmath` 1.4.1, `scipy` for nnls; `pip check` clean).
- **Lean:** library **FULL BUILD GREEN 3770 jobs** (2026-08-25, #202); registry 95 targets, 44 OPEN / 0 DRAFTED / 51 DISCHARGED; `F2bSkeleton.lean` deliberately unimported (V-F2b-1..5 sorry-free; one sorry; `MergeFloorTarget` a named Prop). The ~09-02 batch is next-steps item 1.
- **Publications:** P1 merged 2026-07-07; P2 #41132 in the review queue (blocked on the dependency PR); **P12 LaTeX/PDF BUILT** (remaining with Owen: the arXiv upload, the optional geometry tightening, the courtesy emails); **P13 candidate flag raised by the closing audit (gate NOT yet run)**. Registry: [PUBLICATIONS.md](PUBLICATIONS.md).
- **Git:** head at `22719e9` (the #212 drift fix, on `e0e0a74`); the #213 round is UNCOMMITTED pending Owen's per-action authorization.
- **Evidence integrity:** unchanged: the repo-level `scratchpad/` is ABSENT on this machine, so tracked citations into it are dangling here (documented at the P10/P1 citation sites; evidence rule in CLAUDE.md Conventions). The P12 evidence lives in tracked `publications/weil_ground_state/_evidence/`; the frame's evidence is all in tracked `docs/03_research/` and `experiments/spectral/` per the rule.

## Falsifiability triggers (live)

- The corridor exit rule (#163) FIRED same day via falsifier 2 (#164), conditional on the two named residuals; the reopen conditions are exactly those residuals resolving in the machinery's favor (a meromorphic majorant theory that poses on $L_a$, or a KNS-class identity at log-growth nodes). The majorant residual is now QUINTUPLE-hardened: next-step 2(a) was EXECUTED as frame session F3 (2026-08-26, #207, adversary-replicated NO-IN-PRINT), so the positive branch is closed by sweep and the indefinite branch at source (e1w); any future reopen must pass the survey dossier's Section 10 split screen ((ii-b) pole-COUPLING data in the criterion or value formula = reopen; (ii-a)-only = assign a deep read), which the WATCH sweeps inherit.
- Standing meta-rule (REPAIRED 2026-08-25, LEARNINGS #201): three consecutive UNMOVED verdicts inside one frame trigger a frame audit of that frame, not another probe inside it. A frame may NOT pre-exempt itself by declaring its verdicts in advance ("UNMOVED by design" does not disarm the rule): expected-UNMOVED work registers at creation as an INSTRUMENT frame with a finite completion list and an expiry (list drained or a session budget), and is audited at expiry instead of per-verdict. Audits reset the counter. Companion rule from the same audit: every minted-coordinate claim must pass the DERIVABILITY CHECK (if its content can be worded from the #148/#160/#194 clause using the entry's own cross-refs, it is a re-measurement of the wall, not a new coordinate, and must be labeled so).
- The SP-backlog frame audit EXECUTED 2026-08-25 (#201, [`sp_backlog_frame_audit.md`](docs/03_research/sp_backlog_frame_audit.md)): verdict INSTRUMENT-COMPLETE-AND-CLOSE; counter reset; live residue = the audit's five falsifiers (Section 5), of which (2) resolved lands-where-priced (#204) and (4) FIRED on the atom clause (#205).
- **The funding-boundary frame CLOSED 2026-08-30 by its closing audit (LEARNINGS #213, [`funding_boundary_audit_2.md`](docs/03_research/funding_boundary_audit_2.md), independent lens CONCUR_WITH_FIXES applied): CLOSE-AS-ATTACK-LANDED.** Final ledger: sessions 4/6, tripwire never fired (chain broken at F2b's non-UNMOVED), exits 1/3a/3b/4/5 never fired and exit 2 DISCHARGED. The frame's tripwire and exits are DEAD with it; custody of the parked G4/G15 specs transfers to the successor registration. **Frameless-window guard: until the successor deliberation registers a frame, only the standing lanes run and no session grades progress or mints coordinates (the #201 derivability rule stands repo-wide regardless).** The deliberation (next-steps item 3) is the next graded session; its door list is the audit's Q5 (a)-(h).

## How to update this file

Amend the Current state block in place (short paragraphs, not an append-only log); refresh Last verified state every session (tests, Lean, publications, git, last LEARNINGS #); rotate superseded paragraphs into the archive. The LEARNINGS entry is the canonical finding record; this file is navigation.
