# ADVERSARY report: e1af funding-wall typing probe (frame session F1, same-session grading)

> **Role**: ADVERSARY, mandated same-session pass on the F1 build ([`e1af_funding_wall.md`](e1af_funding_wall.md), [`e1af_funding_wall.py`](e1af_funding_wall.py)) and grader of the whole F1 session under the [`successor_frame_deliberation.md`](../../docs/03_research/successor_frame_deliberation.md) Section 6 verdict wiring. Date 2026-08-26.
> **Sources verified first-hand**: the build dossier and probe source; the deliberation Sections 4-7; [`af_funding_inputs_verification.md`](../../docs/03_research/reading_notes/af_funding_inputs_verification.md) (post-correction state); [`proportion_support_landscape.md`](../../docs/03_research/reading_notes/proportion_support_landscape.md); LEARNINGS #201-#207; the tracked npz; two quick runs, two full `--deep` runs on this host.
> **Overall verdict: PASS_WITH_FIXES.** The probe reproduces bit-exactly, the meters are genuinely matched, the pre-registration is real at rule level, and the PROPOSED typing verdict survives AS SCOPED. Ten required fixes below, none of which changes a gate outcome. Session grade: **UNMOVED** (tripwire 2/3), with one wiring consequence flagged for ORCHESTRATOR adjudication (exit 3's letter is met).

---

## A1. Reproduction: MISSED (the probe survives; reproduction is exact)

- `--quick`: **22/22**, 6 s, run twice: stdout byte-identical. Calibration facts match the prototype disclosure exactly (TRUE parity 2.000 at $X = 10^6$; snap-mid ratio 0.888 vs tri model 0.886; ramp $t = 10^{-4}$ ratio 0.021).
- full `--deep`: **23/23**, 173 s, run twice: stdout identical modulo elapsed; the saved npz arrays are **bit-identical between the two runs and bit-identical to the tracked original** (provenance differs only in `elapsed_s`; original restored after testing).
- Every headline number checked against the npz and the live gate lines: TRUE parity $1.99999975$ (printed 2.000), $R^2 = 0.99999$, $R^2_{\rm even} = 0.99996$, mean $0.9713$, $\mathrm{rms}_e = 0.0022$; Beurling parities $-0.0005/+0.0026/-0.0007$ (max $0.0026$); Cramer max $0.0012$; SNAP $0.0416 / 0.2406 / 2.0000$ with $|P - P_{\rm TRUE}| = 2.2\times10^{-16}$ and max $|b - b_{\rm TRUE}| = 7.7\times10^{-12}$ at $D = 10^8$; C3 match $6.7\times10^{-13}$; ramp $+0.4540/+0.0068/+0.0005$; model values tri $0.114$ / uni $0.122$ / ramp $0.223$; band $[0.057, 0.207]$, measured ratio $0.120$; deep TRUE $2.000$ / $\mathrm{rms}_e\ 0.0007$, deep Beurling $+0.0002$; trend $0.0053 \to 0.0022 \to 0.0007$; $\pi(10^7) = 664579$; $C_2 = 0.6601618197$; $\mathfrak S(6)/\mathfrak S(2) = 2.000$; $\overline{\mathfrak S} = 0.9723$; prereg P1 FIRED / P2 REFUTED / P3 FIRED / P4 FIRED / P5 FIRED. All match the dossier.

## A2. The derivation (Task 1 bookkeeping): LANDED on attribution; the arithmetic itself verified line by line

**Verified correct.** $H(\delta) \asymp X/T = T^\delta = X^{1-1/\Delta}$ (checked); $w_\delta(h) = \log(H/h) + O(1)$ from $\int_{hT}^X dx/x$ (checked); the kernel bound $|K_T(u)| \le \min(1, 2/(T|u|))$ (checked); MV pricing $O(X\log X)$ vs diagonal $T\log^2 X$, domination exactly while $X \lesssim T$, consistent with the funding-inputs note's at-source balance ($\mathcal O_1 \ll L^2 X$ vs main terms $TL^3$) and with BGSTB's closed endpoint $\alpha = 1$; the second-order Abel computation $\sum_{h\le H}\mathfrak S(h)\log(H/h) = H - \tfrac14\log^2 H + O(\log H)$ vs the flat model's $H - \tfrac12\log H + O(1)$ (both re-derived here, correct); the $\mathfrak S$ implementation gated at the standard local factor $(p-1)/(p-2)$ (checked at $\mathfrak S(6)/\mathfrak S(2) = 2$, $C_6 = 4C_2$); the family/window/support arithmetic ($H = 100$, $X = 10^7$, $T = 10^5$, band $(1, 1.4]$) checked. The off-diagonal-as-$\{B_x(h)\}$-functional reduction matches the funding-inputs note's Section 6 surface map (frame kernels concentrated at $|\log(n/m)| \lesssim 1/T$, $X > T$, integer shifts weighted by the singular-series class), with the kernel-generic scope honestly stated.

**The catch (the task's own criterion: new-bookkeeping claims that are in print are LANDED).** The "Known vs this probe's bookkeeping" paragraph claims the $H(\delta) = X/T$ scaling and $\log(H/h)$ profile as THIS PROBE'S. Against the landscape note, the substance is occupied: Goldston first-party (landscape Section 5 item 1) states the off-diagonal-consumes-HL-pairs fact; row E2 records Bolanz 1987 as the in-print HL-to-$F$ bookkeeping on $[1, 2-\epsilon]$ (the same weighted-$B_X(h)$ reduction, 131 pages of it); gap 2 records Chan carrying "all second order terms" through Montgomery's $F$ heuristic, which is exactly the species of the dossier's second-order register computation; and the shift range $h \lesssim X/T$ is already read off at source in the sibling survey note's surface map. The KNOWN list contains none of Montgomery-1973-$\alpha\ge1$/Bolanz/Chan. What is genuinely unoccupied per the landscape (gap 3) is the standalone marginal-$\delta$ VALUE statement, and per gap 2 the use of the second-order register in any proportion budget: the dossier is entitled to those, not to the scaling/profile. Fix 7.

## A3. The typing inference (the 2x2 four-corner argument): LANDED on (iii); (i) and (v) need wording fixes; (ii) and (iv) clean

**(i) Pre-registration of the SNAP band.** The numeric band $[0.057, 0.207]$ is NOT fitted to the measured $0.2406$: it is derived at runtime as $[0.5\cdot\min, 1.7\cdot\max]$ over the two frozen model variants (tri $0.114$ at $u = D/2$, uni $0.122$ at $u = D$), and the $[0.5\times, 1.7\times]$ rule plus both variants are in the frozen docstring; the gate recomputes the model from frozen code (verified live: "band [0.057, 0.207] (model tri 0.114 / uni 0.122)"). The model itself was refit once on quick-mode calibration data (the in-window model was replaced by the alternating-sum model after failing calibration): DISCLOSED, and the full-scale numbers are genuine out-of-sample tests of the frozen form. Two residual faults. (a) The frozen docstring hedges the uniform variant as "$\sim 0.05$-$0.14$" while the .md presents "uni $0.122$" under "Full-scale predictions": the exact number was computed post-freeze (deterministically, from frozen code, so no data leakage, but the .md's presentation overstates what was written down). (b) The SNAP-lo rung's model prediction is $0.00148$ (npz `ret_predictions[0]`) against measured $0.0416$: a **28x miss**, absorbed silently by the loose dead-gate $|P| \le 0.1$; the .md's "The displacement law holds quantitatively" and the docstring's "SNAP D_lo ... < 0.01" gloss this, and the "deterministic lattice-alignment floor" explanation in limit 4 is post-hoc, not registered. Structural note, no fix possible: the within-session freeze is builder-attested (no interim commit exists; the files were created in one session), which is the repo's standard PreRegistry pattern; the prototype disclosure is what makes it credible. Fixes 8.

**(ii) Meter-matching: clean.** TRUE(C3), SNAP, Beurling, ramp all pass through `windowed_pair_meter` with the same half-width $0.5$, the same $\log b$ bilinear weights, the same $1/X$ normalization; for integer configurations with $w < 1$ the windowed meter provably equals the exact meter (code logic checked: the open window $(b_i + h - w,\, b_i + h + w)$ contains exactly $b_i + h$), and the C3 gate measures the match at $6.7\times10^{-13}$. No format mismatch manufactures the contrast.

**(iii) The Beurling control's flatness IS construction-baked, and the mandated scope check was skipped: LANDED.** C1 draws $\varepsilon_p$ iid at scale $0.25$: multiplicative jitter of size $\sim p/4 \gg$ the window for every $p > 2$, so locally-Poisson difference statistics (hence second-order flatness) are guaranteed by the construction. C1 therefore refutes exactly "density alone" (its designed role), NOT "every latticeless system". The landscape note flags precisely this (gap 4: "Beurling variance literature (Hilberdink and others) was NOT systematically searched here; F1a should run that check before treating the comparator as unclaimed") and its Section 8 feed makes it an obligation ("gap 4's scope caveat (Hilberdink check) must be cleared first"). The build dossier neither runs nor cites it: `grep -i hilberdink` over both build files returns nothing. The Section 5 clause "purchasable from NONE of: density alone (C1 ...)" is correct for the generic comparator and over-scoped as written. Fix 6.

**(iv) The Cramer draw: fair.** Density-matched ($\min(1, 1/\log n)$ over ALL integers), congruence-free by construction, three seeds, meter-matched (exact meter, integer-valued); mean $1.002$ calibrates the level. One sharpening worth a sentence, not a fix: an odds-only Cramer variant would show parity $= 2$ trivially (odd minus odd is even), so the parity component of the TRUE signal is purchasable from the single datum "all elements odd"; the discrimination's load-bearing meter is $R^2_{\rm even}$ (mod-$p$ structure, $0.99996$ on TRUE, $\le -5.8\times10^4$ on every stochastic control), which an odds-only Cramer would also fail. The dossier's conclusion does not rest on parity alone, but its presentation is parity-first.

**(v) The $D = 10^8$ corner: it demonstrates sub-resolution invariance, and the dossier must say so where the verdict is stated.** At $D = 10^8 = 10X$ the snap displaces every $b_p$ by $\le 0.05 \ll$ the $0.5$ window, so ANY meter of this resolution is invariant: the bit-identical output is the #172/#198 continuity law, not new information about the L2 axis. In exact terms SNAP-hi is L2-commensurate AND exactly L1-dead (no integer differences at all) while the meter reads alive: so the four-corner table's row labels "L1/HL signal ALIVE/DEAD" are METER statements, and what the probe types is the finite-data shadow of L1 ("additive alignment concentration at integer shifts at bounded window"), not #152's completed clause ($N = x + O(1)$, Poisson, theta FE). The dossier's Section 7 limit 3 states exactly this, honestly and in the right words; but the Section 5 verdict paragraph and the one-line verdict assert "lattice-class (L1 congruence data)" without the qualifier, and Section 4's "The meter provably reads neither L2 axis value" is fine only because co-occurrence proves non-function of the axis: the wording should not be quotable without limit 3. Not an overclaim as a whole document; an overclaim if Section 5 is quoted alone. Fix 5.

## A4. The competitor co-landing (P3): GLANCED

**P3's framing is honest.** The HL family is present and clean at accessible $X$ ($\mathrm{rms}_e = 0.0053 / 0.0022 / 0.0007$ at $10^6/10^7/10^8$, verified from npz + deep run), the decay is square-root-cancellation-class (unrounded slope $X^{-0.425}$; the quoted $-0.44$ is an artifact of the rounded endpoints; say $-0.42$ or "0.38-0.47 per decade"), and the conclusion "what is missing at the funding joint is a PROOF of uniform error control" matches the landscape's own provability-boundary section (items 1-4: the in-print blame is uniformly off-diagonal cancellation / variance, i.e. the competitor). The conjunction reading (P1 types WHAT is consumed, P3 types WHY it is unproven) was pre-registered in the frozen docstring and is the deliberation's own expected outcome.

**The outright-win condition is not decision-grade as worded.** "Can be arranged to consume the family ONLY through aggregates in which the congruence structure contributes $o(1)$ ... AND those aggregates are provable without congruence inputs" has no decidable trigger: "arranged", "aggregates", and "without congruence inputs" are all open quantifiers, so as a STANDING KILL nobody can ever certify it fired or did not. Proposed replacement (Fix 9), splitting it into a computable clause and a literature-event clause:

> The competitor wins OUTRIGHT if both hold. **(W1, computable now)**: an identity reduces the AF certificate's off-diagonal at some support $1 + \delta$ to functionals $\sum_{h \le H} w(h) B_X(h)$ with $w$ smooth at scale $H$ and residue-blind (no dependence on $h \bmod q$, any $q \ge 2$), such that substituting $B_X(h) \to X$ (the density model) changes the certificate constant by $o(1)$: checkable by running Section 2's computation on the actual AF Gabor kernel. **(W2, literature event, WATCH-screened)**: an unconditional evaluation of those functionals to the needed accuracy exists whose inputs are PNT-class only (no primes-in-APs, no singular-series identity). W1 alone kills F1a's typing at the data-class register; W2 alone changes nothing.

## A5. The Vedana negative control (#204(v)): GLANCED

The dossier's design-level argument satisfies the control's letter: it names what would have fired on failure (P2, armed and live: "If the meter had fired identically on C1 or C2, the typing would run on any Vedana-class system and would be WRONG"), and the #204(v) quotation ("the additive lattice enters the uniqueness clause nowhere", formula-existence system-generic) is faithful to the entry. Residuals: (a) the argument routes through C1 as a PROXY for a Vedana-class system ("supplies density and spectral data of exactly the kind C1 carries" is asserted, not constructed; no formula-carrying no-lattice instance was actually run through the meter); (b) it therefore inherits A3(iii)'s single-family scope: a Vedana-class system with adversarially structured Bohr spectrum is not obviously C1-like, though concentration of its difference set on $2\mathbb Z$ specifically would itself be lattice structure, which is the design-level point. Adequate for a design-level control, correctly labeled "design-level" in both files; the residual is absorbed by Fix 6's scope sentence.

## A6. Discipline audit: MISSED (no violation; one pending action, one decorative gate)

- **K1**: grep of the module finds no `zeros()` call (all matches are `np.zeros` allocations), no `zetazero`, no cache paths. The import chain audited: `harness` imports the zeta/D-H/Beurling modules at import time, but zero caches load only inside `zeros()` METHOD calls (verified in `_shared/zeta.py`: `open()` only inside the method; the module-level singleton is a bare constructor), and `flat_primes` is a sieve. The docstring's claim "the harness import loads no zero lists at import time" is TRUE as stated. Note: gate 21 ("K1: no zero data consumed") passes on a hard-coded `True`: it is a design-fact record, not a check; the actual verification is this report.
- **D-H**: type refusal STATED, not simulated, in both files, with a faithful #202(iv) citation ($-f'/f$ poles in $\sigma > 1$, no von-Mangoldt-type series, $B_{DH}(h)$ unposable at the funding joint). Correct discipline for a funding-joint probe.
- **Beurling**: the LIVE control (C1), as the bracket requires for a counting-side probe.
- **Artifacts**: the npz sits next to the script, no `_cache` path, bit-reproducible. **All three e1af files are currently untracked** (`??` in git): the evidence rule wants them tracked at write time; the builder cannot commit (per-action authorization), so this is a REQUIRED ACTION at the session commit, not a violation. Fix 10.
- **Conventions**: standalone module with `main()` and exit code, the harness "N/N passed" contract, real quick mode (same gates, npz skipped). No em or en dashes in either file (grepped).

## A7. Fidelity: LANDED (two attribution errors, one pre-named; three numeric errors; several verified-faithful)

1. **The pre-named catch, confirmed.** .md Section 2(a): "the Aryan / Baluyot-Goldston-Suriajaya-Turnage-Butterbaugh evaluations **the AF paper carries as hypotheses**". This repeats the fetch-summarizer confabulation corrected 2026-08-26: the funding-inputs note's Finding 1 ("The AF paper does not import either credited theorem. It re-proves the needed evaluation in-house, and the credits are priority/lineage credits") and Section 5 (Appendix A verbatim: "their types carry no hypotheses"). The .py does not contain the phrase. Fix 1.
2. **The funding split misattributed.** .md Section 2 Setup: "The AF budget entries $\mathrm{tr}\,\tilde G$ and $\|\tilde G\|_{HS}^2$ are evaluated by the explicit formula (their §5, the Aryan and BGSTB mean-value inputs)". Contradicts Finding 5 at source: the trace is Proposition 4.2, funded zero-side (Gabor frame + Riemann-von Mangoldt + $N(t,t+1) \ll \log t$), "no prime sum enters the trace"; the prime side funds ONLY the HS budget; and the Aryan/BGSTB attribution repeats the lineage-vs-import error. Fix 2.
3. **$B_X(2)/B_X(1) \sim 10^5$ is wrong by two orders.** Measured from the tracked npz: $b(1) = 7.615\times10^{-8}$ (the single pair $(2,3)$), $b(2) = 1.326$, ratio $= 1.74\times10^{7}$. Fix 3.
4. **"odd shifts empty to one part in $\sim 10^5$" is wrong by two orders.** Measured: $\overline{b}_{\rm odd}/\overline{b}_{\rm all} = 1.25\times10^{-7}$, and $2 - P = 2.5\times10^{-7}$: one part in $\sim 10^7$. Fix 3.
5. **The one-line verdict's "all with $R^2 \le -4.4\times10^4$" is false for its SNAP member.** Coarse-SNAP: $R^2 = -878$, $R^2_{\rm even} = -1765$ (npz). True for the six stochastic seeds only. Fix 4.
6. Minor: "$O(X^{-1/2}\log X) \approx 3\times10^{-4}$": the number matches $X^{-1/2}$ ($\psi - \theta \sim \sqrt X$ against $\psi \sim X$); the expression as written evaluates to $5\times10^{-3}$. And the trend exponent (A4 above). Fix 10 (optional items).

**Spot-checked and faithful** (for balance): the AF ceiling quote "the off-diagonal prime sum is no longer dominated by the diagonal" (verbatim per the note's Section 6 [FETCHED x2] block); "higher moments add nothing" kept in the moments form per #206's A6 correction; the L1/L2 ledger paragraph matches #206(ii) exactly; the #202(iv) and #204(v) citations faithful; Gallagher-1976 average-1 and the MS second-order display consistent with landscape E4/E5 (algebra re-derived); the MS-variance entanglement sentence matches E5's $B = 1 - C_0 - \log 2\pi$ datum; $\overline{\mathfrak S} = 0.9723$ vs TRUE mean $0.9713$ agree to $0.1\%$ as claimed; the unit-spacing separation remark consistent with the note's $\delta_n^{-1} \le 2n$ datum; window/support arithmetic exact.

---

## A8. The session grade (the Section 6 wiring; assigned by this ADVERSARY)

**Wiring**: non-UNMOVED iff the session produces a Section 5 metric item (a)-(d) AND the minted claim passes the #201 derivability check (not wordable from existing ledger clauses via the entries' own cross-refs).

**Metric items**: (a) theorem-shape: none (the outright-win condition is a posed condition; statements are cheap per the deliberation's own bar). (b) located obstruction with mechanism: the wall was located by #202/#206; this session typed its data class: no new location. (d) frontier movement: none claimed. So everything rides on (c), the four candidate minted claims:

**(i) The F1a typing verdict** ("the funding wall's data class is lattice-class, L1 congruence data, at the second-order register"). **Fails the derivability check as a coordinate.** The claim is stated VERBATIM in #206(ii)/the deliberation Section 4 as the frame's pre-registered F1a, itself derived there from #152's conservation clause ("the HL singular series lives as congruence data inside $\mathbb Z$" is the deliberation's own sentence). What the session added is a measured confirmation at the pre-stated register with matched controls: exactly the category the deliberation's Section 5 excludes ("registers measured ... do NOT count as progress"). High-quality measurement, real value to F2a's design, not a coordinate.

**(ii) The four-corner meter datum** (L1/L2 anti-alignment realized in both directions on one meter). **Fails.** The anti-alignment itself is #206(ii)'s A2 catch ("the SNAP/TRUE controls ANTI-ALIGN the two", with both original corners stated). The two corners the session added are each wordable: Beurling (independent + dead) is the deliberation's own expected comparator behavior; SNAP-hi (commensurate + alive) is an instance of the #172/#198 continuity law (sub-resolution displacements are invisible to any finite meter: "arithmetic enters finite instruments only through floors and limits"). The quantitative direction along the SNAP family (coarser $D$ = deader signal) is instrument-grade realization, not a constraint.

**(iii) The unit-integer-spacing datum** (the support-$\le 1$ off-diagonal bound already consumes the additive lattice; funding-inputs note Section 6). **Fails, on the dossier's own showing.** The strong reading (the below-wall funding NEEDS lattice data) is refuted by the repo's documented Beurling mean-value line (density-class facts hold latticelessly), which the dossier's own remark (ii) concedes ("at this support the funding is DENSITY-CLASS in the operative sense"). The weak reading (AF's proof ROUTE touches the lattice below the wall, via MV separation from unit spacing) is a genuine at-source anatomy datum not in #202, but it is proof-route anatomy with no constraint content; correct filing is enrichment of the trojan ledger (proof-route lattice-touching is not statement-level lattice-consumption; the D-H/Beurling bracket detects the latter).

**(iv) The GLSS I/II datum** (100 percent simple-and-on-line at full pair funding, without RH, from either competing law; landscape rows D3/D4). **Fails as a constraint; file as enrichment.** #206's Wall 2 already carries the content from the AF paper's own ceiling (full HL* tops out at 100 percent SIMPLE; budgets bound HOW MANY, never WHERE). The GLSS-II twist (law-indifference at full funding: the landscape's discrepancy 6, "not yet in any repo document") is genuinely new INPUT and strengthens F2a's motivation, but nothing becomes newly forbidden or required beyond the existing Wall 2 clause: corroboration, per the #204(v) enrichment precedent.

**Grade: UNMOVED.** No metric item lands with a derivability pass. One paragraph of justification: the session executed its charter cleanly (the wall's data class measured as pre-registered, the kill armed and refuted, the competitor discriminated into a conjunction, the controls matched), and everything it found was either pre-stated by the deliberation, wordable from #152/#172/#198/#206, or in print via the landscape's own rows; under the pre-wired vocabulary that is a well-run measurement session, which the deliberation itself predicted would grade UNMOVED, with the audit as cadence. **Tripwire count: 2/3** (F3 was 1/3 per #207; this grade sets two; the expected session-three audit fires if F2a grades UNMOVED).

**Wiring consequence flagged for ORCHESTRATOR adjudication (not this adversary's call to execute).** Exit 3 of the deliberation reads: "Two consecutive minted claims fail the #201 derivability check: the frame is re-measuring the wall; the audit fires immediately." F3's minted claim (the structural reading) was graded wordable-from-ledger by its own adversary (#207(iii)); F1's minted claim is graded wordable here. On the letter, exit 3's condition is MET after session two: the audit fires now, one session before the tripwire's expected cadence. The counter-reading: exit 3 targets claims ASSERTED as coordinates that fail on inspection, and both sessions filed their claims modestly, so only the 3-UNMOVED tripwire should govern; under that reading exit 3 fires on every honest measurement pair and strictly dominates the tripwire, which may or may not be intended. My recommendation: honor the letter. The previous frame ran twenty-two UNMOVED sessions unaudited on exactly a generous reading of tripwire language (#201(i)), the audit is one deliberation session, and the deliberation itself calls the early audit the frame's cadence, not its failure. If the ORCHESTRATOR instead adopts the narrow reading, that adoption must be recorded as an explicit wiring decision in the ledger, not applied silently.

---

## Required fixes (SYNTHESIZER to apply; none changes a gate outcome or a prereg resolution)

1. **[.md Section 2(a)]** Replace "and in the Aryan / Baluyot-Goldston-Suriajaya-Turnage-Butterbaugh evaluations the AF paper carries as hypotheses" with "and in the Aryan / BGSTB evaluations AF credits as lineage (AF re-proves its budget in-house as Theorem 5.7; its Appendix A claims full coverage with no hypotheses on the main theorems' types: [`af_funding_inputs_verification.md`](../../docs/03_research/reading_notes/af_funding_inputs_verification.md) Finding 1 and Section 5)".
2. **[.md Section 2 Setup]** Correct the funding split per Finding 5: the trace is funded zero-side (Prop 4.2: Gabor frame + RvM count; no prime sum enters the trace); the prime side funds only the HS budget (their §5, in-house Theorem 5.7); drop "the Aryan and BGSTB mean-value inputs" as a consumed-step attribution.
3. **[.md Sections 2 and 4]** "$B_X(2)/B_X(1) \sim 10^5$" becomes the measured $\sim 1.7\times10^{7}$; "odd shifts empty to one part in $\sim 10^5$" becomes "one part in $\sim 10^7$" ($\overline b_{\rm odd}/\overline b_{\rm all} = 1.25\times10^{-7}$; $2 - P = 2.5\times10^{-7}$).
4. **[.md verdict line]** Fix "all with $R^2 \le -4.4\times10^4$": true for the six stochastic seeds; coarse-SNAP has $R^2 = -878$, $R^2_{\rm even} = -1765$. Suggested: "stochastic controls at $R^2 \le -4.4\times10^4$; coarse-SNAP at $R^2_{\rm even} = -1765$".
5. **[.md Section 5 + verdict line]** Append the limit-3 qualifier inline where the typing verdict is stated: "lattice-class (L1 congruence data) **in the finite-data sense of Section 7.3**: additive-alignment concentration at integer shifts at bounded window; the completed-totality clause (membership in $\mathbb Z$, Poisson, theta FE) is meter-invisible by the #172/#198 continuity law".
6. **[.md Sections 3 and 5]** Add the landscape gap-4 scope: C1's flatness is construction-guaranteed for the iid law (locally-Poisson differences), so C1 kills density-alone, not every latticeless system; the Beurling variance literature (Hilberdink et al.) is unswept; either scope the Section 5 clause to "the generic density-alone comparator" or queue the Hilberdink sweep (S-item, rides a WATCH slot). This also absorbs A5's proxy residual.
7. **[.md Section 2 known-vs-new]** Add to KNOWN: Montgomery 1973's $\alpha \ge 1$ HL heuristic; Bolanz 1987 (landscape row E2); Chan's second-order carry through the $F$ heuristic (landscape gap 2); and note the shift range $h \lesssim X/T$ is read off at source in the sibling note's Section 6. Narrow THIS PROBE'S to: the AF-normalized assembled statement, the concrete $(X, H)$ instantiation, the four-corner control design, the displacement law, the measurements.
8. **[.md Section 3]** Mark the uniform-variant $0.122$ as computed post-freeze from the frozen model (the frozen docstring hedged "$\sim 0.05$-$0.14$"); state the SNAP-lo model miss (predicted $0.0015$, measured $0.0416$, 28x, a deterministic-lattice floor outside the displacement model, absorbed by the registered dead-gate $0.1$); scope "The displacement law holds quantitatively" to the mid/hi rungs and the ramp.
9. **[.md Sections 2 and 5]** Replace the outright-win condition with the two-clause decidable form (A4's W1/W2 or equivalent): W1 computable on the AF kernel now, W2 a WATCH-screened literature event, with the consequence of each stated separately.
10. **[housekeeping]** `git add` all three e1af files plus this report at the session commit (all currently untracked; evidence rule). Optional: "$O(X^{-1/2}\log X)$" to "$O(X^{-1/2})$-class"; quote the trend slope from unrounded values ($X^{-0.42}$, or "0.38-0.47 per decade").

---

# VERDICT: PASS_WITH_FIXES

The probe survives reproduction bit-exactly on this host (22/22 quick twice, 23/23 full `--deep` twice, npz arrays bit-identical to the tracked artifact), the derivation's arithmetic is correct line by line and consistent with the at-source surface map, the meters are genuinely matched (C3 at $6.7\times10^{-13}$), the pre-registration is real at rule level with the model refit disclosed, and all five branches resolved as registered with the P2 kill armed and live. The PROPOSED typing verdict stands AS SCOPED: at the pre-registered second-order register, on this meter, the funding family's discriminating content is integer-shift congruence data, purchasable from none of the matched controls, and independent of the L2 axis in both meter directions; the required fixes bind it to its honest scope (finite-data L1 shadow, generic density-alone comparator, Hilberdink unswept) and purge two repeated at-source misattributions, of which one (the "carried as hypotheses" confabulation) was the tasking's pre-named tripwire and did fire. The session grades UNMOVED (tripwire 2/3), the frame's own predicted cadence; exit 3's letter is met and goes to the ORCHESTRATOR.
