# E1T: the compactness trojan at the CCM D_log family

**Date**: 2026-07-22. **Roles**: BUILDER (this probe), with one mid-round SURVEYOR input folded in (canonical-system compactness; see the fold-in section). **Status**: ADVERSARY round DONE same day, verdict PASS_WITH_FIXES ([`_e1t_adversary.md`](_e1t_adversary.md)); all fixes applied in place and marked [ADVERSARY].

**Artifacts**: [`e1t_compact_class_limit.py`](e1t_compact_class_limit.py) (post-ADVERSARY: 23/23 self-tests full, ~15 s warm / ~90 s cold; 22/22 quick, ~7 s, saves no npz; was 21/21 both before the round), `e1t_compact_class_limit.npz` (full mode only), build cache `_cache/e1t_build_*.npz` (gitignored).

**Verdict in one line**: `trojan_trades_M4_away = NO` (split verdict, all three links measured); the trade RELOCATES the uniform det-class limit into the normalization gauge, the lambda-uniformity joint, and the pin's lattice clause. The compactness half's builder-defined certification vector is DMV-blind (identical for the Beurling fake); the one measured output-level exception found by ADVERSARY is the T1f gauge-positivity face (family-separating in-sample, gauge-relative), so the discrimination picture is: input-typed gates at the pin, plus one gauge-conditional measure-face lead handed to e1u.

## The trojan framing

The residual open step of the CCM D_log arc (LEARNINGS #154/#158/#160/#161) is the Section-7 uniform determinant-class limit xihat_lambda to Xi, which is M4. The trojan asks whether that ONE uniform statement can be traded for three individually cheaper links: (i) certify the finite objects into a positivity class compact after renormalization, so subsequential limits exist for free (Montel/Helly, no rate); (ii) a Carleman-type determinacy condition making all subsequential limits coincide; (iii) the #160 Hamburger pin identifying the unique limit as Xi. If all three were cheap, uniformity would never need to be proven, only membership, determinacy, and identification. Each link is measured here, none assumed; the pre-registered exits treat a negative at any link as a finding about WHERE the RH content relocates.

## Pre-registered expectations and exits (stated before results)

| Q | Pre-registered expectation | Pre-registered exit |
|---|---|---|
| Q1 | The #160 type divergence (type ~ 0.93..0.98 x log lambda) blocks any fixed-type class; three normalizations tried: (a) type-rescale, (b) ghost/dressing quotient, (c) lattice-tail-aware coefficient measure | If no normalization bounds the type without destroying the target, the trojan dies at link (i) and M4 relocates into the normalization, verbatim |
| Q2 | Reality is CF-manufactured (#158), so zeta, D-H, and the fake should ALL certify; D-H must be UNPOSABLE at the Euler gate (sign-changing comb), not merely numerically different; the fake must run and fail at a NAMEABLE lattice link | If certification is family-blind, the class membership carries no arithmetic bits and the discrimination must sit elsewhere (locate it) |
| Q3 | From #160/#161 (types diverge, floors stagnate): no in-sample Cauchy trend for zeta, branch scatter dominant, window-tracking moment radii | Link (ii) not certified by finite data; the uniformity joint relocates, unchanged |
| Q4 | DMV screen fires on the compactness half; K1 guards never trip | Honest relocation accounting with an overclaim tripwire (all-links-certified would be a K1 alarm, not a success) |

Threshold provenance: the *shapes* of the expectations above were pre-registered in the task; the *numerical thresholds* in the checks (e.g. 0.85..1.05 type ratio, the 0.10/0.20 alternating-mass split, the 0.05 reality band) were pinned from a calibration run of the same code on the same builds, and are labeled as pinned, not pre-registered.

## Methods

- **Harness**: the e1k `build_float` machinery, generalized to `build_comb` which consumes an explicit frequency comb (ell, w) = (log of comb point, Lambda-weight x point^{-1/2}). T0a/T0b verify `build_comb` is bit-identical (max|dQ| = 0.0) to `build_float` on both integer streams at the toy config (N = 4, lam = 1.8, dps 25), which makes the "all three L-functions flow through identical code" claim checkable rather than asserted. [ADVERSARY] The toy-scale scope was closed: T0d rebuilds a REAL mixed-family config (D-H 2.6/16, an e1n `build_float` cache reuse) via `build_comb` and finds exact agreement (max|dxi| = 0.0, |deps| = 0.0), so the within-family mixing of e1n caches and fresh `build_comb` builds injects no builder-dependent differences.
- **Streams**: ZETA (von Mangoldt comb, prime powers only), D-H (dense sign-changing comb from the period-5 Dirichlet recursion), BEURLING fake (`_shared/beurling.py` canonical system, b_p = p e^eps, eps iid U[-0.25, 0.25], seed 149; comb on prime powers b_p^k at NON-integer frequencies k log b_p). The fake is density-matched by construction choice: it enters with zeta's own archimedean density and pole term; only the comb differs.
- **Builds**: ZETA and D-H standard configs reused bit-identically from the e1n cache ((2.2,12), (2.6,16), (3.0,32), (sqrt13,48)); new builds (D-H 2.2/3.0, BEUR 2.2/2.6/3.0) built once by `build_comb` and cached. dps = 25 (the e1l/e1m-characterized branch; e1n's dps-branch-specificity caveat inherited). Truth values (Xi, Phi_DH) at mpmath dps 30, VALUES only.
- **Normalizations** (Q1): (a) G(w) = F(w/tau_q)/F(0) with tau_q the in-build measured type after ghost quotient; (b) division by prod(1 - z^2/z0^2) over the object's own low-band real zeros, gated by FE data (below: the gate); (c) the coefficient measure mu = sum c_n delta_{phi n}, justified by the exact tail identity xihat(phi m) = 0 for |m| > N (T0c: ~1e-16 for all three streams, so the finite object IS its in-band lattice data).
- **The ghost gate**: classifying a low-band zero as spurious requires the function's own FE-derived RvM budget saying that band is empty (zeta: below 14.13; D-H: below 5.09). The fake has no FE, hence no own-RvM curve, hence no "spurious" notion: normalization (b) is UNPOSABLE for it in principle. This is an input-typed gate exactly parallel to the #161 Euler gate.
- **Instruments**: band-shrink winding reality certificate (strip count at |Im| <= 0.4 vs |Im| <= 0.05 must agree; grid-free, robust to close real pairs that a sign-change scan misses); projective sup distances on K = [0, 6] (inside the measured e1n escape window t_dir ~ 6..7; the pin identifies F = c Xi only up to constant, so distances are scale-aligned first); Taylor coefficients by Cauchy integrals on |z| = 2; moment radii R_k = (m_2k/m_0)^(1/2k) on |c|; the type-subtracted m-proxy m_red = xihat'/xihat - (L/2)cot(zL/2) - ghost terms on C+ segments (surveyor fold-in); mass-escape fraction esc_12 (coefficient mass beyond |phi n| > 12).
- **K1**: runtime guards on `mp.zetazero` and the D-H zero scanner (installed, never tripped), source token scan, per-test input ledger. No zero list is consumed anywhere; landmark heights 14.13/5.09 bound ghost-scan windows only.

## Results

### Q1 RENORMALIZATION: the trojan dies at link (i), as pre-registered (T1a-T1e)

Per-build panel (full grid):

| build | tau (x log lam) | tau_q | gate (b) | ghosts | sup(G-1) | fneg_raw | fneg_alt | r95x / phiN | esc12 | eps |
|---|---|---|---|---|---|---|---|---|---|---|
| ZETA 2.2/12 | 0.741 (0.939) | 0.741 | POSABLE | 0 | 0.085 | 0.500 | 0.026 | 12.0/47.8 | 0.027 | +3.1e-05 |
| ZETA 2.6/16 | 0.912 (0.955) | 0.804 | POSABLE | 3 | 0.247 | 0.495 | 0.598 | 29.6/52.6 | 0.567 | -3.1e-04 |
| ZETA 3.0/32 | 1.020 (0.928) | 1.020 | POSABLE | 0 | 0.038 | 0.500 | 0.028 | 11.4/91.5 | 0.031 | +3.2e-05 |
| ZETA sqrt13/48 | 1.256 (0.979) | 1.078 | POSABLE | 5 | 0.216 | 0.500 | 0.283 | 51.4/117.6 | 0.999 | -3.8e-05 |
| D-H 2.2/12 | 0.766 (0.972) | 0.766 | POSABLE | 0 | 0.992 | 0.441 | 0.122 | 15.9/47.8 | 0.063 | +8.7e-03 |
| D-H 2.6/16 | 0.930 (0.973) | 0.930 | POSABLE | 0 | 0.729 | 0.488 | 0.090 | 6.6/52.6 | 0.022 | +1.0e-04 |
| D-H 3.0/32 | 1.063 (0.967) | 1.063 | POSABLE | 0 | 0.568 | 0.499 | 0.054 | 5.7/91.5 | 0.002 | +8.1e-05 |
| D-H sqrt13/48 | 1.250 (0.974) | 1.250 | POSABLE | 0 | 0.341 | 0.500 | 0.052 | 7.3/117.6 | 0.002 | +3.3e-05 |
| BEUR 2.2/12 | 0.768 (0.973) | 0.768 | UNPOSABLE | 0 | 4.198 | 0.333 | 0.390 | 39.8/47.8 | 0.358 | -0.88 |
| BEUR 2.6/16 | 0.936 (0.980) | 0.936 | UNPOSABLE | 0 | 3.459 | 0.343 | 0.324 | 36.2/52.6 | 0.963 | -1.10 |
| BEUR 3.0/32 | 1.101 (1.002) | 1.101 | UNPOSABLE | 0 | 2.955 | 0.417 | 0.241 | 40.0/91.5 | 0.621 | -1.38 |

- **T1a (obstacle confirmed)**: type = 0.928..1.002 x log lambda across all eleven builds of all three families, strictly increasing within each family. No normalization-free bound exists. The pre-registered #160 obstacle stands, now confirmed on the fake too.
- **T1b (normalization b)**: the ghost quotient lowers the measured slope at the dressed builds (0.912 to 0.804; 1.256 to 1.078) but the quotiented types still increase along the grid: (b) removes dressing, not divergence. The gate is FE-typed: POSABLE for zeta/D-H, UNPOSABLE for the fake.
- **T1c/T1d (normalization a)**: the type-rescale IS compact in-sample (sup|G| <= 5.2 over all builds on |w| <= 2.5) and it COLLAPSES toward the constant 1 for all three streams (zeta clean 0.085 to 0.038; D-H 0.99 to 0.34 monotone over four points; BEUR 4.20 to 2.95 monotone over three). Compactness is free exactly where it is information-free: the limit of the rescaled family is a constant regardless of arithmetic, and the identification content sits in what the rescale discards, whose lambda-uniform control is the #160 growth clause. ([ADVERSARY precision] not in tau_lambda's VALUE, which T4a measures as family-blind at the ratio level, diff 0.034, but in the uniform control of the collapse rate.)
- **T1e (normalization c, RAW/ALTERNATING gauges)**: the raw coefficient measure splits ~50/50 in signed mass (a structural alternating gauge: the kernel-comb form xihat = sin(zL/2) x Cauchy transform makes alternation the price of in-band zeros off the lattice). The alternating gauge a_n = (-1)^n c_n is nearly one-signed ONLY on the clean zeta branch (2.6-2.8 percent negative mass) and on D-H at larger lambda (5-9 percent); it fails on the dressed builds (28-60 percent) and on the fake (24-39 percent). In these gauges no family-uniform positive-measure certificate exists, and tightness is branch-conditional too (r95x ~ 11.5-12 stable on clean zeta, ~ 6-7 on D-H, but 30-51 on dressed and 36-40 on the fake).
- **T1f [ADVERSARY] (the gauge re-scope of T1e)**: the dressed-branch alternation is EXACTLY the ghost polynomial's sign pattern. In the FE-gated ghost-quotient gauge aq_n = a_n / q(phi n) (invertible, object-computable, gated by the same ghost gate as normalization (b)), the ZETA family becomes near-positive AND tight family-wide: fneg_q = {0.026, 0.0002, 0.028, 0.0005} with the DRESSED branches the cleanest, r95_q = {12.0, 6.6, 11.4, 7.3}, esc12_q <= 0.031. D-H (no ghosts, quotient trivial) stays at {0.122, 0.090, 0.054, 0.052} and BEUR at {0.39, 0.32, 0.24}. So measure-face positivity is GAUGE-RELATIVE, and in this gauge it is a family-SEPARATING computed output (zeta cleanest at every grid point), not an input-typed gate. Priced caveats: the separation is in-sample (the D-H sequence decreases along the grid and may close), the un-normalized mass collapses on the deep-dressed branch (sum|aq| spans 2.9 down to 0.0017, so Helly without renormalization can hit the zero measure; Prokhorov on the NORMALIZED measures is the clean framing), and the tightness gain is partly gauge-forced (dividing by a degree-2g polynomial damps the high band). Also measured: the normalized gauged-measure Kolmogorov coherence is family-MIXED (zeta {0.257, 0.142, 0.256}, D-H {0.162, 0.214, 0.212}), so the gauge fixes positivity, not coincidence.

**Q1 verdict (measured, ADVERSARY-scoped)**: NEGATIVE at link (i) on the FUNCTION face, the pre-registered exit: (a) bounds the type at the price of a constant limit; (b) leaves the divergence. On the MEASURE face the verdict is gauge-relative: raw gauges fail positivity (T1e), the FE-gated quotient gauge revives it for zeta, family-separating in-sample (T1f). M4 still relocates into the normalization (now sharpened: WHICH gauge, and the lambda-uniform control of what the gauge discards) rather than being removed.

### Q2 CLASS CERTIFICATION: family-blind where it succeeds, input-typed where it discriminates (T2a-T2e)

- **T2a (reality, band-shrink winding, identical code)**: 11/11 builds certify REAL (strip count at |Im| <= 0.4 equals count at |Im| <= 0.05; zeta 4/11/13/29, D-H 7/12/19/32, BEUR 7/11/19). The sharpest new number: the fake's truncated Weil form is O(1)-INDEFINITE (eps = -0.88, -1.10, -1.38) while zeta sits at its razor margin (|eps| <= 3.1e-04), yet the fake's determinant zeros are exactly as real as zeta's. Reality is CF-manufactured and does not even need the positivity margin: #158's information-free finiteness extended to a non-lattice Euler comb.
- **T2b (Euler gate, #161 shape)**: the nonnegative comb is checked as an explicit hypothesis. ZETA: CERTIFIED (gate passes, series converges, frequencies exactly on {log m}, displacement 0.0). D-H: UNPOSABLE_EULER_GATE (min weight -4.65 at the comb level; nothing downstream is computed, by construction, not by numerical failure). BEUR: passes the gate, runs, and FAILS at the named lattice clause (low-band frequency displacement 0.033 from the {log m} lattice, against a 0.02 threshold pinned at the lattice spacing at n = 50, log 51 - log 50 ~ 0.0198; [ADVERSARY] the earlier "half the spacing" provenance phrase was arithmetically wrong, half would be 0.0099). [ADVERSARY scoping] The gate is a TYPE-level refusal: nothing downstream in THIS probe's certificate consumes nonnegativity (gate-stripped, the signed D-H comb passes the lattice clause with displacement exactly 0.0 and fails only the truncation-sensitive convergence clause, 0.098 against the pinned 0.05; the |comb| rearrangement likewise fails only convergence at 0.113). The genuine positivity consumer the gate stands in for is e1n T5's Abel-summation one-sided upgrade, which is pin-side. "Consumed as a hypothesis" therefore means input typing, honest as such, not a downstream computation in this file.
- **T2c (limit-level lattice fuel)**: Z theta FE defect 1.5e-36 vs fake defect 0.37. The pin's Poisson/theta engine has no fuel for the fake: its link-(iii) failure is nameable and sits at the lattice, exactly the #152 fourth clause.
- **T2d (D-H type exclusion re-verified)**: own conductor-5 FE exact to 1.7e-30; Riemann-type FE defect 1.72 = O(1). D-H exits link (iii) at the pin's H1 data.
- **T2e (the moving class)**: the certified class at cutoff lambda is PW(type tau_lambda) with tau diverging: there is no single fixed HB/PW class containing the family, so fixed-class Montel is unavailable; the substrip-local-boundedness route (the compacts where Section 7 lives) is in-sample flat for zeta/D-H (sup on [0,10] x [-0.35, 0.35], 0-normalized: 1.001..1.010) but gauge-fragile for the fake (6.93 at BEUR 2.6, caused by a low zero near the normalization point). Local boundedness on substrips is exactly the #160 growth clause: consistent in-sample, open in principle, and not a discriminator either.

**Q2 verdict (measured, ADVERSARY-scoped)**: certification into the class succeeds for ALL THREE streams identically; within the builder-defined certificate set, every discriminating clause (Euler nonnegativity, FE type, lattice membership, theta fuel) is an input-typed gate belonging to link (iii), the pin, not to compactness. The one measured exception found by ADVERSARY sits outside this set: the T1f gauge-positivity output separates the families in-sample on the measure face, conditional on the FE-gated gauge.

### Q3 DETERMINACY SURROGATE: absent in-sample, and RH-blind where visible (T3a-T3e)

- **T3a (Taylor trajectories, r = 2 Cauchy)**: a_2 by family: zeta {-0.0073, -0.0230, -0.0062, -0.0363} vs Xi truth -0.0231; D-H {-0.0717, -0.0811, -0.0856, -0.0768} vs Phi truth -0.0981; BEUR {-0.244, +0.090, -0.387}. Family ranges disjoint and each orbits its own truth (input-faithful), but the zeta scatter STRADDLES Xi with no lambda trend, and the fake scatters at O(1) with a sign flip.
- **T3b (rate-free Cauchy read on K = [0,6])**: consecutive projective gaps: zeta {0.267, 0.238, 0.432} (non-monotone, ends UP); D-H {0.035, 0.014, 0.023}; BEUR {0.750, 1.039}. The D-H family, whose true limit is off-class at gamma ~ 85.7, is the MOST Cauchy-coherent of the three by an order of magnitude (max D-H gap 0.035 < min zeta gap 0.238; [ADVERSARY] also true raw, without the ghost quotient: zeta {1.34, 1.20, 1.13}). In-sample coincidence fails for zeta and this function-face surrogate is blind to which family's limit satisfies RH.
- **T3b addendum [ADVERSARY], the blindness claim face-scoped**: a surrogate hunt across the computed data found the ordering is NOT universal. Faces where D-H is cleanest: function-face projective gaps (T3b, raw and quotiented), a2/a4 relative scatter, m-proxy gaps, raw mass-escape, r95/esc stability. Faces that order ZETA first: the eps trajectory (max consecutive gap 3.4e-4 vs D-H's 8.6e-3 on the full grid, a 25x separation, though it FLIPS 7x the other way if the lam = 2.2 point is dropped, so it is margin data, not limit-identity data) and the T1f gauge-positivity face (zeta below D-H at every grid point). Face that is mixed: normalized gauged-measure Kolmogorov gaps. So "RH-blind" is a correct verdict on the function/germ faces the trojan's link (ii) actually needs, not on every surrogate; no face certifies coincidence for zeta, which is the load-bearing negative.
- **T3c (truth-anchored floors, diagnostic)**: zeta to Xi distances {0.264, 0.0023, 0.290, 0.106}: the closest build is the DRESSED 2.6 branch (ghost-quotiented), not the largest lambda: convergence is branch-selected, the e1n dressing-migration finding recurring on the compact-set face. D-H stays 3-10x closer to Phi_DH than to Xi at every lambda.
- **T3d (Carleman uniformity read)**: R6 moment radii grow with the window inside every family (zeta 19.8 to 56.8 = 2.9x; D-H 31.0 to 49.9 = 1.6x; BEUR 35.1 to 57.0 = 1.6x) and R6/phiN has no family-uniform profile (0.32..0.73). Each fixed-lambda object is trivially determinate (compact support); the lambda-UNIFORM Carleman input that link (ii) needs is exactly what the family does not certify.
- **T3e (canonical-germ read, surveyor fold-in)**: the type-subtracted m-proxy m_red = T'/T (which carries NO exponential type, by the structural split xihat = sin(zL/2) T(z)) is no more Cauchy than the function face: consecutive sup gaps on [1,10] + 1.5i are zeta {0.504, 0.509, 0.541} (flat at ~0.52 on a scale ~1), D-H {0.426, 0.323, 0.314}, BEUR {1.024, 0.934}; at Im = 0.8 all are ~1 (zero-microstructure-bound). The mass-escape read (the surveyor's no-mass-escape clause): in the RAW gauge D-H is the only family with a stable no-escape profile (esc12 <= 0.063, decreasing to 0.002); zeta is branch-fragile (0.027/0.031 clean vs 0.567/0.999 dressed); BEUR erratic (0.36/0.96/0.62). [ADVERSARY] The zeta branch-fragility is a GAUGE ARTIFACT: in the T1f quotient gauge zeta's escape is <= 0.031 family-wide (dressed builds 0.002/0.005), so the raw dressed escape was ghost dressing, and the no-mass-escape clause joins positivity as gauge-relative. Both germ-level readings remain branch-dominated or face-scoped-blind.

**Q3 verdict (measured, ADVERSARY-scoped)**: SPLIT. Per-object determinacy trivial; every lambda-uniform input (tight moments, Cauchy trend, stable germ) is absent in-sample; the coherence ordering is blind on the function/germ faces (the RH-false twin is cleanest there), with the eps and gauge-positivity faces ordering zeta first (neither is a coincidence certificate). Link (ii) relocates the uniformity joint, unchanged.

### Q4 KILL TESTS (T4a-T4c)

- **T4a DMV screen: FIRED.** The compactness-certification vector (reality certified, tail exactness < 1e-12, type ratio, collapse direction) is indistinguishable between zeta and the density-matched fake (type-ratio difference 0.034; both collapse monotonically; both tails exact; both certify real). Links (i)-(ii) consume only density/growth data: structurally non-discriminating, per the DMV screen exactly as designed. Discrimination within this vector sits in the input-typed gates of link (iii); [ADVERSARY] the T1f gauge-positivity output is the one measured exception (fneg_q separates zeta 0.028 / D-H 0.05+ / BEUR 0.24+ in-sample) and is excluded from the vector as gauge-relative and in-sample-trending, a choice now stated rather than implicit.
- **T4b K1: clean.** Source scan clean (numpy allocation exempted with the WHY recorded), runtime guards installed and never tripped, per-test ledger printed in-run. Nothing RH-equivalent is assumed anywhere; the tripwire in T4c additionally raises if all three links ever report certified-with-content.
- **T4c relocation accounting** (the honest outcome of the trade):

| link | status | where the RH content relocates |
|---|---|---|
| (i) compactness | NOT certified with content | type divergence kills fixed-class Montel; the type-rescale is compact but collapses to the constant: content moves into the lambda-uniform CONTROL of the collapse (the #160 growth clause; [ADVERSARY note] tau_lambda's VALUE is itself family-blind at the ratio level per T4a, diff 0.034, so the content is in the rate control, not the scalar); measure-face positivity/tightness gauge-relative (raw-fragile per T1e, revived family-separating in the FE-gated quotient gauge per T1f) |
| (ii) determinacy | NOT certified | window-tracking moment radii + non-Cauchy branch scatter + face-scoped-blind coherence ordering (D-H cleanest on the function/germ faces): the lambda-uniformity joint, unchanged |
| (iii) pin | open clause = M4 | lattice-consuming (theta fuel, lattice clause, FE type); by #160 the pin's open clause is conditionally EQUIVALENT to the identification (given the limit + growth package, per e1m) |

## Surveyor fold-in: the canonical-system route (deferred, with its germ read done)

Mid-round SURVEYOR input (folded in where cheap; the pre-registered Q1-Q4 were not restructured): among candidate compact positivity classes, the one compactness that is free, type-divergence-proof, and reality-preserving is the space of trace-normed canonical systems / de Branges chains (trace-normed Hamiltonians form a compact metric space with the Hamiltonian-to-Weyl-m map a homeomorphism; Remling, cf. Forester-Remling arXiv:2409.04862). Laguerre-Polya closure is verbatim the Hurwitz frame (no gain); bounded-type normal families die on the measured type divergence (Q1 here is CONSISTENT with that survey); Lorentzian cones die on degree growth. In the canonical-system encoding the RH content relocates into exactly two clauses: (a) identification of the limit chain (= the #160 pin, the lattice site) and (b) a non-degeneracy / no-mass-escape clause for the limit Hamiltonian (the Euler site).

What was cheap in this harness (T3e): a type-subtracted Weyl-m proxy (the structural split xihat = sin(zL/2) T(z) puts ALL the diverging type in the sin factor, so m_red = T'/T is type-free with a = L/2 structural, not fitted) and a mass-escape indicator on the coefficient measure. Measured: the germ-level m read is zero-microstructure-bound (gaps flat ~0.52 for zeta at Im = 1.5 on scale ~1; D-H again most coherent at 0.31-0.43; BEUR ~1.0) and the no-mass-escape clause is branch-dominated and RH-blind (D-H cleanest at <= 0.063; zeta dressed escapes at 0.57-0.999; BEUR erratic). So the canonical route's power, if any, is NOT visible at single-lambda germs; it would have to live at the chain level (nesting in N, which e1s proved is exact Cauchy compression: D_log^(N) is the exact central block of D_log^(N+1)).

**Deferred rung (e1u, designated next)**: the trace-normed canonical-system embedding proper: build the finite Hamiltonian chain from the nested matrices, trace-normalize, and measure whether the CHAIN data (not the germ) behaves Cauchy while raw type diverges, with the mass-escape clause posed on the limit Hamiltonian. Deferred because the honest embedding (de Branges structure Hamiltonian from the reproducing kernel of the PW space weighted by the Weil form) is a new construction, not a read of existing caches.

**Surveyor's pre-registered adversary question, recorded**: whether the non-degeneracy clause of the canonical route is just the uniform det-class control in Hamiltonian coordinates (reformulation, not reduction), i.e. whether e1u would end where e1m ended (REFORMULATED, NOT REDUCED). Also recorded: the ghost-quotient step is load-bearing for any de Branges reading because the raw finite objects certify only into kappa <= 1 classes (an indefinite inner-product index), not kappa = 0, until the low-band dressing is removed; and this probe's T1e/T3c show the quotient is exactly the branch-sensitive step.

## Discipline outcomes

- **D-H (form-side control)**: enters through identical code at all four cutoffs. Certifies REAL everywhere (T2a), is the MOST Cauchy-coherent family on the function/germ-face surrogates (T3b function face, T3e m face, T3e raw mass escape; [ADVERSARY scope] not on every face: the eps trajectory and the T1f gauge-positivity face order zeta first, and the gauged-measure Kolmogorov face is mixed), and its true limit Phi_DH is off-class at gamma ~ 85.7. This is the trojan's structural kill: finite in-class certification plus free subsequential limits CANNOT imply the limit is the completed L-function, because the RH-false twin passes the function-face coherence certificates more cleanly than zeta. D-H's exits are input-typed (UNPOSABLE at the Euler gate T2b, excluded by FE type at the pin T2d) plus the in-sample T1f output separation. The #158 quarantine (discrimination lives only in the uniform limit) is confirmed on the compactness face, with T1f recorded as the one gauge-conditional, in-sample exception.
- **Beurling (counting-side control)**: builds and certifies identically (T0c tail exact, T1a type ratio 0.97-1.00, T2a reality 3/3, T1d collapse monotone). The DMV screen FIRES (T4a): the compactness half consumes only density/growth data. The fake fails NAMEABLY at the lattice: the ghost gate (no FE, T1b), the low-band frequency displacement 0.033 off {log m} (T2b), and the theta-FE fuel defect 0.37 vs zeta's 1.5e-36 (T2c). Note the fake's Weil form is O(1)-indefinite (eps ~ -0.9..-1.4): a density-level mismatch of the coarse matching, reported as such and excluded from the certification vector. [ADVERSARY, measured] The exclusion was the right call: rebuilding the fake at matching eps = 0.05 and 0.01 (lam 2.2, N 12, same seed) gives ground eps = -0.132 and -0.025, so the Weil margin SCALES with the matching coarseness (it is density data, approaching zeta's basin as the comb approaches the true lattice, and both finer fakes still certify REAL). The quantity that does NOT scale down is the measure-face alternation: fneg_alt stays ~0.31 at eps = 0.01 (vs zeta's 0.026 at the same config), so the lattice-sensitive bit on this face is the alternation structure, not the Weil margin.
- **K1**: runtime guards installed on `mp.zetazero` and the D-H scanner, never tripped; source scan clean; ledger printed per test; overclaim tripwire armed in T4c (a state where all three links certify with content raises, because a finite probe proving Section 7 would be circular, not successful).

## Honest limits

1. **Faithful reimplementation, not the paper's operator** (e1k caveats inherited): razor-thin zeta margin, imperfect pole term, dps-25 branch. The dressed/clean branch dichotomy (e1n #161) DOMINATES several Q3 readings; branch scatter is not lambda noise, it is the near-degenerate ground-state family's branch selector, and this probe's distances quantify how badly it pollutes any naive limit read.
2. **Thresholds pinned, not pre-registered**: all numerical cutoffs in the checks were set from a calibration run of the same code. The check SHAPES follow the pre-registered task spec; the numbers do not carry inferential weight beyond "the measured values sit where the table says".
3. **Small grids**: 4 zeta, 4 D-H, 3 BEUR cutoffs; N and lambda co-vary along the grid (as in e1m/e1n), so lambda trends and N trends are not separated. Two-point "trends" (zeta clean pair) are levels, not laws.
4. **The coarse fake**: eps = 0.25 density matching is loose. [ADVERSARY: ANSWERED] Rebuilt at eps = 0.05 and 0.01: the Weil margin scales to -0.132 and -0.025 (coarseness, not lattice structure), reality still certifies, and the measure-face alternation persists at ~0.31; see the Beurling discipline paragraph. A Diamond-style exact-density system remains the residual finer control.
5. **Negative results here are in-sample**: the absence of a Cauchy trend or a uniform Carleman bound at lambda <= 3.6 does not prove their asymptotic absence; it proves the finite family does not CERTIFY them, which is the trojan's load-bearing question (the whole point was to avoid needing the asymptotics).
6. **The m-proxy and the mass-escape are germ reads**, not the trace-normed chain compactness the surveyor designated; the canonical route is deferred (e1u), not killed.
7. **Q1c positivity used the |c| total-variation gauge and the (-1)^n alternating gauge**. [ADVERSARY: ANSWERED, T1f] The FE-gated ghost-quotient gauge makes the ZETA measure face near-positive and tight family-wide (dressed builds 0.0002/0.0005 negative mass) while D-H stays >= 0.05 and BEUR >= 0.24: the Helly positivity clause revives for zeta, gauge-conditionally, with the mass-collapse and in-sample caveats priced in T1f. Also noted: "any weighting computable from the object alone" is ill-posed as a criterion (the trivial sign gauge w_n = sign(a_n) is object-computable and makes anything positive); the honest criterion, used by T1f, is sign-fixed + invertible + object-computable.

## Handed forward

1. **e1u (designated next rung)**: the trace-normed canonical-system / de Branges chain embedding of the nested D_log matrices (Cauchy-compression nesting is exact per e1s), with the surveyor's two-clause relocation (identification = lattice site; non-degeneracy = Euler site) posed as measurements, and the pre-registered adversary question (reformulation-not-reduction) attached.
2. **The adversary gauge sweep on the measure face** (honest limit 7): [DONE by ADVERSARY, T1f] the FE-gated ghost-quotient gauge revives Helly positivity for zeta, family-separating in-sample. What remains open and is handed to e1u: whether the gauged separation (zeta 0.028 vs D-H 0.05+) persists or closes as lambda grows, and whether the mass collapse on deep-dressed branches (sum|aq| down to 0.0017) can be controlled without consuming the identification.
3. **A finer-matched Beurling fake** (honest limit 4): [DONE by ADVERSARY at eps = 0.05 and 0.01] the Weil-margin indefiniteness is matching coarseness (scales to -0.025); the alternation is the lattice-sensitive residue (~0.31 at eps = 0.01). Diamond-style exact-density remains untested.
4. **The branch selector**: T3c's branch-selected convergence (dressed 2.6 at distance 0.0023 from Xi on [0,6], 100x closer than the clean builds) repeats e1n's finding on a new face; if dressed branches are generally `dressing x Xi`, the Section-7 statement is a dressing-migration statement, and the ghost gate (FE-typed here) becomes a load-bearing normalization: worth its own probe. [ADVERSARY reinforcement] T1f independently confirms this: the quotient gauge makes the DRESSED branches the cleanest of all (0.0002/0.0005 negative mass), so dressing x (near-positive core) is now measured on the measure face too.
5. **The close-pair instrument note** [ADVERSARY: CORRECTED]: the "ZETA 3.0: 13 zeros vs 11 crossings" anecdote did not reproduce (both the plain phi/16 grid and the e1m offset grid count 13 crossings; the close pair 40.86/41.03 has separation 0.178 > the phi/16 step 0.131). The band-shrink winding certificate (0.4 vs 0.05) is kept for its in-principle grid-independence, not that observation. Cross-branch note: the ZETA 3.0 strip count is dps-branch-dependent (dps 15/25/35 give 18/13/12 zeros), each branch reality-certified with no zero at |Im| in (0.02, 0.05]; the "13" is a dps-25-branch number, inheriting e1n's branch caveat.

## ADVERSARY round summary (2026-07-22, PASS_WITH_FIXES)

Full record in [`_e1t_adversary.md`](_e1t_adversary.md). Catches banked: (1) T1e's no-positive-gauge claim was raw-gauge-only (T1f revival, the round's main find, now a tracked check); (2) the Q3 "RH-blind" phrasing was face-overbroad (eps + gauge-positivity counter-instruments recorded); (3) the close-pair "13 vs 11" anecdote falsified (13 = 13 on both grids; strip count dps-branch-dependent 18/13/12); (4) the T2b Euler gate is a type refusal with no downstream positivity consumer in this probe (bypass measured; the consumer is e1n T5); (5) the #160 equivalence citation dropped its conditionality (restored: given the limit + growth package); (6) the tau_lambda relocation wording contradicted T4a's own family-blind type ratio (content is in the rate control, not the scalar); (7) the lattice-clause threshold provenance was arithmetically off by 2x (it is the full spacing at n = 50). Attacks that did NOT land: harness bit-identity (verified exactly at a real config, T0d), quick/npz discipline (byte-identical npz, quick saves nothing), K1 (guards trip correctly, no side-channel found in the import graph), DMV screen (fires as designed; the finer fakes at eps 0.05/0.01 still certify REAL and vindicate the eps exclusion), T1d's dichotomy (the sin-quotient fourth normalization fails at link (ii), not link (i)), and the headline verdict itself: `trojan_trades_M4_away = NO` survives with links (ii) and (iii) untouched.

## Verification targets (for VERIFIER)

1. **The structural split**: xihat(z) = sin(zL/2) x (2 L^{-1/2} sum_n c_n/(z - phi n)) on the finite basis, and the exact tail xihat(phi m) = 0 for |m| > N (a finite algebraic identity over the basis functions; Lean-friendly).
2. **The collapse lemma (elementary)**: for F entire, even, F(0) = 1, of exponential type tau with zero set bounded away from 0 by r0, sup_{|w| <= R} |F(w/tau) - 1| -> 0 as tau -> inf whenever sum 1/|z_k|^2 is bounded and r0 tau -> inf faster than R (the measured T1d trend is the numerical shadow; the clean statement makes "compact but information-free" a theorem, not a reading).
3. **The gate lemmas (already part-landed)**: the Euler-gate inequality is #S4C-1 in `lean/ZetaRH/S4Carrier.lean`; the ghost gate's FE-typing (no FE => no own-RvM budget => no spurious classification) is a definitional statement worth a Lean-side type: certificates as functions that REQUIRE an FE token.
4. **The trojan no-free-lunch shape**: from Hurwitz (limits of real-zero entire functions, locally uniform, are real-zero or identically zero) + the existence of the D-H family (finite-real, limit off-class), derive formally: finite in-class certification + subsequential limits do NOT imply limit in-class without uniformity ON THE COMPACT CONTAINING THE OFF-CLASS ZERO. This is the compactness-face form of the #158 quarantine and should be a short formal statement over abstract families.

## Adversarial test cases (posed for ADVERSARY, with the round's outcomes)

1. **Break T1d**: find a normalization (not in {a, b, c}) that bounds the type WITHOUT collapsing the limit: any candidate must beat the measured dichotomy (bounded type + non-constant limit + posable for zeta without truth data). [OUTCOME: attack did not land] The natural fourth candidate, the sin-quotient T = xihat / sin(zL/2) (type-free by the structural split, non-constant, truth-free), was measured: its projective gaps on [1,10]+1.5i are zeta {0.46, 0.56, 0.54}, D-H {0.21, 0.18, 0.27}, BEUR {0.70, 0.42}: no in-sample limit exists, so it fails the "non-constant LIMIT" clause at link (ii) rather than link (i). T1d's dichotomy survives with its scope sharpened: bounded type is purchasable, a certified non-constant limit is not.
2. **Break T3b's blindness claim**: [OUTCOME: landed as a scoping kill] two counter-instruments order zeta above D-H (the eps trajectory on the full grid, 25x by max consecutive gap, fragile to dropping lam = 2.2; the T1f gauge-positivity face, at every grid point), and the gauged-measure Kolmogorov face is mixed. The absolutist "NONE exists" phrasing is dead; the face-scoped claim (function/germ coherence faces are D-H-cleanest, and no face certifies zeta coincidence) survives and is what the .md now says.
3. **The gauge sweep**: [OUTCOME: landed, T1f] the FE-gated ghost-quotient gauge drives the dressed builds to 0.0002/0.0005 negative mass (better than the target 0.05 by two orders); T1e re-scoped to raw gauges. The criterion "any weighting computable from the object alone" was also found ill-posed (trivial sign gauge); replaced by sign-fixed + invertible + object-computable.
4. **The eps question**: [OUTCOME: builder's exclusion vindicated] eps = 0.05/0.01 rebuilds give ground eps -0.132/-0.025: the Weil margin is matching-coarseness and the T2a exclusion stands; the lattice-sensitive residue is the alternation (~0.31 at eps = 0.01).
5. **Reproduce the close-pair diagnosis**: [OUTCOME: anecdote falsified, certificate robust] 13 crossings on BOTH grids (the "11" did not reproduce; the pair 40.86/41.03 is resolved at phi/16); winding at bands 0.4/0.05/0.02 all give 13 at dps 25; dps 15/35 rebuilds give 18/12 zeros respectively (branch-dependent count), reality-certified at every branch, with zero hits in |Im| in (0.02, 0.05] anywhere.
