# E2AP: testing SP2 at finite scale (certificate, Euler sourcing, design matrix)

**Date**: 2026-08-20. **Status**: BUILDER round, executed; 15/15 checks (54 s). **Code**: [`e2ap_sp2_tests.py`](e2ap_sp2_tests.py). **Data**: `e2ap_sp2_tests.npz` (tracked, evidence rule). **Prior art**: [`weil_positivity_prior_art_sweep.md`](../../docs/03_research/reading_notes/weil_positivity_prior_art_sweep.md) (run the same day; placements below are cited from it). **Parent**: #179's object and the "how can we test SP2" question; executes backlog items A1 and the Euler half of A2.

## 0. What was asked

SP2 (the endomorphism) cannot be confirmed by any finite test: its completeness clause is RH, a $\Pi^0_1$ sentence, and #174 closed the prime-side observable route. What tests CAN do: certify at scale, falsify candidates, and isolate which inputs each clause consumes. Three parts do those three things.

## 1. Part A: the completeness-at-scale certificate

The #179 measurement ("29/29 zeros found, none spurious") upgraded to a certificate. Two transforms (M and its $\tau$-derivative, one extra FFT) plus a five-term explicit error budget (lattice truncation exactly zero at this window; left tail $2.5\times10^{-13}$ anchored on the measured Poisson remnant with 10x safety; right tail closed-form $5.7\times10^{-18}$; rectangle aliasing $3.7\times10^{-8}$ by fourth-derivative bound; fp $1.4\times10^{-10}$) give a Taylor floor per grid cell,
$$|M(\xi)| \ge |M(\tau_i)| - \tfrac{h}{2}|M'(\tau_i)| - \tfrac{h^2}{8}C_2 - E,$$
positive on **99.98 percent of [5, 100]**. The excluded set has **exactly 29 components** (= $N_{\mathrm{RvM}}(100)$), each containing one detected dip and (oracle validation) one true zero, and every true zero inside the excluded set. Certified statement: every zero of the object's multiplier in [5, 100] lies within the 29 flagged slivers (each about $\pm 5\times10^{-4}$ wide). Honest labels: the left-tail term is empirically anchored, not interval arithmetic (a VERIFIER upgrade path); the epistemology is Turing-method verification rebuilt inside the object's own coordinates (known in substance; the coordinate system is the contribution). One instrument lesson: an excluded grid point certifies nothing on its own half-cells, so components must be widened by $h$; the first run's containment failures were exactly that off-by-half-cell.

## 2. Part B: Euler sourcing on the S-finite ladder

For $P$-smooth lattices enumerated to $e^{50}$ (56 points at $P=2$ up to 745k at $P=13$), the object's transform equals the product of local Euler factors $\prod_{p\le P}(1-p^{-1/2-i\tau})^{-1}$ to **max rel err $3.9\times10^{-6}$** (truncation-tail limited, as predicted: the first run at $e^{38.6}$ measured $3.6\times10^{-4}$, matching the tail estimate). SP2(a) tested constructively: the multiplier is ASSEMBLED from local Euler data, rung by rung. And the zeros exist only in the limit: the $\gamma_1$ dip deepens monotonically ($0.586 \to 0.379 \to 0.268 \to 0.215 \to 0.169 \to 0.167$) while staying bounded away from zero at every finite $P$; no pole at any rung (zero density, $R = 0$ by construction); duality defect $\ge 0.23$ at every rung (no FE at finite $P$). The S-finite column of the conservation law: Euler passes exactly, every lattice cell fails. In-print home (prior-art sweep item 6): Gonek 2007 (RH iff short truncated Euler products approximate $\zeta$), GHK's hybrid product; this part is a finite bracket-typed instance, framed as such.

## 3. Part C: the Cramer control (backlog A1: the design matrix completed)

Lattice $\nu_n = n + u_n$, $u_n$ iid uniform$(-0.45, 0.45)$, seed 181: counting $x + O(1)$ EXACTLY, multiplicative structure destroyed. Measured: residue $R = 1.0005$ with exponent $0.500$ (the pole is pure density: PASS); duality defect $0.460$ against zeta's $2.4\times10^{-11}$ (the FE is NOT purchasable by perfect density: the fourth clause survives its pre-registered kill); descent drift $6.9\times10^{-4}$: **strictly between** zeta ($2.1\times10^{-12}$) and Beurling ($5.1\times10^{-1}$), exactly as pre-registered (jitter degrades the Poisson cancellation to $\sim e^{s}$ without destroying convergence). The descent axis now has three measured positions, and the design matrix has all four corners: zeta (Euler+lattice), D-H (FE without Euler), Beurling (Euler without lattice), Cramer (density without either). No direct prior instance of this control surfaced in the sweep; neighbouring theory (Broucke-Debruyne-Vindas) knows counting underdetermines analytic structure.

## 4. Verdict and hand-off

Frontier UNMOVED (tests and controls; no new structure). Banked: the certificate (SP2 completeness certified to height 100 inside the object's coordinates), the constructive Euler-assembly identity with the condensation curve, the completed design matrix with the third descent position. Handed forward: (i) **B2c, the xi-convergence test** (from the same-day prior-art sweep: test Suzuki's conjecture (1.2), that the localized ground state's Fourier transform converges to $\xi(1/2+iz)$, using the 50-digit minimizer already built in `visualizations/research/make_figs.py`); (ii) the interval-arithmetic upgrade of the certificate's left-tail term (VERIFIER); (iii) the coupled ladder (push the certificate's height ceiling with the data meter grown in step).
