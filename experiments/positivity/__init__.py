"""Architecture 3: Direct positivity (Weil / Li).

The four experiments under this architecture (3A-3E) test whether the
positivity statements equivalent to RH (Li lambda_n >= 0, Weil quadratic
form Q(f) >= 0) hold computationally for zeta, and crucially fail for
the Davenport-Heilbronn control.

See ../PROOF_ARCHITECTURES_PLAN.md for context.
"""
