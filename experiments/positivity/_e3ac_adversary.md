# ADVERSARY report: e3ac entropic exports (the density-matrix costume), 2026-09-04

**VERDICT: PASS_WITH_FIXES.** The probe's computations are correct (every number in the dossier, LEARNINGS #218, ledger Section 11 and breadth screen 14 reconciles with the full-mode stdout; 12/12 full and quick reproduced; corpus suite 26/26; hygiene clean). The disqualifier verdict (frontier unmoved; entropic export has no sign to flip) survives every attack below and is in fact STRENGTHENED by one (the $t$-blindness is an exact local-unitary theorem, not a measurement). What does not survive is part of the framing: the "not-a-state corner" is a sign convention, the "never touches the strip" sentence is false for the natural quantum object, "RH-false at every $\lambda \ne 0$ below height 200" is contradicted by the pencil's own S3 table, and one corpus rule string claims a screen the machine does not fire. No BLOCKING item.

Reproduced: `python3 -m experiments.positivity.e3ac_entropic_exports` (12/12, 4.3 s), `--quick` (12/12, 0.5 s), `python3 -m experiments.lemma_db.test_breadth_corpus` (26/26), `python3 -m experiments.lemma_db.breadth_corpus` (runs; both new rows at $-4$, bottom of the ranking). Adversary computations quoted below were run at $N = 10^5$ (fold, $|a_B| = a_A$) and $N = 2\times10^4$ (pure-state table); they should be ported into the probe as gates rather than cited from a scratchpad (evidence rule).

## A. Mathematics

**1. [NOTE] A(i) Total correlation and the I-projection: correct, but G11 is a theorem, not a test.** `marginal_tables` builds $P(v_p = k)$ from the tail masses $P(v_p \ge k) = \sum_{p^k \mid n} p(n)$, which is exact; $n \mapsto (v_p(n))_p$ is a bijection so $H(n)$ is the joint entropy and $C = \sum_p H(v_p) - H(\text{joint}) = D(p \| \otimes_p \mathrm{marg}_p)$ (G12 measures $1.1\times10^{-16}$). But for EVERY product $q$ the Pythagorean identity $D(p\|q) = C + \sum_p D(\mathrm{marg}_p \| q_p)$ holds; I verified $D_\zeta - C = \sum_p D(\mathrm{marg}_p\|\zeta_p) = 0.19507$ to $5\times10^{-15}$. So G11 ("the marginal product is the nearest") cannot fail unless the code is broken, and the dossier's 3(e) sentence "the nearest Euler product to the Epstein state is its own marginal product, not $\zeta$'s and not the Euler product it was built from" is a textbook identity presented as a finding. Fix: in `e3ac_entropic_exports.py:454` rename G11 to "consistency: the Pythagorean identity $D(p\|q) = C + \sum_p D(\mathrm{marg}_p\|q_p)$ makes the marginal product the I-projection"; in the dossier line 58 add "(the I-projection theorem; the gate is a consistency check)".

**2. [NOTE] A(ii) "product state iff multiplicative": exactly right for $a_n \ge 0$, $a_1 > 0$.** Multiplicative (not completely multiplicative) is the correct condition: $p(n) = \prod_p f_p(v_p)$ with $a_{p^k} = f_p(k) p^{k\beta}/f_p(0)$. The truncation to $n \le N$ is handled honestly as the floor. One unstated fact worth one line at dossier line 15: at $\lambda = -1$ the state has $p(1) = 0$ ($r_{Q_1}(1) = 0$); the identification still works because the exponent-vector distribution is what is used.

**3. [NOTE] A(iii) The window $[-1, 1]$ is a theorem, and the endpoints are witnessed by $n = 1$ and $n = 2$.** Genus theory: $r_{Q_0} + r_{Q_1} = w\sum_{m|n}\chi_{-15}(m) = 2a_A$, $r_{Q_0} - r_{Q_1} = 2a_B$, so $r_{Q_0} = a_A + a_B$, $r_{Q_1} = a_A - a_B$ (G3 exact to $10^5$) and $a_A \ge |a_B|$ follows. "Exactly" needs the exit witnesses: $r_{Q_1}(1) = 0$ gives coefficient $-\epsilon$ at $\lambda = -1-\epsilon$ and $r_{Q_0}(2) = 0$ ($a_A(2) = 2$, $a_B(2) = -2$) gives $-2\epsilon$ at $\lambda = 1+\epsilon$; the probe only checks $\pm1.01$ numerically. Stronger and unrecorded: $|a_B(n)| = a_A(n)$ for EVERY $n \le 10^5$ (checked exactly; it is forced prime by prime: for $\chi_{-15}(p) = 1$ both give $k+1$, for $\chi_{-15}(p) = -1$ both give $[k \text{ even}]$, for $p \mid 15$ both give 1). Hence $r_{Q_0}, r_{Q_1} \in \{0, 2a_A(n)\}$: the state window is literally the genus sign pattern of $a_B$. This is the fact that undercuts finding 7 below. Fix: state it at dossier line 43 and use it in finding 7.

**4. [NOTE] A(iv) G2 identity and tolerance: identity correct; the comment's heuristic is off by a constant, tolerance safely loose.** $S = \log\zeta(\beta) - \beta\zeta'/\zeta(\beta)$ and $S_p = -\log(1-p^{-\beta}) + \beta\log p\, p^{-\beta}/(1-p^{-\beta})$ are right. The truncated-state deficit is not "mass $\times \log(1/\text{mass})$" (`.py:324-325`) but $-T + m(S-1)$ with $T = \sum_{n>N} p\log(1/p) \approx 3\log N/(2N^2\zeta(3)) = 1.5\times10^{-9}$ at $N = 10^5$, which is the measured $-1.5\times10^{-9}$ exactly; the mode-sum tail $3/(2N^2) = 1.5\times10^{-10}$ is also exact. Tolerance $30\log N/N^2 = 3.5\times10^{-8}$ is 23x loose. Fix: replace the comment with the two exact tails.

**5. [FIX] A(v) G9 is a straw man as designed, and "zero bits about zero location" is overclaimed on the pencil.** (a) $C \ge 0$, smooth, minimized at the floor near $\lambda = 0$, so $C(+\lambda) = C(-\lambda) + O(\lambda^3)$ for any such functional; the measured asymmetry $0.9\%/1.8\%$ at $0.01/0.025$ is the cubic term ($[C(+\lambda)-C(-\lambda)]/C(\lambda) \approx -0.75\lambda$). (b) The third clause of G9 (`.py:429`, `PENCIL_TSTAR[0.025]/PENCIL_TSTAR[-0.025] > 3`) tests a hard-coded dictionary; the gate computes nothing about zeros. (c) Within a one-parameter family $C \approx c_2\lambda^2$ pins $|\lambda|$, and $|\lambda|$ plus a sign pins the zero data exactly, so $C$ carries every bit but the sign, not zero bits. The fair statement is the FOLD, which I computed at $N = 10^5$, $\beta = 2$: $C(+0.1) = C(-0.096648) = 2.092622\times10^{-3}$ (census $T^*$: 20.737 vs the 13.79 pair, continuously off-line from $-0.1$ to $-0.05$ with $\beta_{\max}$ 1.305 to 1.015); $C(+0.05) = C(-0.049128)$ (43.391 vs 13.80); $C(+0.025) = C(-0.024761)$ (43.384 vs 13.805). Converse: the SAME off-line pair (43.38) is $T^*$ at $\lambda = 0.01, 0.025, 0.05$ while $C$ varies 19x. Fix: replace G9 by a fold gate (bisect $\lambda' < 0$ with $C(\lambda') = C(\lambda)$ for $\lambda = 0.025, 0.05, 0.1$, assert the census $T^*$ on the two sides differ by $> 1.5$x and that $T^*$ is constant across $0.01$ to $0.05$ while $C$ varies $> 10$x); replace "C carries zero bits about zero location" (`.py:42`, `.py:44`, corpus row text `breadth_corpus.py:285`) with "C is 2-to-1 on the pencil with different $T^*$ at equal $C$, and one $T^*$ pair persists across a 19x range of $C$: neither determines the other"; rewrite dossier line 56 last sentence and the `.py:49-51` docstring accordingly. The $T^*$ citations themselves are transcribed correctly (all 14 against S1 rows 19-33; the $d=-47$ values 24.66 / 32.05 against `negative_proof.md:101` and LEARNINGS 722).

## B. The readings

**6. [FIX] The natural quantum object was not run, and it falsifies two sentences while strengthening the verdict.** The vector state $|\psi_s\rangle = \sum_n a_n n^{-s}|n\rangle$ (rank-one density matrix) exists for $\sigma > 1/2$ for EVERY control, signed coefficients included ($\|\psi\|^2 = \sum|a_n|^2 n^{-2\sigma}$, which for $\zeta$ is $\zeta(2\sigma)$: the critical line is exactly its normalizability boundary, the Hardy space $H^2$ of Nyman-Beurling). Its reduced density matrix on mode 2 is NOT diagonal (max off-diagonal 0.2 to 0.4). Entanglement entropy across {mode 2 | odd part} at $N = 2\times10^4$, $\sigma = 0.75$: $\zeta$ $8.8\times10^{-3}$ (floor), $A$ $5.05\times10^{-2}$, $B$ $5.05\times10^{-2}$ (a STATE, and equal to $A$ because $|a_B| = a_A$ with multiplicative signs = a local unitary), Epstein $Q_0$ 0.624, $Q_1$ 0.665, D-H 0.346 (a STATE). And it is exactly $t$-independent: $n^{-it} = \prod_p (p^{-it})^{v_p(n)}$ is a product of local unitaries, so every entanglement measure of $\psi_s$ is a function of $\sigma$ alone (verified bit for bit at $t = 0, 14.135, 85.699$). Consequences: (a) dossier line 66 and ledger Section 11 line 108 "the object never touches the strip" is FALSE for the rank-one costume, which lives on $1/2 < \sigma$; (b) dossier line 62 (ii) "any quantum-information quantity of $\rho_\beta$ is a classical functional" is true of $\rho_\beta$ only; (c) the conclusion (reads the Euler axis only, blind to the zeros) survives in a stronger, theorem-grade form. Fix: add a paragraph "The rank-one costume" to dossier Section 4 with the table above and the local-unitary argument, port it as gates G13 (D-H and $B$ are states with $S_{\rm ent} > 0$ and $= 0$ resp.) and G14 ($S_{\rm ent}$ invariant under $t$ to $10^{-12}$), and rewrite "never touches the strip" as "the Gibbs state lives at $\beta > 1$ and the rank-one state at $\sigma > 1/2$; both are $t$-blind by local-unitary invariance, so neither can locate a zero even where it sits on top of one".

**7. [FIX] "State-hood is neither necessary nor sufficient" is a statement about a sign convention, and the "not-a-state corner" is an artifact of it.** With $|a_B| = a_A$ exactly (finding 3), $B$'s Gibbs state under the weight $|a_n|$ IS $A$'s state, and under the rank-one costume $B$ and D-H are states outright. The honest version: "positivity of the Dirichlet coefficients is neither necessary nor sufficient for RH" (necessity conditional on GRH for $\chi_{-3}, \chi_5$, or numerically to height 200 via the pencil's $\lambda = \infty$ end; the dossier's "under GRH" label is honest, the breadth-program line 222 "an RH-true Euler product" drops it and must not). Fix: dossier line 41 and `.py:42`: "coefficient positivity (the Gibbs convention) is neither necessary ... nor sufficient"; LEARNINGS #218 and Section 11 line 110: replace "the cleanest statement yet that state-hood and RH are independent coordinates" with "coefficient sign and RH are independent coordinates; under the rank-one costume every control is a state and the Euler axis is read by entanglement instead"; `breadth_program.md:222` add "(under GRH)"; `breadth_corpus.py:71` "in opposite RH classes" add "(B under GRH)".

**8. [FIX] "RH-false at every $\lambda \ne 0$ below height 200" is contradicted by the pencil's own table.** S3 row (48.003, 48.005) has $\lambda_c = +4.9\times10^{-4}$, "the closest below 200", so for $0 < \lambda < 4.9\times10^{-4}$ the pencil has NO off-line zero below 200. The census establishes the claim on its grid ($|\lambda| \ge 0.01$); "every $\lambda \ne 0$" is true at SOME height (joint universality of the four characters mod 15), not below 200. Sites: dossier line 43 "Inside the segment every $\lambda \ne 0$ has off-line zeros below height 200 (the census)"; LEARNINGS line 11 "RH-false at every $\lambda \ne 0$ below height 200"; ledger line 106 same phrase; `breadth_program.md:221` and `breadth_corpus.py:65` "RH-false throughout except $\lambda = 0$" (acceptable only if read at some height). Fix: "RH-false at every census point $|\lambda| \ge 0.01$ below height 200, and at every $\lambda \ne 0$ at some height (joint universality); the smallest Lehmer threshold below 200 is $4.9\times10^{-4}$".

**9. [FIX] The #121 regime sentence is false for the pencil, and the corpus does not fire #121.** Dossier line 64, ledger line 108 and LEARNINGS line 11 say the state lives "at $\beta > 1$, where the L-function has no zeros at all". At $\lambda = -0.25$ the Gibbs state exists at $\beta = 2$ (min coefficient 0) while the census certifies a zero at $\mathrm{Re}\,s = 1.895$, height 4.256; at $\lambda = -0.5, -1$ the pencil places 22 pairs near $\sigma \approx 2.6$. Only Euler products (and $\zeta$) are zero-free in the state's half-plane. This makes the finding sharper, not weaker: the state and the zeros coexist in one half-plane and the entropy still does not see them. Separately, `breadth_corpus.py:281` tags `regime="Re(s)>1"`, which `battery()` (lines 398-416) does not key on; the machine fires only #48 and #218 on the Gibbs row, so the rule string at line 282 ("+ special-value regime (#121)") and "Three screens fire" (dossier line 64, LEARNINGS, `breadth_program.md:225`) are not machine-backed. Fix (minimal, keeps consistency with the KMS row at line 314 which also uses "Re(s)>1"): rule string "e3r polarity (#48) + entropic export (#218); regime Re(s)>1 (the #121 tier, by hand)", and "Two machine screens fire (#48, #218); the regime observation is the #121 tier"; reword the regime sentence to "where an Euler product has no zeros (the pencil's negative side has certified zeros at $\mathrm{Re}\,s = 1.895$ inside the state's own half-plane)". Alternative: tag `regime="special-value"` (fires #121, debits 3 more) and add `assert any("#121" in r for r in battery(row.skel))` to `test_e3ac_corpus_rows_disqualified`.

**10. [NOTE] "Diagonal shadow of costume 2": fair, not contradicted, but a truism as stated.** Every density matrix is $e^{-\beta H}/Z$ for some $H$, so "a density-matrix costume is costume 2 at a temperature" is definitional. The repo agrees: the KMS type III$_1$ row (`breadth_corpus.py:313`, the $\beta \le 1$ non-diagonal state) is already DISQUALIFIED "blind to the strip", and NP-1 (#111's open continuation question) was resolved NO in #112. The sentence should be broadened per finding 6: the non-diagonal option is not only "the shift or the scaling flow"; the rank-one state is diagonal in no prime-adapted basis and is still Euler-only and $t$-blind. Also, per #111's load-bearing adversary correction, the Gibbs state DETERMINES $L$ (hence every zero) by continuation; the honest phrasing is the representation one: "operations internal to the state (entropies, correlations) are strip-blind; the state itself carries every bit". Fix: one sentence at dossier line 66 and ledger line 108.

**11. [NOTE] Beurling reading: a correct but vacuous use of the discipline.** $C = 0$ on any product state whatever the prime logs, so the fake's row is a tautology, not a test; the discipline (CLAUDE.md, e2ak) is built to kill constructions that CLAIM RH leverage, and $C$ claims none. The sentence "cannot see the additive lattice" is true on product states only (for non-product states $C$ depends on $\{\log p\}$ through the weights). Fix: dossier line 62 "(the fake is a product state, so $C = 0$ by definition: the discipline is satisfied vacuously, as it should be for a detector that claims nothing)".

**12. [NOTE] Small overstatements in Limits and 3(b).** "decays like $1/N$" holds at $\beta = 2$ only; measured slopes $0.44 / 0.96 / 1.98$ at $\beta = 1.5 / 2 / 3$, i.e. $N^{-(\beta-1)}$ (dossier lines 41, 73). "Plateau rows converged to three digits at $N = 10^4$" holds at $\beta = 2$; at $\beta = 1.5$ $Q_1$ moves $0.6665 \to 0.6628$ and $d=-47$ non-principal $0.4285 \to 0.4224$ (dossier line 73). Fix: add "at $\beta = 2$" to both.

## C. Corpus encoding

**13. [NOTE] Skeleton values defensible; two conventions worth recording.** Gibbs row: `polarity="unconditional"` (G-list: $C \ge 0$ always), `dh_engages=1` (the state is a product iff Euler, and D-H is separated by the Euler axis in both costumes) are right. ACS row: `dh_engages=0` fires the D-H screen, whereas #177 says the discipline is UNPOSABLE there ("type refusal, not a pass"); the corpus has no 'unposable' value and the Hirzebruch/Witten rows use the same convention, so consistent, but the rule string should say "D-H unposable (no L-function consumed)" rather than imply a failed test. `export_type="torsion"` for ACS is right (value group $\mathbb{Q}/\mathbb{Z}$ or $\mathbb{Z}_p$, #177).

**14. [NOTE] Screen 14 cannot kill a signed export by construction, but "entropic vs signed" is a self-declared tag and needs a scope clause.** The screen only fires on rows tagged 'entropic'/'torsion' (defaults 'na'): the Witten index row, log-determinants, and the Killip-Simon register are untouched today. Two risks: (a) a candidate in which an entropy is an INTERMEDIATE of a signed identity (Killip-Simon sum rules: relative entropy $=$ eigenvalue sum $+$ constant, the repo's named sequence-level template #170/#172) could be mis-tagged 'entropic' by its ingredient; (b) gaming: any $Q \ge 0$ becomes "signed" as $Q - c$. The stable discriminator is the measured #48 polarity on the control cube, which screen 14 pre-empts before measurement. Fix: in the `export_type` comment (`breadth_corpus.py:122`) add "tag the object HANDED TO M4 as the polarization, not an internal intermediate (a sum rule with an entropic term is 'signed' if its export is the signed identity); 'signed' is provisional until the #48 polarity is measured on the cube".

## D. Hygiene

**15. [FIX] e3z still prints the retired label at runtime.** `e3z_multiplicativity_obstruction.py:147-148` prints "Eps47-principal ... (RH-TRUE)" and "the RH-true (Epstein) non-Euler controls" after the docstring correction. Fix: "(RH-FALSE since #217; see docstring CORRECTION)" and "both non-Euler controls are RH-false; the defect detects NON-EULER-ness".

**16. [NOTE] Untracked artifact.** `experiments/positivity/e3ac_entropic_exports.npz` is untracked; the convention is tracked npz next to the script (the dossier cites it). `git add` it at commit time. Cosmetic: gates print G8 before G7 and G12 before G11.

Clean: no em dashes in the diff or the two new files; no control characters; no fatalistic phrasing; every relative link in the dossier, LEARNINGS #218, ledger Section 11, breadth_program screen 14 and the ACS "BANKED" note resolves; TODO pointer correct; `breadth_corpus` still runs; 26/26.

## K1-K4 and disciplines

| Test | Result |
|---|---|
| D-H | Type refusal under the Gibbs convention; under the rank-one costume D-H is a state with $S_{\rm ent} = 0.35$ (Euler axis). Separation is the known necessary-not-sufficient axis; no RH leverage claimed. PASS as a disqualifier. |
| Beurling | Vacuous PASS ($C = 0$ on any product state). |
| K1 | Not applicable (no RH-equivalent claim). |
| K3 | For a curve over $\mathbb{F}_q$ the Gibbs state is a product over closed points, $C = 0$, and no entropic functional sees $|\alpha| = \sqrt q$: inert, as a disqualifier must be. Consistent. |
| Marginal-positivity prior | Satisfied: the probe claims nothing soft; the one theorem-grade addition (exact $t$-blindness) points the same way. |

## Number reconciliation (stdout vs text)

All quoted values match the full-mode run: G1 $2.2\times10^{-16}$; $S_\infty = 0.678502221866$, modes $-1.5\times10^{-10}$, truncated $-1.5\times10^{-9}$; control table (all 21 entries); separation 20790; D-H $(1, 0.2841, -0.2841, -1, 0)$; $\min a_B = -36$, $a_B(2) = -2$; window probes $-0.36 / -0.32$; $C(\lambda)$ table (all 17 rows) and $T^*$ (all 14 against S1); asymmetry $0.018 / 0.009$; slopes $1.99 / 2.01$; floor $6.5\times10^{-6}$; $D = 0.2614 / 0.4564 / 0.4631$, $|D - C| = 1.1\times10^{-16}$. The only text-vs-machine mismatch is finding 9 (#121 not fired).

## Re-check (2026-09-04, after the fixes were applied)

**VERDICT: PASS.** Every FIX and NOTE item above is applied and the applied versions are correct. Reproduced: `python3 -m experiments.positivity.e3ac_entropic_exports` 14/14 (12.3 s), `--quick` 14/14 (1.6 s); `test_breadth_corpus` 26/26; `breadth_corpus` runs with both new rows at $-4$; the edited e3z runs end to end and prints the corrected labels.

**Confirmed, as asked.**
- Numbers: every value in the new dossier (status, 3a tails, the 3b table, 3c witnesses and $\lambda_c = 4.9\times10^{-4}$, the 3d fold table with $\lambda' = -0.024761 / -0.049128 / -0.096648$, common $C$ $1.398\times10^{-4} / 5.357\times10^{-4} / 2.093\times10^{-3}$, ratios 1.74 / 3.14 / 1.50, spread $2.5\times10^{-4}$, 25x / 19x raw; 3e $0.2614 / 0.4564 / 0.4631$ with residuals $1.3\times10^{-15}$ and $3.6\times10^{-16}$; the 3f rank-one table, norms and $S_{\rm ent}$ for all eight controls, $t$-deviation $2.5\times10^{-15}$; Section 7 gate list), in LEARNINGS #218, in ledger Section 11, in screen 14, in the corpus docstring and row notes, and in the module docstring matches the new stdout. The derived quantities quoted without a print line (asymmetry 0.9 / 1.8 percent; slopes $0.44 / 0.96 / 1.98$ for $N^{-(\beta-1)}$) follow from the printed table.
- Gate code read, not just its PASS line: G3 asserts `np.array_equal(np.abs(a_B), a_A)` and `r_Q1[1] == 0 and r_Q0[2] == 0` (the exit witnesses) as exact integers; the fold bisects on $[-0.3, 0]$ in the correct direction (monotonicity from G10), 26 halvings, and its bracket convention takes the LARGER census $T^*$ around $\lambda'$, so every ratio is conservative; the converse uses floor-subtracted $C$; `rank_one_entanglement` builds $M[k, (m-1)/2] = \psi(2^k m)$ and takes the spectrum of $MM^\dagger$, which is the mode-2 reduced density matrix, with the norm reported before normalization ($\zeta$: 2.5982 against $\zeta(1.5) - 2/\sqrt{N}$); G13's $B = A$ check is at $10^{-12}$ and G14's at $10^{-10}$ against a measured $2.5\times10^{-15}$; G12 checks the Pythagorean identity against independently built local factors, matching my own $0.195$ for $D_\zeta - C$.
- Hygiene: no em dashes and no control characters in any added line of the ten touched files or in the two untracked new files; all nine relative links in the new text resolve (including the re-pointed `../../experiments/lemma_db/breadth_corpus.md` in `breadth_program.md` and the two links to this report).
- The phrase "RH-false at every $\lambda \ne 0$ below height 200" is gone from every site (repo-wide search); all four sites now read "at every census point $|\lambda| \ge 0.01$ below height 200 (and at some height for every $\lambda \ne 0$)". The old "three screens fire" and "12/12" are gone.

**Residuals (all NOTE, none blocking).**
1. `experiments/LEARNINGS.md:111` contains a control character (BEL, a `\a` inside `\alpha` in the #172 entry); it is pre-existing (`HEAD` line 109), not from this package. Worth a separate one-character hygiene fix.
2. `docs/03_research/trojan_horse_m4.md:13` (the Section 2 costume-1 row) still says "exists exactly on the RH-false segment"; harmless at the theorem level, but for consistency with the corrected sites: "the segment between the two Epstein forms of $d = -15$ (RH-false off $\lambda = 0$)".
3. Dossier line 41: "at $\beta = 1.5$ the last digit still moves" understates it; $Q_1$ moves $0.667 \to 0.663$ (third significant digit). Suggest "the third digit still moves".
4. `e3z_multiplicativity_obstruction.py:154` still prints "it is a KMS mixture c1 phi^(1) + c2 phi^(2)" for D-H, the pre-correction reading the docstring CORRECTION disowns (signed weights: not a Gibbs state; a rank-one state with entanglement 0.35). One-line fix.
5. The $\zeta$, $\beta = 3$, $N = 10^5$ entry of the control table is at float64 noise ($2.07\times10^{-11}$ this run, $3.71\times10^{-11}$ in the previous code version): two entropies near 0.68 cancel to $10^{-11}$. Not quoted anywhere and G5 uses $\beta = 2$ only; a comment in the table footer would prevent a future reader from reading it as a measurement.
6. Artifacts: `e3ac_entropic_exports.npz` is untracked and should be added at commit. My e3z re-run overwrote the tracked `e3z_multiplicativity_obstruction.npz`; I restored it with `git checkout` (the png is gitignored).
