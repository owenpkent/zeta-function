# Direction 12: RH as exact criticality (a variational proof that the de Bruijn-Newman constant is zero)

> **Status: speculative, proposed 2026-05-30.** Turns the project's marginal-positivity thesis from a mood into a theorem target.
>
> **Headline.** Rodgers-Tao (2020) proved the de Bruijn-Newman constant $\Lambda \ge 0$, and $\mathrm{RH} \Leftrightarrow \Lambda \le 0$, so $\mathrm{RH} \Leftrightarrow \Lambda = 0$. That is *literal criticality*: $\zeta$ sits exactly on a phase boundary. Prove RH as a **statistical-mechanics criticality theorem** by exhibiting a free-energy functional whose unique critical point is $\zeta$ with no symmetry breaking. Promotes the project's Level-3 multifractal thread to a Level-4 tool.

> **UPDATE 2026-06-02 (E_DBN1 ran; see [`experiments/criticality/e_dbn_kernel.md`](../../../experiments/criticality/e_dbn_kernel.md), LEARNINGS #38).** The cheap "first step" of probing the $t=0$ object directly was run, in the sharpest form: the **Pólya kernel positivity $\Phi\ge 0$** (de Bruijn's input). Verdict: $\Phi\ge 0$ is **K2-FAILING and orthogonal to RH** — the RH-false Davenport-Heilbronn passes it identically to $\zeta$ (clean degree-1 comparison), because an off-line zero at height $\gamma$ is archimedean-suppressed in the kernel to $\exp(-\tfrac\pi4 d\gamma)\sim10^{-29}$ (the stealth window in the heat basis). This **rules out the $t=0$ shortcut and vindicates this direction's actual proposal: the content is the FLOW (the sign of $\Lambda$ in the gap $(0,1/2]$), not the kernel.** Honest framing: the orthogonality is established in the literature — **Dobner 2020** (extended Selberg class $S^\#$, which *includes* D-H), **Newman-Wu 2019**, and convergent independent work **Michalowski 2026** (same Toeplitz-minor method). So Direction 12 must NOT be pursued via $t=0$ kernel/positivity statements; the only non-trivial route is the flow. Sharp Level-4 sub-target now isolated (sec 5).

## 1. The idea in one paragraph

The marginal-positivity thesis ("RH is just barely true, no buffer for soft proofs," the project's central structural finding) has an *exact* mathematical avatar: $\Lambda = 0$. The de Bruijn-Newman flow $H_t$ is a heat flow on the zeros; $\Lambda$ is the time at which all zeros become real. RH $\Leftrightarrow \Lambda = 0$ says $\zeta$ is exactly critical. Direction 12 proposes to build a **free-energy functional** $F[\cdot]$ on the (log-correlated) zero field such that the de Bruijn-Newman flow is its gradient flow, $\partial_t(\text{config}) = -\,\mathrm{grad}\,F$, and to prove $\Lambda = 0$ as "no spontaneous symmetry breaking": the order parameter (off-line displacement) stays zero along the flow. The Euler product enters as the **interaction term** that pins the field to be exactly log-correlated (critical), rather than super- or sub-critical.

## 2. Formal kernel

- **Order parameter.** $m = $ mean-square off-line displacement of zeros, $m = 0 \Leftrightarrow$ RH.
- **Free energy.** $F[\mu] = $ a rate functional on the zero point process $\mu$, built from the explicit formula read as an energy: a logarithmic (Coulomb / log-correlated) pair interaction plus an arithmetic potential from the prime side $\sum_p (\log p)\,\hat\phi(\log p^k)$.
- **Flow.** Identify $H_t$ (de Bruijn-Newman) with $-\mathrm{grad}\,F$ under the right metric (Wasserstein / Dirichlet form on the field).
- **Criticality theorem.** Show $\zeta$ is the unique stationary point with $m=0$, and that the arithmetic interaction forbids a symmetry-broken ($m>0$) phase. $\Lambda = 0$ follows.

The link to the project's existing work: the multifractal experiments (E0-E3) already model the log-correlated structure of $\log|\zeta|$. Direction 12 supplies the missing Level-4 ingredient: the arithmetic interaction that makes criticality (Level 4) rather than mere log-correlation (Level 3).

## 3. Why it could break Davenport-Heilbronn (K2)

D-H has off-line zeros, hence its analogue $\Lambda_{DH} > 0$: it is **not** critical, it has already symmetry-broken ($m_{DH} > 0$). The functional $F$ must be built from the Euler product so that "criticality at $\Lambda=0$" is an *arithmetic* statement. A generic log-correlated field (Level 3) is compatible with a broken phase (some zero at $\beta = 0.51$); only the arithmetic interaction forces $m=0$. This is the project's Level-4-not-Level-3 commitment made into the engine of the proof: D-H realizes the broken phase the generic field permits.

## 4. Kill-criteria status

- **K1 (signature not trace):** borderline. The positivity here is *variational* (a free energy is minimized / a Hessian is positive), not a trace identity and not literally a Hodge signature. Whether this counts as escaping R3.5 depends on the Hessian being a genuine new positivity input rather than the explicit formula in disguise. This is the direction's main risk and must be checked early.
- **K2:** clean *only at the level of the flow*. D-H is supercritical ($\Lambda_{DH} > 0$, off-line zeros, so $H_t^{DH}$ is not real-rooted at $t=0$). But E_DBN1 (#38) shows the **$t=0$ kernel positivity $\Phi\ge 0$ is K2-FAILING**: D-H passes it identically to $\zeta$ (its off-line obstruction is archimedean-suppressed to $\exp(-\tfrac\pi4 d\gamma)\sim10^{-29}$). So a D-H discriminator must come from the *flow* (locating $\Lambda$), never from a $t=0$ positivity/kernel statement.
- **K3:** for a curve over $\mathbb{F}_q$, "criticality" should be the statement that Frobenius eigenvalues sit exactly on $|{\cdot}| = \sqrt q$, i.e. Weil's bound as a marginal condition. Milestone 12.3.

## 5. First step (reuses E0-E3 multifractal thread)

Construct the candidate $F$ numerically from the explicit formula (log-correlated interaction + prime potential) and measure whether $\zeta$'s zero configuration is a **critical point** ($\mathrm{grad}\,F \approx 0$) while D-H's is not ($\mathrm{grad}\,F$ points off-line). If running the de Bruijn-Newman flow on the D-H zeros reveals a nonzero order parameter that the same functional assigns to $\zeta$ as exactly zero, Direction 12 has a variational D-H discriminator and earns further work. The numerics reuse the multifractal field machinery already in `experiments/multifractal/`.

**Update (E_DBN1, 2026-06-02).** The *cheapest* version of "probe the $t=0$ object" was run and is now CLOSED: the $t=0$ Pólya kernel positivity is K2-failing and orthogonal to RH (above; LEARNINGS #38). The free-energy / flow program survives unscathed (it never relied on $t=0$ positivity), but the experiment converts its vague "first step" into a **sharp Level-4 sub-target**:

> **Sub-target 12.4 — RESOLVED 2026-06-02 (bridge FALSE, with reason; LEARNINGS #39).** The hypothesis was: identify Rodgers-Tao's monotone functional with a fragment of the Weil/Hodge/Rosati positivity, making $\Lambda\ge 0$ a proven piece of the arithmetic Hodge standard conjecture. An equation-level survey of Rodgers-Tao (arXiv:1801.05914) settles it **negatively**. Their functional is the **log-gas / Dyson Hamiltonian** $H(t)=\sum_{j\neq k}\log\frac{1}{|x_j-x_k|}$ on the zeros, with $dH/dt=-4E(t)$, $E=\sum 1/|x_j-x_k|^2\ge 0$ — a function of zero positions only, with **no archimedean term, no prime term**, and not a quadratic form in a test function. It is **RH-AGNOSTIC**: $dH/dt\le 0$ holds *always* ($E\ge 0$ unconditionally), so the sign carrying RH is invisible to it (exactly as E_DBN1 #38 found for the $t=0$ kernel). The proof's arithmetic input (Montgomery pair correlation + CGGGHB small gaps) is consumed as **Level-3 statistics**, not as the Weil quadratic form. So $\Lambda\ge 0$ is the **Level-3 half**; $\mathrm{RH}=(\Lambda\le 0)$ is a genuine **Level-4 statement**, but a *criticality* one (a phase boundary), NOT a polarization (calling it "the Level-4 signature" was a re-description, **retracted 2026-06-28** per the frame-audit: criticality is a distinct Level-4 form whose D-H discriminator, the flow, is unbuilt, and subsuming it into "the signature" by fiat was the one place the convergence was enforced by definition rather than by experiment); the squeeze $0\le\Lambda\le1/2$ straddles the Level-3/Level-4 boundary and Rodgers-Tao proved the Level-3 half. Consistent with the project's commitment that Level-3 cannot close RH. **The flow does NOT short-circuit to the signature.**
>
> **Refined target 12.4′. RESOLVED 2026-06-28 (prediction CONFIRMED, MIRROR; LEARNINGS #133, [`e_dbn_flow_dh.md`](../../../experiments/criticality/e_dbn_flow_dh.md)).** The original target: via the Gaussian-smoothed explicit formula (Balanzario-Cardenas, arXiv:2312.00108), rewrite the heat-flow zero-sum $H_t$ as $\mathrm{arch}(f_t)+\mathrm{prime}(f_t)$, compute $dH/dt$ in that representation, and test (D-H discipline) whether it reproduces a *discriminating* sub-block of Bombieri's $W(f)$. **Predicted FALSE** (E_DBN1 suppression: the off-line obstruction is archimedean-suppressed to $\sim e^{-(\pi/4)d\gamma}$). **Outcome (ran).** The faithful flow puts $e^{tu^2}$ on the Fourier variable, re-weighting the $n$-th prime term by $e^{t\log^2 n}$; applied to the repo's validated Bombieri prime block, the decomposition reproduces e3f's $W(f_0)=0.09509938457843$ bit-for-bit at $t=0$ (the mandatory validation gate). The re-weighted PRIME block sums over $n<b^2$ and never reaches the off-line zero at $\gamma=85.699$ (it lives in the archimedean tail), so it separates D-H from $\zeta$ no better than the $t=0$ block (both via low-$n$ coefficient signs $b_n^{DH}$, not the off-line zero). The dual zero-side amplification $e^{t\gamma^2}$ DIVERGES (totals $0.3\to1.4\times10^{14}$ by $t=0.005$: no $t\to0^+$ limit, no convergent positivity functional). MIRROR on both sides. Together with the Nyman-Beurling-Baez-Duarte probe ([`e_nb_baez_duarte_dh.md`](../../../experiments/criticality/e_nb_baez_duarte_dh.md)), the two non-polarization Level-4 routes are now BOTH graded by experiment rather than by definition, and both are D-H-blind by the same archimedean stealth window. Direction 12's RH content is the flow LOCATING $\Lambda$ (Level-3-proven for $\Lambda\ge0$, Level-4-open for $\Lambda\le0=$RH), not any prime-block positivity object.

## 6. Honest assessment

Probability of closing RH: medium-low, and gated on the K1 question (is the variational positivity genuinely new input?). The appeal is that it gives the marginal-positivity thesis a precise theorem to aim at ($\Lambda = 0$ as criticality) and recruits the project's existing Level-3 multifractal work toward a Level-4 goal, which no other direction does. Even a partial result (a rigorous free-energy whose gradient flow is the de Bruijn-Newman flow) would be a notable contribution to the spectral/statistical-mechanics view of RH.

## 7. References

- Rodgers, B.; Tao, T. (2020). *The de Bruijn-Newman constant is non-negative*. Forum Math. Pi 8.
- Newman, C. (1976). *Fourier transforms with only real zeros*. Proc. AMS 61.
- de Bruijn, N. G. (1950). *The roots of trigonometric integrals*. Duke Math. J. 17.
- Polymath15 (2019). *Effective approximation of heat flow evolution of the Riemann xi function*. Res. Math. Sci.
- Dobner, A. (2020). *A proof of Newman's conjecture for the extended Selberg class*. arXiv:2005.05142; Acta Arith. (2021). [$\Lambda_F\ge 0$ for all of $S^\#$, which includes Davenport-Heilbronn; kernel never assumed $\ge 0$ — pre-empts the $t=0$-kernel orthogonality.]
- Newman, C.; Wu, W. (2019). *Constants of de Bruijn-Newman type in analytic number theory and statistical physics*. arXiv:1901.06596. [Nonnegative kernels with empty real-zero set; the 3-fact decomposition isolating $\Phi\ge 0$ as powering only $\Lambda\le 1/2$.]
- Michalowski, W. (2026). *On the Pólya Frequency Order of the de Bruijn-Newman Kernel*. arXiv:2602.20313. [Convergent independent work, same Toeplitz-minor method; kernel positivity "should not be confused with $\Lambda$".]
- Stopple, J. (2013). *Notes on low discriminants and the generalized Newman conjecture*. arXiv:1301.3158. Andrade, Chang, Miller (2013). *Newman's conjecture in various settings*. arXiv:1310.3477.
- Project: `experiments/criticality/e_dbn_kernel.{py,md}` (E_DBN1, LEARNINGS #38), `docs/02_graduate/log_correlated_fields_intro.md` (four-level framing), `experiments/multifractal/` (E0-E3).
