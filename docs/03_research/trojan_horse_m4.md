# Trojan-horsing M4: the costume inventory, the conservation law, and the compactness round

**Date**: 2026-07-22. **Status**: ARC COMPLETE AND CLOSED (two rounds same day: the #170 survey + e1t round, then the #171 e1u rung; the e1u closing exit fired by its own pre-registered criterion, Section 8). **Finding records**: LEARNINGS #170, #171. **Artifacts**: [`e1t_compact_class_limit.py`](../../experiments/spectral/e1t_compact_class_limit.py)/[`.md`](../../experiments/spectral/e1t_compact_class_limit.md)/`.npz` + [`_e1t_adversary.md`](../../experiments/spectral/_e1t_adversary.md); [`e1u_canonical_chain.py`](../../experiments/spectral/e1u_canonical_chain.py)/[`.md`](../../experiments/spectral/e1u_canonical_chain.md)/`.npz` + [`_e1u_adversary.md`](../../experiments/spectral/_e1u_adversary.md); [`compact_class_determinacy_survey.md`](reading_notes/compact_class_determinacy_survey.md) + [`_compact_class_survey_adversary.md`](reading_notes/_compact_class_survey_adversary.md); [`remling_suzuki_canonical_pin.md`](reading_notes/remling_suzuki_canonical_pin.md).

## 1. The question

"How can we trojan horse a solution to M4?" M4 is the arithmetic Hodge-index / Rosati polarization; in CCM coordinates it is the Section-7 uniform determinant-class limit (LEARNINGS #154/#158). A trojan horse is any construction in which the SIGN comes for free from a structure (a state, a self-adjoint operator, a convex body, a compact class), and the RH content is relocated into a clause that does not look like a positivity: an identification, a convergence mode, a growth bound, a logical meta-statement. The question is legitimate because positivity cannot be approximated into existence (the marginal-positivity compass): if M4 is ever proven, the sign will almost certainly be INHERITED from a structure rather than checked. The trojan program asks which structure, and what the inheritance costs.

## 2. The costume inventory (per-costume status from the repo record)

| Costume | How the sign comes free | Where the cargo reappears | Repo status |
|---|---|---|---|
| State / GNS (Bost-Connes shape) | every state on a *-algebra gives a positive form definitionally | the identification "this trace IS the Weil distribution" | #160: the corrected Hamburger pin gives the identification a positivity-free surface; zero net logical reduction, one new attackable surface |
| Self-adjointness (Hilbert-Polya / CF) | eigenvalues of a self-adjoint operator are real, no sign checked | the uniform $\lambda \to \infty$ limit | #158: finite reality is information-free (the D-H twin is real even at its off-line height); discrimination quarantined to the uniform limit |
| One-sided (Landau) | $\psi(x) \le x + C_\epsilon x^{1/2+\epsilon}$ for every $\epsilon$ forces RH with no lower bound ever proven | the $\lambda$-uniform one-sided bound = the S4 face | #161-#163/#169: S4 closed at every buildable corner; cheap mechanisms cap at the parity factor 2; the absence IS the $\mathbb{Q}$-linear independence of $\{\log p\}$ |
| Convexity (Hodge-Riemann / Alexandrov-Fenchel / Lorentzian) | the sign comes from an ample class or a convex body | the carrier itself (the self-product, R1/SP3) | Breadth Program #119-#122: the fixed-indefinite-form space outside arithmetic geometry is mapped and insufficient |
| Logic ($\Pi^0_1$ back door) | independence of RH from a strong theory proves RH true | proof theory | [`rh_logical_status.md`](rh_logical_status.md): the Godel lever is real, the dead corners are mapped |

## 3. The conservation law

Five costumes, two customs posts. Every executed trojan pays its tariff at either the identification/uniformity joint (M4 in its CCM Section-7 form) or the carrier joint (C1/R1); no costume has ever moved the content anywhere else. The tariff is always the same: the load-bearing clause must consume the Euler product AND the additive lattice at the same joint (the Tate package, the #152 bracket). The two disciplines are precisely the fare-dodging detectors: D-H (FE without Euler product) catches form-side dodges, Beurling (Euler product without lattice) catches counting-side dodges, and the DMV screen pre-kills any mechanism running on density data alone. So the operational form of the trojan question is not "can we avoid paying" but "at which customs post is the tariff payable with tools that actually exist". This round executed that question for the newest costume: compactness.

## 4. The compactness trojan: the three-link split and the survey

The move: trade the ONE uniform statement ($\hat\xi_\lambda \to \Xi$ in determinant class, uniformly) for three individually cheaper links: (i) certify the finite-$\lambda$ objects into a positivity class COMPACT after renormalization, so subsequential limits exist for free (Montel/Helly, no rate); (ii) a Carleman-type determinacy condition forcing all subsequential limits to coincide (a one-sided growth statement, not a positivity); (iii) the #160 pin identifying the unique limit as $\Xi$.

Survey verdicts ([`compact_class_determinacy_survey.md`](reading_notes/compact_class_determinacy_survey.md), all load-bearing citations FETCH-VERIFIED after the adversary re-fetch round):

- **Laguerre-Polya: KNOWN-TO-REPO.** Closure under locally uniform limits IS the Hurwitz frame the CCM corpus already runs; closed but not compact, and the Montel bound needed for pre-compactness is exactly the open uniform control. Zero gain.
- **Bounded-type normal families: OFF-TARGET.** Fixed type excludes both the family (types $\approx 0.93..1.00 \times \log\lambda$ diverge, #160, re-measured in e1t on all three streams) and $\Xi$ itself; rescaling to fixed type keeps only density data = DMV-killed.
- **Lorentzian cones: OFF-TARGET.** Compactness is real but degree-pinned (Branden-Huh Thm 2.25/2.28); the family's degree grows with $\lambda$, and at growing degree the class collapses into Laguerre-Polya.
- **Polya frequency / Edrei-Thoma: ADJACENT-WATCH.** Thoma-simplex compactness is genuinely free in a weaker-than-uniform topology, but the RH content relocates into mass conservation on the simplex = a two-sided weighted trace equality = the C1 joint, with no literature route.
- **Determinate moments (Carleman/Krein): glue only.** Determinacy is density-only, hence correctly weightless under the DMV screen; standalone OFF-TARGET.
- **De Branges chains / trace-normed canonical systems: NEW-LOAD-BEARING.** The one compactness that is free, type-divergence-proof, and reality-preserving: trace-normed Hamiltonians form a compact metric space with the Hamiltonian-to-Weyl-m map a homeomorphism (Remling; at-source published corroboration Hur arXiv:1501.01268, which also pins the boundary condition and the degenerate elements inside the compact target). In this encoding the RH content relocates into exactly two clauses: (a) identification of the limit chain (= the #160 pin, the lattice site) and (b) a non-degeneracy / no-mass-escape clause for the limit Hamiltonian (the Euler site).
- **The composite move is UNPOSED.** No one poses compactness + determinacy in place of uniform convergence for zeta's limits: zero hits across the CCM corpus (2511.22755 full-text re-scanned by the adversary, 2511.23257 included), de Branges's own attempts (which ASSUMED positivity axioms; Conrey-Li), Kaltenback-Woracek (no indefinite extremal theorem exists; the #168 $HB_1$ question stays unclaimed), and the sum-rule school. Killip-Simon (math-ph/0112008, verified at theorem level) is the proven template for exactly this trade in a neighboring field: sum rule + Helly compactness + one-sided semicontinuity replacing uniform control, with the catch that the support $[-2,2]$ and sign structure are INPUTS there and would be the OUTPUTS for zeta. Closest miss: the RMT canonical-systems school (Valko-Virag stochastic zeta, Hur, Painchaud), which runs the compactness half as standard technology but only for Level-3 universality objects, probabilistically identified, with no determinacy pin and no $\Xi$. Adjacent prior on the encoding side: Suzuki arXiv:1204.1827 builds a canonical system from zeta with RH equivalent to positive semidefiniteness of the Hamiltonian family (the encoding exists; the compactness trade is not posed there).

## 5. The e1t measurement (what the trade costs, measured)

The probe ([`e1t_compact_class_limit.md`](../../experiments/spectral/e1t_compact_class_limit.md), post-adversary 23/23 full / 22/22 quick) ran zeta, the D-H twin, and a density-matched Beurling fake through identical code (`build_comb`, verified bit-identical to the e1k harness at a real mixed-family config) on an 11-build grid, and measured all three links. Headline: `trojan_trades_M4_away = NO`, with the content relocated, never removed.

- **Link (i)**: the type divergence blocks every fixed class; the type-rescale IS compact but collapses all three families to the constant 1: compactness is free exactly where it is information-free, and the content moves into the $\lambda$-uniform CONTROL of the collapse rate (the #160 growth clause). On the measure face the verdict is gauge-relative: raw gauges fail positivity, and the adversary's FE-gated ghost-quotient gauge (T1f, the round's main catch) revives positivity AND tightness for zeta family-wide (dressed builds at 0.0002/0.0005 negative mass, the cleanest of all) while D-H stays at $\ge 0.05$ and the fake at $\ge 0.24$: a family-SEPARATING computed output, gauge-conditional and in-sample.
- **Link (ii)**: no $\lambda$-uniform determinacy input is certified (window-tracking moment radii, non-Cauchy branch scatter), and the coherence ordering is face-scoped: on the function/germ faces the RH-false D-H twin is the CLEANEST family by an order of magnitude, which is the structural kill of any naive compactness trojan (finite in-class certification + free subsequential limits provably cannot deliver "limit = the completed L-function" without uniformity on the compact containing the off-class zero). Two faces order zeta first (the $\epsilon$ trajectory, fragile; the T1f gauge face); no face certifies coincidence for zeta.
- **Link (iii)**: all arithmetic discrimination concentrates at the pin's input-typed gates: the Euler gate (D-H UNPOSABLE at the comb level, a type refusal), the lattice clause (the fake displaced 0.033 off $\{\log m\}$), the theta fuel (zeta defect 1.5e-36 vs the fake's 0.37), the FE type (D-H Riemann-type defect 1.72). The DMV screen FIRED on the certification vector (indistinguishable zeta vs fake), exactly as the conservation law predicts.
- **Adversary catches worth carrying**: the Q3 "RH-blind" absolutism killed by its own criterion and re-scoped to faces; the "13 vs 11" close-pair anecdote falsified (13 = 13; strip counts dps-branch-dependent 18/13/12); the #160 citation restored to CONDITIONALLY equivalent; the fake's Weil-form indefiniteness priced as matching coarseness (scales to -0.025 at $\epsilon$ = 0.01) with the alternation structure (~0.31, non-scaling) the lattice-sensitive residue.

## 6. The surviving door: e1u (trace-normed canonical-system embedding)

> **OUTCOME (2026-07-22, same day; LEARNINGS #171)**: the rung was BUILT and MEASURED, and its pre-registered closing exit FIRED. Section 8 carries the result; this section is kept as the spec that was executed.

The designated next rung, assembled from the survey's NEW-LOAD-BEARING class and the probe's germ reads:

- **Build**: encode the ghost-quotiented finite objects as finite trace-normed canonical systems via the Cauchy-compression nesting ($D_{\log}^{(N)}$ is the exact central block of $D_{\log}^{(N+1)}$, proven in e1s), embed in the compact space of trace-normed Hamiltonians, and measure whether the CHAIN data (not the single-$\lambda$ germ, which e1t showed is zero-microstructure-bound) behaves Cauchy while raw type diverges.
- **Clauses to measure**: (a) identification of the limit chain (the lattice site, = the #160 pin) and (b) no-mass-escape of the limit Hamiltonian (the Euler site). The citation adversary sharpened both: they are ENTANGLED (an off-line pair of the true $\Xi$ shows up as a local mass defect against zeta's global data), and (b) alone is Beurling-satisfiable at density level, so it carries zero discrimination by itself; the RH weight sits in the conjunction.
- **Named builder-must-fix**: the embedding of a FINITE chain into the compact space requires a normalization choice (the indivisible-tail extension); this is a load-bearing convention, not a detail.
- **The T1f lead**: does the gauged zeta/D-H separation (fneg_q 0.028 vs 0.05+) persist or close as $\lambda$ grows, and can the deep-dressed mass collapse (sum$|a_q|$ down to 0.0017) be controlled without consuming the identification? Prokhorov on normalized measures is the clean framing.
- **The $\kappa$ caveat**: raw finite objects certify only into $\kappa \le 1$ indefinite classes; the ghost quotient is the load-bearing step that reaches $\kappa = 0$, and e1t measured it as exactly the branch-sensitive step.

## 7. Honest limits and the pre-registered e1u adversary question

The conservation law held again: this round did not move the frontier, it measured a new costume's tariff and found one door whose tariff is not yet priced. The pre-registered adversary question for e1u is the reformulation-not-reduction test: is clause (b) just the uniform det-class control in Hamiltonian coordinates? The citation adversary attacked this at statement level and it did NOT land (M4 implies (b); no converse is visible; (b) lives in a strictly weaker topology and the free framework already supplies subsequential m-convergence): the residual risk is PRICE-level (proving (b) may consume the same explicit-formula inputs), and if e1u ends there it ends where e1m ended: REFORMULATED, NOT REDUCED, which by the #160 precedent is still a new proof surface. In-sample caveats inherited from e1t: dps-25 branch, small grids with N and $\lambda$ co-varying, thresholds pinned not pre-registered, and the dressed/clean branch selector dominating several readings. The survey hardening item (a source read of Remling's book Section 5.2) was RESOLVED BY SUBSTITUTION the same day: the whole load is pinned in three open documents, two by Remling himself ([`remling_suzuki_canonical_pin.md`](reading_notes/remling_suzuki_canonical_pin.md); Hur's full text carries the compactness proof, the metric, and the homeomorphism; Remling-Scarbrough 1811.07067 pins the book numbering and the in-print tail embedding; only the book's internal labeling stays SECONDARY, with no mathematical statement resting on it).

## 8. The e1u outcome: the arc closes as the third reformulation (LEARNINGS #171)

The rung was executed the same day (BUILDER e1u + SURVEYOR source pins, one ADVERSARY round split heavy/light: PASS_WITH_FIXES on construction and mechanics, FAIL on the two headline zeta-first faces, both withdrawn in place; the citation spot-check PASS_WITH_FIXES with the Suzuki gloss surviving a fresh re-fetch verbatim). Full records: [`e1u_canonical_chain.md`](../../experiments/spectral/e1u_canonical_chain.md) (post-adversary 21/21 full, 18/18 quick) + [`_e1u_adversary.md`](../../experiments/spectral/_e1u_adversary.md).

**The door was built, and it is real as an encoding.** The embedding is EXACT and well-posed: measure to Jacobi (stable discrete Stieltjes at dps 50) to Kac-Krein footprint chain to trace-normed compact space, round trips at 1e-49..1e-51 on all 22 build-faces plus the off-line config, with the in-print tail convention (Remling-Scarbrough $P_{\beta+\pi/2}$) reported and swept rather than invented. The source base is fully pinned (Section 7).

**Both apparent zeta-first faces died under the probe's own pre-registered kills.** (1) The chain m-face coherence flip (zeta cleanest, ~40x improvement, survived leave-one-out and the tail swap) was killed by the geometry controls: symmetric band exclusion at $|t| \ge 13.6$ restores D-H-first and BEUR parity, a disk beside each family's first zero puts zeta LAST, and the $\epsilon = 0.01$ fake reaches D-H parity. What the face measures is central-gap depth relative to the evaluation disk: first-zero-position data, density-typed (tracked control U2c). (2) The Suzuki-analogue Weyl tail-mass stabilization was killed as a UGRID-floor artifact: the "constant" u_half was the first grid step; on a fine grid the ordering inverts, and the band-equalized fake reproduces zeta's degenerate pattern exactly (tracked control U3d). Structurally the u-normalized profile is a broken instrument for the gated families: their central gap IS an exponentially long near-indivisible stretch (germ trace-length to 4e10, the #160 type divergence relocated into the length coordinate with its measured shape), which collapses all Weyl mass onto $u = 0^+$.

**The price question is ANSWERED.** A discriminating $\lambda$-uniform rate clause must be posed in UNNORMALIZED trace-length, where it equals uniform Christoffel / orthonormal-polynomial growth control at the footpoint for the window zero-counting measures, which IS the #160 growth clause in Jacobi coordinates; and the part of the profile behavior purchasable from density + gate data alone is measured to be exactly the weightless part (family-blind after band equalization, $\epsilon$-insensitive). The "actual reduction" branch is refuted by measurement. With both headline faces dead and clause (b) family-blind on every measured face, the pre-registered Q2 closing exit FIRED: **the compactness arc closes as the THIRD reformulation of M4**: variational (CCM Section 7), lattice-Hamburger (#160), Weyl-spectral-chain (this arc). The conservation law held a third time, by the arc's own pre-registered criterion.

**The durable keepers.**
- The exact embedding and the U1 conditioning panel: "the type divergence relocates into the length coordinate" now has a measured shape (central spectral gap = exponentially long near-indivisible stretch; the gated chains sit 8-12 orders of magnitude nearer the degenerate boundary than the fake), and a candidate theorem form (VERIFIER target 6: total trace-length grows like the reciprocal Christoffel function at the footpoint).
- The entanglement quantification: an off-line defect is seen exactly by the identification pin but through location-concentrated data (conditioning asymmetry location-dependent, 10x-490x, the sweep CONFIRMING the localization reading), the (b)-indicators are exactly blind to it, and the finite chain at the TRUE off-line height (D-H 3.7/36, window covering 85.7) hides the defect entirely: #158 restated in the chain gauge, with the defect measured as limit-level (a)+(b) CONJUNCTION data.
- The T1f gauge face persists as the only zeta-first face (fneg_q separation reproduced to 0.0), correctly attributed to e1t (an admission price carried into the chain, not chain output), with D-H's decreasing trend still the honest caveat.
- Clause (b) carries zero discrimination alone, now with the saturation caveat: the agreement of the degeneration indicators is boundary saturation (deltaK spans 1.0e-2 down to 9.2e-14), and those indicators are chain-geometry reads, not escape detectors (flat under genuine spectral escape).
- The Suzuki dual load split, verified verbatim at source (1606.05726 Thm 2.3/2.4): his gauge holds the arithmetic and deforms $\omega$ (identification free, positivity open); ours holds the line and deforms $\lambda$ (positivity free, identification open). Two costumes, one conservation law, and still no rate form for the far-end clause anywhere in print.
- Two new tracked negative controls (U2c symmetric band exclusion, U3d fine-grid profile) that any future chain-face claim must pass.

**The one genuinely new implication: M4's growth clause has a Christoffel-function costume.** The price-table answer identifies the $\lambda$-uniform bill, in this gauge, with uniform Christoffel/orthonormal-polynomial growth control at the footpoint. That names a corpus the repo has never swept: the Christoffel-function and orthogonal-polynomial growth literature (Totik, Nevai, Lubinsky, Mate-Nevai-Totik, plus the Killip-Simon sum rules already in Section 4). The sweep question is exactly the S4-shaped one: is there ANY known mechanism giving $\lambda$-uniform Christoffel control at a point against measures whose atoms sit at $\mathbb{Q}$-linearly independent positions, for a structural (Euler + lattice) reason? Flag, per the four-level discipline: Lubinsky-type universality results are Level-3 statements (bulk asymptotics compatible with off-line worlds) and cannot close the clause by themselves; the sweep must demand the uniformity mechanism, not the universality limit. This is a NAMED sweep candidate parallel to the modular/Hecke rung, not a replacement for it.
