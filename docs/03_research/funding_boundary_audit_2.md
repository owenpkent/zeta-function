# The funding-boundary frame's CLOSING audit

> **2026-08-30. Trigger: completion, not an exit.** The frame's registered session order
> (F3, F1, F2a, F2b: [`successor_frame_deliberation.md`](successor_frame_deliberation.md)
> Section 7) is COMPLETE as of LEARNINGS #212, with exit 2 discharged at its deadline
> session; by the repaired exit 3a's own text, any further frame session would be exit
> 3a's subject (it fires unless the session lands a derivability-passing metric item),
> so the frame cannot continue without either this audit or a re-registration
> (adversary-lens fix 2). This is the frame's second
> audit (the first, [`funding_boundary_audit_1.md`](funding_boundary_audit_1.md),
> LEARNINGS #209, ran at session two under exit 3's original letter and repaired that
> exit). Wiring: drafted by the ORCHESTRATOR lens; an INDEPENDENT ADVERSARY lens
> attacked the draft before any state file was touched, per the #206 precedent, and its
> full report is the separate tracked file
> [`_funding_boundary_audit_2_adversary.md`](_funding_boundary_audit_2_adversary.md);
> Section 7 carries its verdict block and the application record of its twelve
> required fixes, each tagged "(adversary-lens fix N)" in place.
> Finding-level record: LEARNINGS #213. No em dashes anywhere.

## Q1. The ledger against the registration, session by session

The registration (Section 6 of the deliberation) armed the tripwire from #207, set the
six-session expiry backstop, pre-registered five exits, and pre-wired the verdict
vocabulary (non-UNMOVED iff a Section 5 metric item (a)-(d) lands AND passes the
derivability check, graded by the session's ADVERSARY). What happened:

| Session | Date | Registered spec | Delivered | Grade (adversary) |
|---|---|---|---|---|
| F3 (#207) | 2026-08-26 | de Branges 2(a) survey, bounded, yes/no exit | NO-IN-PRINT, second-toolchain replication, corridor trigger serviced and hardened (fifth); P12 LaTeX rider | UNMOVED (B6), tripwire 1/3 |
| F1 (#208) | 2026-08-26 | funding wall typed (F1a at the second-order register; F1b posed, not inherited) | F1a SUPPORTED AS SCOPED (the pre-registered conjunction exactly); W1/W2 kill made decidable; at-source verifications; three of the frame's four confabulation catches; F1b POSED only, per spec | UNMOVED (A8), tripwire 2/3 (adversary-lens fix 1); exit 3's letter met |
| audit 1 (#209) | 2026-08-26 | (exit 3's letter honored) | HEALTHY-PER-PLAN; exit 3 REPLACED by 3a/3b; tripwire reset 0/3 | (audit; not a frame session) |
| (#210) | 2026-08-26 | (not a frame session: PR salvage, hygiene/synthesis class) | both parallel branches closed with salvage; the D1 correction; the Euler positive control | (not frame-graded) |
| F2a (#211) | 2026-08-26 | the certificate class POSED; same-session definition adversary; AF Lean skim + GLSS prep riders | POSED, then FAILED AS POSED (four landed attacks), RE-POSED per the adversary's own prescription; the naive no-go DECIDED soft-true in-frame; F2b re-aimed | UNMOVED (A7), tripwire 1/3 |
| F2b (#212) | 2026-08-28 | the re-aimed theorem at the Section 5 bar, delta-check first | delta-check PASS_WITH_EDITS (the link fine-class smuggler closed); the visibility-floor law at the Section 5(a) bar; e1ag 16/16; Lean skeleton; session adversary PASS_WITH_FIXES, ten fixes applied in-session | **NON-UNMOVED** (M2 NEW-COORDINATE as scoped), chain broken 0/3 |

**Arithmetic at close.** Frame sessions used 4 of 6; two audits (not frame sessions);
one non-frame hygiene round. Tripwire history 1/3 (F3), 2/3 (F1), reset 0/3 (audit 1),
1/3 (F2a), broken 0/3 (F2b non-UNMOVED) (adversary-lens fix 1): it never reached its
firing count. Every session executed its registered spec; nothing was
added, repeated, or substituted.

**The exits, final state, one by one.**

1. **Exit 1 (F1a's typing dies): NEVER FIRED.** F1a survived its own adversary
   (PASS_WITH_FIXES, reproduction bit-exact) with the four-corner control matrix; the
   competitor typing co-landed at a different register (provability) rather than
   winning, and the outright-win condition was made decidable (W1/W2), where it
   remains: W2 is WATCH-screened; W1's computation on the actual AF window is open
   and UNASSIGNED, inventoried as door (f) in Q5 (adversary-lens fix 3).
2. **Exit 2 (F2 dry by F2b): DISCHARGED, not fired.** The firing condition was the
   attack NOT landing by the deadline; the visibility-floor law landed at the Section
   5 bar at its narrowed scope, the session adversary's own strict assessment
   (outcome (a) as scoped, explicitly not the #211(iv)(b) relabeling case).
3. **Exit 3a/3b (re-measurement drift): NEVER FIRED.** 3a: every graded session was
   one of the four registered ones executing its spec. 3b: no session filed a minted
   claim AS a coordinate that its adversary then failed, let alone two consecutively
   (#211 filed a definition and said so; #212's M4 was filed AS a candidate and
   graded candidate-not-landed: the label was honest before the check ran).
4. **Exit 4 (external movement): NEVER FIRED.** No in-window event moved the
   proportion past the in-class ceiling or posed the class question in print (GLSS
   I/II predate the frame and entered as boundary data, not as an event; the next
   sweep is ~2026-09-08).
5. **Exit 5 (portfolio decay): NEVER FIRED.** The standing lanes stayed on cadence
   through the frame's span: P2 polled and correctly blocked upstream, P12 carried to
   LaTeX+PDF (its remainder is Owen's), the WATCH slot not yet due, the VERIFIER
   batch dated and now enriched.

## Q2. The decay markers at close (the #201 four, plus the frame's own trap)

- **(i) Underived-constraint production: ABSENT.** The frame minted exactly one
  coordinate-grade claim (M2), graded by its session adversary with the derivability
  run; M1/M3 were filed and graded as enrichment/corroboration; M4 as
  candidate-not-landed; the naive no-go and the contrapositive were REFUSED as mints
  by their own documents before any adversary asked.
- **(ii) Cell-filling as metric: ABSENT.** The Section 5 wiring held: instrument
  pricings and covered registers were never filed as progress; e1ag is calibration
  and says so in its own header; the frame's one progress claim is the registered
  item (a).
- **(iii) Verdict inflation: ABSENT AT THE PRESSURE POINT.** Audit 1's Q4 named F2b
  as the generosity pressure point in advance. The record: the F2b grade was assigned
  by the session's ADVERSARY per the #206 wiring, its independence anchored by
  bit-exact reproduction at seed 212 and the independent typecheck (adversary-lens
  fix 11), and it returned TEN blocking fixes, refused M4, demoted
  M1 and M3, narrowed M2's scope, and made its non-UNMOVED conditional on the fix
  pass, which was then executed and re-verified in-session. That is the opposite
  signature of inflation. Standing blind spot, carried not solved: all grading is
  same-day, mitigated by mechanical anchors (bit-exact reproduction at fixed seed,
  independent typecheck, the em-dash-level textual checks) and, for this audit
  itself, by the independent lens in Section 7.
- **(iv) Unscheduled measurement sessions: ABSENT** (3a's record above).
- **The ceiling-as-output trap (the registration's own Section 5 warning): AVOIDED
  AS SPECIFIED.** The registered distinction was: a ceiling theorem counts only if it
  is about the mechanism CLASS holding the external record, not about this project's
  own meters. The visibility-floor law quantifies over the class containing AF and
  GLSS by formal clause-by-clause verification (the delta-check's re-run), its
  support axis is priced in the record holder's own funding currency (e1af Section
  2), and its contrapositive is the sharpened compass, not a note that our meter is
  blind. The trap's variant (pricing our own AF reimplementation) did not occur:
  no AF reimplementation exists in-repo; e1ag runs on synthetic configurations and
  is cited as instance, never as the theorem.

## Q3. The yield inventory, graded honestly

1. **The visibility-floor law** ([`f2b_visibility_floor.md`](f2b_visibility_floor.md),
   #212): the frame's one Section 5(a) item. Two-axis exchange law over the class,
   carried at M2's NARROWED scope everywhere (adversary-lens fix 4): defect floor
   $\Theta(\varepsilon_G TL)$ sharp two-sided over STANDARD batteries; location
   frontier $\delta^{\ast} = (1+o(1))\log(TL)/\Theta_G$ two-sided with thin band,
   its ceiling under the NAMED (P-M) hypothesis $|M| \le CTL$ (underivable from
   satisfiability per the wild-$M$ construction) and its floors under the L4 regime
   condition with the unconditional $k = O(1)$ / C4-loc endpoint; the axes decouple;
   AF's information-wall sentence derived as the support-1 endpoint; no conjectural
   hypotheses. Open scope NAMED in-file: the L4 regime
   condition's excluded region, the clustered multi-pair constant, non-countable
   batteries, and M4's batch-bound proof (VERIFIER lane).
2. **The certificate class $\mathcal{C}$, delta-checked**
   ([`f2a_certificate_class.md`](f2a_certificate_class.md)): a reusable interface
   artifact with its collapse-safety certificate, and a worked END-TO-END example of
   the definition-before-attack discipline (posed, failed by its own adversary,
   re-posed per prescription, delta-checked, then built upon): the frame's
   transferable METHOD yield.
3. **The typed funding wall** (F1a, #208 + [`e1af`](../../experiments/spectral/e1af_funding_wall.md)):
   SUPPORTED AS SCOPED (the finite-data L1 reading: the discriminating content is L1
   congruence data, and the meter reads neither direction of the L2 axis), with the
   competitor co-landing at the provability register as the pre-registered
   conjunction (adversary-lens fix 5): unconditional funding ends at support 1 (the
   MV range); beyond, the price list is the L1-congruence-structured shift family at
   stated weights; the four-corner matrix; the competitor kill decidable (W1/W2).
4. **The corridor's fifth hardening** (F3, #207): the allow-poles question closed
   NO-IN-PRINT with the Section 10 screens inherited by WATCH.
5. **Standing instruments**: e1af, e1ag (both regression-grade with tracked npz),
   `F2bSkeleton.lean` (typechecks clean; V-F2b-1..5 proved), and the frame's full
   adversary/audit artifact chain as a methods record (adversary-lens fix 12):
   `_debranges_allow_poles_adversary`, `_e1af_adversary`,
   `funding_boundary_audit_1`, `_f2a_class_adversary`, `_f2a_delta_check`,
   `_f2b_adversary`, and this file with its lens report.
6. **Side yields**: the at-source verification set (Aryan, BGSTB, GLSS I/II, the AF
   Lean repository skim with the P2 quarantine pattern and the `RHLinalg`
   Mathlib-upstreamable flags); the Mueller 1983 note (RH-conditional, channels stay
   separate); the $E(T)$-at-two-error-classes register fact; FOUR fetch-summarizer
   confabulation catches across the frame's span (instances 2 through 5 of the
   month's five: the BBH abstract at F3; the AF "carried as Lean hypotheses" clause
   and two landscape-sweep fabrications at F1), now a standing instrument discipline
   (double-route quotes) (adversary-lens fix 6).
7. **What did NOT land, stated plainly** (completed per adversary-lens fix 7): M4
   (the site-selection lemma) is a candidate with its batch bound unproven (VERIFIER
   lane, priced); F1b's pricing was posed, never executed (per its registration, but
   still open); the naive class no-go is soft-true and worthless as a mint (decided
   at F2a, correctly never minted); W1's computation on the actual AF window is
   open; the Mueller reading is [SINGLE-SOURCE] with the removability of its RH
   dependency an open thread; the AF Lean repository's kernel tier remains
   NOT-DETERMINABLE-WITHOUT-BUILD (priced out at the skim, never built); and the
   class definition itself names two certificate families its theorems do NOT
   constrain (the family-averaged $q$-aggregate class and the moment-funded class,
   its Section 6 items 2-3). The program's open kernel (M4-the-wall / R1) is
   exactly where it was: the frame priced the information wall, it did not breach
   it, and it never claimed otherwise.

## Q4. The publication flag (raised, not adjudicated)

The registration's own words for the F2 target: "registry-grade mathematics with an
external audience either way." The closed frame's package (the class definition, the
visibility-floor law with its build and Lean skeleton, the scope sentence) is a
PUBLICATIONS.md gate candidate (the next registry slot, P13, if it survives the
gate; adversary-lens fix 10): the gate's evaluation (novelty pass at
sources, the M4 batch bound proven or explicitly scoped out, external-format rewrite,
and Owen's decision) is its OWN process and is not run inside this audit. Flagged to
the successor deliberation as a shipping-support lane, alongside the standing note
that P12's arXiv upload remains the portfolio's loudest pending human action.

## Q5. Disposition: CLOSE AS ATTACK-LANDED

**Verdict: the frame CLOSES, with its registered question answered at the honest
register.** The question ("can any sequence of finite-rank certificates whose inputs
are prime-correlation data certify location-completeness in the limit; and if not, is
the missing thing exactly #148's clause") resolves as: over the class, NO at every
jointly satisfiable granted profile (the soft half, decided cheaply at F2a and
deliberately unminted), and the quantitative content is the two-axis visibility-floor
law, with the missing ingredient priced as exact-class contact: the compass keeps its
direction and now carries a RATE. What would continuation look like? Proving the L4
batch bound, closing the open band, formalizing `MergeFloorTarget` instances: all
instrument work on the frame's own theorem, which is the bank pattern's precise
signature and is already lane-assigned (the ~2026-09-02 VERIFIER batch). There is no
registered session left, no exit demanding service, and no frame-worthy attack
remaining INSIDE this frame's scope sentence: the sentence itself says the untouched
region is the exact-identity register, which is a different frame.

**The successor is chosen by a DELIBERATION, not by this audit** (the #206 pattern,
with its own same-session adversary), scheduled AFTER the two due lanes so it
deliberates on current information: (1) the ~2026-09-02 VERIFIER batch (now enriched
with V-F2b-6, the L4 batch bound, the V-F2a extraction lemmas, `MergeFloorTarget`
instances, plus the queued #199/#200/#196/#192/#191 nuggets and upstream candidates);
(2) the ~2026-09-08 WATCH sweep (the Hilberdink S-item with its named screen; the
Section 10 split screens; the Suzuki-crowding pressure re-check). **The door list
handed to that deliberation** (inventory, not choice; completed per adversary-lens
fix 8): (a) the exact-identity register the scope sentence names (the EF as
identity, the Euler product, the Hadamard factorization: where the priced compass
points; in ledger coordinates the CCM Section-7 uniform determinant-class limit,
#148's clause, M4 seen from the counting side, with 08A's M3-analytic vocabulary
absorbed per #206); (b) the recovery register (#205's measured candidate, standing
as instrument-on-first-need; its first-need condition HALF-MET: F1a landed its
typing, F1b's digits-currency pricing was posed and never executed); (c) the parked
G4/G15 specs (custody TRANSFERS from the frame's now-dead exit-3a clause to the
successor registration); (d) whatever the sweep surfaces (the ACS WATCH trigger and
the Hilberdink screen both standing); (e) the publication package as a shipping
lane, not a frame (Q4's flag); (f) W1's computation on the actual AF window (open,
unassigned, decidable as specified at e1af Section 2); (g) the two out-of-class
input families the class definition names as unconstrained (each could seed a
class-extension question if a deliberation wants it); (h) the honest
instrument-frame option: register the curve's own completion list (the L4 batch
bound, the open band, `MergeFloorTarget` instances) as an INSTRUMENT frame with a
finite list and an expiry, per the repaired meta-rule, if the deliberation prefers
that work frame-shaped rather than lane-shaped. The deliberation is free to re-pose
the certificate class at the exact-identity register (the A4(i) analysis of what
happens with the EF in-frame is already written) or to go elsewhere entirely.

**Counter state at close.** The tripwire dies with its frame (the successor
registration arms its own, per the repaired meta-rule); frame sessions final 4/6
with two audits; the standing lanes (P2, P10 gated behind it, P12 with Owen, WATCH
~09-08, VERIFIER ~09-02) carry over unchanged. **Frameless-window guard
(adversary-lens fix 9):** between this close and the successor registration the
repo is FRAMELESS: only the standing lanes run; no session in that window grades
progress or mints coordinates (the #201 derivability rule stands repo-wide
regardless), and the deliberation is the next graded session.

## Q6. The wiring meta-check (what this frame proved about the process)

- **What worked and should carry forward**: the pre-wired verdict vocabulary with
  adversary-assigned grades survived its named generosity pressure point; the
  Section 5 artifact gate did real work (the Lean skeleton and build were demanded,
  produced, attacked, and repaired); the delta-check-before-theorem critical path
  caught a frame-collapsing hole (the link fine-class smuggler) at wording cost
  instead of theorem cost; and the definition-before-attack discipline (F2a's
  FAIL-as-posed round) converted an adversary's kill into the theorem's actual
  content (the perturbation constructions became the floor). These are method
  yields, transferable to any successor registration.
- **One device to wire explicitly next time**: the CONDITIONAL grade (F2b's
  non-UNMOVED conditional on the fix pass executing in-session) worked here because
  the fixes were mechanical and the verification anchors were bit-exact, but it is
  one more same-session judgment; the successor registration should state in
  advance when a conditional grade is admissible and who verifies its discharge.
- **Standing blind spots, unchanged**: same-day grading (mitigated, not removed);
  the audit self-grading risk (mitigated here by the independent lens below); the
  cross-machine replication sliver (e1ad/e1v, inherited, still open).

## Q7. The independent ADVERSARY lens (executed; verdict block; fixes applied above)

Full report: [`_funding_boundary_audit_2_adversary.md`](_funding_boundary_audit_2_adversary.md)
(2026-08-30; attacks A-F run against the registration, both prior audits, LEARNINGS
#207-#212, the F2b adversary report, and the theorem document; all numbers verified
at source except the ones its fixes name).

**Verdict: CONCUR_WITH_FIXES.** Twelve required fixes, none touching the
disposition; every one is applied above and tagged "(adversary-lens fix N)" in
place. Concurrence calls, verbatim in substance: (i) CLOSE-AS-ATTACK-LANDED: CONCUR
(both continuation steelmen fail; the remaining work is lane-scheduled instrument
work); (ii) the door list: DIVERGE AS DRAFTED, CONCUR once fixes 3, 7, 8 land
(applied); (iii) the deliberation's scheduling after the two due lanes: CONCUR,
conditional on fix 9's frameless-window guard (applied); (iv) the publication flag:
CONCUR as raised-not-adjudicated with fix 10's registry-number front-run removed
(applied). Attacks that LANDED, for the record: A (the tripwire history omitted the
2/3 state at F1; the header overstated exit 3a's conditional; "W1 lane-assigned"
was false); B (M2's narrowed scope and F1a's AS-SCOPED tag were dropped from the
inventory a successor will quote; the confabulation statistic undercounted four as
three; item 7 omitted three open items); C (the door list omitted a named kill
against a listed yield, the out-of-class families, the instrument-frame option, and
three custody notes; the close opened an unpoliced frameless window); D (the P13
numbering front-ran the gate); E ("fresh-context adversary" was an undocumented
credential in the very paragraph rebutting verdict inflation); F (the
seven-artifact chain was labeled as four). Attacks B's inflation charge and C's
counter-disposition otherwise MISSED: the yield grades match the session records
and the close survives both steelmen.

## Same-day actions (SYNTHESIZER)

1. LEARNINGS #213 minted from this dossier.
2. PHASE_STATE: new top paragraph; the funding-boundary trigger bullet moved to its
   CLOSED form; the milestone ROTATION executed (the superseded current-state
   paragraphs into PHASE_STATE_ARCHIVE.md); next-steps rewritten to the three-lane
   state (VERIFIER ~09-02, WATCH ~09-08, then the successor deliberation).
3. TODO: the closing-audit item struck; the deliberation item added with the door
   list; the publication-candidate flag recorded (gate not yet run; adversary-lens
   fix 10).
4. No code changes this session; battery state unchanged (last full run 15/15,
   2026-08-28).
