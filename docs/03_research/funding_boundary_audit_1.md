# The funding-boundary frame audit 1: exit 3 fired on its letter, the frame is healthy per plan, and exit 3 gets its repair

> Dossier, 2026-08-26. Trigger: exit 3 met on its own letter after sessions F3 + F1 (#207/#208; the
> adjudication at #208(iv): the session ADVERSARY referred the call, the ORCHESTRATOR honored the letter,
> one session before the expected three-UNMOVED cadence). Discipline per the #201 precedent
> ([`sp_backlog_frame_audit.md`](sp_backlog_frame_audit.md)): audit the FRAME, not the findings; a frame
> may not talk its way out of its own tripwires. Frame under audit:
> [`successor_frame_deliberation.md`](successor_frame_deliberation.md) (Section 6); wiring exercises:
> [`_debranges_allow_poles_adversary.md`](reading_notes/_debranges_allow_poles_adversary.md) B6 and
> [`../../experiments/spectral/_e1af_adversary.md`](../../experiments/spectral/_e1af_adversary.md) A8.
> Bounded audit of a two-session frame, not a #201-scale deep audit. Integrates as LEARNINGS #209.

## Q1. Re-measurement vs attack: HEALTHY PER PLAN

The #201 decay markers, checked one by one. **Coordinate inflation: absent** (both sessions filed their
mints modestly; neither dossier asserts a coordinate; #207(iii)/#208(iv) accept their adversaries'
derivability failures without dispute; enrichments routed per the #204(v) precedent). **Ceilings filed
as output: absent** (no ceiling theorem minted; F1's ceiling facts are in-print landscape rows).
**Pre-exemption: absent, and its opposite observed** (the tripwire is armed and counting; honoring the
exit's letter one session EARLY is the exact inverse of #179's "UNMOVED by design"). **Cell-filling:
absent** (no unscheduled session; the recovery register NOT built on spec, F1b posed not inherited,
per the registration; the P12 slot was a standing lane, not frame work).

The against-side, weighed honestly: two sessions, zero derivability-passing constraints, and F1's
build self-describes as numerical verification of known territory with the typing content in the
control contrast. But those two sessions are the registered FRONT HALF of a four-session plan whose
attack (F2a/F2b) is the back half, and Section 5 excludes measurements from progress BY DESIGN, so
honest F3/F1 could not have graded otherwise. Burn rate is a real fact (2 of 6 sessions spent before
the attack starts); it becomes DRIFT on the first measurement-species session outside the plan, the
bright line the repaired exit 3 now draws. **Verdict: executing its charter.**

## Q2. The exit-3 repair

As written, exit 3 conflates the honest cadence with the decay signal. The letter reading fires on
EVERY honest pair of measurement sessions (the predicted path), strictly dominating the three-UNMOVED
tripwire and collapsing the frame to a two-session audit cycle. The narrow reading (claims ASSERTED
as coordinates that fail on inspection) is nearly vacuous: the Section 6 wiring already runs the
check at mint time, so the failed label never sticks. The work exit 3 should do, distinct from both,
is to catch the two bank-pattern decay modes #201 actually found. **Replacement text for Section 6,
exit 3 (verbatim):**

> 3. Re-measurement drift, two prongs, each checkable by the session's ADVERSARY against written state with
>    no ORCHESTRATOR discretion. **(3a) Unscheduled measurement:** any session graded under this frame's
>    wiring that is not one of the four Section 7 sessions executing its registered spec (added, repeated,
>    or substituted work) and that produces no Section 5 metric item passing the derivability check fires
>    the audit immediately: the frame has grown itself a measurement session, the bank pattern's signature.
>    (A session forced by exit 4's own deep-read clause is graded under exit 4, not 3a.) **(3b) Label
>    inflation:** two consecutive sessions whose dossier or LEARNINGS draft files a minted claim AS a
>    coordinate, constraint, or theorem-shape which the session adversary's derivability run (or the
>    Section 5 bar) then fails fire the audit immediately: the check caught the label both times, and
>    repetition is the pattern. Honest UNMOVED grades on registered sessions never trip this exit: those
>    belong to the three-UNMOVED tripwire and to exit 2's session-four deadline. This exit cannot be
>    disarmed by editing the Section 7 plan: sessions added after registration are 3a's subject, not its exemption.

Consequence, for the record: under the repaired text this audit would not have fired at F1 (both sessions
registered, both mints filed modestly). Honoring the letter cost one bounded session and was the correct
conservative branch given #201's precedent (twenty-two sessions unaudited on a generous reading); the
repair prevents recurrence while keeping both detectors armed.

## Q3. Disposition: (A) PROCEED to F2a as planned

**Counter state, explicit.** This audit resets the three-UNMOVED tripwire to **0/3** (the repaired
meta-rule's letter: audits reset the counter). The reset does NOT touch the session-indexed
enforcement: frame sessions used **2/6** toward the expiry backstop (the audit is not a frame
session), and exit 2 binds unchanged at F2b = frame session four (its two clauses coincide there),
so the reset cannot extend the frame's life. The binding constraint is now exit 2: the attack lands,
or the audit fires with "the information wall resists both branches" recorded. Exits 1, 4, 5
unchanged; exit 3 replaced per Q2, effective now.

**Scope ruling 1, the Hilberdink Beurling-variance sweep:** S-item, rides the ~2026-09-08 WATCH
slot, NOT inside F2a. It scopes F1a's already-scoped verdict (the generic density-alone comparator);
F2a does not consume it (Beurling enters F2a prime-side only; the class definition needs no
variance data); survey work inside the attack session is exactly the boundary Q1 drew. Named
screen: a Hilberdink-class system with second-order integer-shift structure WITHOUT an additive
lattice would re-open F1a's scoping.

**Scope ruling 2, the GLSS I/II deep reads (2503.15449, 2507.06823):** ride INSIDE F2a as its
bounded prep block, per #208's own queueing. GLSS is direct boundary data for the class definition
(full-support funding of either law buys density-1 without RH, so any posed class must contain
GLSS's certificates and place their yield short of completeness, else it is too narrow on
arrival); a separate session would split one design task and be 3a's subject.

**Falsifier-5 lane check: nothing past its stated cadence today.** WATCH due ~2026-09-08 (not due).
P2 #41132: polled 2026-08-25, correctly blocked on the dependency PR. P10: gated behind P2. P12:
agent side complete (LaTeX+PDF 2026-08-26); the remainder is Owen's, the arXiv upload the portfolio's
highest-value pending human action (scoop clock loudest). The queued VERIFIER batch (the
#199/#200/#196/#192/#191 nuggets + upstream candidates) has no stated cadence and is the lane #201
flagged as decay-prone: it gets one, ~2026-09-02, next non-frame slot (the AF Lean skim rides F2a).
Hygiene flag: the #207/#208/audit artifacts are uncommitted pending per-action authorization (A8 fix
10's git-add list included).

## Q4. The wiring meta-check: FUNCTIONING AS DESIGNED, no repair

Both exercises decomposed the candidate mint explicitly against the ledger (B6: hb1 + e1w + the ledger,
citing #205's "horizon is register-relative" as the calibrated PASS exemplar; A8: four mints graded
separately), both graded against their session's nominal interest (UNMOVED twice, catches banked), and
the one ambiguity met (exit 3's two readings) was ESCALATED rather than resolved by discretion: this
audit is that referral's output. Against the #201 blind-spot list: same-session grading is structurally
present, but both rounds carried independent mechanical anchors (second-toolchain replication at B6;
bit-exact double reproduction plus independent re-fetches at A8) and no leniency appears in either
direction. The generosity pressure point is F2b (a generous theorem-shape PASS would dodge exit 2),
already guarded by the Section 5 bar's artifact requirement (a machine-checked or VERIFIER-drafted
skeleton with the hypothesis load priced, outside adversary prose). Coverage note, not a failure: only
the FAIL branch has been exercised in-frame; the first PASS lands under that artifact gate by construction.

## Same-day actions (SYNTHESIZER; this audit itself applies no edit beyond this dossier)

1. [`successor_frame_deliberation.md`](successor_frame_deliberation.md) Section 6, exit 3: replace
   with the Q2 text verbatim; one line noting the source (this audit, 2026-08-26).
2. [`../../PHASE_STATE.md`](../../PHASE_STATE.md), funding-boundary trigger bullet + current state,
   folded with the pending #208 rotation as one edit: F1 graded 2/3, exit 3's letter honored, this
   audit executed, tripwire RESET to 0/3, sessions 2/6, exit 3 replaced, exit 2 binding at session
   four; NEXT = F2a with the GLSS prep riding.
3. LEARNINGS #209: mint from this dossier (#208 already names it).
4. The ~2026-09-08 WATCH sweep spec: add the Hilberdink Beurling-variance S-item with its named
   screen (the sweep also inherits #207's Section 10 split screen, unchanged).
5. Next-steps: date the next VERIFIER batch ~2026-09-02, non-frame slot.
6. Flags for Owen (no edits): one authorized commit for the #207/#208/audit artifacts; the P12 arXiv
   upload remains the loudest pending human action.
