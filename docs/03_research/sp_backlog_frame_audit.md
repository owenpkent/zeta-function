# The SP-pipeline / construction-backlog frame audit: the bank is complete, the tripwire was disarmed, and the rule gets a repair

> Dossier, 2026-08-25. A frame audit of the e2an SP-object pipeline and its construction
> backlog (LEARNINGS #179-#200; experiments e2an through e2be), per the #163 precedent
> ([`ccm_corridor_frame_audit.md`](ccm_corridor_frame_audit.md)): audit the frame, not the
> findings. Two independent agent lenses ran the same day: a frame-assumption audit
> (Appendix A: THEOREM vs MEASURED vs CHOICE, the loop's blind spots, falsifiers) and a
> portfolio/opportunity-cost audit (Appendix B: what decayed while the frame ran, ranked
> next sessions). One-line verdict, both lenses concurring: **INSTRUMENT-COMPLETE-AND-CLOSE.**
> Integrated as LEARNINGS #201.
>
> Cross-links: [`construction_backlog.md`](construction_backlog.md) (the frame's own queue,
> audited here), [`missing_object_interface.md`](missing_object_interface.md) (the typing the
> frame instantiated), the six dossiers e2an / e2ao / e2az / e2bb / e2bd / e2be under
> [`experiments/arithmetic_geometric/`](../../experiments/arithmetic_geometric/),
> [`LEARNINGS.md`](../../experiments/LEARNINGS.md) (#179-#201). No em dashes anywhere.

## 1. The trigger, and the enforcement gap the audit found first

The standing meta-rule says three consecutive UNMOVED verdicts inside one frame trigger a
frame audit, not another probe. The nominal trigger was #192-#200: nine consecutive. The
audit's first finding (Appendix A, scope note) is that the true count is **twenty-two**:
every entry the frame has produced since it opened (#179, 2026-08-19) grades "frontier
UNMOVED." The rule did not fire for six days and nineteen further entries because #179
pre-declared "UNMOVED by design," which reclassified non-movement as intended output and
disarmed the tripwire. The declaration was honest (the backlog's builds were measurements
and hardenings), but a frame that can pre-exempt itself from the only rule that audits
frames is a hole in the discipline, not a use of it. Section 7 repairs the rule.

## 2. What the frame banked (the inventory, with the honest re-count)

Twenty-two entries in seven days. The deliverables that survive the audit as durable assets:

- **The assembled instrument bank**: the e2an pipeline (any coefficient lattice scored
  cell-by-cell), the F_q shadow column with its exact table (e2ay, the standing regression
  control), the zeta-to-Beurling ramp as a calibration axis (e2bc), the Lean `SPInterface`
  with the genus-1 satisfiability matrix machine-checked (#197), and two certificates:
  completeness failure as a THEOREM about the control (#199, $c_{DH} = 0.330$) and
  certification cost as a THEOREM about the instrument (#200, budget sharp to 3-5x,
  $\sigma^* = 0.2367$, the $e^{\gamma_1^2\sigma^2}$ price with its constant).
- **Genuinely new objects, mostly early**: the $4(\cosh(\delta u)-1)$ implant envelope and
  the $U^* = 1.41/\delta$ detector curve (#192), the Lorentzian ghost, the pole-array
  regularizer (#193), the measured trilemma (#194), the eta-Hessian holomorphy mechanism
  and $b_6(\kappa) = (\kappa^2+1)\log 6$ (#196), the AFE point-vs-sweep imbalance lesson
  (#199).
- **The re-counted coordinates.** The ledger counts coordinate systems #4 and #5 plus two
  refinements. The audit's derivability test (Appendix A, tier row 7) re-counts: #194,
  #195, and #199 are ONE wall statement (funding must flow through uniformity/limit
  structure exactly where the Euler side's transform vanishes; #148/#160's clause) seen
  from the metric, family, and control sides respectively; #196 is separate (a route
  closure with a mechanism). Net: **one wall restatement from three sides, plus one route
  kill.** The three sides are not worthless (each is a different instrument's testimony to
  the same wall, and #196's kill is real), but the ledger's counting overstated the
  constraint production, and PHASE_STATE language should inherit the re-count.
- **Four ceiling theorems/laws** (#191, #195, #199, #200): each is the frame's own
  mathematics proving that its instrument class cannot decide the open joints at reachable
  scale. The audit's sharpest observation: these are exit conditions that the loop filed
  as output. #200 even prices the one probe left in the queue (B1) in advance.

## 3. Choices stated as law (condensed; full table in Appendix A)

Three load-bearing clauses turn out to be CHOICES with no failure state: **"wrongness gets
coordinates"** (22/22 builds minted something; no outcome can grade the frame wrong),
**"UNMOVED by design"** (the corridor audit's choice G rebuilt verbatim: the frontier is
defined as exactly what the frame's own theorems place past every finite instrument, so
the miss is guaranteed and then exempted), and **the SP five-component typing** (at the one
machine-checkable register, #197's `sans5_sp5_iff_hasse` shows the missing fifth field is
EQUIVALENT to the full Hasse content: the decomposition relocates RH into two cells and
makes the rest scoreable; measured compression at that register is zero). The theorems
(#199, #200) are genuine but scoped to control and instrument; the horizon laws'
universal quantifiers ("every direct-minimization instrument") are class generalizations
from the families the frame owns, with #188's horizon-optimality question the recorded
open edge of exactly that quantifier.

## 4. Blind spots (condensed; full text in Appendix A)

1. **No derivative term in the scoring**: one entry per build, one coordinate per entry, so
   the loop cannot see that underived-constraint production hit zero around #197 (late
   entries self-describe as "no new mathematics," "confirmed," "restated," "nothing
   mysterious remains"). That profile is what a completed instrument bank looks like; the
   bookkeeping had no way to say so.
2. **The bracket discipline degrades at the frontier**: controls run as input twins through
   the one pipeline (never rival pipelines); #195 proved the bracket's separating power
   dies at accessible precision exactly where the limit clauses live; late builds pay rule
   2 with typed refusals (a control that cannot be posed tests nothing); and there is no
   sufficiency-side control (nothing in the battery consumes both clauses and still fails,
   the R3.5 shape).
3. **Same-session grading**: poses, builds, nulls, grades, mints, all one session: the #133
   topology's third instantiation. No external reader has checked any zeta-column claim
   yet (P12 unsent).

## 5. Frame falsifiers (adopted as standing checks)

From Appendix A, kept live even after closure: (1) the DERIVABILITY CHECK: any future
minted coordinate whose content can be worded from #148/#160/#194's clause in the entry's
own cross-refs is a re-measurement, not a coordinate (this check is adopted repo-wide for
coordinate claims, effective now); (2) if B1 lands where #200's budget prices it, the bank
is fully priced and closed with no residue; if instead C1 BINDS inside the costed meters,
that is a genuine event and reopens attention; (3) a bracket section consisting only of
typed refusals pays nothing; (4) if super-resolution beats the birthday/precision scales
(#188's hand-off), the ceiling quantifiers are family artifacts and the correct response
is a NEW instrument frame; (5) further portfolio decay while bank-maintenance builds run
would repeat the corridor pattern.

## 6. Opportunity cost (condensed; full report in Appendix B)

The decay gradient points at shipping and verification, not the frontier: the WATCH graded
sweep 24 days overdue (the portfolio's one unbounded cost; partially mitigated by
`lit_watch`'s ungraded harvest); P12 four days idle post-draft with three external groups
(CCM, Suzuki, Groskin) live on the same object (the highest scoop risk); P2 review
momentum four days old with the dependency PR merging; the day-old local-Lean capability
(#197) unused against a 44-target VerifierQueue that #199/#200 just enlarged; adversary
debt (e1ad's six posed cases, e1v's two open cases, the e1z conservation anomaly). State
drift caught and fixed same-day (Section 8). Appendix B's ranked sessions are adopted in
Section 7.

## 7. Verdict, disposition, and the rule repair

**Verdict: INSTRUMENT-COMPLETE-AND-CLOSE**, both lenses concurring, effective now.

1. **The frame closes as a completed instrument bank.** No new probes open inside it. The
   pipeline, the F_q column, the ramp axis, the SPInterface, and the two certificates are
   preserved as regression infrastructure (e2ay stays the standing control for any future
   candidate object).
2. **B2d and B1 are reclassified** from "hardening tier" to BANK MAINTENANCE: bounded,
   pre-costed closing moves, permitted later (Appendix B ranks them fourth), carrying
   falsifier 2 as their pre-registration. The frame closes now regardless; B1's landing
   only decides whether the bank's last cell reads "priced" or "C1 binds here."
3. **The successor question is deliberately not answered inside the audit.** The audit
   hands forward the sharpened wall statement (uniform-in-cutoff funding on the vanishing
   locus, one statement, three instruments' testimony) and the constraint that the
   successor must attack the limit clause directly and must NOT inherit cell-filling as a
   progress metric. Choosing the successor frame is the next ORCHESTRATOR decision, taken
   after the decayed assets clear (sessions 1-3 below), with the named candidate doors:
   the #188 horizon-optimality probe (the one queued item that could invalidate the
   ceilings' quantifiers), the #196 ACS path-integral door, next-step 2(a)'s positive
   meromorphic de Branges survey, and the 08A spine's M3-analytic rung.
4. **Next sessions, adopted from Appendix B**: (1) hygiene + the overdue WATCH graded
   sweep; (2) the first local VERIFIER round (the five VerifierQueue drafts, then the
   #199/#200/#196/#192/#191 nuggets); (3) P12 completion (figures, length pass, Owen's
   A/B/C decision sheet, then the courtesy communication); (4) B1 + B2d as the bank's
   closing session; (5) the horizon-optimality probe plus the e1ad/e1v adversary debt.
5. **The meta-rule is repaired** (PHASE_STATE, falsifiability triggers, same-day): the
   three-consecutive-UNMOVED rule stands, and a frame may NOT pre-exempt itself by
   declaring its verdicts in advance. A frame that expects UNMOVED verdicts must instead
   register at creation as an INSTRUMENT frame with a finite completion list and an expiry
   (list drained or a session budget); instrument frames are audited at expiry instead of
   per-verdict. Audits reset the counter. Retroactively, this backlog was an instrument
   frame with a finite list; under the repaired rule it would have been audited at the
   list's draining, which is within one session of when this audit actually ran: the
   repair codifies the near-miss.

The stance reading, for the record: this closure is progress, not retreat. The frame
turned a directive ("build the object, wrong is ok") into an instrument bank whose two
headline cells are now theorems, and its ceilings are the sharpest statement yet of where
the real proof cannot come from. Banking it and moving the sessions to the decayed assets
is the compass working.

## 8. Same-day resolutions and actions (2026-08-25)

- TODO.md: the three stale open checkboxes (A3, C1, B2: executed 2026-08-22/23 as
  #192/#193/#194) struck; the backlog line retitled to bank-maintenance.
- PHASE_STATE.md: the stale Last-verified git block corrected (origin in sync at `28c0aa4`
  at audit time); the header pointer advanced through #201; the falsifiability trigger
  rewritten per Section 7.5; the audit recorded as the current-state paragraph; next steps
  replaced by Section 7.4's ranking.
- The regression battery re-run (the after-every-merge rule was two merges behind);
  result recorded in PHASE_STATE's Last-verified block.
- LEARNINGS #201 written (this audit); the backlog doc's header annotated CLOSED
  (bank-maintenance items B1/B2d remain listed with their reclassification).

## Appendix A: Report 1, the frame-assumption audit (verbatim)

# Frame audit: the e2an SP-object pipeline / construction-backlog frame (#179-#200)

Scope note before the table: the trigger names nine consecutive UNMOVED verdicts (#192-#200). The ledger shows the true count is twenty-two: every entry this frame has produced since it opened (#179, 2026-08-19) grades "frontier UNMOVED." The standing meta-rule fires at three. It did not fire for six days and nineteen further entries because #179 pre-declared "UNMOVED by design," which reclassified non-movement as intended output and disarmed the tripwire. That enforcement gap is itself a finding of this audit.

### 1. Tier table

| # | Claim | Tier | Justification |
|---|---|---|---|
| 1 | "Wrongness gets coordinates" (backlog rule 5) | CHOICE | A scoring rule with no failure state: 22/22 builds minted something (a law, a lesson, a refusal, a "coordinate"). Refuted pre-registrations are findings (#180, #183), unlisted fork branches are findings (#196), kills that do not fire are findings (#194). No build outcome can grade the frame itself as wrong. |
| 2 | "Frontier UNMOVED by design" (#179) | CHOICE | The corridor audit's choice G rebuilt verbatim: the frontier is defined as C1/C2, which the frame's own theorems place past every finite instrument, so UNMOVED is structurally guaranteed; "by design" then converts the guaranteed miss into a non-event and exempts the frame from the three-UNMOVED trigger. Self-sealing. |
| 3 | The SP five-component decomposition is the right typing of the missing object (interface doc; #179, #197) | CHOICE | The interface doc is self-declared synthesis ("no new mathematics"); SP1 "separates nothing" by its own status line. At the one register where the typing is machine-checkable, #197's `sans5_sp5_iff_hasse` proves the missing fifth field is EQUIVALENT to the full Hasse content: measured compression at the checkable register is zero. The decomposition relocates RH into two cells (SP2-completeness IS RH, uniform SP5 IS M4) and makes the other cells scoreable; that it carves anything is untested. |
| 4 | D-H invisibility is structural, any circumference ($c_{DH} = 0.33032$) (#199) | THEOREM | Ball-arithmetic certified, two independent mathematical routes, selectivity controls certified. A genuine theorem, but about the CONTROL: it proves the frame's own meter class blind. |
| 5 | The $e^{\gamma_1^2\sigma^2}$ certification price is forced (#200) | THEOREM at family scope, CHOICE at totalization | T1-T3 proven for the e2ao assembly family ("within this assembly family, ANY parameter schedule pays it"); sufficiency proven, necessity measured (factor-5 bracket). "The frame proved its own ceiling" is accurate for the families the frame owns and unproven across all instruments: #188's horizon-optimality hand-off records exactly that as open. |
| 6 | #194's vanishing-locus coordinate is a new constraint | MEASURED, quietly generalized | The trilemma is real measurement, but scoped to three diagonal metrics (dossier: non-diagonal polarizations "deliberately untouched"; "a measured pattern with a structural reason, not a theorem"). The locus statement itself is a tautology about the multiplier ($L$ vanishes at its zeros), and the entry's funding conclusion lands verbatim on #148's determinant-class clause and #160's growth clause: a restatement with a mechanism, minted as "coordinate system #4." |
| 7 | The four M4 coordinates (#194, #195, #196, #199) are four new constraints | CHOICE of counting | Derivability test: #199 self-describes as "the #194 coordinate restated from the control side"; #195 self-describes as a "refinement" of #194, and its content (uniform-in-$\lambda$ funding) is #148/#154's Section-7 clause, the prior audit's choice D pattern (measured-identical at one family, promoted to a law). Only #196 is not derivable from #194: it is a route closure (eta curvature harmonic), itself the second-order face of #178's parity kill. Net: one wall restatement seen from three sides, plus one route kill. Counted as four. |
| 8 | #196's (1,1) forcing is conductor-independent | MEASURED, generalized | Measured at two conductors; the holomorphy mechanism is a "proof-sketch," the dossier says so; PHASE_STATE states it flatly as forced. |
| 9 | #191's horizon law ("horizon-limited for EVERY direct-minimization instrument") | MEASURED + certified bound | The bound $\lg B(a)$ is certified at seven rungs; the law $-2\pi e^{2a}/\ln 10$ is a fit; the quantifier "every direct-minimization instrument" is class generalization from one instrument family plus two in-print siblings read through the same lens. |
| 10 | The D-H/Beurling bracket disciplines every build (backlog rule 2) | CHOICE of axes | The 2x2 design matrix (Euler x lattice, completed at #182) assumes the conservation law's two clauses span failure space. In late builds the bracket degrades to typed refusals (#197: D-H unposable at the type level, Beurling a type error): discipline by definition, not by test. |

### 2. Blind spots

**The scoring has no derivative term, so deceleration is invisible.** Coordinate-minting rate, early vs late. #192-#196 minted genuinely new objects: the $4(\cosh(\delta u)-1)$ envelope and $U^* = 1.41/\delta$ curve, the Lorentzian ghost instrument, the pole-array regularizer and the F_q table, the measured trilemma, the holomorphy mechanism and the $b_6(\kappa)$ closed form. #197-#200 are, by their own language, formalization and hardening of the existing bank: #197 "no new mathematics" (typing), #198 "confirmed on the assembled object's own meters" (third register of #172/#188), #199 "restated from the control side," #200 "nothing mysterious remains in the #180 floor." The loop counts both phases identically (one entry per build, one coordinate per entry), so it cannot see that new-constraint production hit zero around #197. That is what a completed instrument bank looks like; the frame's bookkeeping has no way to say so.

**The frame's crowning theorems certify its own blindness, and the loop treats them as output rather than as an exit condition.** #199 proves the meter class cannot see the completeness failure; #200 proves the assembly family cannot reach the uniform margin; #191 and #195 price the window and discrimination directions. Four ceilings, each of them the frame's own mathematics saying further probes inside measure the instrument, not the problem. #200 even costs the one remaining probe (B1) in advance. No entry draws the operational conclusion.

**What the bracket discipline fails to test.** (a) Controls are input twins through the one e2an pipeline, never rival pipelines: the corridor audit's finding, unrepaired. (b) The bracket's separating power provably dies exactly where the frontier lives: #195 measured that true-RH and false-RH forms are indistinguishable at accessible precision in the CCM family, so bracket verdicts at finite scale carry no information about the limit clauses, by the frame's own theorem. (c) There is no sufficiency-side control: no object with both clauses but no polarization exists in the battery, so the bracket detects fare-dodging but cannot detect a construction that consumes both clauses and still proves nothing (the R3.5 shape). The K1 oracle counter checks input provenance, not claim circularity: e2an's own scope section concedes the emergent-zero mechanism is "$|L|$ dipping on the line, dressed as a cokernel." (d) Typed refusals (#197) convert the discrimination test into a definition: a control that cannot be posed tests nothing.

**Who grades movement.** Same session poses, builds, defines nulls, grades UNMOVED, mints the coordinate: the #133 topology, third instantiation. No external reader has checked any zeta-column claim (P12 drafted 2026-08-21, unsent; the courtesy communication pending).

### 3. Frame falsifiers

1. **Derivability check on the next minted coordinate.** If the next two builds' coordinates can each be worded as #148/#160/#162/#194 ("restated," "refined," "confirmed" in their own cross-refs), the frame is re-measuring its wall. Checkable: the LEARNINGS entry's own citations.
2. **B1 lands where #200's budget says it must.** If the coupled SP4 ladder's residual floor tracks T1's costing with no binding inside the costed meters, the last measured-only cell becomes another priced ceiling and the bank has no cells left that are not theorem-about-instrument or RH-equivalent. Checkable: B1 dossier vs e2be Section 4.1.
3. **A bracket verdict consisting only of typed refusals.** Any future build whose D-H/Beurling section names no run-and-fail cell has stopped paying backlog rule 2; continuing under "discipline passed" would be wrong. Checkable: each dossier's bracket table.
4. **Horizon-optimality resolves against the ceiling.** If a super-resolution register beats the birthday/precision scales (#188's open hand-off), the ceiling theorems are family artifacts, "priced, not inhabited" is wrong, and the correct response is a new instrument frame, not this one. Checkable: the e1ad hand-off status.
5. **Portfolio decay repeats the corridor pattern.** P10 CLEAR-TO-PR since 2026-07-02 (eight weeks), WATCH cadence overdue since ~2026-08-01, P12 finishing items idle: if these age further while hardening builds continue, the frame is consuming sessions its own theorems say cannot move the frontier. Checkable: PHASE_STATE / PUBLICATIONS dates.

### 4. Verdict recommendation

**INSTRUMENT-COMPLETE-AND-CLOSE.** (1) The frame's own strongest results (#199, #200 theorems; #191, #195 laws) certify that its entire instrument class cannot decide either open joint at any reachable scale, and #200 pre-prices the one probe left in the queue: by the frame's own mathematics, further probes measure the instruments, not the problem. (2) The scoring rules ("wrongness gets coordinates" plus "UNMOVED by design") admit no observable failure state, and minting has demonstrably decayed to restatement since #197: a frame that cannot fail and no longer produces underived constraints should be banked as regression infrastructure (the pipeline, the F_q column, the ramp axis, the SPInterface), with B1/B2d optionally run later as bank maintenance, and the sharpened M4 statement (uniform-in-cutoff funding on the vanishing locus) handed to a successor frame that attacks the limit clause directly. The successor should not inherit cell-filling as a progress metric.

## Appendix B: Report 2, the portfolio and opportunity-cost audit (verbatim)

# Portfolio and opportunity-cost audit: the e2an/backlog frame (2026-08-19 to 2026-08-25)

Method replicated from `docs/03_research/ccm_corridor_frame_audit.md` Section 5 / Appendix B (2026-07-17). Repo state verified on disk 2026-08-25: HEAD = origin/main = `28c0aa4` (#200), tree clean. One correction to the tasking's known facts up front: the #199/#200 rounds are now committed AND pushed (PHASE_STATE's Last-verified git block, which still says "#199 unpushed, #200 uncommitted", is stale). The P2 dependency-on-another-PR detail is taken as given; the in-repo record ends at the 2026-08-21 reviewer engagement.

### 1. Decay list

**HIGH stale-risk (the decay gradient points here):**

| Item | Idle | Cost of idleness | Clear |
|---|---|---|---|
| WATCH graded sweep | Last full sweep 2026-07-18; cadence due ~2026-08-01: 24 days overdue, 38 days since sweep | The inventory's one unbounded cost (a missed R1 event); partially mitigated by `lit_watch` (2026-08-20, 22 papers, listing-level, ungraded against the R1 shapes / D-H / Beurling screens) | S |
| P12 finishing | Measurement closed and draft v0.2 written 2026-08-21; 4 days idle on Owen decisions, figures F1-F4, length pass | Highest scoop risk in the portfolio: CCM (2511.22755), Suzuki (2606.09096), Groskin (2605.20224, 2607.02828) are actively publishing on exactly this object, and the horizon law addresses their self-named main obstacle | M (figures + length pass one session; Owen minutes for decisions), then the courtesy communication |
| P2 Mathlib PR #41132 | Reviewer engaged 2026-08-21 (4 days) | Review momentum decays by response latency; the stated dependency PR merging is a poll-now event; P10 is queued behind it | S (poll + reply prep in Owen's words) |

**MEDIUM stale-risk:**

| Item | Idle | Cost of idleness | Clear |
|---|---|---|---|
| P10 Mathlib PR | CLEAR-TO-PR 54 days; port build-verified 39 days | Master drift raises the re-port cost weekly (the port was verified against a 2026-07-17 checkout); sequencing behind P2 is a choice, not a blocker for prep | S-M (steps recorded as mechanical) |
| VerifierQueue exploitation | Local `lake build` GREEN since 2026-08-24 (1 day); 44 OPEN targets; 5 sorry-bodied drafts unimported since 2026-08-20; #199/#200 added the e2bd certificate skeleton, the Gaussian-tail chain, the Rosser-Schoenfeld partial-summation lemma ($\sum\Lambda(n)/\sqrt n \le 2.08\sqrt N$), the interpolation lemma; #196/#192/#191 nuggets queued | Targets are stable, but the week's theorem-grade findings sit measured-not-machine-checked while a new local capability goes unused; adversary/verifier debt is the loop's known blind spot | S-M per batch |
| Adversary debt | e1ad round: six cases posed 2026-08-20, none run (5 days); e1v cases 3 and 7 open since 2026-08-08 (17 days); e1z conservation-failure question (#175 residue) ~1 week | Findings in the record without adversarial hardening; the e1z conservation failure is an unexplained anomaly in a control (could mask an instrument bug) | S each |

**LOW / NONE stale-risk (conjecture-stable or human-gated):** B1 coupled SP4 ladder (queued 6 days, cost M, now pre-costed by e2be's T1 budget) and B2d frontier-capacity function (S): by design, not decaying. The #188 horizon-optimality question (5 days; frontier-adjacent, the only queued item that could weaken a standing obstruction, #172). The #196 ACS path-integral door (2 days; the eta arc's only open trigger, M). Next-step 2(a), the positive meromorphic de Branges extremal survey (16 days; it is the corridor exit rule's named reopen condition, so leaving it unresolved leaves a live falsifiability trigger unserviced; S-M). C3's costed residue (D-H flip at horizon dps; M compute, priced). P12's optional depth-double to T~3000. P9 (human-gated 55 days, scoop risk only). P4/P5/P6/P7 portfolio decisions (Owen-gated, ~2 months, now out of date with the registry). Connes 50-entry reproduction (9 days). Citation-check #5 re-run (39 days, contained by its checkbox). DigammaExtras `logDeriv_mul` breakage (latent; fires only on a Mathlib bump). Toy sandbox, older research threads, docs/viz/ML backlogs: no decay.

**State drift caught this pass (same class the 2026-07-17 audit caught):**
- TODO.md lines 19-21 still show A3, C1, B2 as open checkboxes; all three were executed 2026-08-22/23 (#192, #193, #194). Stale for 2-3 days short of the compaction rule but factually wrong now.
- PHASE_STATE.md: the Last-verified git block predates the #199/#200 commit+push (says unpushed/uncommitted; reality: origin in sync at `28c0aa4`); header pointer still reads "LEARNINGS.md (through #198)" while #200 exists.
- `run_all_tests` last GREEN 15/15 at #188 (2026-08-20); two merges since without a battery run (housekeeping rule says run after every merge; e2bd/e2be add no shared code, so risk is low but the rule is broken).

### 2. Ranked next 3-5 sessions

1. **Hygiene + watch sweep (S; hygiene + instrument).** Fix the three stale TODO checkboxes and the PHASE_STATE git block/pointer, run the battery, poll P2 (and the dependency PR) with reply prep, then execute the graded WATCH sweep (the `lit_watch` harvest makes it cheap). Justification: clears the only unbounded-cost exposure and every drift item in one session.
2. **First local VERIFIER round (S-M; instrument + shipping).** Import and verify the five VerifierQueue drafts, then discharge the newest finite nuggets (#200's three, #199's skeleton, #196 holomorphy-tracelessness, e2ax max-at-zero, e2aw seed facts). Justification: converts the week's measured theorems into machine-checked ones, exploits a 1-day-old capability against a 44-target queue, and services the loop's verification debt.
3. **P12 completion (M; shipping).** Figures F1-F4, length pass, and an A/B/C decision sheet for Owen (name block, categories, courtesy communication). Justification: the portfolio's highest scoop risk, with three external groups live on the same object; finished mathematics idle past the audit's own two-week line is exactly what the 2026-07-17 precedent front-loads.
4. **B1 + B2d as the frame's closing session (M + S; frontier-facing).** B1 is pre-costed by e2be's budget and targets the bank's one remaining measured-only cell (the SP4 residual floor, the C1/R1 joint); B2d is small. Justification: bounded completion moves, pre-registered as the frame's last entries, no additions after.
5. **Horizon-optimality probe + e1ad adversary round (S-M; frontier-facing).** Prony/ESPRIT against the D-ladder, six adversary cases run in the same session. Justification: the only queued question whose answer could weaken a standing obstruction (#172 becomes a two-level theorem if the wall holds, frontier-adjacent if it breaks), plus adversary debt cleared.

### 3. Should the backlog frame close now?

Yes, with B1/B2d as its pre-registered closing entries rather than a reason to keep it open. The frame did what it was built for: the SP-object assembled, every joint measured, two headline cells upgraded from measurements to theorems (#199 control-side, #200 instrument-side), the heavy tier complete, five M4 coordinate systems minted: and all 22 entries returned "frontier UNMOVED", which is the precedent's exact signature of a healthy but self-similar frame whose marginal session buys a coordinate, not wall movement. B1 and B2d differ from a continuation in kind: they are bounded, pre-costed completion moves that close the bank's last measured-only cell, the same class the 2026-07-17 audit allowed as "corridor completion, then pause". So: declare the frame CLOSING now, admit no new probes into it, run sessions 1-3 (the decayed assets) before B1/B2d, and close the frame when B1 lands regardless of its verdict. The decay gradient, as last time, points at the shipping/verification pipeline, not at the frontier.
