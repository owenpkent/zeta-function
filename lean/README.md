# Lean 4 / Mathlib formalization of the zeta function proof program

> Formal verification stack for the AI-only proof program. See [`docs/03_research/proof_program_ai_only.md`](../docs/03_research/proof_program_ai_only.md) §2.2 for context.
>
> **Goal**: every structural claim in the project (R3.5 no-shortcut theorem, 4E.3 line-restriction lemma, the eventual Hodge index theorem) is translated to Lean 4 and verified by Mathlib's kernel.

## Status

**Update (2026-07-12): the five classical e1o VERIFIER targets landed (`S4Carrier.lean`, #S4C-1..#S4C-5), the S4/R1 arc (LEARNINGS #162), all sorry-free and axiom-clean.** The e1o probe ([`../experiments/spectral/e1o_s4_carrier.md`](../experiments/spectral/e1o_s4_carrier.md)) posed Stepanov's S4 move on the CCM carrier and measured the slot empty; its five flagged targets are deliberately CLASSICAL and are now machine-checked. #S4C-1 the prime-side/Euler-gate inequality (nonnegativity of the coefficients is the explicit, load-bearing hypothesis, the corrected #161 form; `Λ ≥ 0` discharges it, `Λ_DH` sign-changing fails it). #S4C-2 the tail divergence (`e^k/(k+1)^2 → ∞` ⟹ the band-limited pairing against the full comb is not summable; the one classical input, a Chebyshev `ψ(x) ≥ c·x`, is carried as a hypothesis = the Mathlib gap). #S4C-3 the structural nil (`span{e^{±iθ}} = span{cos,sin}`, the per-frequency change-of-basis atom behind "the carrier's fixed-type space is the generic trig space"). #S4C-4 the decimation rank-1 collapse (every function in the K-decimated space is constant on the AP nodes, via `span_induction` + `exp_int_mul_two_pi_mul_I`). #S4C-5 the trig-Vandermonde nonsingularity (distinct circle points pay full price, via `Matrix.det_vandermonde_ne_zero_iff`). All nine declarations `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build green (3762 jobs).

**Update (2026-07-10, cont. 2): the Borger VACUITY core landed (`BorgerVacuity.lean`, #MTF-3), #MTF-1 reached its THIRD independent consensus witness (now CANONICAL), and the model-theoretic Frobenius object T2 was advanced to a machine-checked partial with the exact Mathlib wall pinned; T4 is marked BLOCKED-ON-MATHLIB.** New module `BorgerVacuity.lean` (build green, 3761 jobs), sorry-free, all `#print axioms` ⊆ `[propext, Classical.choice, Quot.sound]` (several need only `[propext, Quot.sound]`).
- **#MTF-1 consensus witness #3** (`ng1_rigidity_w3` / `ng1_rigidity_w3_apply`): a THIRD independent re-derivation of the NG1 rigidity kernel (`σ.comp (algebraMap ℚ R) = algebraMap ℚ R` for every ring endomorphism `σ` of every commutative ℚ-algebra `R`), by a route DISTINCT from witnesses #1 (`ModelTheoreticFrobenius.ng1_rigidity` via `RingHom.ext_rat`) and #2 (`TamenessTrade.ng1_rigidity_indep` via explicit `num`/`den` unit cancellation): the universal property of `ℚ` as the localization / fraction field of `ℤ` (`IsLocalization.ringHom_ext` with `Rat.isFractionRing`), reducing to `ℤ` being initial in commutative rings (`Int.subsingleton_ringHom`). With three independent formalizations, **#MTF-1 is now CANONICAL under the four-layer consensus rule**.
- **#MTF-3, the Borger VACUITY** (`borger_lift_congruence_vacuous`, PROVEN): in a commutative ℚ-algebra every standard prime `q` is a unit (`natCast_isUnit_of_ne_zero`), so `Ideal.span {(q:R)} = ⊤` (`span_natCast_eq_top`), the residue ring `R ⧸ (q)` is `Subsingleton` (`quotient_span_natCast_subsingleton`), and the Frobenius-lift congruence `ψ x ≡ x^q [SMOD (q)]` holds for EVERY `ψ : R →+* R` and every `x` (`borger_lift_congruence_vacuous`; the two-endomorphism face `borger_lift_any_two_congruent`). This is the machine-checked corrected #156 finding: the ultrafilter TRIVIALIZES (does not destroy) the Λ-structure by inverting the primes the congruences were about.
- **#MTF-2, PARTIAL** (`productFrobenius` + `productFrobenius_fixes_diag`; `germRingHom`): the coordinatewise Frobenius on Mathlib's genuine DEPENDENT ultraproduct `Filter.Product (fun p => ZMod (P p)) l` is built as a bare function and proven to fix the diagonal integers (the Fermat coordinatewise witness lifted to the ultraproduct, `ZMod.pow_card`; note the proof uses no filter property, the machine-checked shadow of NG1 limit-independence). Separately, `germRingHom` shows a coordinatewise ring endomorphism descends to a ring endomorphism of the NON-dependent germ ring `Filter.Germ l R`. **The exact wall**: Mathlib gives `Filter.Product` only a model-theoretic `FirstOrder.Language.Ultraproduct.Structure` (+ Łoś), NOT a `CommRing`/`Field`/`IsAlgClosed` instance, so `productFrobenius` cannot be a `RingHom` and "σ is a field automorphism / char 0 / Fix σ is pseudofinite" need the ring-language + `CompatibleRing` + Łoś transport that Mathlib does not provide automatically (feasible, multi-day).
- **#MTF-4, BLOCKED-ON-MATHLIB**: the NG2(iii) cardinality dichotomy needs a `Saturated` / κ-saturation predicate on `FirstOrder.Language.Structure`, the type-realization (`CompleteType`) machinery, and the internal-subset (ultraproduct-of-subsets) API on `Filter.Product`; Mathlib has none of these. Precise failure mode recorded in `BorgerVacuity.lean`; building saturation theory is out of scope.

**Update (2026-07-10, cont.): the two surviving tameness-trade pinning toys landed (`TamenessTrade.lean`, #TT-1/#TT-2) and #MTF-1 reached a second independent consensus witness.** The residual Lean targets of LEARNINGS #157 ([`../docs/03_research/tameness_trade.md`](../docs/03_research/tameness_trade.md)), which are VALUE-PINNING toys stating the ingredients of the tameness/wildness fault line (NOT the refuted Leg B). **#TT-1** (`two_mul_mul_eq`, `two_mul_mul_add_sq_eq`): multiplication is expressed through addition and squaring, `2*x*y = (x+y)^2 - x^2 - y^2` (ring form over any `CommRing`) and the subtraction-free `2*x*y + (x^2+y^2) = (x+y)^2` (over any `CommSemiring`, valid over ℕ). This is the ARITHMETIC CORE of J. Robinson's squares definability toy; per the dossier we formalize the algebraic identity rather than the full `Set.Definable` wrapper (which would need a bespoke `FirstOrder.Language` + `Structure` on ℕ for zero RH content), and the docstring records the trade-off honestly. **#TT-2** (`prime_iff_vonMangoldt`): the primes are definable from the von Mangoldt function, the full `↔` `n.Prime ↔ Λ n ≠ 0 ∧ ∀ m < n, Λ m ≠ Λ n` over `ArithmeticFunction.vonMangoldt` (both directions proved: forward via `vonMangoldt_apply_prime` + `log`-injectivity forcing `m.minFac = n` hence `n ∣ m`; reverse via a proper prime power's `n.minFac < n` violating the minimality clause). The "weight recovers the support" (D1 → D3) step, machine-checked. **#MTF-1 consensus witness #2** (`ng1_rigidity_indep` / `ng1_rigidity_indep_apply`): an INDEPENDENT re-derivation of the NG1 rigidity kernel from its statement in `model_theoretic_frobenius.md` Section 3 (`σ.comp (algebraMap ℚ R) = algebraMap ℚ R` for every ring endomorphism `σ` of every commutative ℚ-algebra), by a DISTINCT proof route: not `RingHom.ext_rat`, but the hands-on `map_natCast`/`map_intCast` mechanism plus unit-cancellation of `(q.den : R)`. The existing `ModelTheoreticFrobenius` #MTF-1 declarations were re-`#print axioms`-checked and remain axiom-clean; the NG1 core is now at 2 independent formalizations. All new `#print axioms` ⊆ `[propext, Classical.choice, Quot.sound]` (the two ring identities need only `propext`). Build green (3754 jobs).

**Update (2026-07-10): the model-theoretic Frobenius arc's Lean target T1 discharged (`ModelTheoreticFrobenius.lean`, #MTF-1), sorry-free and axiom-clean.** The NG1 kernel of LEARNINGS #156 ([`../docs/03_research/model_theoretic_frobenius.md`](../docs/03_research/model_theoretic_frobenius.md)): the endomorphism-rigidity of imported arithmetic, in four faces. (a) Prime-ring rigidity: `endo_fixes_natCast` / `endo_fixes_intCast` (every unital ring endomorphism fixes every ℕ/ℤ cast). (b) The division-ring form: `endo_fixes_ratCast` (every endomorphism of a division ring fixes every rational cast; on a characteristic-0 division ring the rational casts ARE the prime field, so the fixed set of any endomorphism-shaped Frobenius contains all of ℚ). (c) The Fermat coordinatewise witness: `fermat_little_zmod` (`x^p = x` on `ZMod p`), `frobenius_zmod_eq_id` (the local Frobenius IS the identity on the prime field at every `p`), `zmod_endo_eq_id` (the prime field is endomorphism-rigid outright). (d) The NG1 composite: `ng1_rigidity` / `ng1_rigidity_apply` (every ring endomorphism of every ℚ-algebra fixes the canonically imported ℚ pointwise, `σ ∘ algebraMap ℚ R = algebraMap ℚ R`; covers the dossier's non-field receptacles, adeles and `∏ ℤ/p²/𝒰` and Witt ultraproducts, in one statement, with no ultrafilter anywhere in statement or proof). Deliberately near-one-liners over Mathlib (`map_natCast`/`map_intCast`/`map_ratCast`/`ZMod.pow_card`/`ZMod.frobenius_zmod`/`RingHom.ext_zmod`/`RingHom.ext_rat`); per the dossier, the value is pinning the project-facing statements. All eight `#print axioms` ⊆ `[propext, Classical.choice, Quot.sound]` (three need no `Classical.choice`). T2-T4 (#MTF-2..#MTF-4: the ultraproduct object, the Borger vacuity statement, the cardinality dichotomy) remain open. Build green (3753 jobs).

**Update (2026-07-02): the Gauss-lemma floor generalized to Mathlib's RationalRoot idiom (`RationalRootFloor.lean`, #RR-1/#RR-2), plus the minimal-degree rigidity clause (#GF-6), all sorry-free and axiom-clean.** The P10 FORMAL-axis deliverable (PUBLICATIONS.md P10; SURVEYOR prior-art check 2026-07-02 = CLEAR-TO-PR): the rational root theorem WITH MULTIPLICITY, stated over a general UFD `A` with fraction field `K` in exactly the typeclass context of Mathlib's `RingTheory/Polynomial/RationalRoot.lean` (whose `den_dvd_of_is_root` is the `m = 1` case). **#RR-1** `den_pow_rootMultiplicity_dvd_leadingCoeff`: `(den A r) ^ rootMultiplicity r (p.map (algebraMap A K)) ∣ p.leadingCoeff`, unconditional (multiplicity 0 is trivial; a guard `example` re-derives the `m = 1` theorem, and a second guard instantiates `ℤ/ℚ`). **#RR-2** `prod_den_pow_rootMultiplicity_dvd_leadingCoeff`: the multi-point product over any `s : Finset K`. The denominators of distinct points need NOT be coprime in `A` (over `ℤ`: `1/4` and `3/4`), so unlike #GF-3 the recombination cannot happen on the `A` side; the honest route is the polynomial side, where the root factors `X - C r` at distinct points are pairwise coprime over the field `K`. Supporting lemmas, each independently PR-worthy: `isPrimitive_pow` / `isPrimitive_prod` (powers and finite products of primitive polynomials over a UFD, obtained through the `Nonempty (NormalizedGCDMonoid A)` instance inside the proofs so the public statements stay UFD-only) and `dvd_of_fraction_map_dvd` (ONE-SIDED Gauss descent: only the divisor needs to be primitive, routing the dividend through its primitive part; relaxes Mathlib's two-sided `IsPrimitive.dvd_of_fraction_map_dvd_fraction_map`). **#GF-6** (`GaussFloor.lean`): `canonical_dvd_of_vanishing` (the canonical product divides EVERY prime-forced vanisher; the linear factors `p·X - 1` are NOT pairwise coprime in `ℤ[X]`, e.g. `2X-1 ≡ 5X-1 mod 3`, so the recombination is again polynomial-side at the distinct roots `1/p`) and `gauss_floor_rank_one` (any vanisher of degree at most `D = ∑ m_p` IS an integer multiple of the canonical product: the vanisher lattice at the minimal degree has rank 1, the e2ah minimal-degree rigidity probe machine-checked). All new `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build green (3752 jobs).

**Update (2026-07-01): the Gauss-lemma height floor of the vF disc model (`GaussFloor.lean`, #GF-1..#GF-5), sorry-free and axiom-clean.** The van Frankenhuijsen deep-read ([`../docs/03_research/reading_notes/vanFrankenhuijsen-2008-Nevanlinna-RH.md`](../docs/03_research/reading_notes/vanFrankenhuijsen-2008-Nevanlinna-RH.md), Section 8) flagged one Mathlib-reachable target: the disc model's no-Siegel-lemma floor. Formalized end to end: `pow_dvd_leadingCoeff` (`(pX-1)^m ∣ f ⟹ p^m ∣ lead f`, via multiplicativity of `leadingCoeff`; NO polynomial coprimality is needed anywhere, the primes recombine as coprime INTEGER powers), `prod_primePow_dvd_leadingCoeff` (the finite-set divisibility floor via `Finset.prod_dvd_of_coprime`), `gauss_floor` (`∏ p^{m p} ≤ |lead f|` for `f ≠ 0`), the sharpness pair `canonical_leadingCoeff` / `canonical_attains_floor` (the vF canonical product ATTAINS the floor), and the capstone `gauss_floor_of_vanishing` (hypothesis = genuine VANISHING at `1/p` over ℚ via `rootMultiplicity`; the ℤ-descent is Gauss's lemma: `linearFactor_pow_isPrimitive` + `dvd_of_map_dvd`, a one-sided extension of Mathlib's two-sided-primitive `IsPrimitive.Int.dvd_iff_map_cast_dvd_map_cast` through the primitive part). With the vF multiplicities `m_p = ⌊log_p x⌋` the log of the floor is exactly Chebyshev `ψ(x)`: any prime-forced vanisher already has height at least the quantity the transferred Stepanov engine must bound, so the model's S3 pigeonhole slot is provably empty and the only open slot is the S4/R1 cheap-multiplicity operator. All four audited theorems `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Numerical companion [`e2ah_gauss_floor.py`](../experiments/arithmetic_geometric/e2ah_gauss_floor.py) (canonical product = `lcm(1..x)` integer-exact, cofactor + naive-interpolation stress, minimal-degree rigidity) PASS. Build green (3751 jobs).

**Update (2026-06-30): Front A (the real Lean floor) milestone 1 -- the `C x C` divisor / intersection-form scaffold, deriving `G_prim` from the intersection numbers (`CurveSquareIntersection.lean`, #2G-2).** Front A's goal is to formalize Weil's function-field RH end-to-end; the geometry Mathlib lacks (curves, divisors, Chow groups, the intersection product on `C x C`, the Frobenius correspondence) is the gap. Milestone 1 builds the Neron-Severi data of `C x C` ABSTRACTLY -- as the intersection NUMBERS, the geometric content -- and machine-checks the one step `HodgeIndex.lean` (2G) previously only ASSERTED in a comment: that "after projecting out the hyperbolic plane `{f1,f2}` the primitive form on `{Delta, Gamma}` is `G_prim`". `primPair` is the primitive pairing (project off the hyperbolic plane: `<D,E>_prim = D.E - (D.f1)(E.f2) - (D.f2)(E.f1)`); `primGram` assembles the `2x2` primitive Gram from the standard numbers (`Delta^2 = 2-2g`, `Gamma^2 = 2q(1-g)` by adjunction, `Gamma.Delta = q+1-t = #C(F_q)` by Lefschetz, fiber degrees `Delta.fi = 1`, `Gamma.f1 = q`, `Gamma.f2 = 1`); and `primGram_eq_Gprim` proves it EQUALS `Gprim g q t = !![-2g,-t;-t,-2gq]` sorry-free (via the four one-line `prim_*` intersection-number identities). Endpoints: `primGram_det` (`det = 4g^2q - t^2`, the Hasse discriminant), `curveSquare_negDef_iff_hasseWeil` (the `C x C` Hodge index ⟺ the Hasse bound, via the keystone `negDef_iff_hasseWeil`), and `curveSquare_elliptic_negDef_iff` (genus-1, the `EllipticFrobeniusData.hodge_index` input `t^2 < 4q` exhibited as the Hodge index of the primitive `E x E` form). So the geometry → `G_prim` step is now a theorem, narrowing the Front-A gap to the intersection NUMBERS themselves (the Frobenius correspondence / curve intersection theory Mathlib lacks). All `#print axioms` = `[propext, Classical.choice, Quot.sound]` (no `sorryAx`; the pre-existing #2H-1 sorry in HodgeIndex is NOT a dependency). Full build green (3747 jobs).

**Update (2026-06-28): lever B O1 isolated as a SINGLE typed contract on Mathlib's real object, everything downstream machine-checked (`FrobeniusExistence.lean`, #FF-O1-iface).** The new module states O1 directly on `WeierstrassCurve.localPolynomial`: `FrobeniusTateData R W` (a rank-2 integer Frobenius `A` with `det A`, `trace A` pinned to Mathlib's `q=#κ`, `a=q+1-#W(κ)`, and non-negative isogeny degrees). It is carried as the ONE named axiom `frobeniusTateData_exists` (cited Silverman III.8.6 / V.1.1; the FLT-adjacent scheme-theoretic content the pin lacks), NOT a `sorry` and NOT false. Non-vacuity (the #106 guard): `frobeniusTateData_companion` proves sorry-free, axiom-clean that the companion matrix `!![0,-q;1,a]` satisfies the contract whenever Hasse `a²≤4q` holds, so the axiom admits a KNOWN-TRUE statement. The derived `localPolynomial_root_normSq_of_O1` / `functionfield_RH_localPolynomial` close the unconditional conclusion sorry-free: every root of `W.localPolynomial` (good reduction) has `|β|²=1/q` (reciprocal eigenvalues on `|α|=√q`), via `hasse_of_matrix` + a new boundary-inclusive `localFactor_root_normSq_le` (non-strict, all finite fields). `#print axioms` of both derived results = `[propext, Classical.choice, Quot.sound, frobeniusTateData_exists]` -- exactly the foundational axioms plus the one named O1 axiom (no `sorryAx`). Route-B audit re-run against the pin: `E[n]`/Tate module/Weil pairing/isogeny-degree all ABSENT, FLT not a dependency. Build green (3746 jobs).

**Update (2026-06-27): four deferred Lean targets landed, all sorry-free and axiom-clean (`#print axioms` = `[propext, Classical.choice, Quot.sound]`; full library build green, 3744 jobs).**
1. **VT-NP1** (`VT_NP1.lean`, #VT-NP1): finite Euler-product zero-freeness on `Re(s) > 0`. `localFactor_ne_zero` (each local factor `1 - p^{-s} ≠ 0`, since `‖p^{-s}‖ = p^{-Re s} < 1` for `p ≥ 2`), `finiteEulerProductInv_ne_zero` (any finite truncation `∏_{p∈S}(1-p^{-s})` is zero-free), and `np1_reduction` (the M4-reduction: every finite stage is zero-free, so the off-line obstruction lives only in the infinite-product limit; NP-1 = NO, e2vv / #128). The analytic-side companion of #2G-1.
2. **R3.5 `GeometricPositivity` tightened** (#119): the placeholder `RiemannHypothesis L` is split so the cheap half cannot be read as the hard half. `RealizesSignatureInteger` (the free signature INTEGER σ = b₊ − b₋, RH-BLIND: `realizesSignatureInteger_rhBlind` holds for ζ AND Davenport-Heilbronn) vs `PrimitivePartDefinite` (the RH-equivalent definiteness = arithmetic Hodge standard conjecture). `GeometricPositivity` now routes through the latter (`geometricPositivity_iff_RH`), and there is deliberately no theorem `RealizesSignatureInteger L → RiemannHypothesis L`, blocking the "realizes the integer ⟹ proves definiteness" conflation.
3. **Toy Lean face** (`ToyModel.lean`, #TOY-1): the genus-1 grader correctness, BOTH directions, the formal companion of `experiments/toy/`. `toy_reproduce_weil` (`t² ≤ 4q ⟹ |α|² = q`, reusing `eigenvalue_modulus_le`) and the NEW off-line witness `toy_reject_fake` (`4q < t² ⟹ ∃` a Frobenius root OFF the circle, `|α|² ≠ q`, via the explicit real root `(t+√(t²−4q))/2`), packaged in `toy_grader_genus1_correct`.
4. **O1 named** (`FunctionFieldRH.lean`): the single open residual of lever B is now a precisely-stated Prop, `FrobeniusMatrixExists p` (existence of the rank-2 integer Frobenius matrix `A` with `det A = p` and non-negative isogeny degrees), with `functionfield_RH_of_frobeniusMatrixExists` reducing RH-for-the-curve to it. It is a `def` (an open target), NOT an axiom or `sorry`; the months-long FLT-adjacent construction is left open and named.

**Update (2026-06-27, cont.): the two isolable Arakelov-assembly targets VT1/VT2 formalized (`ArakelovAssembly.lean`, #2M-VT), sorry-free and axiom-clean.** The 2M Arakelov-side construction attempt (LEARNINGS #132, [`../experiments/arithmetic_geometric/2M_arakelov_construction_attempt.md`](../experiments/arithmetic_geometric/2M_arakelov_construction_attempt.md)) walled at the same R1+M4+PROP-global gap, but Front 3 isolated two RH-INDEPENDENT, K1-clean facts the dossier flagged for Lean; both now land. **VT1** (`perPrime_negDef_iff_hasse`, `perPrime_negDef_of_hasse`): the per-prime `(1,p)`-bidegree primitive Gram `!![-2g,-t;-t,-2g·p]` is negative definite ⟺ `t² < 4g²p`, which is `HodgeIndex.IntersectionSignature.negDef_iff_hasseWeil` at `q = p`, so VT1 is honestly a SPECIALIZATION (not new content). **VT2** (the new content): `conj_lowerRight` (the `D_s = diag(1,s)`-conjugated lower-right entry is `-2g·p·s²`, so equalizing the diagonal needs `p·s² = 1`), `no_single_normalizing_scale` (for distinct primes `p ≠ q` there is NO single `s` with `p·s² = 1 ∧ q·s² = 1`, the #25 `(1,p)`-bidegree obstruction as an assembly failure), `forced_scale_asymmetry_unbounded` (for any fixed `s > 0` the forced asymmetry `n·s²` is unbounded), and `assembly_obstruction` (the combined e2af no-go: both per-prime forms negative definite, yet no single scale assembles them). This does NOT close M4: the SIGN of the single regularized adelic scalar into which the per-prime couplings relocate is the open kernel. All `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build green (3745 jobs).

**Update (2026-06-15, cont.): pivot to the real target M4 -- the arithmetic polarization face of RH for ζ, formalized at the zero level (#M4-1).** New module `ArithmeticPolarization.lean` (build green, axiom-clean) lifts the function-field eigenvalue extraction to Spec(ℤ), making the #61/2HH "perfectness is free, positivity is the entire gap" decomposition machine-checked: `fe_partner_mem_nonTrivialZeros` proves that the **functional equation provides the perfect pairing** `ρ ↔ 1−ρ` on the nontrivial zeros (via Mathlib's `riemannZeta_one_sub`; a genuine theorem about ζ's zeros, not vacuous), and `riemannHypothesis_zeta_iff_fe_polarization` proves **RH ⟺ that FE pairing coincides with conjugation** (`1 − ρ = conj ρ`, i.e. the cup product is a polarization). The **reflection half is also proved**: `riemannZeta_conj` establishes `ζ(s̄) = conj ζ(s)` (from the L-series on `Re s > 1` + the identity principle -- a genuine result Mathlib lacks, itself upstreamable), so `reflection_mem_nonTrivialZeros` makes `conj ρ` a genuine zero too; the polarization condition `1 − ρ = conj ρ` is thus literally "two genuine zeros (the FE-partner and the conjugate) coincide". This is the exact arithmetic analogue of `FunctionFieldRH.eigenvalue_modulus` (where the conjugate root pairing + positivity forced `|α|²=q`, zeros on the circle; here the FE pairing + positivity forces `Re=1/2`, zeros on the line). Perfectness (the FE) is proved and is free (it holds even for D-H); positivity (the coincidence) is RH = the arithmetic Hodge standard conjecture (M4), the deep content NOT in this file. All `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build green (3730 jobs).

**Update (2026-06-15, cont.): the Weil-pairing machinery is now COORDINATE-FREE -- O2 holds on the abstract rank-2 Tate module, not just a fixed basis (#FF-M1B).** `TateModule.lean` adds the basis-independent layer: for any rank-2 `R`-module `M` with basis `b` and any alternating bilinear (Weil) pairing `ω`, `det_transform` proves the symplectic determinant law `ω (f x) (f y) = (LinearMap.det f)·ω x y` (any endomorphism scales the abstract pairing by its determinant -- the genuine, basis-free reason `deg=det`), `scaling_eq_det_free` gives the Weil degree = `LinearMap.det`, and `functionfield_RH_of_tate` is the coordinate-free route-B endpoint (rank-2 ℤ-lattice + alternating perfect pairing + Frobenius `frob` + degree-as-Weil-scaling + `deg ≥ 0` ⟹ `|α|²=det frob`, all finite fields). This matches the actual Tate module `T_ℓ E` (free rank 2, no canonical basis) and the abstract Weil pairing. All sorry-free, axiom-clean. The residual is now purely O1: construct `M`, `b`, `ω`, `frob`, and `deg ≥ 0` from a real curve (the FLT-adjacent scheme-theoretic input). Build green (3729 jobs).

**Update (2026-06-15, cont.): lever B M-1 route-B Weil-pairing substrate -- O2 ("deg is a quadratic form") is now a THEOREM, not an assumption (#FF-M1B).** New module `TateModule.lean` (build green, axiom-clean) formalizes the linear-algebra core of route B: `weilForm` (the symplectic model of the Weil pairing on `T_ℓ E ≅ ℤ²`), `weilForm_mulVec` (the symplectic determinant law: any endomorphism `A` scales the alternating form by `det A` -- the rank-2 reason the Weil-pairing formula `e(φx,φy)=e(x,y)^{deg φ}` forces `deg=det`), `scaling_eq_det` (the Weil-pairing degree IS the determinant), and `functionfield_RH_of_weil_pairing` (the route-B endpoint stated INTRINSICALLY, with NO `det` in its hypotheses: Frobenius matrix `A` + the degree given as the Weil-pairing scaling factor + non-negative degrees ⟹ `|α|²=det A`, all finite fields). This internalizes route B's O2 entirely; the open residual narrows to O1 (the existence of `A`, the Weil pairing, and `deg ≥ 0` -- the scheme-theoretic content Mathlib lacks, FLT-adjacent). All `#print axioms` = `[propext, Classical.choice, Quot.sound]`.

**Update (2026-06-15): lever B M-1 Phase A endpoint wired -- RH for the curve directly from the Frobenius matrix (route B), sorry-free and axiom-clean.** `FunctionFieldRH.lean` now closes the loop from Phase A's `hasse_of_matrix` to the eigenvalue extraction. `matrix_charpoly_root` grounds the abstract Frobenius "roots" in the genuine eigenvalues of the rank-2 representation: a complex `α ∈ spectrum ℂ (A.map (ℤ→ℂ))` is a root of `X²−(tr A)·X+(det A)` (via `Matrix.mem_spectrum_iff_isRoot_charpoly` + `Matrix.charpoly_fin_two`, with trace/det commuting through the cast). `functionfield_RH_elliptic_of_matrix` (the headline): over a prime field, a rank-2 integer Frobenius matrix `A` with `det A = p` and non-negative isogeny degrees (`det(m·1+n·A) ≥ 0` on the lattice, i.e. `deg = det`) forces every Frobenius eigenvalue to have `|α|² = p` -- the function-field RH for the curve. This is task 5 ("Wire") of the lever-B plan and the literal finite-field rehearsal of the M4 target ("`deg=det=norm` is a quadratic form whose positivity is RH"). The one open input is unchanged: the EXISTENCE of `A` (the scheme-theoretic Frobenius-on-Tate-module construction, O1+O2; coordinate with FLT). All three new `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build green (3638 jobs). **Boundary case (all finite fields):** `eigenvalue_modulus_le` extracts `|α|²=q` from only the NON-strict Hasse bound `t²≤4q` (casing on real vs non-real `α`: the supersingular boundary `t²=4q` gives the real double root `α=t/2` with `(t/2)²=q`), and `functionfield_RH_elliptic_of_matrix_general` drops the prime restriction -- every rank-2 integer Frobenius matrix `A` with non-negative isogeny degrees has `|α|²=det A` over every `𝔽_q`, not just prime fields. Both axiom-clean.

**Update (2026-06-14): lever B function-field RH chain advanced: M-1 (algebraic core), M-1.5, Phase A (deg=det matrix bridge), and O3 wiring, all sorry-free and axiom-clean.** New module `IsogenyDegree.lean` (#FF-M1): `degForm` + the Hasse bridge `disc_nonpos_of_int_nonneg` (degree form `≥ 0` on the lattice `ℤ·1⊕ℤ·φ` ⟹ `t² ≤ 4q`, integer→rational by homogeneity + rational→real by density, absorbing the M-3 glue), strict prime boundary `hasse_strict_of_prime` (via `four_mul_prime_not_isSquare`, the M-4 prime boundary), wired in `FunctionFieldRH.lean` by `EllipticFrobeniusData.ofDegreeNonneg` + `functionfield_RH_elliptic_of_degree` (prime field: `deg ≥ 0` ⟹ `|α|²=p`). **M-1.5** (same file): `quadratic_eq_basis` (a `QuadraticForm ℤ (ℤ×ℤ)` is forced to `m²·Q(1,0)+t·mn+n²·Q(0,1)`) + `hasse_of_quadratic` (non-negative quadratic form with `Q(1,0)=1` ⟹ `t²≤4q`), reducing the residual `hdeg` to "deg is a non-negative quadratic form with two pinned values". **Phase A** (same file, the deg=det bridge): `det_smul_one_add_smul` (`det(m·1+n·A)=m²+tr(A)·m·n+det(A)·n²`) + `hasse_of_matrix` (`A : Matrix (Fin 2) (Fin 2) ℤ`, `det(m·1+n·A) ≥ 0` ⟹ `(tr A)²≤4·det A`), the route-agnostic linear-algebra core ("deg is a quadratic form" = "det of a 2×2 is a quadratic form"). **O3 wiring** (`LocalFactor.lean`, #FF-O3): connects the chain to Mathlib's `WeierstrassCurve.localPolynomial`: `localFactor_root_normSq` (RH for `1-aT+qT²`: `a²<4q` ⟹ roots have `|·|²=1/q`, via the reciprocal `α=β⁻¹`), `localPolynomial_eq_of_goodReduction` (unfolds it to `1-C a·X+C q·X²`, `a=q+1-#W(κ)`, `q=#κ`), `localPolynomial_root_normSq` (roots of Mathlib's `localPolynomial` have `|·|²=1/q` given Hasse). So the trace/count input is Mathlib-native, and the one open input across the whole chain is the Hasse bound `a²<4q` = the scheme-theoretic degree-map residual (O1+O2). All `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Build green (3728 jobs). Strategy to discharge the residual (routes A′ dual-isogeny / B Tate-module-determinant): [`../docs/03_research/lever_b_function_field_plan.md`](../docs/03_research/lever_b_function_field_plan.md).

**Update (2026-06-12):** `CrystalCocycle.lean` added: the #82 LCC/BC-transport lemma pair (V1/V2/V3, all PROVED sorry-free, `#print axioms` = `[propext, Classical.choice, Quot.sound]` for all four theorems). V2 is Lemma 1 (quasi-invariance collapse) in both the full-semigroup and the prime-generated form (induction along the minimal prime factor); V3 is increment nonnegativity of the integrated comb `B = 1 * b`; V1 is Lemma 2 (cocycle rigidity): `B(p*n) - B(n) = log p` for all primes `p`, `n ≥ 1` IFF `b n = Λ n` for `n ≥ 2` (`b 1` free), proved via `ArithmeticFunction.vonMangoldt_sum` plus a divisor-sum determination lemma (cheap Moebius inversion by strong induction). K1-clean by construction: only `ℕ`, divisors, and `Real.log` appear. V4 (the B1 G_log rigidity lemma) is logged as an open target with a feasibility note in the file. Build green (full library, 3146 jobs).

**Update (2026-06-04c):** `FrobeniusAlgebra.lean` added as the formation-level spec for Direction 8A's arithmetic Frobenius algebra / cup target. It introduces `EulerProductData`, `FrobeniusTateTwist`, and `FrobeniusCupTarget`, with the structural theorem `cupTarget_requires_eulerProduct : CanFormCupTarget L -> L.has_euler_product`. The Davenport-Heilbronn control is now a no-sorry K2 guard: `no_dh_cupTarget : ¬ CanFormCupTarget davenport_heilbronn`, because D-H's `has_euler_product` field is `False`. `PrismaticCohomology.Q3a_*` and `HodgeIndex.hodge_index_K2_safe` now point to this guard. This proves only formation/non-formation, not polarization or RH.

**Phase 1 substrate, GREEN BUILD as of 2026-05-25.** Project infrastructure is set up, the placeholder `True` predicates have been upgraded to typed-but-stubbed predicates with concrete VERIFIER targets, and `lake build` succeeds end-to-end against Mathlib v4.13.0 on Windows + Lean 4.13.0. All 2250 modules compile. The remaining warnings are exactly the documented `sorry` markers in the VERIFIER-target table below.

**Update (2026-06-04b): Mathlib bump v4.13.0 -> v4.30.0 (17 versions, ~1.5 years), GREEN, merged to `main`.** `lake build` succeeds (3031 jobs) against Lean/Mathlib v4.30.0. The migration needed only ~7 fixes: 5 import-path renames (`Data.Complex.Exponential*` -> `Analysis.Complex.Exponential*`, `Algebra.BigOperators.Group.Finset` -> `.Finset.Basic`, `Algebra.Ring.Int` -> `.Int.Defs`), fully-qualifying `ArithmeticFunction.sigma`/`.moebius` (the σ/μ notation scope split out of the umbrella `ArithmeticFunction` scope), `pow_le_pow_left`/`le_of_pow_le_pow_left` -> the `₀` variants, and two digamma proof repairs (deriv of `1-z` via `HasDerivAt.const_sub`; a now-redundant `ring` after `field_simp` that closes the goal in v4.30). All 8 `RHEquivalences` sorry-free anchors still report clean axioms `[propext, Classical.choice, Quot.sound]` (no `sorryAx`, no `ofReduceBool`), including the transcendental n=2/n=3 Lagarias instances. NB: Mathlib now ships `Complex.digamma` + recurrence + special values (see [`upstream/digamma_contribution.md`](upstream/digamma_contribution.md)); the reflection, iterated-recurrence, and duplication identities remain the novel surviving contribution and are now VERIFIED against the upstream `Complex.digamma` in [`ZetaRH/DigammaExtras.lean`](ZetaRH/DigammaExtras.lean) (green, `#print axioms` clean for all three) — the PR-ready unit.

**Update (2026-06-04):** `RHEquivalences.lean` added (entry point A of the RH-logical-status excursion, [`docs/03_research/rh_logical_status.md`](../docs/03_research/rh_logical_status.md) §7). The equivalence hub: `robinInequality`, `lagariasInequality`, `mertensBound` are CONCRETE Props over Mathlib (`ArithmeticFunction.sigma` σ, `harmonic`, `Real.eulerMascheroniConstant`, `ArithmeticFunction.moebius` μ); `li_criterion` and `nymanBeurling_criterion` bundle the analytic data Mathlib lacks (sum-over-zeros, L²(0,1) closure), mirroring `ExplicitFormula.WeilExplicitFormula`. Distinct from `ExplicitFormula.lean`'s Weil-form positivity (#EF-2): this module owns the elementary/criterion faces. The headline object is `RH_arith := lagariasInequality`, the Π⁰₁ arithmetic surrogate, with `RH_arith_iff_RiemannHypothesis` reusing #LG-1 (no new sorry). Sorry-free anchors (all with `#print axioms` = `[propext, Classical.choice, Quot.sound]`, no `sorryAx`, no `ofReduceBool`): `riemannHypothesis_zeta_iff_nonTrivialZeros` (definitional), `riemannHypothesisMathlib_iff_zeta` (Basic re-export), `lagarias_holds_at_one` (the n = 1 equality case, the reason the criterion uses ≤ not strict <), `lagarias_holds_at_three` (transcendental instance σ(3)=4 ≤ 11/6 + e^{11/6}·log(11/6) ≈ 4.08, by order-1 bounds), `lagarias_holds_at_two` (the hardest small case, σ(2)=3 ≤ 3/2 + e^{3/2}·log(3/2) ≈ 3.32, needing sharp integer-vs-rational power bounds: e^{3/2}≥4 from (e^{3/2})²=e³=(e¹)³≥(27/10)³≥16, log(3/2)≥2/5 from (e^{2/5})⁵=e²≤(68/25)²≤(3/2)⁵; margins are non-monotone in n, the tight cases are highly composite n, and the real marginal-positivity trace is that no uniform soft bound covers all n, which would be RH), `rh_arith_refutable` (entry point B: the Σ⁰₁ refutability structure, `¬ RH_arith ↔ ∃ n ≥ 1, ¬ lagariasInequalityAt n`, the formal core of `rh_logical_status.md` §2), and `sigma_one_six` / `sigma_one_twelve` (the σ matrix side is kernel-computable via `decide`). Five documented sorries (#RB-1, #LG-1, #MT-1, #LI-1, #NB-1). Build green (2284 modules).

**Update (2026-06-03):** `AccidentAudit.lean` added (cheap-probe 5 of the "RH solved by accident" dossier, LEARNINGS #49). It formalizes the truncated non-circular Weil/Rosati Gram `weilGram = A_arch + P_fin + B_pole` from the Gamma factor (`Real.log Real.pi`), the von Mangoldt prime weights (`ArithmeticFunction.vonMangoldt`), and the pole residue, naming NO zero. `weilGram` and its symmetry anchor `weilGram_isSymm` are SORRY-FREE, and `#print axioms weilGram_isSymm` emits exactly `[propext, Classical.choice, Quot.sound]` (no `sorryAx`, no `riemannZeta`/`RiemannHypothesis`/`nonTrivialZeros`): a kernel-checked NON-CIRCULARITY certificate (a control confirmed `#print axioms` does report `sorryAx` on the deferred targets, so the verdict is genuine). Two documented sorries added (#ACC-1 positivity, #ACC-2 the K2 necessary-not-sufficient statement). Build green.

Build command:

```powershell
cd lean
lake build
```

First-time setup needs `lake update` (downloads ~5370 prebuilt Mathlib oleans, ~10 min).

### Phase 1 deliverables (this pass)

- `Basic.lean`: `LFunction` now carries `evaluate : ℂ → ℂ`, `conductor : ℕ`, `poles : Set ℂ`, plus typed predicate fields. `zeta : LFunction` wired to Mathlib's `Complex.riemannZeta`. `RiemannHypothesisMathlib` connected to `RiemannHypothesis zeta` by an actual proof (no sorry).
- `DavenportHeilbronn.lean`: D-H constructed as `c · L(s, χ₅) + c̄ · L(s, χ̄₅)` with explicit Conrey-Ghosh constant. The first off-line zero is wired to `nonTrivialZeros davenport_heilbronn` via a typed proof skeleton (only the actual zero-value step is `sorry`, as VERIFIER target #DH-zero).
- `LineRestriction.lean`: `UnivariateCosPoly` and `BivariateCosPoly` are real data types with real coefficient functions and real evaluation. Non-negativity is a real Prop. Fejér ceiling uses `Real.cos`. The line-restriction theorem is stated with the correct typed inequality (proof body is the trivial witness; structural work in VERIFIER target #LR-2).
- `LambdaBlueprints.lean`: `Blueprint` has a real `CommSemiring` carrier, multiplicative submonoid, and relation set. `LambdaBlueprint` adds typed `psi` family with commutativity and Fermat-Frobenius axioms.
- `FrobeniusAlgebra.lean`: Direction 8A formation guard for the Frobenius cup target. `CanFormCupTarget L` structurally implies `L.has_euler_product`, and Davenport-Heilbronn is no-sorry excluded from the target.
- `MathlibBridge.lean`: New module collecting Mathlib lemmas needed downstream, each tagged PRESENT / PR / TODO with VERIFIER target IDs.

### What is NOT yet done

Everything else from the original skeleton plan: prismatic cohomology and prismatic foliation remain placeholder `Unit` types (Mathlib lacks the underlying infrastructure); the central `Spec(ℤ) × Spec(ℤ)` Hodge index theorem remains `True := by sorry`; KillCriteria K3, K4 remain placeholder. The full multi-year program continues.

**Update (2026-05-29b):** discharged three `MathlibBridge` targets to real kernel-checked proofs (no sorry): #MB-1 (ζ pole at s=1, via `riemannZeta_residue_one`), #MB-2 (ζ≠0 on Re s>1, via `riemannZeta_ne_zero_of_one_lt_re`), #MB-6 (functional equation `Λ(1-s)=Λ(s)`, via `completedRiemannZeta_one_sub`). In `ExplicitFormula.lean` the archimedean kernel is now concrete: `digamma := logDeriv Complex.Gamma` with `digamma_eq` proved, `archKernel` built from it, and the recurrence `digamma_add_one` (`ψ(s+1)=ψ(s)+1/s`) proved from `Complex.Gamma_add_one` (all no sorry; kernel part of #EF-arch discharged). Project sorry count 26 → 23; build green.

**Update (2026-05-29):** `ExplicitFormula.lean` adds the project's highest-leverage Mathlib target (LEARNINGS #17): the **Weil explicit formula** for ζ and the **Weil positivity criterion**. The prime side `primeSum` is CONCRETE (a `tsum` against Mathlib's `ArithmeticFunction.vonMangoldt`); the spectral/archimedean/pole functionals are bundled into `WeilExplicitFormula`, with `weil_explicit_formula_zeta` (#EF-1) asserting the bundle exists for ζ and `weil_positivity_criterion` (#EF-2) stating `weilForm`-positivity ⟺ `RiemannHypothesis zeta`. This is the Architecture-3 (trace/positivity) face of the same positivity whose Architecture-2 (signature) face is `HodgeIndex.negDef_iff_hasseWeil`. Build green; three documented sorries (#EF-1, #EF-2, #EF-2a) plus the structural targets #EF-arch (no `digamma` in Mathlib), #EF-class, #EF-K2.

**Update (2026-05-28):** `HodgeIndex.lean` now also carries the two "positivity from a signature" results validated in experiments 2G and 2H. The 2G function-field template (`IntersectionSignature` namespace) is FULLY PROVED with no `sorry`: the primitive intersection Gram matrix `G_prim = !![-2g, -t; -t, -2gq]` is negative definite (as a quadratic form) iff the Hasse-Weil bound `t² < 4g²q` holds (`negDef_iff_hasseWeil`), with `det(G_prim) = 4g²q − t²` (`Gprim_det`) tying it to the matrix determinant. The 2H arithmetic Hodge index (`ArithmeticHodgeIndex` namespace) is stated faithfully (`heightPairing_posDef`: the Néron-Tate height-pairing Gram matrix is `Matrix.PosDef`) with a single documented `sorry` (#2H-1), since Mathlib lacks canonical heights and Faltings-Hriljac.

## Structure

```
lean/
├── lakefile.lean                    # Lake build configuration
├── lean-toolchain                   # Lean version pin (v4.13.0)
├── ZetaRH.lean                      # Main module: imports all sub-modules
└── ZetaRH/
    ├── Basic.lean                   # LFunction, RH, Selberg class; wired to Mathlib riemannZeta
    ├── MathlibBridge.lean           # NEW: collected Mathlib lemmas needed (PRESENT/PR/TODO)
    ├── DavenportHeilbronn.lean      # D-H via χ₅ + Conrey-Ghosh constant; first off-line zero
    ├── R3_5.lean                    # No-shortcut theorem: trace-formula NCG has P ⟺ RH
    ├── LineRestriction.lean         # 4E.3 line-restriction lemma (typed CosPoly)
    ├── ExplicitFormula.lean         # Weil explicit formula + Weil positivity criterion (LEARNINGS #17)
    ├── LambdaBlueprints.lean        # Direction 1: blueprint as CommSemiring + relations
    ├── FrobeniusAlgebra.lean        # Direction 8A: structural formation guard for the Frobenius cup target
    ├── PrismaticCohomology.lean     # Direction 3: prismatic cohomology of W(ℤ) (placeholder)
    ├── PrismaticFoliation.lean      # Direction 4: prismatic foliation hypothesis M3 (placeholder)
    ├── HodgeIndex.lean              # Direction 8: the central open problem
    ├── FrobeniusExistence.lean      # Lever B O1 as a single typed contract on Mathlib's localPolynomial; one named axiom, derived local-factor RH sorry-free (#FF-O1-iface)
    ├── ArakelovAssembly.lean        # 2M (#132): VT1 per-prime Hasse sign + VT2 no-single-scale assembly no-go (sorry-free)
    ├── SenDefiniteObstruction.lean  # Direction 8E Class A no-go: skew+nilpotent ⟹ 0 (sorry-free)
    ├── TraceBlindObstruction.lean   # Direction 8E Class C no-go: no t-blind signature decides Hasse-Weil (sorry-free)
    ├── KillCriteria.lean            # K1-K4 formalizations
    ├── AccidentAudit.lean           # Cheap-probe 5: de-smuggling audit of the C_mu Weil form (sorry-free non-circularity certificate)
    ├── CrystalCocycle.lean          # #82 LCC/BC-transport lemmas: V1 cocycle rigidity, V2 flat-ray collapse, V3 monotonicity (all sorry-free)
    ├── RHEquivalences.lean          # RH equivalence hub (Robin/Lagarias/Mertens/Li/Nyman-Beurling) + the Π⁰₁ kernel witness RH_arith
    ├── TamenessTrade.lean           # #157 pinning toys: mult-from-squares (#TT-1), primes-from-vonMangoldt (#TT-2), + independent NG1 witness #2 (#MTF-1w2)
    ├── BorgerVacuity.lean           # #156 T2/T3: Borger vacuity core (#MTF-3), NG1 witness #3 canonical (#MTF-1w3), partial ultraproduct object (#MTF-2), T4 blocked (#MTF-4)
    └── S4Carrier.lean               # #162 e1o S4/R1 arc: prime-side/Euler-gate ineq (#S4C-1), tail divergence KERNEL (#S4C-2), exp/trig span atom (#S4C-3), decimation rank-1 collapse (#S4C-4), trig-Vandermonde nonsingularity (#S4C-5); all sorry-free
```

## VERIFIER target IDs (Phase 1)

Each `sorry` introduced in the Phase 1 substrate carries a VERIFIER target ID for tracking:

| ID         | Module                        | What it asks for                                                                    |
|------------|-------------------------------|-------------------------------------------------------------------------------------|
| #FE-1      | Basic.lean                    | Replace `HasFunctionalEquation` placeholder with the real classical statement.      |
| #EP-1      | Basic.lean                    | Replace `HasEulerProduct` placeholder with the real Euler product convergence form. |
| #S-1..#S-3 | Basic.lean                    | Add Selberg-class axioms S1 (convergence), S2 (continuation), S4 (Ramanujan).       |
| #DH-c      | DavenportHeilbronn.lean       | Verify `dhCoefficient` against Conrey-Ghosh 1988 (and Titchmarsh 1986 §10.25).      |
| #DH-conv   | DavenportHeilbronn.lean       | Prove convergence of `davenportHeilbronnSeries` on Re s > 1.                        |
| #DH-cont   | DavenportHeilbronn.lean       | Construct meromorphic continuation via Hurwitz-zeta decomposition.                  |
| #DH-zero   | DavenportHeilbronn.lean       | Verify `dh_first_offline_zero` is in `nonTrivialZeros davenport_heilbronn`.         |
| #Fejer-1   | LineRestriction.lean          | Prove the Fejér ceiling `c_1 ≤ cos(π/(N+2))` (Mathlib upstream candidate).          |
| #LR-2      | LineRestriction.lean          | Define the restriction-to-1D operator and give the actual c_1 bound.                |
| #LR-3      | LineRestriction.lean          | Give the precise LP-witness form and derive contradiction-from-violation.           |
| #BP-1      | LambdaBlueprints.lean         | Define the blueprint quotient `ℕ[B•] / ≈` carrying the relation set.                |
| #BP-F1    | LambdaBlueprints.lean         | Correct F_1 model (not the trivial PUnit collapse).                                  |
| #BP-fiber | LambdaBlueprints.lean         | The central open computation Spec(ℤ) ×_F_1 Spec(ℤ).                                  |
| #MB-1      | MathlibBridge.lean            | ζ pole at s=1 `(s-1)ζ(s)→1`. **DISCHARGED** via `riemannZeta_residue_one` (no sorry). |
| #MB-2      | MathlibBridge.lean            | ζ(s)≠0 for Re s>1. **DISCHARGED** via `riemannZeta_ne_zero_of_one_lt_re` (no sorry). |
| #MB-3..#MB-5 | MathlibBridge.lean          | Hurwitz availability (#MB-3), L-Hurwitz decomposition (#MB-4, sorry), Fejér (#MB-5, sorry). |
| #MB-6      | MathlibBridge.lean            | ζ functional equation `Λ(1-s)=Λ(s)`. **DISCHARGED** via `completedRiemannZeta_one_sub` (no sorry). |
| #2G-1      | HodgeIndex.lean               | 2G function-field signature: `G_prim` negative definite ⟺ Hasse-Weil `t² < 4g²q`. PROVED (no sorry). |
| #2G-2      | CurveSquareIntersection.lean  | Front A milestone 1: the `C×C` divisor scaffold derives `G_prim` from the intersection numbers (`primGram_eq_Gprim`); endpoint `curveSquare_negDef_iff_hasseWeil`. Machine-checks the geometry→`G_prim` reduction 2G asserted in a comment. PROVED (no sorry), axiom-clean. |
| #FF-1      | FunctionFieldRH.lean          | Lever B, the eigenvalue-extraction link (genus 1): a non-real root of `X²−tX+q` has `|α|² = q` (`eigenvalue_modulus`, via Vieta on the conjugate); `t² < 4q ⇒ the roots are non-real` (`root_nonreal`); hence `NegDef ⇒ |α| = √q` (`functionfield_RH_elliptic_of_hodge`); and the full chain `functionfield_RH_elliptic` from `EllipticFrobeniusData`, wiring #2G-1. PROVED (no sorry). |
| #FF-geom   | FunctionFieldRH.lean          | Lever B gap, now carried as an explicit HYPOTHESIS, not a sorry: the Castelnuovo-Severi inequality `t² < 4q` is the `hodge_index` field of `EllipticFrobeniusData` (`negDef_of_curve` derives `NegDef` from it sorry-free). The whole file is now SORRY-FREE; discharging this hypothesis (proving Castelnuovo-Severi from a curve's definition) needs Mathlib algebraic-curve intersection theory. This also repaired the previous `hodge_index_curve_elliptic`, which admitted a FALSE proposition (`NegDef 1 q t` for all `q,t`) via `sorry`. |
| #FF-M0     | FunctionFieldRH.lean          | Lever B roadmap milestone M-0 (the discriminant bridge for the elementary Hasse proof): a positive-semidefinite real binary form `m²+t·mn+q·n²` has `t² ≤ 4q` (`disc_nonpos_of_posSemidef`); positive-definite gives the strict `t² < 4q` (`disc_neg_of_posDef`). The positive mirror of #2G-1; the step from "deg ≥ 0 for isogenies" to the Hasse bound. PROVED (no sorry). Plan: `docs/03_research/lever_b_function_field_plan.md`. |
| #FF-M1     | IsogenyDegree.lean            | Lever B milestone M-1, the isogeny DEGREE API (algebraic core). `degForm t q m n = m²+t·mn+q·n²` (= `deg(m·1+n·φ)`); **Hasse bridge** `disc_nonpos_of_int_nonneg`: degree form `≥ 0` on the lattice `ℤ·1⊕ℤ·φ` ⟹ `t² ≤ 4q` (integer→rational by homogeneity, rational→real by density via `exists_rat_btwn`; absorbs the M-3 glue); strict prime boundary `hasse_strict_of_prime` via `four_mul_prime_not_isSquare`. Wired in FunctionFieldRH.lean by `EllipticFrobeniusData.ofDegreeNonneg` + endpoint `functionfield_RH_elliptic_of_degree` (over a prime field, `deg ≥ 0` ⟹ `|α|²=p`). **M-1.5** (same file): `quadratic_eq_basis` (a `QuadraticForm ℤ (ℤ×ℤ)` is forced to `m²·Q(1,0)+t·mn+n²·Q(0,1)`) + `hasse_of_quadratic` (non-negative quadratic form with `Q(1,0)=1` ⟹ `t²≤4q`), reducing `hdeg` to the honest minimum "deg is a non-negative quadratic form with two pinned values". PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]` (quadratic_eq_basis: no Classical.choice). **Phase A** (same file, the deg=det route-agnostic bridge): `det_smul_one_add_smul` (the 2×2 mixed-determinant identity `det(m·1+n·A)=m²+tr(A)·m·n+det(A)·n²`) + `hasse_of_matrix` (`A : Matrix (Fin 2) (Fin 2) ℤ`, `det(m·1+n·A) ≥ 0` on the lattice ⟹ `(tr A)²≤4·(det A)`), reusing `Matrix.det_fin_two`/`trace_fin_two`; "deg is a quadratic form" = "det of a 2×2 is a quadratic form" (route B's O2, free). **Phase A endpoint** (`FunctionFieldRH.lean`, 2026-06-15): `matrix_charpoly_root` (an eigenvalue `α ∈ spectrum ℂ (A.map (ℤ→ℂ))` is a root of `X²−(tr A)·X+(det A)`, via `mem_spectrum_iff_isRoot_charpoly` + `charpoly_fin_two`) + `functionfield_RH_elliptic_of_matrix` (over a prime field, `det A = p` + non-negative isogeny degrees ⟹ every Frobenius eigenvalue has `|α|²=p`, the function-field RH for the curve) + the boundary-inclusive `eigenvalue_modulus_le` (`t²≤4q` ⟹ `|α|²=q`, handling the supersingular real double root `α=t/2`) and `functionfield_RH_elliptic_of_matrix_general` (drops the prime restriction: any `𝔽_q`, `|α|²=det A`), all axiom-clean. **Non-vacuity witness** (`IsogenyDegree.lean`, 2026-06-15): `companion t q = !![0,-q;1,t]` (the companion matrix of `X²−tX+q`, trace `t`, det `q`) + `companion_degForm` (`det(m·1+n·companion)=m²+tmn+qn²`) + `companion_degForm_nonneg` (for any Hasse-valid `(q,t)`, the companion's isogeny degrees are `≥0`, via `4·degForm=(2m+tn)²+(4q−t²)n²`), confirming the matrix-endpoint hypothesis is satisfiable and exactly equivalent to Hasse (guards against the #106 vacuity failure mode; a consistency check, not part of the RH proof). Residual M-1: construct the rank-2 integer Frobenius representation `A` with `det(m·1+n·A) ≥ 0`, `det A = q`, `trace A = a` (the scheme-theoretic `deg`, still Mathlib-absent; route B's O2 now done via `TateModule.lean`, the open part is O1). |
| #FF-M1B    | TateModule.lean               | Lever B M-1 route B (Tate module): the Weil-pairing / determinant mechanism behind `deg = det`, internalizing O2. **Coordinate version:** `weilForm x y = x₀y₁−x₁y₀` (the symplectic model of the Weil pairing on `T_ℓ E ≅ ℤ²`); `weilForm_mulVec` (`weilForm (A·x) (A·y) = (det A)·weilForm x y`); `scaling_eq_det` (Weil-pairing degree = det); `functionfield_RH_of_weil_pairing` (route-B endpoint, no `det` in hypotheses). **Coordinate-free version (the real Tate module):** for any rank-2 `R`-module `M` with basis `b` and any alternating bilinear form `ω`, `bilin_alt_two` (`ω` is determined by `ω(b₀,b₁)`), **`det_transform`** (`ω (f x) (f y) = (LinearMap.det f)·ω x y` -- the symplectic determinant law on an abstract module, the genuine basis-independent reason `deg=det`), `scaling_eq_det_free` (the Weil degree IS `LinearMap.det`), and `functionfield_RH_of_tate` (the coordinate-free route-B endpoint: rank-2 ℤ-lattice `M` + alternating perfect `ω` + Frobenius `frob` + degree-as-Weil-scaling + `deg ≥ 0` ⟹ `\|α\|²=det frob`, all finite fields). PROVED (no sorry); all `#print axioms` = `[propext, Classical.choice, Quot.sound]`. So route B's O2 ("deg is a quadratic form") is now a THEOREM (the symplectic determinant law), coordinate-free; the open residual is O1 (the existence of `M`, `b`, `ω`, `frob`, `deg ≥ 0` -- the scheme-theoretic Tate-module construction, FLT-adjacent). |
| #FF-O3     | LocalFactor.lean              | Lever B O3 wiring: connects the chain to Mathlib's elliptic-curve local L-factor. `localFactor_root_normSq` (RH for the abstract local factor `1-aT+qT²`: `a²<4q` ⟹ every complex root has `|·|²=1/q`, via the reciprocal `α=β⁻¹` and the eigenvalue extraction #FF-1); `localPolynomial_eq_of_goodReduction` (unfolds `WeierstrassCurve.localPolynomial` to `1-C a·X+C q·X²` with `a=q+1-#W(κ)`, `q=#κ`, T. Browning's Mathlib def -- trace/count now Mathlib-native); `localPolynomial_root_normSq` (roots of Mathlib's `localPolynomial` have `|·|²=1/q`, given the Hasse bound `a²<4q`). PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]` only. The Hasse bound is the one open input (the `hdeg`/#FF-geom residual). |
| #ClassA-1  | SenDefiniteObstruction.lean   | Direction 8E Class A no-go: `skew_nilpotent_eq_zero` (real skew + nilpotent ⟹ 0, elementary trace proof) + `sen_no_nilpotent_part` + `sen_nilpotent_part_not_isNilpotent` (a non-semisimple Sen module admits no definite invariant cup form; Petrov ν≠0) + `bskew_nilpotent_eq_zero_of_gram` (Gram-factor reduction) + `bskew_nilpotent_eq_zero` (UNCONDITIONAL in `B.PosDef`, via `CFC.sqrt`). 5 theorems, PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]` only. |
| #ClassC-1  | TraceBlindObstruction.lean    | Direction 8E Class C no-go: `negDef_depends_on_trace` (the Hasse-Weil/Rosati `NegDef` predicate is non-constant in the trace `t`) + `no_trace_blind_signature` (no `t`-blind predicate `P g q` decides `NegDef g q t`; the arithmetic-blind AHK combinatorial signature cannot carry RH-positivity, #48). PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]` only. |
| #2H-1      | HodgeIndex.lean               | 2H Faltings-Hriljac arithmetic Hodge index: Néron-Tate height-pairing Gram matrix is `PosDef`. Sorry (needs Mathlib canonical heights + Faltings-Hriljac). |
| #ZC-1      | RiemannZetaConj.lean          | **Upstream-staged** (Mathlib-style, dependency-free): the conjugation symmetry `riemannZeta_conj` (`ζ(s̄) = conj ζ(s)` for `s ≠ 1`) + `riemannZeta_conj_eq_zero_iff` (zeros are conjugation-symmetric). Absent from Mathlib (which has the functional equation `riemannZeta_one_sub` but not this reflection); proved from the L-series on `Re>1` + the identity principle. PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Ready-to-submit PR body: `upstream/riemann_zeta_conj_pr_body.md`. |
| #M4-1      | ArithmeticPolarization.lean   | M4 (the real target): the arithmetic polarization face of RH for ζ, the Spec(ℤ) lift of `eigenvalue_modulus`. `critical_line_iff_fe_reflection` (`1−ρ = conj ρ ⟺ Re ρ = 1/2`); `fe_partner_mem_nonTrivialZeros` (the functional equation `riemannZeta_one_sub` provides the perfect pairing `ρ ↔ 1−ρ` on the nontrivial zeros -- a genuine, non-vacuous theorem about ζ, the "perfectness is free" half, K2-blind); `riemannHypothesis_zeta_iff_fe_polarization` (RH ⟺ the FE pairing coincides with conjugation `1−ρ = conj ρ` -- the cup-is-a-polarization condition, #61/2HH). **Reflection (proved):** `riemannZeta_conj` (`ζ(s̄) = conj ζ(s)` for `s ≠ 1`, via the L-series tsum on `Re>1` + the identity principle `AnalyticOnNhd.eqOn_of_preconnected_of_eventuallyEq` on the connected `ℂ∖{1}`, with `conj∘ζ∘conj` analytic by `HasDerivAt.conj_conj`) now lives in its own Mathlib-style dependency-free unit `RiemannZetaConj.lean` (#ZC-1; staged for upstreaming, a genuine result absent from Mathlib -- see `upstream/riemann_zeta_conj_pr_body.md`), and `reflection_mem_nonTrivialZeros` (`conj ρ` is a nontrivial zero) uses it, so `1−ρ = conj ρ` is literally "two genuine zeros coincide". PROVED (no sorry); all `#print axioms` = `[propext, Classical.choice, Quot.sound]`. The deep content -- a polarization FORCES `1−ρ = conj ρ` -- is M4 (the arithmetic Hodge standard conjecture, 08A), not formalized here. |
| #EF-1      | ExplicitFormula.lean          | The Weil explicit formula for ζ: a `WeilExplicitFormula` bundle exists (spectral side = arch + pole − prime). Sorry (needs digamma kernel + sum-over-zeros theory). |
| #EF-2      | ExplicitFormula.lean          | **The Weil positivity criterion**: `weilForm`-positivity on all admissible tests ⟺ `RiemannHypothesis zeta`. The Architecture-3 centerpiece (LEARNINGS #17). Sorry. |
| #EF-2a     | ExplicitFormula.lean          | Construct `weilForm` (the Hermitian form `∑_ρ f̂(ρ)\overline{f̂(\barρ)}`) from the bundle via the positive-type/self-dual test. Sorry. |
| #EF-arch   | ExplicitFormula.lean          | The archimedean kernel. Kernel part **DISCHARGED**: `digamma := logDeriv Complex.Gamma`, `digamma_eq` proved, `archKernel` concrete, and the basic algebraic identities `digamma_add_one` (`ψ(s+1)=ψ(s)+1/s`), `digamma_reflection` (`ψ(1-s)-ψ(s)=π cot(πs)`), and `digamma_add_nat` (`ψ(s+n)=ψ(s)+∑_{k<n} 1/(s+k)`, by induction from the recurrence), `digamma_duplication` (`ψ(2s)=½(ψ(s)+ψ(s+½))+log 2`, from Legendre's doubling `Complex.Gamma_mul_Gamma_add_half`), and the special values `digamma_one` (`ψ(1)=-γ`, from `Complex.hasDerivAt_Gamma_one`) and `digamma_half` (`ψ(½)=-γ-2log 2`, from duplication at `s=½`) **PROVED** (no sorry). Six sorry-free digamma identities total, from `Complex.Gamma_add_one`, `Complex.Gamma_mul_Gamma_one_sub`, `Complex.Gamma_mul_Gamma_add_half`, and `Complex.hasDerivAt_Gamma_one`. Remaining: the integral pairing (#EF-class). |
| #EF-class  | ExplicitFormula.lean          | The analytic side-conditions on `AdmissibleTest` (smoothness/decay/strip of holomorphy) that make the functionals well-defined and #EF-1 true. |
| #EF-K2     | ExplicitFormula.lean          | The D-H instance showing the criterion does NOT certify RH for Davenport-Heilbronn (prime side delocalises; experiment 3M #20). |
| #FA-1      | FrobeniusAlgebra.lean         | Replace the placeholder `EulerProductData` fields with the real Euler-product local-factor data, convergence on `Re(s)>1`, uniqueness, and the von-Mangoldt log-derivative. The formation guard itself (`no_dh_cupTarget`) is proved no-sorry. |
| #FA-2      | FrobeniusAlgebra.lean         | Replace the toy `Unit` `FrobeniusTateTwist`/`FrobeniusCupTarget` with the actual `H^2 = C(-1)` fundamental class and arithmetic `H^1`, then prove bilinearity/perfectness/flow derivation. Positivity remains the M4 standard-conjecture target. |
| #ACC-1     | AccidentAudit.lean            | The numerical positivity certificate `(weilGram K N b).PosDef` for zeta (min-eig +0.035; e3c/e3m). Necessary-not-sufficient. Sorry (needs concrete numerical entries + a PosDef witness). |
| #ACC-2     | AccidentAudit.lean            | `weilGram_noncirc`: positivity of `weilGram` does NOT entail `RiemannHypothesis zeta` (the same construction for Davenport-Heilbronn would otherwise prove a false RH; the M2.6 stealth window #34). Sorry (needs the explicit D-H witness Gram + its off-line zero, ties to #DH-zero). The non-circular-because-necessary-not-sufficient statement. |
| #RB-1      | RHEquivalences.lean           | `robin_criterion`: Robin's inequality `∀ n ≥ 5041, σ(n) < e^γ n log log n` ⟺ RH. Sorry (full RH-equivalence; unformalized in any prover). |
| #LG-1      | RHEquivalences.lean           | `lagarias_criterion`: Lagarias's inequality `∀ n ≥ 1, σ(n) ≤ H_n + e^{H_n} log H_n` ⟺ RH. Sorry. Also backs `RH_arith_iff_RiemannHypothesis` (the Π⁰₁ kernel witness, no new sorry). |
| #MT-1      | RHEquivalences.lean           | `mertens_criterion`: the Mertens bound `M(x) = O(x^{1/2+ε})` ⟺ RH. Sorry. |
| #LI-1      | RHEquivalences.lean           | `li_criterion`: Li/Keiper positivity `∀ n ≥ 1, λ_n ≥ 0` ⟺ RH, given the `LiData` Keiper-Li representation. Sorry. #LI-def (the convergent symmetric-pairing form of the sum-over-zeros identity) is the `LiData.keiperLi` field's deep content. |
| #NB-1      | RHEquivalences.lean           | `nymanBeurling_criterion`: the Báez-Duarte distances `d_N → 0` ⟺ RH, given `NymanBeurlingData`. Sorry. #NB-def (the tie of `dist` to the actual L²(0,1) closure of dilated fractional parts) is the opaque data. |
| V1         | CrystalCocycle.lean           | #82 Lemma 2 (cocycle rigidity): `increments_eq_log_iff_eq_vonMangoldt`, the increments `B(p*n) - B(n) = log p` (all primes `p`, all `n ≥ 1`) ⟺ `b n = Λ n` for `n ≥ 2` with `b 1` free. **PROVED** (sorry-free; axioms = `[propext, Classical.choice, Quot.sound]`). |
| V2         | CrystalCocycle.lean           | #82 Lemma 1 (quasi-invariance collapse): `flat_ray_of_quasiInvariance` (full ℕ× form) and `flat_ray_of_prime_quasiInvariance` (prime-generated form, induction on the minimal prime factor); the quasi-invariant combs are exactly the flat ray `c n = c 1 * n^(-β)`. **PROVED** (sorry-free). |
| V3         | CrystalCocycle.lean           | #82 increment nonnegativity: `divisorSum_le_divisorSum_mul_of_nonneg`, `0 ≤ b → B n ≤ B (p*n)` via `Nat.divisors_subset_of_dvd`. **PROVED** (sorry-free). |
| V4         | CrystalCocycle.lean           | #86 B1 G_log rigidity (`b1_glog_rigidity.md`): prime-translate quasi-invariant Radon measures on ℝ form the ray `c·exp(-βx)dx`. **OPEN** (not attempted). Mathlib ingredients exist: `AddSubgroup.dense_or_cyclic`, `Measure.withDensity`, `MeasureTheory.Measure.isAddLeftInvariant_eq_smul` (Haar uniqueness); the missing assembly is the vague-continuity upgrade from dense-translate invariance to full translation invariance. Feasibility note at the end of `CrystalCocycle.lean`. |
| #VT-NP1    | VT_NP1.lean                   | NP-1 (e2vv / #128): finite Euler-product zero-freeness on `Re(s)>0` + the M4-reduction. `localFactor_ne_zero` (`1-p^{-s} ≠ 0` for `p ≥ 2`, via `‖p^{-s}‖ = p^{-Re s} < 1`), `finiteEulerProductInv_ne_zero` (a finite product `∏_{p∈S}(1-p^{-s})` is nonzero), `np1_reduction` (every finite stage is zero-free, so the off-line obstruction is only in the infinite limit = M4). PROVED (no sorry); axiom-clean. The analytic companion of #2G-1. |
| #119/R3.5  | R3_5.lean                     | Tighten the `GeometricPositivity` placeholder so "realizes the signature INTEGER" is not read as "proves the primitive-part DEFINITENESS" (#119). `RealizesSignatureInteger` (free, RH-blind: `realizesSignatureInteger_rhBlind` holds for ζ and D-H), `PrimitivePartDefinite` (= RH-equivalent), `GeometricPositivity := PrimitivePartDefinite` with `geometricPositivity_iff_RH`; no theorem routes the integer realization to RH. PROVED (no sorry); axiom-clean. |
| #TOY-1     | ToyModel.lean                 | The toy sandbox Lean face (companion to `experiments/toy/`): genus-1 grader correctness, both directions. `toy_reproduce_weil` (`t² ≤ 4q ⟹ |α|² = q`) and the NEW off-line witness `toy_reject_fake` (`4q < t² ⟹ ∃` a root off the circle, via `(t+√(t²−4q))/2`), packaged in `toy_grader_genus1_correct`. PROVED (no sorry); axiom-clean. |
| #2M-VT     | ArakelovAssembly.lean         | 2M Arakelov-assembly residues (#132), the two isolable RH-independent targets Front 3 flagged. **VT1** (per-prime Hasse sign): `perPrime_negDef_iff_hasse` (the per-prime `(1,p)` Gram `!![-2g,-t;-t,-2g·p]` is negative definite ⟺ `t²<4g²p`, = `negDef_iff_hasseWeil` at `q=p`, a SPECIALIZATION not new) + `perPrime_negDef_of_hasse`. **VT2** (no single scale, the new content): `conj_lowerRight` (`D_s`-conjugated lower-right = `-2g·p·s²`), `no_single_normalizing_scale` (distinct `p≠q` ⟹ no single `s` with `p·s²=q·s²=1`, the #25 `(1,p)`-bidegree obstruction), `forced_scale_asymmetry_unbounded`, and `assembly_obstruction` (combined: both fibres definite, no global scale). Does NOT close M4 (the adelic-scalar sign is open). PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #FF-O1     | FunctionFieldRH.lean          | Name the single open residual of lever B as a Prop: `FrobeniusMatrixExists p` (existence of the rank-2 integer Frobenius matrix `A`, `det A = p`, non-negative isogeny degrees) + `functionfield_RH_of_frobeniusMatrixExists` (RH-for-the-curve REDUCES to it). The reduction is PROVED (no sorry, axiom-clean); the existence itself is the months-long FLT-adjacent O1 construction, left OPEN and precisely named (a `def`, not an axiom). |
| #FF-O1-iface | FrobeniusExistence.lean     | Lever B O1 as a SINGLE typed contract on Mathlib's REAL object (`WeierstrassCurve.localPolynomial`), with everything downstream machine-checked. `FrobeniusTateData R W` (the O1 existence: a rank-2 integer Frobenius `A` with `det A = qOf R`, `trace A = aOf R W` PINNED to Mathlib's `q=#κ`, `a=q+1-#W(κ)`, and non-negative isogeny degrees). `frobeniusTateData_exists` is the ONE named **axiom** (cited Silverman III.8.6 / V.1.1; the FLT-adjacent scheme-theoretic content the pin lacks), NOT a `sorry` and NOT false. **Non-vacuity (#106 guard):** `frobeniusTateData_companion` proves (sorry-free, axiom-clean) the companion matrix `!![0,-q;1,a]` satisfies the contract whenever Hasse `a²≤4q` holds, so the axiom admits a KNOWN-TRUE statement. **Derived unconditional conclusion (sorry-free):** `localPolynomial_root_normSq_of_O1` + `functionfield_RH_localPolynomial` -- every root of `W.localPolynomial` (good reduction) has `|β|²=1/q` (reciprocal eigenvalues on `|α|=√q`, the critical line), via `hasse_of_matrix` + the new boundary-inclusive `localFactor_root_normSq_le` (non-strict `a²≤4q`, all finite fields, no primality). `#print axioms` of both derived results = `[propext, Classical.choice, Quot.sound, frobeniusTateData_exists]` -- foundational axioms + the one named O1 axiom, nothing else (no `sorryAx`). So O1 is the ONLY gap; everything downstream on Mathlib's genuine object is kernel-checked. Build green (3746 jobs). |
| #SC-1      | SchurCohn.lean                | The #143 Schur-Cohn kernel, Cohn's criterion at genus 1: `roots_on_circle_of_window` (`c² ≤ 4` ⟹ every root of `z² − cz + 1` has `normSq z = 1`, via `eigenvalue_modulus_le` at `q = 1`) + converse `window_of_roots_on_circle` (explicit off-circle real root `(c+√(c²−4))/2`), the gate `schur_cohn_gate` (`0 ≤ SchurCohn₁(2z−c) = 4−c²` ⟺ root `c/2` in the closed unit disk ⟺ `c² ≤ 4`), `schur_cohn_certifies_circle` (the certificate IS the circle condition), and the un-normalized tie `schurCohn1_normalized` + `certificate_iff_hasse` (the toy grader's genus-1 certificate `4 − t²/q` ⟺ Hasse `t² ≤ 4q`). PROVED (no sorry); all `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #SC-2      | SchurCohn.lean                | The reversal identity behind Cohn's derivative trick: for self-inversive `p` (`reflect (natDegree p) p = p`) over any commutative ring, `reflect (natDegree p − 1) (derivative p) = natDegree p • p − X · derivative p` (`reflect_derivative_self_inversive`), proved coefficient-wise (`coeff_reflect`/`revAt` + `coeff_derivative`; boundary: `j = n` cancels, `j > n` vanishes). Mathlib-native statement, upstreamable. PROVED (no sorry); axiom-clean. |
| #SC-3      | (Mathlib)                     | Gauss-Lucas: every root of `derivative p` lies in `convexHull ℝ` of the roots of `p`. Mathlib ALREADY HAS THIS (`Polynomial.rootSet_derivative_subset_convexHull_rootSet`, in `Mathlib.Analysis.Complex.Polynomial.GaussLucas`, with explicit convex-combination weights via `eq_centerMass_of_eval_derivative_eq_zero`). No new code; used directly by #SC-4. CLOSED (upstream). |
| #SC-4      | GaussLucas.lean               | Cohn forward, general degree: if every root of a nonconstant `p : ℂ[X]` lies ON the unit circle (`normSq z = 1`), every root of `derivative p` lies in the closed unit disk (`derivative_roots_in_disk`). Proof: the closed ball is convex and contains the roots, hence their hull; apply #SC-3. PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #SC-5      | GaussLucas.lean               | Cohn converse, general degree (the hard half): for self-inversive `p` with conj-fixed (real) coefficients and `natDegree p ≥ 1`, all roots of `p'` in the closed disk ⟹ all roots of `p` ON the circle (`cohn_converse`; real-coefficient form `cohn_converse_real`; the iff capstone `cohn_criterion` = the general-degree Cohn criterion whose genus-1 shadow is #SC-1). Route: factor identity `normSq(1−conj r·z) − normSq(z−r) = (1−normSq r)(1−normSq z)` (`normSq_factor_identity`) ⟶ product form of the conjugate-reflection `reflect n (g.map conj) = conj(lead)·∏(1−conj rᵢ·X)` (`reflect_conj_prod_form`, via `reflect_mul` on the splitting) ⟶ reflection dominance `normSq(g(z)) ≤ normSq(gStar(z))` on the disk (`normSq_eval_le_normSq_reflect_eval`) + `gStar` root-free strictly inside (`reflect_conj_eval_ne_zero`) ⟶ the pinch at a root strictly inside via #SC-2's reversal identity ⟶ outside roots pair to inside via `eval₂_reflect_mul_pow`. `p(0) ≠ 0` is automatic (palindromic ⟹ `coeff 0 = leadingCoeff ≠ 0`), so not assumed. PROVED (no sorry, full generality, no degree fallback); all 12 theorems `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |

| #GF-1      | GaussFloor.lean               | The prime-forced factor's leading coefficient: `leadingCoeff_linearFactor` / `leadingCoeff_linearFactor_pow` (`lead((p·X-1)^m) = p^m` over `ℤ[X]`, via `leadingCoeff_linear` + `leadingCoeff_pow`). PROVED (no sorry); axiom-clean. |
| #GF-2      | GaussFloor.lean               | The single-prime floor: `pow_dvd_leadingCoeff` (`(p·X-1)^m ∣ f ⟹ p^m ∣ lead f`, by multiplicativity of the leading coefficient, `leadingCoeff_dvd_leadingCoeff`). No polynomial-coprimality argument anywhere in the chain. PROVED (no sorry); axiom-clean. |
| #GF-3      | GaussFloor.lean               | The divisibility floor over a finite prime set: `prod_primePow_dvd_leadingCoeff` (`∀ p ∈ P` prime, `(p·X-1)^{m p} ∣ f ⟹ ∏_{p∈P} p^{m p} ∣ lead f`), recombining the per-prime floors on the INTEGER side (`isCoprime_primePow` + `Finset.prod_dvd_of_coprime`). PROVED (no sorry); axiom-clean. |
| #GF-4      | GaussFloor.lean               | The height floor + sharpness: `gauss_floor` (`f ≠ 0 ⟹ ∏ p^{m p} ≤ \|lead f\|`; with vF multiplicities `m_p = ⌊log_p x⌋` the log form is `log\|lead f\| ≥ ψ(x)`) and the equality case `canonical_leadingCoeff` / `canonical_ne_zero` / `canonical_attains_floor` (the vF canonical product `∏ (p·X-1)^{m p}` attains the floor exactly). The no-Siegel-lemma no-go of the vF disc model, machine-checked. PROVED (no sorry); axiom-clean. |
| #GF-5      | GaussFloor.lean               | The Gauss's-lemma step + capstone: `linearFactor_isPrimitive` / `linearFactor_pow_isPrimitive`, `dvd_of_map_dvd` (one-sided Gauss descent: primitive `g ∣ f` over ℚ ⟹ over ℤ, routing `f` through `primPart`), `linearFactor_pow_dvd_of_rootMultiplicity` (vanishing at `1/p` over ℚ with multiplicity `m` ⟹ `(p·X-1)^m ∣ f` in `ℤ[X]`), and `gauss_floor_of_vanishing` (vanishing data ⟹ the height floor). PROVED (no sorry); all `#print axioms` = `[propext, Classical.choice, Quot.sound]`. Companion probe: `experiments/arithmetic_geometric/e2ah_gauss_floor.py` (PASS). |
| #GF-6      | GaussFloor.lean               | Minimal-degree rigidity (the e2ah rank-one clause): `map_linearFactor_pow` (the ℚ-factorization of the mapped prime factor), `canonical_dvd_of_vanishing` (the canonical product `∏ (p·X-1)^{m p}` divides EVERY prime-forced vanisher; the root factors at the distinct points `1/p` are pairwise coprime over ℚ and the product descends by primitivity, while the linear factors `p·X-1` themselves are NOT pairwise coprime in `ℤ[X]`, e.g. `2X-1 ≡ 5X-1 mod 3`), `canonical_natDegree` (degree exactly `∑ m p`), and `gauss_floor_rank_one` (any vanisher of degree `≤ ∑ m p` IS an integer multiple of the canonical product: the vanisher lattice at the minimal degree has rank 1). PROVED (no sorry); axiom-clean. |
| #RR-1      | RationalRootFloor.lean        | **The rational root theorem with multiplicity** (P10 FORMAL axis, Mathlib-PR unit): over a UFD `A` with fraction field `K` (the exact typeclass context of Mathlib's `RationalRoot.lean`), `den_pow_rootMultiplicity_dvd_leadingCoeff`: `(den A r) ^ rootMultiplicity r (p.map (algebraMap A K)) ∣ p.leadingCoeff`, unconditional; `m = 1` recovers Mathlib's `den_dvd_of_is_root` (guard `example` included, plus the `ℤ/ℚ` instantiation guard). Route: the reduced linear factor `C (den A r)·X - C (num A r)` is primitive (`num_den_reduced`), over `K` it is a unit multiple of `X - C r`, one-sided Gauss descent (`dvd_of_fraction_map_dvd`, only the divisor primitive, via `primPart`; the `NormalizedGCDMonoid` machinery is obtained inside proofs from `Nonempty (NormalizedGCDMonoid A)` so statements stay UFD-only), then leading-coefficient multiplicativity. Supporting PR-worthy lemmas: `isPrimitive_pow`, `isPrimitive_prod`, `dvd_of_fraction_map_dvd`, `map_den_mul_X_sub_C_num`, `isPrimitive_den_mul_X_sub_C_num`, `leadingCoeff_den_mul_X_sub_C_num`. PROVED (no sorry); all `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #RR-2      | RationalRootFloor.lean        | The multi-point product version: `prod_den_pow_rootMultiplicity_dvd_leadingCoeff`: for any `s : Finset K`, `∏_{r ∈ s} (den A r) ^ rootMultiplicity r (p.map (algebraMap A K)) ∣ p.leadingCoeff`. The denominators of distinct points need NOT be pairwise coprime in `A` (over `ℤ`: `1/4` and `3/4` share denominator 4), so the #GF-3 integer-side recombination is unavailable in general; the recombination is on the POLYNOMIAL side (root factors at distinct points are pairwise coprime over the field `K`, `isCoprime_X_sub_C_of_isUnit_sub` + `Finset.prod_dvd_of_coprime`), then one Gauss descent of the assembled primitive product. Subsumes the divisibility half of #GF-3/#GF-5 and strengthens it (no coprimality hypothesis at all). PROVED (no sorry); axiom-clean. |
| #MTF-1     | ModelTheoreticFrobenius.lean  | **T1 of the model-theoretic Frobenius arc (LEARNINGS #156): the NG1 endomorphism-rigidity kernel.** (a) Prime-ring rigidity: `endo_fixes_natCast` / `endo_fixes_intCast` (any `f : R →+* R` fixes every ℕ/ℤ cast; Mathlib's `map_natCast`/`map_intCast` restated as the project-facing statements). (b) Division-ring form: `endo_fixes_ratCast` (every endomorphism of a division ring fixes every rational cast, `map_ratCast`; in characteristic 0 the rational casts are the prime field, so the forced fixed set is infinite = the S1 kill). (c) The Fermat coordinatewise witness: `fermat_little_zmod` (`x^p = x` on `ZMod p`, `ZMod.pow_card`), `frobenius_zmod_eq_id` (the local Frobenius is the identity on the prime field at every `p`, `ZMod.frobenius_zmod`), `zmod_endo_eq_id` (the prime field is endomorphism-rigid outright, `RingHom.ext_zmod`). (d) The NG1 composite: `ng1_rigidity` / `ng1_rigidity_apply` (every ring endomorphism of every ℚ-algebra fixes the imported ℚ pointwise, `σ ∘ algebraMap ℚ R = algebraMap ℚ R`, via `RingHom.ext_rat`; covers adeles, `∏ ℤ/p²/𝒰`, Witt ultraproducts; limit-independent, no ultrafilter in statement or proof). All near-one-liners by design (the value is pinning the statements). PROVED (no sorry); all eight `#print axioms` ⊆ `[propext, Classical.choice, Quot.sound]`. T2-T4 = #MTF-2..#MTF-4, open (the ultraproduct object; the Borger vacuity statement; the cardinality dichotomy NG2(iii)). |
| #MTF-1w2   | TamenessTrade.lean            | **#MTF-1 consensus witness #2 (independent second formalization of the NG1 rigidity kernel).** `ng1_rigidity_indep` (`σ.comp (algebraMap ℚ R) = algebraMap ℚ R` for every ring endomorphism `σ` of every commutative ℚ-algebra `R`) + `ng1_rigidity_indep_apply` (the pointwise form). Re-derived from the mathematical statement in `model_theoretic_frobenius.md` Section 3, WITHOUT reading `ModelTheoreticFrobenius.lean` and by a DISTINCT proof route: not `RingHom.ext_rat`, but `q = q.num/q.den` ⟹ `algebraMap q · (q.den:R) = (q.num:R)`, apply `σ` (fixes casts via `map_natCast`/`map_intCast`), then cancel the unit `(q.den:R) = algebraMap (q.den:ℚ)` by its explicit right inverse `algebraMap ((q.den:ℚ)⁻¹)`. The existing 8 `ModelTheoreticFrobenius` #MTF-1 declarations re-`#print axioms`-checked green in the same pass, so the NG1 core stands at 2 independent witnesses. PROVED (no sorry); both `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #TT-1      | TamenessTrade.lean            | **Multiplication from addition and squaring (Robinson squares toy, arithmetic core).** `two_mul_mul_eq` (`2*x*y = (x+y)^2 - x^2 - y^2` over any `CommRing`) + `two_mul_mul_add_sq_eq` (the subtraction-free `2*x*y + (x^2+y^2) = (x+y)^2` over any `CommSemiring`, valid over ℕ). A VALUE-PINNING toy of the tameness-trade interpretation direction (#157): the arithmetic core of "an innocuous multiplicative predicate + addition recovers `×`". Achieved as the algebraic identity, NOT the full `Set.Definable` wrapper (a bespoke `FirstOrder.Language` + `Structure` on ℕ is heavy overhead for a pinning toy carrying no RH content; the identity IS the definability mechanism, docstring records this). PROVED (no sorry); both `#print axioms` = `[propext]`. |
| #MTF-1w3   | BorgerVacuity.lean            | **#MTF-1 consensus witness #3 (CANONICAL at 3 witnesses).** `ng1_rigidity_w3` (`σ.comp (algebraMap ℚ R) = algebraMap ℚ R` for every ring endomorphism `σ` of every commutative ℚ-algebra `R`) + `ng1_rigidity_w3_apply` (pointwise). Third independent formalization, route DISTINCT from #1 (`RingHom.ext_rat`) and #2 (`num`/`den` cancellation): `IsLocalization.ringHom_ext` (ℚ = Frac ℤ, `Rat.isFractionRing`) reducing to `Int.subsingleton_ringHom` (ℤ initial). Under the four-layer consensus rule #MTF-1 is now CANONICAL. PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #MTF-3     | BorgerVacuity.lean            | **The Borger VACUITY core (T3, PROVEN).** `natCast_isUnit_of_ne_zero` (`(q:R)` a unit in any ℚ-algebra for `q ≠ 0`), `span_natCast_eq_top` / `span_natCast_eq_top_of_prime` (`Ideal.span {(q:R)} = ⊤`), `quotient_span_natCast_subsingleton` (`Subsingleton (R ⧸ (q))`, the residue ring `R/qR = 0`), and the vacuity `borger_lift_congruence_vacuous` (`ψ x ≡ x^q [SMOD (q)]` for EVERY `ψ : R →+* R`, `x`; via `SModEq.top`) with faces `borger_lift_any_two_congruent` (any two endomorphisms congruent) and `borger_lift_congruence_vacuous_prime` (ℚ-algebra + `q.Prime`). Machine-checks "the ultrafilter TRIVIALIZES the Λ-structure by inverting every standard prime; the lift congruence carries zero descent data" (corrected #156 finding). PROVED (no sorry); `#print axioms` ⊆ `[propext, Classical.choice, Quot.sound]` (`span_natCast_eq_top` needs only `[propext, Quot.sound]`). |
| #MTF-2     | BorgerVacuity.lean            | **The ultraproduct object (T2, PARTIAL).** `productFrobenius` (coordinatewise Frobenius `[(x_a)] ↦ [(x_a^{P a})]` on Mathlib's genuine DEPENDENT ultraproduct `Filter.Product (fun a => ZMod (P a)) l`, a bare function) + `productFrobenius_coe` + `productFrobenius_fixes_diag` (fixes the diagonal integers `d(n)`, the Fermat coordinatewise witness `ZMod.pow_card` lifted to the ultraproduct; proof uses NO filter property = NG1 limit-independence) + `ultraproductFrobenius` (the `↑U` alias). Fragment (A): `germRingHom` / `germRingHom_id` / `germRingHom_coe` (a coordinatewise ring endomorphism descends to a ring hom on the NON-dependent germ ring `Filter.Germ l R`, using Mathlib's `CommRing (Germ l R)`). **WALL**: `Filter.Product` has only a model-theoretic `FirstOrder.Language.Ultraproduct.Structure` (+ Łoś), NO `CommRing`/`Field`/`IsAlgClosed`; so `productFrobenius` is not a `RingHom`, and the automorphism / char-0 / pseudofinite-fixed-field facts need the `FirstOrder.Language.ring` + `CompatibleRing` + Łoś transport Mathlib lacks (feasible, multi-day). PROVED fragments (no sorry); `#print axioms` ⊆ `[propext, Classical.choice, Quot.sound]`. |
| #MTF-4     | (Mathlib gap)                 | **The cardinality dichotomy (T4, BLOCKED-ON-MATHLIB).** NG2(iii): every internal subset of `K` is finite or of cardinality `≥ 2^{ℵ₀}`, so no internal set is the prime-indexed diagonal. MISSING from Mathlib: a `Saturated` / κ-saturation predicate on `FirstOrder.Language.Structure`; the type-realization (`CompleteType`, realizing a type in a saturated model) machinery; the internal-subset (ultraproduct-of-subsets) API on `Filter.Product`. Precise failure mode recorded in `BorgerVacuity.lean`; not attempted (building saturation theory is out of scope). |
| #TT-2      | TamenessTrade.lean            | **Primes definable from von Mangoldt.** `prime_iff_vonMangoldt`: the full `↔` `n.Prime ↔ Λ n ≠ 0 ∧ ∀ m < n, Λ m ≠ Λ n` over `ArithmeticFunction.vonMangoldt`. Both directions proved: forward (`vonMangoldt_apply_prime` gives `Λ n = log n ≠ 0`; any `m < n` with `Λ m = log n` is a prime power whose `minFac` equals `n` by `log`-injectivity, forcing `n ∣ m` hence `n ≤ m`, contradiction) and reverse (a non-prime prime power has `n.minFac < n` with `Λ (n.minFac) = log (n.minFac) = Λ n`, violating the minimality clause). The tameness-trade "weight recovers the support" (D1 → D3) step, machine-checked. PROVED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #S4C-1     | S4Carrier.lean                | **The prime-side inequality (the Euler gate), e1o VERIFIER target 1 (S4/R1 arc, #162).** `prime_side_inequality`: with NONNEGATIVE coefficients `w` (the Euler gate, stated explicitly), a pointwise majorant `chi i ≤ sel i`, and `0 ≤ chi i`, `∑_{i∈S} w i·chi i ≤ ∑_{i∈T} w i·sel i` for `S ⊆ T` (majorant step via `mul_le_mul_of_nonneg_left` + horizon extension via `Finset.sum_le_sum_of_subset_of_nonneg`) = the pairing `ψ(x) = ∑ Λ(n)·chi(log n) ≤ ∑_{n≤Xh} Λ(n)·S(log n)`. `prime_side_inequality_vonMangoldt`: `Λ ≥ 0` discharges the gate via `ArithmeticFunction.vonMangoldt_nonneg` (the CORRECTED nonnegativity-explicit form per #161; D-H's sign-changing `Λ_DH` fails the `hw` hypothesis = the route is unposable for Davenport-Heilbronn, T5a). DISCHARGED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #S4C-2     | S4Carrier.lean                | **Tail divergence via Chebyshev blocks (KERNEL), e1o target 2.** `tendsto_exp_div_succ_sq_atTop` (`e^k/(k+1)^2 → +∞`, via `Real.tendsto_exp_div_pow_atTop 2` + an eventual comparison `(1/4)e^k/k^2 ≤ e^k/(k+1)^2`) + `tail_pairing_not_summable` (if the e-adic block masses dominate `c·e^k` and the majorant tail weights dominate `1/(A(k+1)^2)`, the block-paired series `b` is NOT summable: its terms `→ +∞`, contradicting `Summable.tendsto_atTop_zero`). Formalizes T2a's ill-posedness clause: the band-limited majorant pairing against the full comb diverges (density `e^u` beats the `(δ(u-L))^{-2}` tail), so the classical skeleton needs a horizon device. **Mathlib gap** (carried as a hypothesis, honest KERNEL): no directly usable Chebyshev lower bound `ψ(x) ≥ c·x`. DISCHARGED for the kernel (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #S4C-3     | S4Carrier.lean                | **The structural nil (change-of-basis atom), e1o target 3.** `span_exp_pair_eq_span_trig`: over ℂ, `Submodule.span ℂ {e^{+iθ}, e^{-iθ}} = Submodule.span ℂ {cos θ, sin θ}` as subspaces of `ℝ → ℂ` (Euler, via `Complex.cos_add_sin_I`/`cos_sub_sin_I`/`I_sq` and `Submodule.mem_span_pair` both ways). With `θ u = 2πk u/L` this is the per-frequency statement behind claim (a): the carrier's degree-`N` exponential space IS the generic degree-`N` trig space, so no carrier-native majorant beats the generic extremal at a fixed type. DISCHARGED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #S4C-4     | S4Carrier.lean                | **Decimation rank-1 collapse, e1o target 4.** `decGen_eval_eq` (the generator `e^{2πi(Km)u/L}` at `u_0+j(L/K)` equals its value at `u_0`; the phase advances by `2πmj ∈ 2πℤ`, invisible to `exp`, via `Complex.exp_int_mul_two_pi_mul_I`) + `decimation_rank_one` (by `Submodule.span_induction`: EVERY `f ∈ V_K = span{decGen K L m : m ∈ ℤ}` has `f(u_0+j(L/K)) = f(u_0)`, so the K evaluation functionals at the decimation nodes all COINCIDE = rank 1). The clean kernel of T4c's control (the F_q "Frobenius" of the K-fold cover of the log-circle); over ℤ the log-prime comb is Q-linearly independent so no AP structure exists and the collapse does not fire (cost ratio 1.000). DISCHARGED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |
| #S4C-5     | S4Carrier.lean                | **Full price at incommensurable points (the trig Vandermonde), e1o target 5.** `trig_vandermonde_nonsingular` (distinct points `z_j = e^{2πi u_j/L}` on the circle, i.e. `z` injective, give `(Matrix.vandermonde z).det ≠ 0`, via Mathlib's `Matrix.det_vandermonde_ne_zero_iff`) + `vandermonde_entry_eq_freq` (the entries `z_j^k = e^{2πi k u_j/L}` are the frequency-`k` evaluations, tying the abstract Vandermonde to the carrier's exponential basis). Distinct incommensurable nodes pay FULL PRICE for interpolation (no rank collapse = the measured cost ratio 1.000 of T4c). DISCHARGED (no sorry); `#print axioms` = `[propext, Classical.choice, Quot.sound]`. |

## Mathlib coverage gaps

As of 2026-05, Mathlib does NOT have:
- Prismatic cohomology (Bhatt-Morrow-Scholze).
- Lambda-rings or blueprints.
- Noncommutative geometry beyond C*-algebras basics.
- Tropical Hodge theory.

For verification targets in these areas, VERIFIER agents must EITHER:
1. Propose a minimal Mathlib extension (a new definition or lemma to contribute back), OR
2. Reduce the claim to existing Mathlib lemmas + axioms with the axioms flagged.

The eventual goal is to upstream the foundational definitions to Mathlib, making the proofs canonical.

## Build

```bash
cd lean
lake build
```

(Requires `elan` / `lean4` installed. See [https://leanprover-community.github.io/get_started.html](https://leanprover-community.github.io/get_started.html).)

**Smoke-test status as of 2026-05-25**: GREEN. `lake build` succeeds on Windows 11 with Lean 4.13.0 + Mathlib v4.13.0. All 2250 modules compile. Remaining warnings are exactly the documented `sorry` markers (#FE-1, #EP-1, #DH-zero, #LR-2, #Fejer-1, #BP-fiber, #MB-1..#MB-6) and stale skeleton sorries in PrismaticCohomology/PrismaticFoliation/HodgeIndex/R3_5.

## How agents use this

- **BUILDER**: writes mathematical definitions in `ZetaRH/`.
- **VERIFIER**: translates BUILDER definitions into Lean and proves theorems. Picks a VERIFIER target ID from the table above and converts the `sorry` to a real proof.
- **ADVERSARY**: writes Lean-formalized counterexamples or attacks proposed theorems.
- **SYNTHESIZER**: maintains this README, the VERIFIER target table, and cross-references to the rest of the project.

## Cross-references

- [`../docs/03_research/proof_program.md`](../docs/03_research/proof_program.md): AI-augmented variant of the proof program.
- [`../docs/03_research/proof_program_ai_only.md`](../docs/03_research/proof_program_ai_only.md): AI-only variant.
- [`../docs/03_research/research_directions/`](../docs/03_research/research_directions/): the eight research directions.
- [`../experiments/PROOF_ARCHITECTURES_PLAN.md`](../experiments/PROOF_ARCHITECTURES_PLAN.md): the test plan.
- [`../experiments/LEARNINGS.md`](../experiments/LEARNINGS.md): cross-architecture findings.
