# Riemann Zeta Function — Deep Study Repo

A multi-level exploration of the Riemann Zeta Function and the Riemann Hypothesis: from intuitive visual understanding through graduate-level analysis to the frontier of current research. Includes a computational experimental thread organized around testing the four candidate RH proof architectures.

**Operational substrate**: this repo is also structured as the substrate for an AI-augmented (and, speculatively, AI-only) proof program for RH. See [`STATE_OF_THE_PROGRAM.md`](STATE_OF_THE_PROGRAM.md) for a one-page repo-wide strategic snapshot (where every architecture stands and the single most-leveraged next move), [`OPERATIONS.md`](OPERATIONS.md) for how to operate it, [`PHASE_STATE.md`](PHASE_STATE.md) for current state, [`docs/03_research/proof_program.md`](docs/03_research/proof_program.md) for the AI-augmented plan, and [`docs/03_research/proof_program_ai_only.md`](docs/03_research/proof_program_ai_only.md) for the AI-only speculative variant. The repo is a handoff artifact: ~50-70% of Phase 0 is done; Phases 1-5 require expert collaborators and multi-year compute.

## What's Here

This repo is structured so you can enter at any level and go as deep as you want.

```
zeta-function/
├── docs/                        # All written explanations
│   ├── 00_intuitive/            # No math required: visual, conceptual
│   ├── 01_undergraduate/        # Calculus, complex numbers, series
│   ├── 02_graduate/             # Analytic continuation, functional equation,
│   │                            #   log-correlated fields, four-level RH framing
│   ├── 03_research/             # Current approaches, extreme values, PROOF PROGRAMS
│   │   ├── proof_program.md     # ★ AI-augmented (human-led) proof program
│   │   ├── proof_program_ai_only.md  # ★ AI-only speculative variant
│   │   └── research_directions/ # ★ 8 research-grade directions with operational specs
│   │   └── reading_notes/       # ★ Full-depth notes on all 26 reference-library sources
│   ├── implications/            # Why it matters (primes, physics, crypto)
│   ├── solutions/               # Known approaches to the Riemann Hypothesis
│   └── research_atlas/          # Master research map: all attempts, failures, ML directions
├── experiments/                 # ★ Computational thread; proof-architecture tests
│   ├── PROOF_ARCHITECTURES_PLAN.md    # Test plan + AI-centric methodology
│   ├── LEARNINGS.md             # 128 cross-architecture findings
│   ├── _shared/                 # LFunction interface, zeta, Davenport-Heilbronn control
│   ├── positivity/              # Arch 3 (Li coefficients, Weil quadratic form, 3B.3 rigorous)
│   ├── spectral/                # Arch 1 (Berry-Keating discretization)
│   ├── zero_free/               # Arch 4 (LP/SDP family through 4E.8)
│   ├── arithmetic_geometric/    # Arch 2 (2A diff, R1-R5, R3.6, R3.6.3, 2C, 2D, 2E)
│   ├── multifractal/            # Log-correlated field experiments (pre-existing)
│   ├── chaos/                   # Strange attractors: Lyapunov spectra, fractal dimension, multifractal bridge
│   └── toy/                     # ★ The RH toy sandbox: a checkable training ground for the M4 move
├── references/                  # ★ Reference library (26 sources, gitignored PDFs) + tracked index
├── lean/                        # ★ Lean 4 / Mathlib formal verification (skeleton)
│   ├── lakefile.lean
│   ├── ZetaRH.lean              # Main module
│   └── ZetaRH/                  # R3.5, LineRestriction, LambdaBlueprints, HodgeIndex, ...
├── .claude/agents/              # ★ AI agent role specifications for AI-only execution
│   ├── surveyor.md              # Literature survey + scorecard maintenance
│   ├── builder.md               # Propose constructions
│   ├── verifier.md              # Formalize in Lean 4
│   ├── adversary.md             # K1-K4 attack; D-H discipline
│   ├── synthesizer.md           # Integrate outputs into project dossier
│   └── orchestrator.md          # Schedule work, manage compute budget
├── sources/                     # Original PDFs and their converted text
├── visualizations/              # manim animation scripts
├── OPERATIONS.md                # ★ How to operate this repo as the proof-program substrate
├── PHASE_STATE.md               # ★ Current operational state (read by ORCHESTRATOR)
└── CLAUDE.md                    # Project + owner context for AI assistants (merged successor of LLM_ONBOARDING.md)
```

## The Question

The **Riemann Hypothesis** states:

> All non-trivial zeros of the Riemann zeta function have real part equal to 1/2.

It has been open since 1859. It is one of the Millennium Prize Problems (worth $1,000,000). It governs the distribution of prime numbers, and its implications ripple through number theory, physics, cryptography, and beyond.

## Stance

We are trying to solve this. That is the posture of the whole repo.

It is hard. The odds against any single program are long, and 166 years of effort by the best mathematicians alive is the honest baseline. But "hard" is not "impossible," and this project treats RH as a target, not a monument.

Read every negative result here in that spirit. When an experiment shows that a method fails (Level 3 statistics cannot pin a single zero, the classical analytic ceiling sits at the $2/3$ exponent, NCG alone hits the K1 wall, soft positivity has zero margin), that is **progress**: it removes a dead branch and sharpens where the real proof must live. Each "this won't work" is a coordinate that narrows the search, not a verdict that the search is hopeless.

The dominant finding (RH is true only at the margin, so the proof must engage the **exact** structure of $\zeta$) is a compass, not a wall. It tells us where to dig. The job is to keep digging there.

## Levels

| Level | Folder | Prerequisites |
|-------|--------|---------------|
| Intuitive | `docs/00_intuitive/` | None — curiosity only |
| Undergraduate | `docs/01_undergraduate/` | Calculus, basic complex numbers |
| Graduate | `docs/02_graduate/` | Complex analysis, real analysis |
| Research | `docs/03_research/` | Graduate mathematics |
| **Research Atlas** | `docs/research_atlas/` | **Start here for ML research** — full catalog of approaches, failures, obstructions, and ML directions |

## Visualizations (manim)

Built with [manim](https://github.com/3b1b/manim) (3Blue1Brown's animation engine). Ten standalone scenes cover the foundations (series, complex plane, continuation, critical strip, zeros) and research concepts (zero-free region, functional-equation mirror, Robin's inequality, the 3D $|\zeta|$ surface, the five gaps). Three multi-part explainer series build on them:

- [`12_what_is_rh/`](visualizations/12_what_is_rh/): a zero-background "What is the Riemann Hypothesis?" explainer, every term defined with an everyday analogy (primes as atoms, zeta as a number machine, zeros as where it goes silent).
- [`13_functional_equation/`](visualizations/13_functional_equation/): a graduate course, Episode 2 "The Functional Equation", a rigorous derivation of $\xi(s)=\xi(1-s)$ via theta and Poisson summation, opening on the 3D $|\zeta|$ terrain.
- [`11_three_stage_rh/`](visualizations/11_three_stage_rh/): the dense three-stage arc (RH from scratch, the all-roads convergence, the M4 polarization gap), with a full storyboard in `SPEC.md`.

Scenes 12 and 13 carry their narration on screen as subtitles and ship a spoken-narration script (`NARRATION.md`). A standalone context scene, [`14_lorenz_attractor/`](visualizations/14_lorenz_attractor/), renders the Lorenz strange attractor in 3D plus a butterfly-effect divergence beat; it is the visual companion to the chaos thread. See [`visualizations/README.md`](visualizations/README.md) for setup, the full scene index, and how to render each scene.

## Source PDFs

Original source PDFs and their converted text versions live in `sources/`.

## Experimental thread

See [`experiments/PROOF_ARCHITECTURES_PLAN.md`](experiments/PROOF_ARCHITECTURES_PLAN.md) for the test plan. Four candidate RH proof architectures (spectral, arithmetic-geometric, direct positivity, analytic) with the Davenport-Heilbronn L-function as a structural wrong-approach detector throughout. ~25 experiments + literature reviews implemented across the four architectures, plus full dossier coverage for 5 of 6 major Arch 2 candidates (Borger, Lorscheid, Connes, Deninger, Deitmar). Arch 3 (positivity) has the deepest experimental thread, including a Gram-matrix wrong-approach detector validated at four T_max values (`n_neg` exactly equals number of off-line zero pairs in upper half plane at T_max ∈ {200, 300, 350, 500}).

Smoke test:
```powershell
python -m experiments._shared.smoke_test
```

## Cross-cutting findings

Synthesis of structural insights across architectures lives in [`experiments/LEARNINGS.md`](experiments/LEARNINGS.md). The dominant meta-finding from the experimental thread: **RH is true only at the margin**. Every positivity reformulation (Weil-form, Li, de Bruijn-Newman, MT shape factor) is sharp at zero margin. Five independent reinforcing directions (LEARNINGS findings #7, #11, #12, #13 + Arch 3E literature review) point the same way. This is the project's most useful piece of map: it tells us a winning proof must use the EXACT structure of $\zeta$ (Euler product, functional equation, archimedean factor) rather than generic positivity arguments. The margin result rules out the soft routes so that effort concentrates on the structural one.

The 17-constraint framework for Architecture 2 candidates (see [`experiments/arithmetic_geometric/2A_candidate_evaluation.md`](experiments/arithmetic_geometric/2A_candidate_evaluation.md)) operationalizes the comparison of $\mathbb{F}_1$ programs and identifies the geometric route (intersection theory + Hodge index theorem on a constructed surface) as the unique remaining direction for breaking the K1 wall on positivity. The path forward strategic synthesis is in [`experiments/arithmetic_geometric/2A_path_forward.md`](experiments/arithmetic_geometric/2A_path_forward.md).

## Status

| Area | Status |
|------|--------|
| Repo structure | ✅ Complete |
| Publications / upstreaming | 🟡 Registry live ([`PUBLICATIONS.md`](PUBLICATIONS.md)): 2 Mathlib PRs SUBMITTED ([mathlib4#41132](https://github.com/leanprover-community/mathlib4/pull/41132) digamma, [#41133](https://github.com/leanprover-community/mathlib4/pull/41133) `riemannZeta_conj`; ported to current master, CI-green, AI-use disclosed), 6 research/formalization candidates evaluated (D-H discipline, all-roads survey, LP/SDP/SOS ceiling, function-field RH Lean chain, Π⁰₁ note, stealth-window note); evaluation gate + scoring rubric in place |
| Solutions / approach catalog | ✅ `docs/solutions/` |
| Research atlas | ✅ `docs/research_atlas/` |
| Graduate docs (incl. log-correlated, four-level framing) | ✅ Substantial |
| Experiments — Phase 0 infrastructure | ✅ Complete |
| Experiments — Arch 3 (positivity) | ✅ 3A-3D, 3B.2, 3D.2-3D.4, 3E (literature), 3F-3I, 3J (Schur complement against on-line cushion gives ~30x sharper detector than raw spectrum, rel min asymptote -78.7%), 3K (hypothetical off-line zero perturbation, disproof path closed in Gram-matrix family) complete; **3L (Epstein zeta second control): the Schur detector obeys the same counting law (schur_neg = #off-line heights) on a structurally independent off-line construction, generalising the wrong-approach discipline beyond D-H; 3B.4: second Li-criterion off-line witness (Epstein) + rigorous Selberg discrimination** |
| Experiments — Arch 1 (spectral) | ✅ 1A-1C complete; 1D Connes literature brought current (2026-06: Connes-Consani prolate/semilocal + Bender-Brody-Müller, [`quantum_mechanics_signature_dossier.md`](experiments/spectral/quantum_mechanics_signature_dossier.md)); 1E (`e1d`) additive-arithmetic-injection NULL; the CCM semilocal prolate thread deep-dived (`e1f`/`e1g`/`e1h`, all signature-blind by distinct mechanisms, [`ccm_semilocal_prolate.md`](docs/03_research/ccm_semilocal_prolate.md)), the deferred metaplectic operator the un-eliminated route; its **finite-local** model then attacked directly (`e1i`, #118, finite Weil rep over $\mathbb{F}_p$: exact gate $\mathrm{Tr}(F_p)=\varepsilon_p$ + double-cover cocycle, but the finite-local quadratic sign-structure is signature-blind under the positivity compression, which does NOT eliminate the route, it sharpens it to the global $S\to\infty$ assembly = M4); **the metaplectic front then EXECUTED (`e1j`, #138, adversary-verified): the route decomposes into three local channels (geometry = a modulus-blind ultrametric powers-of-$1/p$ ladder / measure = reads magnitude $\sim 1/m$ and is reweighting-invisible / sign = cancels in $g^*g$), each folded with its genuine arithmetic and each locally blind, so no local channel carries the $\zeta$-vs-D-H discrimination and it is pinned at the global assembly = M4** |
| Experiments — Arch 4 (zero-free regions) | ✅ 4B, 4D, 4D.2, 4E, 4E.2-4E.8 complete; 4A+4C unified literature dossier (V-K $2/3$ exponent is a structural ceiling after BDG 2016; Arch 4 is constraint-mapping, not a route to RH); **4E.9 (Heath-Brown multi-zero MT SDP, Direction 7): the multi-zero MT shape factor does not exceed the 1D Fejér ceiling (best ratio ≤ 1, rank-2 certificate). The LP/SDP/SOS family is now fully closed** |
| Experiments — Arch 2 (arithmetic-geometric) | ✅ 2A, 2B, 2E complete (2A includes R1-R5 evaluation framework with hybrid proposals AND 5 of 6 candidate dossiers: Borger, Lorscheid, Connes, Deninger, Deitmar; 2E confirms bare $\psi_p$ on concrete Λ-rings has no zeta-zero-like spectrum, validating R5's cohomology-must-lift framing); **2F (Hodge-index sweep): the function-field RH bound $|\alpha_i| = \sqrt{q}$ holds exactly across a family of curves over finite fields, exhibiting the positivity target a Spec($\mathbb{Z}$) lift must reproduce**; 2C (F_1/Arakelov survey) and 2D (Deninger micro-target → Direction 4) complete. **Direction 8 opened (session 004): 2G — the Hodge index as a SIGNATURE on $C\times C$ (primitive form negative definite $=$ Hasse-Weil, machine-proved in Lean); 2H/2M — the Faltings-Hriljac height pairing over Spec($\mathbb{Z}$) is positive definite at ranks 1-4 (regulators match LMFDB); 2I/2L/2P — the complete Silverman local-height decomposition (archimedean + every finite prime) validated $h_\infty+\sum_p h_p = \hat h$ by the authoritative algorithm; 2K — the dictionary mapping all of this to the would-be $\mathrm{Spec}(\mathbb{Z})\times\mathrm{Spec}(\mathbb{Z})$ intersection form. The single-arithmetic-surface side is complete and validated; the remaining gap is the product surface + Frobenius. **Session 005: 2Q — the place-dependent bidegree obstruction (the arithmetic Frobenius correspondence $\Gamma_S$ must carry a $(1,p)$ bidegree per prime, not a single scale $q$; forces infinite-dim $H^i$ + the Deninger $\mathbb{R}$-flow, pins $\Gamma_S^2$ to the von Mangoldt prime sum); 2R — realizes that $\Gamma_S^2$ concretely as a Ruelle dynamical-zeta log-derivative ($-\zeta'/\zeta$, orbit lengths $\{\log p\}$), with the D-H control showing no such orbit spectrum exists.** |
| Research spine + framing (session 007, 2026-05-30) | ✅ **All roads lead to the signature.** Five new directions proposed (9-13, each attacking the #25 bidegree obstruction); two cheap D-H falsifications (Li log-concavity = non-Euler detector not RH; Jensen/Turán = stealth window; LEARNINGS #27); **Direction 10 (THH/TC over the sphere spectrum)** pushed to Hesselholt's proven $\zeta = \det_\infty(s-\Theta\mid\mathrm{TP})$ template (#28-29). **Synthesis** ([`docs/03_research/all_roads_to_the_signature.md`](docs/03_research/all_roads_to_the_signature.md), #30): every framework realizes $\zeta$ (a determinant/trace), but RH is the SIGNATURE, the same positivity everywhere. **Sharpened** to the standard form ([`08A_rosati_standard_conjecture.md`](docs/03_research/research_directions/08A_rosati_standard_conjecture.md)): **RH = arithmetic Rosati positivity = the arithmetic Hodge standard conjecture**, with an M1-M5 milestone ladder. M1 done (2T: function-field Rosati positivity, four equivalent faces, #31); M2 assembled (2U, #32); M2.5 done (2V, #33: $A_{\mathrm{arch}}$ fixed + $T_{\max}$-validated, diagnostic corrected to $\min\mathrm{eig}$, non-circular Weil form for $\zeta$ POSITIVE $+0.035$, necessary-not-sufficient). **M2.6 done (2W, #34): general-$\mu$ archimedean constant fixed ($C_\mu = \log\pi - \psi((1+\mu)/2)$; D-H block residual $5.58\to0.12$, validated), but the four-way verdict hits the STEALTH WINDOW: D-H reads spuriously positive ($+0.094$) because its off-line obstruction ($\gamma\approx85.7$, $\sim$2.6%) is below the residual floor (#18/#19/#27 on the Rosati side). CONSEQUENCE: M3 must be ANALYTIC** (prove archimedean-dominates-prime domination via the exact off-line structure), not a finer numerical truncation. The non-circular Weil-form infrastructure (any $\Gamma$ factor) is complete + validated; the open problem is the analytic domination. Framing recast to a research-program stance; operating philosophy in [`docs/researcher_mindset.md`](docs/researcher_mindset.md). |
| M3 + stress tests (session 008, 2026-06-01) | 🟡 **M3 is a validated numerical discriminator, not geometric progress.** The attempt (e3m) split $P_{\mathrm{fin}}$ into prime-power $P_{\mathrm{pp}}$ + composite $P_{\mathrm{comp}}$ and formed $M_{\mathrm{euler}} = A_{\mathrm{arch}}+P_{\mathrm{pp}}+B_{\mathrm{pole}}$ by deleting $P_{\mathrm{comp}}$: separates the controls (zeta $+0.035$, D-H $-0.929$). Stress-tested in three threads: **(#35, e3m2)** survives the false-negative probe -- independent Euler products $\chi_3,\chi_4$ stay positive with $P_{\mathrm{comp}}=0$ exactly; **(#36, e3m3)** fires negative on a second, independently-constructed and FE-verified off-line control at conductor 7 ($\rho = 1.0616+28.44i$, $|f(\rho)|\sim$2e-50), removing the single-example fragility (both witnesses still Davenport-Heilbronn-type); **(#37)** the geometric conjecture "absolute prismatic cohomology forces $P_{\mathrm{comp}}=0$" is VACUOUS for $\zeta$ ($P_{\mathrm{comp}}=0$ is the Euler product, a theorem, so $M_{\mathrm{euler}}=M$ already) and INAPPLICABLE to D-H (no Euler product $\Rightarrow$ no surface), so it is struck from the Direction 8 ladder. Net: M3 reorganized bookkeeping; the genuine gap (construct the surface for $\zeta$ + prove its cup-product signature) is unchanged. Earlier "breakthrough / stealth window broken" framing corrected throughout. |
| Direction 12 (de Bruijn-Newman) heat side (session 009, 2026-06-02) | ✅ **Kernel positivity is orthogonal to RH** (E_DBN1, LEARNINGS #38). First heat-side experiment, closing the gap `3e` deferred. A Bochner/Toeplitz + max-modulus test (project idiom) for the Pólya kernel positivity $\Phi\ge0$, validated on $\zeta$ vs Pólya's closed form to 4.4e-16. Finding: $\Phi\ge0$ is **K2-failing** -- the RH-false Davenport-Heilbronn passes it identically to $\zeta$ (clean degree-1), because off-line zeros are archimedean-suppressed in the kernel to $\exp(-\tfrac\pi4 d\gamma)$ ($|\Xi_{DH}(85.7)|=1.5\mathrm{e}{-}29$; measured rates $0.73\!\sim\!\pi/4$ deg-1, $1.59\!\sim\!\pi/2$ deg-2) -- the **stealth window in the heat basis**, the softest detector yet. Verified by a 4-agent workflow (independent re-implementation reproduced every number; D-H off-line zero refined to $0.80852+85.6993i$). **Honest framing: the conclusion is correct but PRE-EMPTED** (Dobner 2020 -- extended Selberg class $S^\#$ incl. D-H; Newman-Wu 2019; convergent Michalowski 2026), so a confirmed coordinate importing 3 corroborating references, not new mathematics. Isolated then **RESOLVED** Level-4 sub-target 12.4 (LEARNINGS #39): the Rodgers-Tao $\Lambda\ge0$ functional $\leftrightarrow$ Weil/Hodge positivity bridge is **FALSE**. An equation-level survey shows Rodgers-Tao's functional is the log-gas Hamiltonian $\sum\log\frac1{|x_j-x_k|}$ on the zeros — RH-agnostic in sign ($dH/dt=-4E\le0$ always), a Level-3 statistical object — not the Weil quadratic form; its input (Montgomery pair correlation) is consumed as Level-3 statistics. So $\Lambda\ge0$ is the Level-3 half and $\mathrm{RH}{=}(\Lambda\le0)$ the Level-4 signature; the flow does not short-circuit to the signature. Confirms the four-level framing and re-isolates Direction 8 (the product surface) as the irreducible target. |
| Direction 8 (product surface) first probe (session 009, 2026-06-02) | 🟡 **The Connes-Consani square's tropical Hodge index is free but arithmetic-blind** (2CC, LEARNINGS #40). First numerical probe of the product object (C-C arithmetic-site square = Newton polygons under convex-hull-union + Minkowski-sum). Its canonical mixed-volume form is Lorentzian by **Alexandrov-Fenchel** -- a Hodge-index signature $(1,k)$ exists FOR FREE (verified, robust over 2000 random collections). BUT it is the **characteristic-1 shadow**: arithmetic-blind, it freezes the Frobenius trace $t$ ($\Delta\cdot\Gamma = p{-}1$, not the point count $q{+}1{-}t$), so its $(1,3)$ signature is UNCONDITIONAL / RH-agnostic -- the trivial extreme of the #38/#39 soft-positivity pattern. Reconciles with the gap's "no signed pairing": the RH-agnostic convex-geometry shadow exists; the **arithmetic** pairing carrying $t$ does not. **Gap sharpened to the arithmetic $q$-lift that restores $t$**, with the $(1,p)$ bidegree (2Q) its locus. Realizes attack angle 4.A (tropical/AHK) on the specific surface and shows why it is insufficient. ADVERSARY-checked (PASS, softened): a *proposed* shadow (toric-BKK analogy), not a constructed intersection theory of the topos. **2CC.2 (#41)** then probed the diagonal: the self-composition $\Psi(\lambda)\circ\Psi(\lambda^{-1})=\mathrm{Id}_\epsilon$ (C-C Thm 7.7) **does** carry the von Mangoldt $-\zeta'/\zeta$ (its tangent $=$ the Deninger-Hesselholt flow generator $\Theta$; $-d/ds\log\det_\infty(s-\Theta)=-\zeta'/\zeta$), refining 2CC -- the diagonal self-intersection is trace-rich where the off-diagonal point-count froze. But it is the TRACE, not the signature; the q-lift gap is unchanged. Connects C-C $\leftrightarrow$ 2R $\leftrightarrow$ Deninger/Hesselholt. **2CC.3 (#42)** then *tried* the q-lift (probe a): it DECOMPOSES into un-idempotent-ization (formal -- the soft-max restores subtraction, so a signed pairing becomes possible; the 2CC idempotency obstruction is removable) + per-prime lift (the known finite-q FF Hodge index, carries t) + the GLOBAL local-to-global assembly = the gap. The attempt crosses the first two and LOCALIZES the third: the zeros live in the analytic continuation Re(s)<1, invisible to the local Euler/orbit data (converges only Re(s)>1; on the critical line $\prod_p(1-p^{-s})^{-1}$ does not converge). The irreducible step is realizing the continuation as a signed pairing on the global $H^1$ = the missing Weil cohomology (M3/#25), unchanged. Not crossed -- localized. |
| Direction 8 (product surface) continued (session 009, 2026-06-02) | 🟡 **Two more coordinates + a consolidated map; the polarization is the universal gap.** **2DB.1 (#43):** the de Branges / Conrey-Li per-zero cross-term $Q(\rho) = -\mathrm{Re}\{\xi'(\rho)\xi(1+\rho)\}$ is the exact CONVERSE of #42 -- the global de Branges pairing DOES see the zeros, but its positivity is strictly-stronger-than-RH and FAILS for $\zeta$ (only $k{=}34$ negative of the first 50; reproduced Conrey-Li's $-5.389\mathrm{e}{-}69$ to 12 sig figs). RH-agnostic / third soft detector after #38/#39; double-archimedean suppression slope $-(\pi/2)/\ln 10$. **2PR.1 (#44):** Bhatt-Lurie's WCart carries TWO operators -- the Frobenius $F$ ($\to$ finite Euler factors) and the Sen $\Theta$ (Hodge-Tate weights $\{-n\}$ $\to$ the archimedean $\Gamma$-factor DIVISOR via Lerch, ratio 1.0); both TRACES, neither contains the non-trivial zeros ($|\det_\infty(\rho-\Theta_{\mathrm{Sen}})|=4.39\mathrm{e}9\ne0$). Real-but-only-heuristic (spectral coincidence; supplies the divisor not $\Gamma_\mathbb{R}/\pi$), RH-agnostic. **Consolidated map** ([`docs/03_research/spec_z_cohomology_landscape.md`](docs/03_research/spec_z_cohomology_landscape.md)): a 15-candidate scorecard against the (i)-trace/(ii)-FE-duality/(iii)-polarization template. Every candidate realizes $\zeta$ as a TRACE; NONE carries the polarization (the universal gap $=$ RH $=$ 08A M4), bracketed by Faltings-Hriljac (proven, too local), AHK (proven signature, arithmetic-blind), de Branges (too strong). Verified 2024-2026 literature: Deninger's foliated space is now CONSTRUCTED (arXiv:1807.06400; corrects the dossier caveat), Petrov's Sen operator is NOT semisimple (Annals, arXiv:2302.11389; blocks eigenspace polarization), Connes-Consani's Feb-2026 Jacobian (arXiv:2602.15941) builds realization structure with no RH-positivity. |
| RH solved by accident: the accident-vector landscape (2026-06-03) | ✅ **Chase Tao's "solved by accident" thesis, made operational.** A multi-agent sweep over 32 candidate "accident channels" (programs NOT aimed at RH that could incidentally supply the missing polarization), each adversarially verified against the target spec and the D-H filter ([`docs/03_research/rh_solved_by_accident.md`](docs/03_research/rh_solved_by_accident.md)). **No-free-lunch across all 28 scored:** in every case the program's own success yields only the realization, and the residual step is itself RH-equivalent. **B-credible tier (right KIND of tool, all pass D-H):** Beilinson-Bloch height positivity (Gao/Chao Li), Chen-Moriwaki/Yuan-Zhang adelic Hodge index, prismatization-over-$\mathbb{Z}$ F-gauge cup form, Deninger foliated Lefschetz, geometric Langlands weight-purity. D-H reframed as a real-time classifier on incoming tools; the marginal-positivity thesis as a compass on which accidents are even possible. **Cheap-probe 3 executed: 3O ([e3o_pslq_stumble.py](experiments/positivity/e3o_pslq_stumble.py), LEARNINGS #45)** -- the PSLQ stumble-yield sweep shows the numerical-stumble accident slot is K2-blind by computability (PSLQ-accessible quantities are the archimedean half D-H shares; the discriminating Li data is precision-limited), and reproduces+defeats the de Branges trap via a precision-stability test. |
| Lean 4 / Mathlib formalization | ✅ Phase 1 substrate green (2026-05-25). **Session 008: six sorry-free digamma identities in `ExplicitFormula.lean` (#EF-arch). 2026-06-04 re-check: Mathlib master now ships `Complex.digamma` with the recurrence + both special values (3 of the 6 preempted); the reflection, iterated-recurrence, and duplication formulas remain novel upstream candidates (staged in [`lean/upstream/digamma_contribution.md`](lean/upstream/digamma_contribution.md)). The six: recurrence $\psi(s+1)=\psi(s)+1/s$, reflection $\psi(1-s)-\psi(s)=\pi\cot\pi s$, iterated recurrence $\psi(s+n)=\psi(s)+\sum_{k<n}1/(s+k)$, duplication $\psi(2s)=\tfrac12(\psi(s)+\psi(s+\tfrac12))+\log 2$, and the special values $\psi(1)=-\gamma$ and $\psi(\tfrac12)=-\gamma-2\log 2$. The half-integer values are the arguments in $\zeta$'s archimedean kernel.** **Session 005: `ExplicitFormula.lean` — the Weil explicit formula + Weil positivity criterion scaffold (the highest-leverage target, LEARNINGS #17); prime side concrete via Mathlib `vonMangoldt`. Discharged #MB-1/#MB-2/#MB-6 (ζ pole, nonvanishing, functional equation) to real proofs, and proved the digamma recurrence $\psi(s+1)=\psi(s)+1/s$ (Mathlib has no digamma). Build green; project sorry count 26 → 23.** Full proof closure remains multi-year |
| RH logical status + Π⁰₁ kernel witness (2026-06-04) | ✅ **Undecidability is a back door to truth, not an escape** (LEARNINGS #64). New doc [`rh_logical_status.md`](docs/03_research/rh_logical_status.md): RH is Π⁰₁ (Robin/Lagarias/DPRM), so independence ⟹ truth (a ZFC-independence proof would itself prove RH true, via Σ₁-completeness; Gödel II forces a Con(ZFC)-strong metatheory); forcing cannot touch it; the dead corners (independence-as-a-program, Chaitin, busy-beaver counts) carry no content; the one open technical core is the reverse-math strength of N(T). New Lean [`RHEquivalences.lean`](lean/ZetaRH/RHEquivalences.lean): the equivalence hub + `RH_arith` (the Π⁰₁ surrogate), **8 sorry-free anchors** (incl. `rh_arith_refutable` = the Σ⁰₁ refutability dual, and Lagarias at n=1,2,3 with escalating effective-bound sharpness = the formalization-layer shadow of marginal positivity), 5 documented sorries (deep equivalences, none closable without RH). Build green. |
| Roadmap synthesis + de-accretion (session 012, 2026-06-04) | ✅ **Converged to one target; soft-detector thread FROZEN.** A 32-agent map / strategize / adversarial-stress-test / synthesize sweep over "where do we go from here": the spine is the correct LOCATION of RH and is now maximally sharp, but **14 of 19 fresh moves were restatements / soft-detector traps / dead corners**. The one irreducible target is **M4 organ (a): the Euler-pole $H^2$ cup-product polarization on the infinite-dim arithmetic $H^1$** (2HH/#61: RH iff the FE-duality cup product is a polarization). Consolidated the soft-detector thread into [`soft_detector_wall.md`](docs/03_research/soft_detector_wall.md) (FROZEN: 5 load-bearing facts #46/#52/#56/#61/#63 + full index; a new detector is dead on arrival unless it separates $\zeta$ from D-H at reachable truncation or is a genuine signature theorem). Fixed the #43-49 → canonical #57-63 finding-number drift and the **Gurney/Drinfeld citation** (arXiv:2301.12392 is Gurney's *Prismatization over $\mathbb{Z}$*, the global-over-Spec($\mathbb{Z}$) construction; Drinfeld's/Bhatt-Lurie's are p-adic; web-verified). Wrote the [foundational-object falsification brief](docs/03_research/foundational_object_dimension_trace_brief.md) (the object EXISTS but its polarization is OPEN; Deninger's foliated space is genuinely constructed, arXiv:1807.06400). **2KK ([e2kk_wcart_finite_model.py](experiments/arithmetic_geometric/e2kk_wcart_finite_model.py), #66):** the first WCart probe to form the cup pairing as a matrix and read its signature — **wrong polarity at rank 2** (unconditionally hyperbolic), so organ (a) needs higher-rank primitive cohomology or a Frobenius-weighted (non-Hodge-star) positivity; the ADVERSARY caught 3 code defects (string-match D-H gate, fake Jordan block, silent non-cup-form), all flagged. The one banked externally-valuable deliverable is the digamma Mathlib PR body ([`lean/upstream/digamma_pr_body.md`](lean/upstream/digamma_pr_body.md)). |
| Euler-Sen monodromy primitive form (session 013, 2026-06-05) | 🟡 **Survives as a formalism; it is transport, not positivity.** **2LL (#67, merged PR #5):** a genuine non-semisimple Sen block ($\Theta=-\tfrac12 I+N$, $N^2=0$) placed at the Tate center satisfies the exact cup-target equation ($\Theta^T\Omega+\Omega\Theta=-\Omega$, residual $0$) and turns the alternating cup form into the Euler/Rosati matrix via $Q_N(x,y)=\Omega(x,Ny)$, recovering the function-field Hasse-Weil sign ($t^2<4g^2q$) and escaping 2KK's rigid hyperbolic diagonal-star trap. Formalized in Lean ([`EulerSenLinearAlgebra.lean`](lean/ZetaRH/EulerSenLinearAlgebra.lean), [`FrobeniusAlgebra.lean`](lean/ZetaRH/FrobeniusAlgebra.lean)). **2MM (#68):** the adversarial K1 audit ([e2mm](experiments/arithmetic_geometric/e2mm_euler_sen_k1_audit.md)) shows the finite form is a TRANSPORT theorem: $\mathrm{signature}(Q_N)=\mathrm{signature}(B)$ for arbitrary symmetric $B$ (0 mismatches across 32 rows, dimensions 1-6), so it manufactures no positivity. Sharpens M4 organ (a) without ambiguity: construct $B$ geometrically from Euler/Frobenius data plus the archimedean metric and prove $B>0$ on primitive pieces without zero-side or RH input (importing $B$ is K1-circular). K2 guard upgraded to data-based (`has_euler_product`, which blocks a renamed Davenport-Heilbronn). |
| WCart primitive-quotient probe (session 014, 2026-06-05) | 🟡 **Petrov non-semisimplicity is routable but unhelpful.** **2LN ([e2ln_wcart_primitive_quotient.py](experiments/arithmetic_geometric/e2ln_wcart_primitive_quotient.py), #69):** the only direct-polarization escape hatch left after 2LL/2MM. On a faithful $\mathrm{sl}_2$/Jordan defective Sen model, the direct Hermitian Rosati/Lyapunov metric $\Theta^* H + H\Theta = -H,\ H>0$ does NOT exist on the full nonsemisimple block (a nilpotent is not similar to skew-Hermitian), but by Jacobson-Morozov the monodromy weight filtration makes $N$ a pure Lefschetz lowering operator, the zero endomorphism on each primitive graded quotient, where the metric DOES exist. So the Petrov nilpotent dies on the primitive quotient for free. The catch: the positivity gap relocates undiminished to the SIGN of the primitive form $Q_k(x,y)=c_E\,\Omega(x,N^k y)$ (POS for $c_E{=}{+}1$, NEG for $c_E{=}{-}1$), fixed by the archimedean Weil operator $C_E$ not by $N$; a misaligned Lefschetz leaves $N$ alive on the quotient, so the missing datum is the geometric Frobenius/Lefschetz operator, not $\Theta$. WCart stays alive; the M4 burden is unchanged, restated on the primitive quotient. Audits A-E pass (incl. the injected-D-H $0.617$ polarization defect). |
| Euler Weil operator construction probe (session 015, 2026-06-05) | 🟡 **The polar formula works locally; the sign is the global trace.** **2LO ([e2lo_euler_weil_operator.py](experiments/arithmetic_geometric/e2lo_euler_weil_operator.py), #70):** tries to BUILD the archimedean Weil operator $C_E$ that 2LN relocated the positivity gap to, via the construction target $A_E := \Omega^{-1} B_E$, $C_E := A_E(-A_E^2)^{-1/2}$. The polar formula WORKS: over $F_q$, $A_E^2 = (t^2 - 4g^2 q)I$, so $-A_E^2$ is positive definite iff $t^2 < 4g^2 q$, $C_E^2 = -I$ to $7\times10^{-15}$, and $Q = \Omega(\cdot, C_E\cdot)$ is positive exactly in the Hasse-Weil window (206/206 rows, reduces to `negDef_iff_hasseWeil`). But K1 fails CONSTRUCTIVELY: the $(1,q)$ bidegree is shared by every curve over $F_q$ while the Frobenius trace $t = q+1-\#E$ is strictly finer (nine traces over $F_5$ give nine distinct $C_E$), so the $(1,p)$ bidegree plus archimedean data do NOT determine $B_E$. The missing piece is the global Frobenius trace = the product-surface / prismatic Poincare-duality assembly (Direction 8). The FE/archimedean-only bypass forms for D-H (D-H-blind, dead). D-H discipline + 0.617 defect + basis invariance pass. A sharp NEGATIVE coordinate: the polar $C_E$ is the right LOCAL formalism, but the SIGN over Spec(Z) is the same global gap as the product surface. |
| Recent-construction audit for the signed $t$-pairing (session 016, 2026-06-05) | 🟡 **No constructed object supplies the global signed trace pairing; the gap is unchanged.** **2P+ ([2P_recent_global_signed_trace_pairing_probe.md](experiments/arithmetic_geometric/2P_recent_global_signed_trace_pairing_probe.md), #71):** a literature-grounded Direction 8 audit (no new finite model) after 2LO named the Frobenius trace $t$ as the single missing datum. The three strongest recent constructions stop at three different two-thirds of the SAME construction: **Tang** prismatic Poincare duality ([arXiv:2210.14279](https://arxiv.org/abs/2210.14279), web-confirmed F-gauge perfect duality $R\Gamma(X)^\vee\simeq R\Gamma(X)\{d\}[2d]$ + trace maps) gives PERFECTNESS not the Hodge-Riemann/Rosati sign; **Gurney** *Prismatization over $\mathbf{Z}$* ([arXiv:2301.12392](https://arxiv.org/abs/2301.12392)) gives the global SUBSTRATE not the cup/sign/$t$; **Connes-Consani 2026** Jacobian ([arXiv:2602.15941](https://arxiv.org/abs/2602.15941)) gives the TRACE/realization not a signed product pairing. The post-2LO gap is unchanged: construct $\Gamma_S$, cup into the Euler-pole $H^2$, prove the sign without RH input. Perfectness is free (even for D-H); the polarization carrying $t$ is the whole gap. |
| Lane 2: Petrov non-semisimplicity branch pruning (session 017, 2026-06-05) | 🟡 **Petrov is not fatal to WCart, but it kills the naive Sen-eigenspace branch.** [lane2_petrov_nonsemisimplicity.md](docs/03_research/lane2_petrov_nonsemisimplicity.md): the theory companion to 2LN/2LO/2P+. A positive Hodge-Riemann form built from the semisimple Sen part $\Theta_{\mathrm{ss}}$ alone forces the nilpotent $N=0$, and Petrov ([arXiv:2302.11389](https://arxiv.org/abs/2302.11389), Annals, web-verified) gives geometric prismatic examples with $N\neq0$. So semisimplicity is not a free property of WCart geometry: the M4 polarization must be the $N$-aware monodromy-primitive form $Q_r(x,y)=Q(x,N^r y)$ built BEFORE diagonalization, carrying the full $F+\Theta$ Frobenius geometry, not the Hodge-Tate/Sen fiber alone. D-H-aware by TYPE (Petrov's obstruction class is undefined for the Euler-product-free D-H). A branch-pruning coordinate, not a signature theorem; wired into 08B task (b). |
| First-principles conjecture program (session 019, 2026-06-09) | ✅ **Nine new RH attack shapes proposed, adversarially filtered to five.** A nine-lens BUILDER run + one batched ADVERSARY pass ([`first_principles_conjecture_program.md`](docs/03_research/first_principles_conjecture_program.md), LEARNINGS #76) asked for genuinely new conjectural routes, then killed the ones failing the D-H discipline / four-level framing / K1 / dead-branch list. **Five PURSUE:** Lonely Crystal (the positive-crystal cone for $\zeta$'s source is a SINGLETON $=$ von Mangoldt; provable D-H firewall; priority 8), Eratosthenes Descent (octave-nested Weil positivity with the EXACT Haynsworth identity, a self-reducing $\Pi^0_1$ certificate; the sieve is the formation rule D-H lacks), Euler Crystal (the primes as a Berry-Keating scatterer crystal with the boundary condition DERIVED from the adelic product formula, distinct from the killed 1C postulated-Hamiltonian), Euler Facet Rigidity (the saturated face of the Weil cone is $\{\Lambda\}$; the dual of Lonely Crystal in coefficient space), Euler Decimation Flow ($\zeta$ as a CENTER fixed point; the angular-mean-one identity explains WHY RH is barely true; D-H Lyapunov exponent exactly $2\beta_1-1=0.617$). **Two kills (negative coordinates):** Semicircular Polarization (free probability) PROVEN dead by a constant (free-edge / archimedean-curvature ratio $\sim$18; the central inequality is false); Adelic Ferromagnet (Lee-Yang, p-adic Teichmuller spins) is a #42 restatement (a factored product measure cannot reach the continuation $\mathrm{Re}\,s<1$). **The pattern:** every survivor reads marginal positivity (#52/#56) as EXTREMALITY, not obstacle (an extreme point / center fixed point / rigid crystal cannot be marginally true by accident). Two high-value merges (Lonely-Crystal + Euler-Facet-Rigidity = one classification; Eratosthenes + Decimation = exact skeleton for the flow); the single named target is the composite-pinching lemma both classification routes share. **First falsification test RAN (e3aa, LEARNINGS #77): the EDF under-decimation discriminator fired POSITIVE on both sub-tests.** Zeta's matched Weil form stays marginally positive ($+0.029/+0.025/+0.020$ at $t=8/9/10$) while under-decimation (delete primes $p>e^{t/2}$) collapses it to $-46/-78/-129$, deepening monotonically, via the predicted low-frequency mechanism (large primes resonate at low frequency near the archimedean trough $\Omega(0)\approx-5.4$); the single-prime $p=2$ center-fixed-point identity is exact to $\sim10^{-16}$ and the resonance dip lands in the predicted band. Honest scope: a falsification instrument that survived, not a proof; D-H also goes negative under decimation, so the clean Euler-structured content is the center identity and resonance dip (no D-H analogue), not the bare sign flip. EDF stays a live PURSUE direction; the LCC and ECC tests remain queued. Process spin-off: [`TOKEN_EFFICIENCY.md`](TOKEN_EFFICIENCY.md). |
| Workaround slate: EDC + the moment-transfer wind tunnel (session 019 follow-on, 2026-06-09/10) | ✅ **Composite pinching localized to a named moment-transfer theorem.** Three locally-runnable experiments from [`local_compute_workarounds.md`](docs/03_research/local_compute_workarounds.md) (8 ideas across 4 workaround axes). **e3dd (EDC octave ladder, #78):** first test of Eratosthenes Descent — every octave Schur complement PSD for $\zeta+\chi_3$ on the non-circular Weil form, Haynsworth identity exact (cond $\le$ 90), NO octave-graded wall (margins flat in $[0.014, 0.039]$ across support $b\in[1.5, 2.8\mathrm{e}9]$, $\sim$1e117 above the $e^{-4\pi a}$ wall); the global marginality is the telescoped accumulation of healthy per-octave margins, EDC's predicted picture. EDC stays a live PURSUE direction. **e2ll (function-field wind tunnel, #79):** the composite-pinching lemma (LCC/EFR target) tested over $F_q$ where RH is a theorem — pinching HOLDS on all 7 curves; RH $=$ PSD Frobenius-moment Toeplitz, closed-point counts invert exactly to nonneg integers (Möbius), Prony recovers exactly the rank($R$) distinct Frobenius angles, rank stabilizes (flat extension) $\Rightarrow$ unique measure $=$ pinched; no ghost comb over $F_q$ (LCC/EFR's cheapest kill ruled out). **MECHANISM IDENTIFIED:** FF composite-pinching IS the flat-extension uniqueness of the truncated trigonometric moment problem (Curto-Fialkow). **e3gg (zeta-side mirror, #80):** the same machinery on $\zeta$'s zeros EXHIBITS the transfer fail mode — $\zeta$'s zero-measure Toeplitz never goes flat (rank $=m{+}1$, ghost room persists $\sim$0.74, $\sim$15 orders of magnitude above the $F_q$ floor; $|c_k|\sim O(1/\sqrt N)$, $C\to I$) because the pole-sourced CONTINUOUS archimedean spectrum keeps it full rank. D-H identical (mechanism illustrator, NOT a detector). **Net:** composite pinching is now localized to a precise open target — a moment-uniqueness theorem for a measure with a continuous (pole-sourced archimedean) component PLUS the Euler structure, not flat extension. LCC/EFR's named open core, made concrete and quantitative. |
| Lateral import: Bost-Connes ghost-room collapse (2026-06-10) | ✅ **The uniqueness technology the #79/#80 transfer gap demands already exists, unconditionally.** A ten-field lateral scan ([`lateral_imports_2026_06.md`](docs/03_research/lateral_imports_2026_06.md)) asked who has a proven theorem of the needed shape (a positive functional with a CONTINUOUS spectral component pinned to a singleton by Euler structure). Answer: **Bost-Connes 1995**: the KMS$_\beta$ simplex of the BC system at $0<\beta\le1$ is a SINGLE POINT of type III$_1$, proved from exactly the program's organ pair: the POLE kills atomic ghosts ($\sum n^{-\beta}$ diverges) and PRIME-ROTATION DENSITY forces unit-shell uniformity (Dirichlet). "The simplex is a point" is the LCC/EFR composite-pinching statement shape; the technology is **group-measure rigidity** (scaling quasi-invariance + ergodicity), NOT flat extension. **[`e2nn_bc_ghost_collapse.py`](experiments/arithmetic_geometric/e2nn_bc_ghost_collapse.py) (LEARNINGS #81)** quantifies it at finite level (exact integers + finite Euler products, no zeros anywhere, 6/6 checks): the organ-A ghost-component index collapses by $m^*\le5$ (a Linnik-flavored least-prime-in-subgroup statistic); the organ-B ghost diameter has the phase transition AT the pole (stretched-exponential below it; log-slow $u\sim C/\log p_m$ at $\beta=1$ exactly; plateau above, ratio 0.9984, atom budget stalled at $1/\zeta(1.5)$); BOTH amputations (pole removed / rotations removed) leave ghosts alive; and Mertens $e^{-\gamma}$ (the #63 constant) is the criticality rate ($\gamma_{\mathrm{est}}=0.5775$ vs $0.5772$): the marginality fingerprint as the log-slow rate of uniqueness at the pole. K1-clean; not a detector (D-H: no Euler semigroup, the mechanism is constructionally undefined, the #55 firewall class); distinct from the killed Mechanism-2 BC costume ($\beta>1$ modular operator as polarization). Queued BUILDER target: the transport, i.e. formulate the LCC log-crystal cone as the KMS-type simplex of a scaling $\mathbb{N}^\times$-action ($T_p: c(n)\mapsto c(pn)$) so the BC template runs the ghost-crystal kill on the source-side cone (respecting the #42 continuation wall). |
| Primitive system + lemma graph + conditional no-go program (session 018, 2026-06-05) | ✅ **Two no-go classes machine-verified in Lean; the disprove direction is structured and bounded.** A purpose-built axiomatic system ([`rh_primitive_system.md`](docs/03_research/rh_primitive_system.md): the Rosati trace form $B$ as the core primitive, the trace demoted, RH reduced to one open axiom $\mathrm{AX\text{-}POL} = $ M4) plus a DuckDB lemma-graph ([`experiments/lemma_db/`](experiments/lemma_db/)) that enforces the D-H discipline as a CI build gate (LEARNINGS #73). The 17 Spec($\mathbb{Z}$) cohomology candidates were forced into the graph: **9 COLLAPSE** onto the single gap node, which decomposes into four proven-droppable properties (global $\wedge$ carries-trace $\wedge$ rh-equivalent $\wedge$ noncircular); the gap is the conjunction, no object has all four, an object with all four proves RH (third independent confirmation of #30). The **conditional no-go program** ([`08E_no_go_program.md`](docs/03_research/research_directions/08E_no_go_program.md), #74): kill criterion K5 (only conditional no-gos are sound; an unconditional one would disprove RH), and the K2 pincer is a COMPASS not a wall (the target straddles both halves by construction; the $F_q$ straddler exists, its $\mathbb{Z}$-existence IS RH). **Two classes MACHINE-VERIFIED sorry-free, axiom-clean (#75): Class A** ([`SenDefiniteObstruction.lean`](lean/ZetaRH/SenDefiniteObstruction.lean), 5 theorems; the Sen-module obstruction UNCONDITIONAL in any positive-definite cup form via `CFC.sqrt`) **and Class C** ([`TraceBlindObstruction.lean`](lean/ZetaRH/TraceBlindObstruction.lean); no $t$-blind signature decides Hasse-Weil). Both prune a polarization source and relocate the demand to the Frobenius/F half; neither disproves RH. |
| Reduction Engine: our own RH-solver algorithm (2026-06-13) | ✅ **A problem-compression loop built on the lemma graph; an adversary killed the clever core before any code, and the discrimination collapsed into the falsifier.** [`reduction_engine.md`](docs/03_research/reduction_engine.md): a reduction-graph search (nodes are statements, load-bearing edges are reductions, sink TGT-rh, floor Lean) driven by a falsification oracle; the proof exists iff a load-bearing path joins TGT-rh to the floor, and the frontier is the current minimal open kernel (M4). ~2/3 of it already existed as the DuckDB lemma graph. **Increment 2 (a four-bit collision engine to mechanize the all-roads convergence) was KILLED before any code** by an independent ADVERSARY pass: the convergence is 17 annotation edges with 0 load-bearing reach into TGT-rh, the signatures are not machine-readable, and the four bits are blind to the K1 axis that does the real discriminating work (Deninger-foliated and Connes-1999 both hash to the empty signature yet split honest-vs-circular). The anti-theater guard firing on the engine's own design is the system working. **Increment 1 BUILT ([`oracle.py`](experiments/lemma_db/oracle.py) + `test_oracle.py` 6/6 + smoke Test 9, 9/9):** the executable falsifier turns the D-H discipline from a hand-set flag into a computed verdict (four cheap-first disqualifiers; kills or parks, never validates; PASS means not-yet-killed, only Lean validates). The payoff is the marginal-positivity wall as a mechanical two-move trap: the canonical Li detector is BLIND to D-H (min $\lambda_n = +0.017$ for $\zeta$, $+0.080$ for D-H, both $\ge 0$) so the flip test kills it, and the only separating detector reads the zeros so K1 kills it; every detector dies by blindness or circularity. **Increment 3 BUILT** (value-function views in [`schema.sql`](experiments/lemma_db/schema.sql), printed by build_db): rank the frontier by DEPTH and PROVEN support, never asserted support, plus the asserted-vs-proven gap that computes the adversary's hand-found number mechanically (`TGT-m4-hodge-standard`: 17 annotation, 1 load-bearing, gap 16 = much believed, little reduced). **Increments 4 and 5 BUILT** ([`engine.py`](experiments/lemma_db/engine.py) loop driver + construction-registry cross-check, [`lean_hook.py`](experiments/lemma_db/lean_hook.py) soundness floor; `test_engine.py` 5/5): the loop runs a cycle and prints the anti-theater kill tally; the dh-flag cross-check confirms the seed's hand-set flags by computation; the Lean hook audits the 18 `lean_ref` nodes (0 dangling/overclaim, 10 consistent `proven_lean`, 1 review-flag) and promotes nothing. All five increments are built; what remains is not more engine but proving the 16 annotation edges into M4 (the arithmetic Hodge standard conjecture). |
| Math-iteration engines capstone (2026-06-13) | ✅ **Synthesis + handoff: [`math_iteration_engines.md`](docs/03_research/math_iteration_engines.md).** The "build our own algorithm for RH" arc closed: a complete generate-evaluate loop that mechanized the disciplines, regenerated the all-roads convergence, and handed back the sharpened M4 spec (the 7-property statement of the object that proves RH, six cheaply-checkable + the one open blind-spot polarization) and the transfer shortlist (Hodge-Riemann / Alexandrov-Fenchel / Bost-Connes pinning). The honest net: narrowed and mapped the search to one precise object, did not touch the blind spot (nothing cheap can), by design. What remains is the construction-grade work on M4 = the arithmetic Hodge standard conjecture. |
| Generative Engine: the generate half of the loop (2026-06-13) | ✅ **COMPLETE: all five increments built; both halves of the math-iteration loop now exist.** (The Reduction Engine is the evaluate half.) **6b transfer-search** ([`transfer_search.py`](experiments/lemma_db/transfer_search.py)): feature-match retrieval over proven positivity theorems against the M4 residual, signature as the hard gate. Validates (Weil/Hodge-index top, Bost-Connes rediscovered, Lee-Yang demoted for its all-positive signature, Hodge-Riemann + Alexandrov-Fenchel the fresh high-matches) but surfaces no escape to a foreign field: every high match is a Hodge-index sibling, sharpening that M4 IS the arithmetic Hodge index; `test_transfer_search.py` 5/5. **Honest net:** the apparatus mechanizes the disciplines, regenerates the all-roads convergence, and sharpens M4, without touching the blind spot, exactly as designed. [`generative_engine.md`](docs/03_research/generative_engine.md): three organs (generate / evaluate / iterate). **BUILT:** 6d the function-field shadow ([`fq_shadow.py`](experiments/lemma_db/fq_shadow.py): genuine $\mathbb{F}_q$ curves pass, the off-line forgery is killed = the positive mirror of D-H), 6a the move-library + 6c the quality-diversity archive ([`generator.py`](experiments/lemma_db/generator.py): proposes variants from a Weil/$\mathbb{F}_q$ seed, the evaluate stack prunes each reproducing every discipline, six distinct cells filled; `test_generator.py` 5/5, oracle reused untouched), **6e new-branch spec generation** ([`branch_specs.py`](experiments/lemma_db/branch_specs.py): reads the forcing question off the top gap, writes the M4 required-property spec with one blind-spot property, and GENERATES the all-roads convergence mechanically: 9 candidates converge on the open polarization residual, 8 bracket out on resolved properties; `test_branch_specs.py` 4/4). The value-signal blind spot (marginal positivity = no cheap evaluator at the goal) forces the loop to iterate on **reformulations and bridges, not proofs**. The one positive gradient is the **function-field shadow** (does a candidate specialized to $\mathbb{F}_q$ reproduce Weil's proof?), the mirror of the D-H negative filter. Generators emit objects in the oracle's `Candidate` schema, so they plug straight into the built evaluate stack. **6a move-library** (structure-preserving rewrites = the project's own move history); **6b transfer-search** (the prize: the untried approach is a BRIDGE not an atom; search (proven theorem in $A$) $\times$ (open target in $B$) for structural isomorphisms = the Bost-Connes import automated; the embedding only retrieves, an LLM judge + the oracle verify); **6e new-branch generation** (a meta-move library, prompted by forcing questions read off the deepest asserted-vs-proven gaps; the real product is a falsifiable **spec** of the missing object, filtered by consistency / realization / the D-H firewall / $\mathbb{F}_q$ reproduction). Iterate organ = **quality-diversity** over failure cells, not optimization (the "negative results are coordinates" epistemology as a search). Three principles: verifier-is-enabler, taste-inversion, the blind-spot invariant (never score progress-to-proof). Honest ceiling: maps to the doorway, does not walk through; strong at spec-and-fill, weak at reframe-so-the-hole-disappears (where the truly new and truly intractable both live); RH might be FLT-shaped. |
| Breadth-over-proof-engines sweep + the AHK lattice spec (2026-06-14) | ✅ **Five from-below signature engines killed; the AHK BUILDER target sharpened (LEARNINGS #97).** An 11-agent workflow deployed the AI breadth-edge on the unspent axis: matching PROOF ENGINES (HOW a theorem manufactures a $(1,n-1)$ signature), not theorem statements (the 6b axis, already converged). **All five killed** through five kill-criteria: AHK flip-induction, Lorentzian polynomials, semismall/relative-HR, tropical / non-archimedean Monge-Ampere (Boucksom-Jonsson), and CKS limit-MHS on the prismatic Sen operator; **0 surviving bridges, a FOURTH all-roads convergence** (after #90-94 acoustic, #95 Lee-Yang, #96 the 7-candidate screen). **AHK is the one retained POSITIVE coordinate** (passes k1-noncircular + fq-shadow: the unique existence-proof that a Hodge-index signature is manufacturable K1-clean with NO variety), killed as a finished object only by off-line-flip + indefinite-gate ($t$-blind, #40; AHK Lemma 9.6) + euler-archimedean. **CKS killed as TRANSPORT not source** (the nilpotent $N$ gives the weight filtration free but imports the polarization $Q$ from a nearby fiber; over Spec($\mathbb{Z}$) there is no disk and no $Q$, so importing $Q$ IS M4). **Deliverable:** the construction-grade spec [`09A_ahk_arithmetic_lattice.md`](docs/03_research/research_directions/09A_ahk_arithmetic_lattice.md): a finite graded prime-lattice whose stars are AHK products, with a $t$-carrying submodular Lefschetz element and an indefinite $(1,n-1)$ primitive form (P1-P5 buildable, P6 positivity = M4 left open); strictly sharper than "build the surface" (asks for the lattice + Lefschetz element, not the variety). **Apparatus:** the Boucksom-Jonsson node added to [`transfer_search.py`](experiments/lemma_db/transfer_search.py) CORPUS with `role='killed'` + a guard test (6/6). The doctrine: breadth-over-engines is now SPENT and CONVERGED like 6b; its honest uses are convergence-detector / corpus-completer / spec-sharpener, with regime-two FRAME-AUDIT the one still-unspent axis. |
| Optimizing RH for an AI to attack: the value-signal program (2026-06-14) | ✅ **The binding constraint is the no-gradient-at-M4 blind spot; three levers executed (LEARNINGS #98, [`optimizing_rh_for_ai.md`](docs/03_research/optimizing_rh_for_ai.md)).** Marginal positivity means there is no cheap value signal at M4, so the optimization is to MANUFACTURE gradients and SHRINK the gradient-free kernel. **(A, built) the proven-case shadow battery** ([`shadow_battery.py`](experiments/lemma_db/shadow_battery.py) + test 6/6): generalizes the function-field shadow into five proven-case checkpoints (CP-fq Weil, CP-hodge surface Hodge index, CP-ahk matroid HR, CP-fh Faltings-Hriljac, CP-af Alexandrov-Fenchel), scored by coverage with the Davenport-Heilbronn firewall as a hard side condition. "No gradient at M4" becomes "k of 5 proven cases + the firewall," and the gradient already separates the three known failure modes (genuine-m4 FULL 1.00; convex-only PARTIAL 0.60; arithmetic-blind BROKEN at CP-fq, the #40 t-blindness; too-strong/de Branges BROKEN at CP-hodge, wrong signature; off-line-forgery BROKEN). The positive mirror of [`oracle.py`](experiments/lemma_db/oracle.py); FULL is necessary, never sufficient (only Lean validates). **(B, scoped) Lean as the non-circular value function:** the keystone `negDef_iff_hasseWeil` (#2G-1, the function-field Hodge-index signature) is sorry-free, as is the formal core; the gap to an end-to-end function-field RH is the GEOMETRY (curve/divisor/intersection theory on $C\times C$ + the Frobenius graph), absent from the repo AND Mathlib, ~2-3 weeks of mostly-upstreamable work around (not in) the keystone. **(C, run) the regime-two frame-audit** (11 agents) = the FIFTH all-roads convergence: zero new genuine collapses; the blind spot P5 is an ISOLATED vertex (no cheap property co-extensive with it); the new datum is that the M4 property graph is exactly two cheap edges, both on the Euler-product vertex P4 (P1-P4 acoustic + P3-P4 Rosati), with P5 a lone island. The live next probe: upgrade the P3-P4 edge from CORRELATED to GENUINE via a finite von Neumann algebra with a faithful normal trace state. |
| The modular polarization carrier + lever B's first Lean result (2026-06-14) | ✅ **MC.1-MC.3 done, MC.4 = M4 (K1-circular), the function-field RH chain machine-checked except the geometry (LEARNINGS #99-#103, [`modular_polarization_carrier.md`](docs/03_research/modular_polarization_carrier.md)).** The #98 frame-audit's one live edge (P3-P4) was run. **#99 (e2oo):** a faithful trace gives only a positive-DEFINITE, D-H-blind form, and the arithmetic algebra is type III$_1$ (no faithful trace), so the "trace upgrade" of P3-P4 is closed; the polarization is steered onto the type III MODULAR structure. The cheap milestones: **MC.1 (#100, e2pp)** the modular structure supplies the weight ladder $\log\Delta$ and the FE duality $J\Delta J=\Delta^{-1}$ a trace lacks; **MC.2 (#101, e2qq)** it FACTORS the gap (the Weil operator $C_E$ and the monodromy $N$ are extra data beyond $\log\Delta$; the real discriminator is carries-$t$, not definite-vs-indefinite); **MC.3 (#102, e2rr)** the finite-prime modular (Bost-Connes) structure is euler-gated, so the carrier passes K2 by construction ($\zeta$'s von Mangoldt comb $\ge0$; D-H's goes negative at $n=3$). **MC.4 (#103, e2ss) = M4:** an adversarially-honest BUILDER attempt on the smallest case is K1-CIRCULAR, with a sharp mechanism: the BC/KMS carrier lives at $\mathrm{Re}(s)>1$ (Euler-product convergence) while the local off-line obstruction lives in $1/2<\mathrm{Re}(s)<1$ (the range $2\sqrt p<|t_p|<p+1$), so the carrier is structurally blind to the trace constraint; the residual is the per-block COUPLING (the global Frobenius/Lefschetz signed-trace pairing = the product-surface / Poincare-duality assembly), M4 undiminished, and per #97 it must be sourced not propagated. **Lever B, first step BUILT ([`lean/ZetaRH/FunctionFieldRH.lean`](lean/ZetaRH/FunctionFieldRH.lean), build green, 3147 jobs, Lean v4.30.0):** the genus-1 function-field RH chain is made explicit and the eigenvalue-extraction end is proved sorry-free (`eigenvalue_modulus`: a non-real root of $X^2-tX+q$ has $|\alpha|^2=q$; `root_nonreal`: $t^2<4q\Rightarrow$ non-real roots; `functionfield_RH_elliptic_of_hodge`: the Hodge-index input $\Rightarrow|\alpha|=\sqrt q$, wiring the keystone `negDef_iff_hasseWeil` #2G-1). The chain's only `sorry` is the geometric Castelnuovo-Severi step (#FF-geom), isolated as the lever-B gap. Net: the cheap modular program is exhausted; MC.4 = M4 (the coupling) and lever B's geometric sorry (curve-intersection theory in Lean) are the construction-grade remainder. |
| The M4 coupling's four faces + lever B made sorry-free + a formalization plan (2026-06-14) | ✅ **The coupling has four unified faces, the AHK face reduces to one property, and the Lean function-field chain is now sorry-free (LEARNINGS #104-#106).** Working the coupling = M4 (the user-directed front): **#104 (e2tt)** closed the open adversary test AT-1 -- the coupling cannot be sourced from the t-blind modular carrier; it is one object with FOUR open faces, all = M4 (the modular $C_E$-cup, the function-field cup [Castelnuovo-Severi / 2G, a theorem], the AHK arithmetic lattice [09A], and the Faltings-Hriljac product + $\Gamma_S$); the modular, AHK, and product-surface threads are four windows on the same missing coupling. **#105 (e2uu)** instantiated the AHK lattice (9A.1-9A.3): the bare combinatorial lattice (Boolean lattice on $\{2,3\}$) already gives P1 (product stars), P2 (Poincare duality), P4, and P5 -- but P5 is unconditional and t-blind (#48), and the degree map has no $t$-slot, so the **single gap is P3** (the t-carrying degree map); the BUILDER target narrows from six properties to one, and 09A is sharpened (P5 demoted, P3 elevated). **#106** made [`lean/ZetaRH/FunctionFieldRH.lean`](lean/ZetaRH/FunctionFieldRH.lean) **fully sorry-free**: discharging #FF-geom revealed `hodge_index_curve_elliptic` was a `sorry` admitting a *false* proposition (`NegDef 1 q t` for all $q,t$); the repair carries the Castelnuovo-Severi inequality $t^2<4q$ as an explicit hypothesis field of an `EllipticFrobeniusData` structure, making the function-field RH chain a sorry-free **conditional** theorem (given Castelnuovo-Severi for a genus-1 curve, $|\alpha|=\sqrt q$). The roadmap to make it **unconditional** -- discharge #FF-geom via the elementary-Hasse / degree-quadratic-form route (not surface intersection theory), with a Mathlib audit and milestones M-0..M-4 -- is [`docs/03_research/lever_b_function_field_plan.md`](docs/03_research/lever_b_function_field_plan.md). |
| Eight-angle sweep: QM, NP-1, string theory, cryptography, + CCM deep dive (2026-06-24/25) | ✅ **Eight angles, all reduce to M4 (LEARNINGS #111-#117, [`eight_angle_sweep_2026-06.md`](docs/03_research/eight_angle_sweep_2026-06.md)).** Realization everywhere, the signature nowhere but M4 -- the marginal-positivity thesis confirmed from eight independent bases. **QM/spectral brought current (#111):** Connes-Consani prolate/semilocal + Bender-Brody-Müller realize $\zeta$ but not the signature; the published archimedean Weil positivity sits on the K2-blind $\Gamma$-factor half; `e1d` additive-arithmetic-injection NULL. **NP-1 (#112):** the finite-prime modular carrier does not continue into the strip as a $t_p$-detector — reduces to M4/#104. **String theory (#113) + cryptography (#116):** corpus-completing closures (Euler-side realization / RH-as-tool with inverted dependency / the function-field-RH shadow = lever B / Level 3 / the Epstein control). **The one live thread, the CCM semilocal prolate operator, mapped to its core (#114/#115/#117, [`ccm_semilocal_prolate.md`](docs/03_research/ccm_semilocal_prolate.md)):** the open step factors into a separately-attackable operator construction; R3.5 status sharpened to CONDITIONALLY ESCAPING (the archimedean fragment proves the sign can be geometric, the deciding bit a zero-free construction of $W_{\lambda,S}$); its three cheap operator surrogates (`e1f` non-idempotent, `e1g` reweighting-blind, `e1h` reads-moments) are all signature-blind, leaving CCM's deferred metaplectic operator the un-eliminated route by elimination. **Follow-up (#118, `e1i`):** the metaplectic route's **finite-local** model (the finite Weil rep over $\mathbb{F}_p$) attacked directly — exact gate ($\mathrm{Tr}(F_p)=\varepsilon_p$, the Weil index, + the double-cover cocycle), but the finite-local quadratic sign-structure is signature-blind under the finite positivity compression (concentration-matched Arbiter, 5/5 body, false-negative-probed); this does NOT eliminate CCM's semilocal $W_{\lambda,S}$ (global $\widetilde{SL(2,\mathbb{A}_S)}$), it sharpens where the arithmetic survives positivity to the $S\to\infty$ assembly = M4. **Three reusable disqualifiers produced:** the L-value/L-derivative BSD-regime rule (retires Kudla + the isogeny-graph near-miss), the cheap-spectral-surrogate disqualifiers, the GRH-as-tool inverted dependency. **One validated identity:** the archimedean Hardy-Titchmarsh scaling operator IS the Meixner-Pollaczek Jacobi matrix (exact). |
| The Breadth Program + two aimed sweeps + an M4 construction probe (2026-06-25) | ✅ **Breadth as the AI edge, built and run to convergence (LEARNINGS #119-#122, [`breadth_program.md`](docs/03_research/breadth_program.md)).** Reframed as transfer-and-compression disciplined by a disqualifier battery, with an executable corpus ([`breadth_corpus.{md,py}`](experiments/lemma_db/breadth_corpus.py), 16/16 tests). **Four-area exploration sweep (#119):** Lorentzian polynomials / SUSY / index theory / condensed geometry, all CLOSE (realization, never the signature); yielded the **discriminant screen** (RH is on the complex-root half; the convex/log-concave engine is real-rooted) and the perfectness-vs-sign structural cut. **acq1 (#120):** the disqualifier-complement draw (Riemann-Hilbert, Lee-Yang failure, transfer-operator/Selberg, Berry-Tabor) all CLOSE, producing four screens (curative-flip, wrong-axis, input/output, spectral-gap=zero-free-region) that compose into a near necessary-and-sufficient **M4 polarity FINGERPRINT** (contingent + complex-root + line-axis + output-indefinite-with-sign-flip + prohibitive-on-a-fixed-locus = a polarization). **acq2 (#121):** tested the fingerprint against its best candidates (Bridgeland, the scattering sign, Frobenius manifolds), all CLOSE; sharpened F4 (the SIGN must flip, not membership), added two screens (selection-not-sign; special-value/period regime), and **CONVERGED** — the fixed-indefinite-form space outside arithmetic geometry is mapped and insufficient (Bridgeland is off by exactly one Hodge weight). **M4 construction probe (#122, `e2ww`):** the breadth fingerprint re-derives e2uu's gap (P3); the honest yield is a **genus-1 faithfulness caveat** (genus 1 is Hasse-1933-elementary, so the 2×2 shadow hides the real M4 difficulty — higher-rank Rosati + the archimedean place + the global assembly). The engine's `aim()` now reports CONVERGED and pivots to the M4 construction + the `R3_5.lean` VERIFIER target. |
| The higher-rank AHK/Rosati object built (2026-06-25) | ✅ **The #122 caveat's named-but-unbuilt object is now explicit (LEARNINGS #123, [`e2xx_higher_rank_rosati.py`](experiments/arithmetic_geometric/e2xx_higher_rank_rosati.py)).** Acting on the #122 pivot, a BUILDER built and an ADVERSARY corrected (PASS-WITH-CORRECTIONS, two headlines withdrawn) the higher-rank generalization of the genus-1 form. **The object** (verified via Weil's intersection formula): the primitive intersection form on the Frobenius *powers* $\Gamma_{q^k}=\mathrm{graph}(\mathrm{Frob}^k)$ IS — up to a positive-diagonal congruence — the **trigonometric moment matrix** $G_m=[c_{|j-k|}]$, $c_n=(q^n{+}1{-}\#C(\mathbb{F}_{q^n}))/q^{n/2}$, with **RH-for-the-curve $\Leftrightarrow$ all $G_m$ PSD** (Carathéodory-Toeplitz); $m{=}1$ reproduces e2uu/e2g exactly (the genus-1 form is its degenerate rank-2 corner). **The genuine structure** (integer-exact): the genus-2 Davenport-Heilbronn analogue $T^4{-}4T^3{+}15T^2{-}20T{+}25$ over $q{=}5$ (RH false) has a rank-3 joint form that is indefinite while *every* $2\times2$ principal minor is PD — the smallest genuine higher-rank Hodge-Riemann structure, whose **positivity over $\mathbb{Z}$ is M4**. **Sharpens** P3 (carry $t \to$ carry the curve's zeta $P(T)=t_1..t_g$) and P6 (binary determinant $\to$ $2g$-dim moment-positivity). Withdrawn by the adversary: "caught only at higher rank" (the 2×2 trace-bound *family* = RH too) and "rank-resolved stealth window" (the flip rank is flat at $2g{+}1$; the *margin* vanishes). K1/K2 clean; P6/M4 untouched. 09A Section 6C. |
| A genuine M4 construction attempt — RH itself, 3 fronts, all wall (2026-06-26) | ✅ **A full-effort attempt at RH on three independent fronts; all wall on the SAME gap; no proof (LEARNINGS #128).** At the user's direction, three BUILDER fronts attacked M4 directly: Weil-form analytic domination, moment-matrix Gram factorization, the Hilbert-Pólya / circle-Frobenius operator. **All three wall, no candidate proof, all on the same universal gap:** the Euler product converges only on $\mathrm{Re}(s)>1$ (a *trace*), the zeros live on $\mathrm{Re}(s)=\tfrac12$ (where the *signature* lives), and crossing the line is the missing Weil cohomology = M4 (#42/#25). The D-H firewall held on all three. The 3-front convergence is the strongest evidence yet that the gap is one object. **Three verified residues:** (1) the per-prime Poisson/Herglotz completion (a real Euler-specific positivity, verified 2.2e-15) walls by wrong sign + loss of the cross-prime cancellation that *is* RH — plus the correction that $A_{\mathrm{arch}}+B_{\mathrm{pole}}$ is **not** PSD, so "archimedean dominates the primes" is false, ζ's positivity is a three-way near-cancellation (the analytic root of #18/#19); (2) **the Li-Hankel is non-PSD even for ζ** ([`e3s`](experiments/positivity/e3s_li_hankel_dissolves.py); the Li sequence is log-concave, not the log-convex a moment sequence needs, so the e2xx moment-*matrix* dissolves over $\mathbb{Z}$ — only the termwise $\lambda_n\ge0$ survives, the #122 caveat one level up); (3) RH $\iff$ a positive metric $\eta>0$ exists (PT-symmetry unbroken; the Connes operator is self-adjoint with *continuous* spectrum, so the zeros are resonances, not eigenvalues). No proof (expected for RH); the value is the 3-front convergence + the residues. M4 untouched. |
| Both faces of P6 + the H¹ chase + the Arakelov re-catalog (2026-06-26) | ✅ **The AHK and Arakelov faces (#124/#125), the H¹/odd-Frobenius chase (#126), and the Faltings-Hriljac certificate (#127), all adversary-corrected.** **AHK face (#124, [`e2yy`](experiments/arithmetic_geometric/e2yy_higher_rank_p3_frobenius.py)):** a matroid Chow ring is purely **even/Tate, no $H^1$** where $\sqrt q$ eigenvalues live — its graded-ring endomorphisms are similitudes of the Lorentzian Chow product, but the interior complex Frobenius preserves the *definite* form, not the Chow product. **Arakelov face (#125, [`e2zz`](experiments/arithmetic_geometric/e2zz_archimedean_moment_bridge.py)):** e2xx is the finite model of the archimedean Weil/Li positivity; the marginal-positivity wall (#18/#19) **is** the infinite-support analogue of the $\mathbb{F}_q$ boundary kernel (adversary inverted a wrong-axis first pass). **H¹ chase (#126, [`e2ac`](experiments/arithmetic_geometric/e2ac_weight_vs_frobenius.py)):** the prismatic/Sen odd operators are weight **lines** ($\Theta=\{-n\}$, $F=\{\log p\}$), not the circle Frobenius — chasing the H¹ lands on the prismatic candidate's *missing signature*, the live M4 target. **Arakelov certificate (#127, [`e2ad`](experiments/arithmetic_geometric/e2ad_fh_gamma_certificate.py)):** the weakest probe — the adversary withdrew "FH = the $(0,0)$ entry" and "$\Gamma_S$ assembles the height"; a re-cataloging of #25/#44/#125/#30 with one weak-form #125 refinement. All reframings, not P6 advances. P6/M4 untouched. |
| Toy sandbox + four Lean targets + doc recovery (2026-06-27) | ✅ **A checkable M4 training ground, four deferred Lean targets landed, and the #128/#119 docs consolidated.** **Toy sandbox** ([`experiments/toy/`](experiments/toy/), `python -m experiments.toy.play`, tests 6/6): the function-field world (RH = a theorem) as a sandbox that GRADES a proposed M4 construction (reproduce-Weil / reject-fakes / K1-clean / D-H-immune); the reference move (the e2xx moment matrix) scores all green, two soft candidates correctly FAIL reject-fakes, D-H is unbuildable by type; plus the Selberg / Hilbert-Pólya spectral toy (self-adjoint $\Rightarrow$ zeros on the line). Honest caveat: the moment-matrix move that is green here DISSOLVES over $\mathbb{Z}$ (#128 Front 2), so it trains the move, not the obstruction. **Four Lean targets, all sorry-free + axiom-clean (full build green, 3744 jobs):** (1) **VT-NP1** ([`VT_NP1.lean`](lean/ZetaRH/VT_NP1.lean)): finite Euler-product zero-freeness on $\mathrm{Re}(s)>0$ + the M4-reduction (the off-line obstruction lives only in the infinite limit; NP-1=NO, the analytic companion of #2G-1); (2) **R3.5 `GeometricPositivity` tightened** (#119): split into the free, RH-BLIND signature INTEGER vs the RH-equivalent primitive-part DEFINITENESS, with no theorem routing the integer to RH (blocks the conflation); (3) **the toy Lean face** ([`ToyModel.lean`](lean/ZetaRH/ToyModel.lean)): genus-1 grader correctness both directions, incl. the NEW off-line witness `toy_reject_fake`; (4) **O1 named** ([`FunctionFieldRH.lean`](lean/ZetaRH/FunctionFieldRH.lean)): the lever-B existence residual as a precise Prop `FrobeniusMatrixExists` + `functionfield_RH_of_frobeniusMatrixExists` reducing RH-for-the-curve to it (the months-long FLT-adjacent construction left open, named, not an axiom). **Docs:** the lost #128 three-front working reports reconstructed into a tracked dossier ([`m4_construction_attempt.md`](docs/03_research/m4_construction_attempt.md)); the #119 four-area sweep folded into [`all_roads_to_the_signature.md`](docs/03_research/all_roads_to_the_signature.md). |
| The Arakelov-face pivot: probe + construction attempt + VT1/VT2 in Lean (2026-06-27) | ✅ **The Arakelov face is the MIRROR of the AHK failure, not an escape; a 3-front construction attempt confirms it; the two isolable residues are formalized (LEARNINGS #131/#132).** **Probe** ([`2L`](experiments/arithmetic_geometric/2L_arakelov_face_probe.md), #131, 5 adversarially-verified dimensions): AHK has the base but not the carrier (dies at R1); the Arakelov face has a carrier AND a proven per-surface polarization (single-surface Faltings-Hriljac/Yuan-Zhang = per-motive M4, a theorem) but dies at the BASE (no $\mathrm{Spec}(\mathbb{Z})\times\mathrm{Spec}(\mathbb{Z})$ + $\Gamma_S$ = PROP-global); M4 is NOT what it lacks (sharpens #130). The BSD severance keeps FH positivity off ζ's zeros (height-to-L = $L'(E,1)$, order-of-vanishing, #113). 2026 lit frontier unmoved (Yuan-Zhang/Moriwaki/CGHX fixed-scheme; Bost theta wrong signature class; Connes-Consani Jacobian trace-side). **New computed artifact** [`e2ae`](experiments/arithmetic_geometric/e2ae_petersson_faltings.py): 2J's deferred $\overline\omega^2 = 12\,h_{\mathrm{Fal}}$ via the Petersson norm, SL₂(ℤ)-invariant + LMFDB-validated (a self-intersection diagonal, not M4). **Construction attempt** ([`2M`](experiments/arithmetic_geometric/2M_arakelov_construction_attempt.md), #132): three BUILDER fronts all wall at the same R1+M4+PROP-global gap = the first construction-side three-front convergence on the Arakelov base; three adversary-withdrawn overclaims recorded. **Lean** ([`ArakelovAssembly.lean`](lean/ZetaRH/ArakelovAssembly.lean), #2M-VT, sorry-free + axiom-clean, build green 3745 jobs): VT1 per-prime Hasse sign (a specialization of `negDef_iff_hasseWeil`) + VT2 the no-single-scale assembly no-go (`no_single_normalizing_scale`, `forced_scale_asymmetry_unbounded`, `assembly_obstruction`). A successful dead branch; the open kernel (the adelic-scalar sign = M4) is untouched. Do NOT run a fourth Arakelov front. |
| First-principles audit of the M4 convergence (2026-06-28) | ✅ **A 27-agent sweep of 8 needle-mover engines found NO escape; the two assertion-only verdicts are now computed (LEARNINGS #133).** Tested the engines that are the COMPLEMENT of the lateral-imports scan and session-019 (Deligne-Weil-II monodromy, de Bruijn-Newman dynamics, Nyman-Beurling-Baez-Duarte, condensed/analytic geometry, Fargues-Fontaine, automorphic-family functoriality, extremality, Weil-etale): **7/8 reduce to M4, the 8th already killed; no genuinely-new coordinate** (the ~13th all-roads confirmation, now from the monodromy direction). **Frame-audit:** the convergence is ~80% genuine law / ~20% artifact, the artifact being one axiom ("Level 4 = positivity = signature" is a CHOICE, not a law; the two non-polarization Level-4 routes had never been D-H-tested). **Acted on it:** (1) the **Nyman-Beurling-Baez-Duarte D-H probe** ([`criticality/e_nb_baez_duarte_dh.py`](experiments/criticality/e_nb_baez_duarte_dh.py), genuine BD object, Möbius-sign recovery 13/13) $=$ **MIRROR** via the archimedean stealth window (does not read the D-H off-line zero, does not detect the missing Euler product; the naive "D-H floors" was a $\zeta$-pole artifact); (2) the **de Bruijn-Newman criticality FLOW probe** (sub-target 12.4', [`criticality/e_dbn_flow_dh.py`](experiments/criticality/e_dbn_flow_dh.py), reproduces e3f's $W(f_0)$ bit-for-bit) $=$ also **MIRROR** by the same stealth window (the heat-flow prime block never reaches the off-line zero; the zero-side $e^{t\gamma^2}$ amplification diverges). So **both non-polarization Level-4 routes are now graded by experiment**, both D-H-blind by the same mechanism (joining Lee-Yang #95 and the de Bruijn kernel #38). (3) **Deligne-Weil-II earns a 4th scorecard bracket, "too shallow"** (Rankin-Selberg positivity: proven, non-circular, Euler-essential, but 2/3-capped at Re$=1$), joining Faltings-Hriljac "too local", AHK "too blind", de Branges "too strong". Retracted the "Level-4 $=$ positivity" over-claim in CLAUDE.md, the four-level framing, and Dir-12 (kept every theorem). Net: a clean negative that HARDENS the M4 wall, plus two computed mirror verdicts and one scorecard bracket. |
| The graph proven-world probe arc (2026-07-01) | ✅ **A second checkable proven world (finite regular graphs, Ihara zeta / Ramanujan) plus a four-probe arc, all one story: the graph world's entire power is the single self-adjoint operator ζ lacks (LEARNINGS #139-#142).** **#139:** [`ihara.py`](experiments/toy/ihara.py) + [`ihara_grader.py`](experiments/toy/ihara_grader.py) upgrade the Selberg toy into a genuine dynamical zeta where graph-RH ⟺ Ramanujan ⟺ \|λ\|≤2√q (the same √q as Weil), with a **native Davenport-Heilbronn** (a non-Ramanujan regular graph); the reference [-1,1]-moment form scores all green, the Hamburger-only candidate reproduces but fails to reject = **self-adjointness is free, the gap is M4**. Plus [`chaos/c4`](experiments/chaos/c4_prime_orbit_spectrum.py): a thermodynamic-formalism probe of 2R (entropy → 1, but the archimedean Γ-factor mean has no orbit source; separates ζ from D-H via prime-power orbit repetitions, past GUE). **#140:** [`interlacing.py`](experiments/toy/interlacing.py): MSS interlacing families SOURCE the √q bound with **no variety** (crossing R1), and the fault line is real-rootedness = the self-adjointness ζ lacks, so R1 sharpens from variety-gated to **self-adjointness-gated**; MSS added to the transfer corpus. **#141:** [`alon_boppana.py`](experiments/toy/alon_boppana.py): Alon-Boppana proves **marginal positivity as extremality** (every regular family saturates 2√q = the universal-cover tree's spectral radius, no buffer). **#142:** [`archimedean_place.py`](experiments/toy/archimedean_place.py): the archimedean place is the passage from a finite atomic spectrum (flat, RH-provable) to the continuous spectrum of the universal cover (never-flat, RH-hard), unifying #141/#79/#80/c4. 12/12 toy + 7/7 transfer tests. Honest caveat throughout: proven-world validation on the self-adjoint spectrum, not a transfer; the M4/R1 gap is unchanged, now stated as one missing self-adjoint operator. |
| The circle-rooted interlacing probe + executable integration (2026-07-01) | ✅ **The #140 follow-up: no circle-rooted variant of the MSS interlacing engine exists, and the miss is structural** (LEARNINGS #143; survey + [`experiments/toy/circle_interlacing.py`](experiments/toy/circle_interlacing.py) 11/11 + adversary). Two thirds of the engine exist on the circle (convolution algebra: Grace-Szegő / Suffridge 1976 / Leake-Ryder 2020 / MSS Thm 1.5 on unitary inputs; interlacing order: POPUC); the missing third is the **extremal-selection step** (an exact locus has no native one-sided order; $x = T + q/T$ re-poses real-rootedness $=$ self-adjointness). **The gem: Schur-Cohn 1922 certifies circle-rootedness of a self-inversive polynomial as functional equation $+$ Hermitian PSD, an independent classical convergence on the M4 shape.** Adversary corrections: possible-modes dichotomy (arithmetic admits both loss-of-real-rootedness and exit-the-window; graph world exit-only) and Haar-exact (not universal) first-moment triviality (Kabluchko 2025). #140's gate refines to **self-adjointness-OR-positivity-gated** (the positivity branch $=$ Lee-Yang polarity, killed #95/#119). **INTEGRATED as executable machinery:** `schur_cohn_candidate` is a second independent all-green reference in the toy grader (validated 400/400 before wiring; genus-1 anchor $[4-t^2/p]$ $=$ the Hasse window; strict margin where the moment matrix is marginal, via Gauss-Lucas on the derivative, $\mathbb{Z}$-side face $=$ the mapped LP/dBN face) + the selection-order disqualifier in the breadth battery (toy 14/14, breadth 19/19). |
| The proof-engine frame-audit arc: Stepanov + parity + van Frankenhuijsen + Deligne Weil I + the Gauss-lemma floor (2026-07-01) | ✅ **The all-roads thesis gets its scope correction and the evidence is now engine-complete: form roads wall at M4; counting and amplification roads wall at R1 + the missing base; the missing object is ONE object, with four named consequence-faces** (LEARNINGS #145-#149; [`stepanov_engine_audit.md`](docs/03_research/stepanov_engine_audit.md), [`parity_vs_polarization.md`](docs/03_research/parity_vs_polarization.md), [`vanFrankenhuijsen-2008-Nevanlinna-RH.md`](docs/03_research/reading_notes/vanFrankenhuijsen-2008-Nevanlinna-RH.md), [`deligne_weil1_engine_audit.md`](docs/03_research/deligne_weil1_engine_audit.md)). Over $\mathbb{F}_q$ the Stepanov-Bombieri engine proves RH-for-curves with **no signature input**; over $\mathbb{Z}$ its form-free moves all exist (Arakelov $h^0$, Siegel lemma, product formula, Landau) and the two Frobenius-powered moves are absent (= R1 twice + PROP-global); the M4-in-disguise check is negative. The sieve parity barrier is the **consumer-side shadow of R1** (fingerprint 1/5; every power-saving parity-break consumes purity as a modulus bound and discards the sign). vF 2008 names both facets verbatim ("there is no diagonal"), zero descendants in 18 years (testimony, not proof). **The third witness (#148) confirmed:** the Deligne Weil I amplification engine walls at R1's trace-formula face (W6: rationality with an invariant-theoretically computed pole budget, over $\mathbb{F}_q$ = the Grothendieck trace formula) + PROP-global, never M4, with the strongest not-M4 certificate of the three (Weil I proves RH over $\mathbb{F}_q$ while the Hodge standard conjecture remains **open** there: purity produced, polarization never produced); the one missing operator gains **four named consequence-faces** (endomorphism S1 / derivation S4 / trace-formula W6 / polarization M4), and with W6 absent the road's all-$k$ closure is exactly Lindelöf (Titchmarsh XIII), so **the Lindelöf-RH gap is the measured size of the missing trace formula**. **The Gauss-lemma floor (#149) is a machine-checked theorem:** [`e2ah_gauss_floor.py`](experiments/arithmetic_geometric/e2ah_gauss_floor.py) + [`GaussFloor.lean`](lean/ZetaRH/GaussFloor.lean) (#GF-1..#GF-5, sorry-free, axiom-clean): the minimal height of a prime-forced vanisher in the vF disc model is exactly $\psi(x)$, equality unique at the minimal degree, so the model has **no Siegel-lemma slot** and the S4/R1 operator is provably its only open slot (PUBLICATIONS-gate candidate; gate not yet run). Other residues: a one-sided $\psi(x) \le x + O(x^{1/2+\epsilon})$ already forces RH (Landau), so counting substitutes need only upper bounds; the modulus-only-consumer screen + the level-of-distribution wrong-axis flavor (documentation-level); the R1 WATCH list now a **triple** (variety-free purity theorem; power-saving bilinear Möbius cancellation near $\sqrt x$ without finite-field geometry; a determinant-class trace formula for $\zeta$ with computable pole budget); Vojta's relative-tangent-bundle shape constraint. Strategic rebalance: **R1 is a co-equal target with M4**, partially confirming #133's 80/20 estimate. |
| Intuitive / undergraduate docs | 🔄 In progress |
| PDF text conversions | 🔄 In progress |
| manim visualizations | 🔄 In progress |

## Quick Start

```powershell
# Set up environment
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt

# Smoke test the experimental framework
python -m experiments._shared.smoke_test

# manim scene
manim -pql visualizations/01_series_intro/series_intro.py ZetaSeriesIntro
```
