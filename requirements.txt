manim>=0.18.0
scipy>=1.11.0
numpy>=1.24.0
matplotlib>=3.9.0
mpmath>=1.3.0
sympy>=1.12
pdfminer.six>=20221105
pypdf>=3.0.0
cvxpy>=1.9.0
duckdb>=1.0.0

# Optional: GPU sieve engine (experiments/primes/primestream_gpu.py).
# Needs an NVIDIA card; the CPU engine remains the reference implementation.
# cupy-cuda12x[ctk]
