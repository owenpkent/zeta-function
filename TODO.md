# TODO — Riemann Zeta Function Study Repo

## Done

### Repo and docs

- [x] Set up repo structure
- [x] Write README.md
- [x] **docs/solutions/** — known approaches and obstructions per architecture
- [x] **docs/research_atlas/** — master research map, all approaches and obstructions
- [x] **docs/02_graduate/log_correlated_fields_intro.md** — log-correlated fields, four-level RH framing, primes-zeros Fourier duality
- [x] **docs/03_research/** — extreme values, log-correlated fields, new mathematics
- [x] **CLAUDE.md** — project context for AI assistants
- [x] **docs/03_research/rh_logical_status.md** + **lean/ZetaRH/RHEquivalences.lean** — RH's logical status (Π⁰₁, undecidability is a back door to truth not an escape); the Π⁰₁ kernel witness `RH_arith`; 8 sorry-free Lean anchors incl. Lagarias at n=1,2,3 and the Σ⁰₁ refutability structure (LEARNINGS #64)

### Experimental thread (see `experiments/PROOF_ARCHITECTURES_PLAN.md`)

- [x] Phase 0: shared infrastructure (LFunction interface, zeta, Davenport-Heilbronn, Dirichlet chi_3/chi_4, smoke test 8/8)
- [x] Arch 3A: zeta Li coefficients, positivity confirmed for n=1..500
- [x] Arch 3B: per-zero Li diagnostic; small-n Li positivity does NOT distinguish zeta from D-H
- [x] Arch 3B.2: large-n Li witnesses lambda_n^DH < 0 at n = 4 x 10^5 via asymptotic + off-line correction
- [x] Arch 3C: Weil quadratic form via Gram matrix; M^DH has negative eigenvalues (wrong-approach detector)
- [x] Arch 3C.3: Selberg-class cross-cut (chi_3) confirms detector direction-selectivity
- [x] Arch 3D: scaling of witness with K and T_max; witness deepens with K
- [x] Arch 3D.2: K-scaling cross-cut over zeta, chi_3, chi_4, D-H
- [x] Arch 3D.3: K-scaling extended to K=1000 — relative min converges to -2.62%, neg count = # off-line zero pairs (fixed at 4)
- [x] Arch 3F: Weil-form duality for zeta (prime-side = zero-side to <2% at T_max=1000)
- [x] Arch 3G: Weil-form duality for D-H (oscillating Dirichlet sum, cancellation 100x looser than zeta)
- [x] Arch 3H: Weil-form duality for chi_3 (intermediate cancellation; tight cancellation is specifically zeta's pole)
- [x] Arch 3I: chi_3 unconditional path blocked by Siegel-Walfisz looseness (factor 30-120 too loose)
- [x] Arch 3J: Schur complement of Weil-form Gram matrix against on-line cushion — irreducible off-line obstruction is ~30x sharper than raw spectrum (rel min asymptote -78.7% vs -2.6%), signature exactly (N_off, N_off) per off-line gamma in UHP; quantitative form of marginal-positivity thesis (see experiments/positivity/e3j_schur_complement.py + README §3J)
- [x] Arch 3K: hypothetical off-line zero perturbation — Schur signal scales as 16*(beta-1/2)^2 across four decades; float64 stealth window (eps < 10^-5) is looser than Platt-Trudgian rigorous bound (eps < 10^-7); disproof in Gram-matrix family has no leverage point (see experiments/positivity/e3k_hypothetical_offline.py + README §3K)
- [x] Arch 1A: Berry-Keating discretization; density-mismatch with zeta documented
- [x] Arch 1B: Sierra-Townsend modifications (centrifugal, Coulomb, modular log); all L-function-agnostic
- [x] Arch 1C: L-function discrimination test — best-affine RMS ratio spans factor 3 around 1
- [x] **Eight-angle sweep (2026-06-24/25, LEARNINGS #111-#117, synthesis [`docs/03_research/eight_angle_sweep_2026-06.md`](docs/03_research/eight_angle_sweep_2026-06.md))** — all eight reduce to M4 (realization everywhere, the signature only at M4):
  - [x] Arch 1D brought current + QM/spectral signature dossier (#111, Connes-Consani prolate/semilocal + Bender-Brody-Müller; `quantum_mechanics_signature_dossier.md`); `e1d` additive-arithmetic-injection NULL (1E)
  - [x] NP-1: finite-prime modular carrier does not continue into the strip as a t_p-detector — reduces to M4/#104 (#112, `e2vv`)
  - [x] String theory angle (#113, `string_theory_rh.md`): corpus closes onto existing kills; the L-value/L-derivative BSD-regime discriminator extracted
  - [x] Cryptography angle (#116, `cryptography_rh.md`): RH-as-tool (inverted dependency) + the function-field-RH shadow; corpus-completing closure
  - [x] CCM semilocal prolate deep dive (#114/#115/#117, `ccm_semilocal_prolate.md`): factored, conditionally escaping; three cheap operator builds `e1f`/`e1g`/`e1h` all signature-blind by distinct mechanisms; archimedean = Meixner-Pollaczek (exact). The deferred metaplectic operator is the un-eliminated route by elimination.
- [x] Arch 4B: non-negative trig polynomial LP saturates Fejér bound
- [x] Arch 4D, 4D.2: single-coefficient d-variate LPs decompose to tensor products
- [x] Arch 4E: balanced-sum LP max c_{1,1} + c_{2,2} exceeds tensor bound by 12% at N=2
- [x] Arch 4E.2: alpha sweep — peak gap +25% at alpha=3, N=2; clean rational LP-optimum
- [x] Arch 4E.3: +25% C-S gap does NOT translate to MT zero-free constant improvement (structural lemma)
- [x] Arch 4E.4: trivariate balanced-sum LP — gap +51% at alpha=3.25, N=2 (doubles bivariate)
- [x] Arch 4E.5: d=4 balanced-sum LP — gap [+55%, +70%] at alpha=4.5, N=2 (sub-linear scaling)
- [x] Arch 2B: Weil RH for E: y^2 = x^3 + x + 1 over F_5 verified end-to-end
- [x] Arch 2A: Weil-proof diff table + 17-constraint spec + candidate evaluation framework (see experiments/arithmetic_geometric/2A_weil_proof_diff.md and 2A_candidate_evaluation.md)
- [x] Arch 2A R1: D-H exclusion sharpening for the six scored candidates (see experiments/arithmetic_geometric/2A_R1_DH_exclusion.md)
- [x] Arch 2A R2: fiber product computation in Borger and Lorscheid (see experiments/arithmetic_geometric/2A_R2_fiber_product.md + companion dossiers 2A_borger_dossier.md, 2A_lorscheid_dossier.md)
- [x] Arch 2A R2.5: Λ-blueprint hybrid proposal (see experiments/arithmetic_geometric/2A_R2_5_lambda_blueprints.md)
- [x] Arch 2A R3: Connes-Consani K1 analysis (see experiments/arithmetic_geometric/2A_R3_connes_positivity.md)
- [x] Arch 2A R3.5: no-shortcut theorem for NCG approaches (see experiments/arithmetic_geometric/2A_R3_5_K1_universality.md)
- [x] Arch 2A R3.6: deep dive on Connes-Consani arithmetic site / hyperrings / characteristic-one geometry (see experiments/arithmetic_geometric/2A_R3_6_arithmetic_site.md) — all formulations probably fail K1; Connes-Consani (xi)-(xiii) sharpened from 🟡 to ❌
- [x] Arch 2A R4: Borger Frobenius + Connes trace formula hybrid analysis (see experiments/arithmetic_geometric/2A_R4_borger_connes_hybrid.md)
- [x] Arch 2A R5: prismatic cohomology investigation (see experiments/arithmetic_geometric/2A_R5_prismatic_cohomology.md)
- [x] Arch 2A path forward strategic synthesis (see experiments/arithmetic_geometric/2A_path_forward.md)
- [x] Arch 2E: numerical probe of bare psi_p spectrum on concrete Lambda-rings (see experiments/arithmetic_geometric/e2e_adams_spectrum.py + 2E_adams_spectrum_probe.md) — confirmed predicted negative result: roots of unity / {0} / powers of p, no structural relation to zeta zeros; randomization control rules out apparent K-theory near-coincidence as signal

### Session 003 (2026-05-28): gap-closing experiments

- [x] Shared: Epstein zeta as an independent second wrong-approach control (experiments/_shared/epstein_zeta.py). Chowla-Selberg / Terras evaluation validated to 1e-31 against the class-number-1 anchor Z_Q = 4 zeta(s) beta(s) and to 1e-32 against its own functional equation. Off-line zeros found for the class-number-5 disc -47 non-principal form (rho ~ 0.634 + 32.05 i); the principal form has none (Selberg-like contrast).
- [x] Arch 3L: Epstein second-control test of the Weil-form Schur detector (experiments/positivity/e3l_epstein_control.py). The counting law schur_neg = #off-line heights, schur_dim = 2 x #off-line heights holds identically on Epstein as on D-H (clean PASS, K up to 300). The wrong-approach detector GENERALISES beyond Davenport-Heilbronn. LEARNINGS #19.
- [x] Arch 3B.4: rigorous Li-criterion discrimination across five L-functions (experiments/positivity/e3b4_li_discrimination.py). Reproduces D-H rigorous negativity (n=336,000), Selberg controls rigorously positive (n=50,000), and gives a SECOND off-line Li witness: Epstein lambda_n < 0 rigorously from n=110,000 (Epstein scanned to T=500 so the tail bound is negligible). Full rigorous discrimination at n=336,000: lambda_n^DH < 0 and lambda_n^Epstein < 0 < lambda_n^zeta, lambda_n^chi3. LEARNINGS #22.
- [x] Arch 2F: function-field Hodge-index positivity sweep (experiments/arithmetic_geometric/e2f_hodge_index_sweep.py). Weil RH bound |alpha_i| = sqrt(q) holds exactly (0.000e+00) across elliptic curves over F_5..F_23 and genus-2 curves over F_5..F_13. Exhibits the positivity target a Spec(Z) lift must reproduce. LEARNINGS #20.
- [x] Arch 4E.9: Heath-Brown multi-zero MT SDP, Direction 7 (experiments/zero_free/e4e9_heath_brown_sdp.py). The multi-zero MT shape factor does NOT exceed the 1D Fejer ceiling (best ratio <= 1, rank-2 certificate). Closes the last LP/SDP/SOS escape from the 4E.3 lemma. LEARNINGS #21.

### Session 004 (2026-05-28/29): Direction 8 single-arithmetic-surface side

- [x] Arch 3M: input-side place-type decomposition of the Weil form M = A_arch + P_fin + B_pole (experiments/positivity/e3m_place_type_balance.py). LEARNINGS #20.
- [x] Arch 2G: the Hodge index as a SIGNATURE on C x C (primitive form negative definite = Hasse-Weil); machine-proved in Lean (negDef_iff_hasseWeil). LEARNINGS #21.
- [x] Arch 2H/2M: Faltings-Hriljac height pairing over Spec(Z) positive definite, ranks 1-4 (regulators match LMFDB). LEARNINGS #22.
- [x] Arch 2I/2L/2O/2P: complete Silverman local-height decomposition (archimedean Green/Petersson + every finite prime) validated h_inf + sum_p h_p = h_hat by the authoritative algorithm. LEARNINGS #23, #24.
- [x] Arch 2J/2K: arithmetic adjunction (omega-bar^2 = 12 h_Fal) + the dictionary to the would-be Spec(Z) x Spec(Z) intersection form (the product-surface gap, the Morishita bridge literature).

### Session 005 (2026-05-29): sharpen Gamma_S + the Lean explicit-formula thread

- [x] Arch 2Q: the place-dependent bidegree obstruction (experiments/arithmetic_geometric/e2q_frobenius_bidegree.py). Gamma_S must carry a (1,p) bidegree per prime, not a single scale q; forces infinite-dim H^i + the Deninger R-flow as consequences; pins Gamma_S^2 to the von Mangoldt prime sum. Sharper K2 (D-H has no local bidegrees). LEARNINGS #25.
- [x] Arch 2R: Ruelle dynamical-zeta realization of Gamma_S^2 (experiments/arithmetic_geometric/e2r_dynamical_zeta.py). Orbit lengths {log p}; prod_p (1-p^-s)^-1 = zeta and sum_n Lambda(n) n^-s = -zeta'/zeta = Gamma_S^2 (verified to 2e-4). D-H Lambda delocalizes off prime powers => no closed-orbit spectrum (dynamical K2). The Deninger half of the Morishita bridge made concrete; NOT a new RH route. LEARNINGS #26.
- [x] Lean: ExplicitFormula.lean — Weil explicit formula + Weil positivity criterion scaffold (#EF-1/#EF-2, the highest-leverage target, LEARNINGS #17); prime side concrete via Mathlib vonMangoldt.
- [x] Lean: discharged #MB-1 (zeta pole at s=1), #MB-2 (zeta nonvanishing Re s>1), #MB-6 (functional equation) to real kernel-checked proofs from existing Mathlib.
- [x] Lean: proved the digamma recurrence psi(s+1)=psi(s)+1/s (digamma_add_one) from Complex.Gamma_add_one (Mathlib has no digamma). Build green; project sorry count 26 -> 23.

### Session 007 (2026-05-30): creative directions (9-13), reframe, and the Rosati spine

- [x] Directions 9-13 proposed (docs/03_research/research_directions/09-13 + README), each attacking the #25 place-dependent-bidegree obstruction: 9 arithmetic matroid (AHK ↔ Li), 10 cyclotomic Frobenius over the sphere spectrum (THH/TC), 11 bidegree-as-a-measure, 12 de Bruijn-Newman criticality (Λ=0), 13 infinite-genus tau-function.
- [x] e3n (Li log-concavity / Hodge-Riemann signature): the log-concavity defect is a NON-Euler detector, not an RH detector — the Epstein d47-principal control (non-Euler, RH holds) fires with the most violations. The #20 reformulation trap in a new basis. LEARNINGS #27.
- [x] e_jensen_turan (Jensen polynomials / Turán inequalities on ξ Pólya moments): zeta, D-H, and Epstein-principal all pass at reachable order — a STEALTH WINDOW (the ξ-moment basis is blind to off-line zeros at height 85.7). Two bases, two different walls. LEARNINGS #27.
- [x] e_thh_vonmangoldt (Direction 10A.ii first pass): Bökstedt's THH(Z) torsion log-orders assemble (exactly, given the i^-s weight) to -ζ'(s); the von Mangoldt sum = Γ_S² (#26) is one Möbius factor 1/ζ away. Per-prime structure via v_p(i) verified. LEARNINGS #28.
- [x] e_necklace_mobius (Gap B): the cyclotomic C_n-fixed-point components are indexed by necklace polynomials = literally Möbius inversion (Metropolis-Rota cyclotomic identity verified exactly). The μ factor is intrinsic to the cyclotomic combinatorics. LEARNINGS #28.
- [x] SURVEYOR literature reconnaissance (survey_tc_zeta_literature.md): the THH→von Mangoldt link is not in the literature as posed; the proven template is Hesselholt's ζ(X,s)=det_inf(s-Θ|TP_odd)/det_inf(s-Θ|TP_ev) over F_q. Reorients Direction 10; the over-Z gap is #25 again; ADVERSARY flags the TC-equalizer=Möbius guess as a likely category error. LEARNINGS #29.
- [x] Synthesis: docs/03_research/all_roads_to_the_signature.md — every architecture/direction realizes ζ (determinant/trace) but RH is the SIGNATURE/positivity, the same object everywhere; the irreducible content. LEARNINGS #30.
- [x] e2s (TP odd/even = Weil Hodge-index split): triple equivalence across genus 1-2 over F_q (primitive intersection negative-definite ⟺ |α_i|=√q ⟺ Hesselholt zeros on Re=1/2). The K3 check for the TP lens.
- [x] 08A: RH reformulated as arithmetic Rosati positivity = the arithmetic Hodge standard conjecture, with the M1-M5 milestone ladder. The standard-conjecture (Grothendieck) home of the problem; non-circular (polarization, not zeros); K2-clean.
- [x] e2t (milestone M1, done): the full 2g-dimensional Rosati trace form has no negative eigenvalues across genus 1-2 over F_q ⟺ |α_i|=√q. Degeneracies (genus-2/F_5) = proper-subalgebra relations. For g=1, G_Rosati = -(2G primitive Gram). Four equivalent function-field faces of RH-for-C now recorded. LEARNINGS #31.
- [x] e2u (milestone M2, attempted): the arithmetic Rosati positivity assembled as the non-circular archimedean-dominance balance (3M place-type split). Verdict CONTAMINATED by the A_arch ~0.06 offset (balance backwards: D-H outranks zeta). Gate isolated and named (M2.5 = recompute A_arch via the bilinear Bombieri integral). LEARNINGS #32.
- [x] e2v (milestone M2.5, done): A_arch fixed via 3F's validated Bombieri physical-space integral (cross-b bilinear block), VALIDATED by T_max-convergence (residual vs zero-side Gram -> 0, so #32's contamination was the zero-side truncation), diagnostic corrected to min eig(A+P+B). RESULT: the non-circular Weil form for zeta is POSITIVE (min eig +0.035, stable across K=5..8; necessary-not-sufficient). D-H deferred to M2.6 (general-mu kernel mis-normalized). LEARNINGS #33.
- [x] e2w (milestone M2.6, done): general-mu archimedean constant fixed (C_mu = log pi - psi((1+mu)/2); the M2.5 bug was psi(1/2+mu)), verified C_0/C_1, D-H block residual 5.58 -> 0.12 (validated). Four-way verdict FAILS to separate: D-H reads spuriously positive (+0.094) -- stealth window (off-line obstruction below the residual floor; #18/#19/#27 on the Rosati side). Consequence: M3 must be ANALYTIC. LEARNINGS #34.
- [x] docs/researcher_mindset.md: the operating philosophy (target not monument; advance a front; negative results are coordinates; honesty is the engine; reformulation must import power; build the ladder; the AI-augmented research group). Wired into CLAUDE.md START HERE. Project framing recast from "long-shot catalog" to "research program with attackable milestones".
- [x] **E_DBN1 (Direction 12, de Bruijn-Newman heat side; session 009, LEARNINGS #38)**: first heat-side experiment, closing the gap `3e` deferred. Bochner/Toeplitz + max-modulus test for Pólya kernel positivity Φ≥0 (project idiom), validated on ζ vs Pólya closed form to 4.4e-16. Finding: Φ≥0 is K2-FAILING and orthogonal to RH — RH-false Davenport-Heilbronn passes it identically to ζ (clean degree-1), off-line zeros archimedean-suppressed to exp(-(π/4)dγ) (|Ξ_DH(85.7)|=1.5e-29; rates 0.73~π/4, 1.59~π/2). Degree-2 Epstein: Φ≥0 orthogonal to RH (s(s-1)-factor confound characterized). Verified by 4-agent workflow (independent re-implementation reproduced every number; D-H off-line zero refined to 0.80852+85.6993i). Conclusion correct but PRE-EMPTED (Dobner 2020 — S# incl. D-H; Newman-Wu 2019; convergent Michalowski 2026): a confirmed coordinate importing 3 corroborating references, not new mathematics. (experiments/criticality/e_dbn_kernel.{py,md})

### Session 010 (2026-06-03): RH solved by accident

- [x] **Accident-vector landscape** (docs/03_research/rh_solved_by_accident.md): chase Tao's "solved by accident" thesis. Multi-agent sweep over 32 candidate programs NOT aimed at RH that could incidentally supply the missing polarization, each adversarially verified against the target spec + the D-H filter. No-free-lunch across all 28 scored (the residual step is RH-equivalent in every case). B-credible tier (right KIND of tool, all pass D-H): Beilinson-Bloch height positivity (Gao/Chao Li), Chen-Moriwaki/Yuan-Zhang adelic Hodge index, prismatization-over-Z F-gauge cup form, Deninger foliated Lefschetz, geometric Langlands weight-purity. D-H reframed as a real-time tool classifier (RH-relevant iff unbuildable for D-H); the marginal-positivity thesis as a compass on which accidents are even possible. 6 cheap probes listed, all verified runnable against real repo infra.
- [x] **Arch 3O: PSLQ stumble-yield scan with the D-H guard** (experiments/positivity/e3o_pslq_stumble.py, LEARNINGS #45) -- executes cheap-probe 3. Re-discovers 3 known archimedean identities (validation, each confirmed real by a precision-stability test), finds no new identity, and reproduces+defeats the de Branges trap (an 8-digit-coefficient relation, residual 2.26e-61 that does NOT vanish at dps=220). Finding: the numerical-stumble accident slot is K2-blind by computability -- the PSLQ-accessible quantities are the closed-form archimedean half D-H shares, while the discriminating Li data is precision-limited (a large-n effect per #2), so the two surfaces are disjoint. A negative coordinate, new angle on #30.
- [x] **Arch 3P: prime-block ablation, the mandatory K2 test for a certificate** (experiments/positivity/e3p_prime_block_ablation.py, LEARNINGS #46) -- executes cheap-probe 2. Ablates the validated M_euler discriminator (A_arch + P_pp + B_pole) by scaling the prime-power block by alpha in [0,1]. At alpha=1 it sign-separates (zeta +0.035 passes, D-H -0.929 fails); at alpha=0 both negative (zeta -2.34, D-H -4.63), so the discrimination DISAPPEARS. Finding: the certificate's RH-discriminating sign is carried by the Euler/{log p} block, not the archimedean block D-H shares => M_euler is K2-GENUINE (the positive counterpart to #45). Validates K2-soundness; does not prove RH, does not make M_euler intrinsic (#37 caveat stands).
- [x] **Arch 3R: wrong-polarity check for the convex-Hodge accident channel** (experiments/positivity/e3r_convex_hodge_polarity.py, LEARNINGS #48) -- executes cheap-probe 6. Tests the B-credible weighted-tropical-Kahler channel against the adversary's polarity objection. The mixed-area (Alexandrov-Fenchel/Lorentzian) signature is UNCONDITIONALLY (1, n-1) for every weighting including w_p=log p (2000/2000 random cases have pos=1) => it can never flip to flag an off-line zero (WRONG polarity); the Weil form flips PSD -> indefinite with an off-line pair (min-eig 0 -> -0.104, RIGHT polarity). The channel is blocked by TWO obstructions: #40 (arithmetic-blind) AND #48 (even a moving signature could not fail). The watchlist-1 signal needs a genuinely new theorem, not weight injection.
- [x] **Lean de-smuggling audit (cheap-probe 5)** (lean/ZetaRH/AccidentAudit.lean, LEARNINGS #49) -- the truncated non-circular Weil/Rosati Gram weilGram = A_arch + P_fin + B_pole (Gamma factor + von Mangoldt primes + pole, naming no zero) and its symmetry weilGram_isSymm are formalized SORRY-FREE; #print axioms weilGram_isSymm = [propext, Classical.choice, Quot.sound] (no sorryAx, no riemannZeta/RiemannHypothesis/nonTrivialZeros): a kernel-checked non-circularity certificate (a control confirmed #print axioms detects sorryAx on the deferred targets). Upgrades the project's "non-circular" CLAIM (M2.5/M2.6) to a kernel-checked FACT. Build green; two documented sorries added (#ACC-1 positivity, #ACC-2 K2 necessary-not-sufficient). The first machine-checked (not numerical) accident-probe result. ALL SIX dossier cheap-probes now executed.
- [x] **Arch 3Q: sharp-margin recovery test** (experiments/positivity/e3q_margin_recovery.py, LEARNINGS #47) -- executes cheap-probe 4. Injects a hypothetical off-line zero at the D-H height (gamma=85.699) and sweeps eps = beta - 1/2. The off-line margin is a clean -3.1*eps^2 law (real D-H zero eps=0.3085 gives -0.77, trivially detected). The float64 stealth window is a REMOVABLE eigensolver-cancellation artifact, NOT structural: the signal source ||Im phi||^2 is clean eps^2 with float64 = mpmath down to eps=1e-12. But removability is moot for certification -- detecting an off-line zero at distance eps requires injecting its location to precision < eps (exactly what rigorous verification supplies), so the certificate is downstream with no independent disproof leverage. The margin must be recovered analytically. Sharpens #3K/#19. (Fixed two bugs from the first version: a Schur sign/degeneracy giving a spurious eps*, and a small-K Part 2 where the cushion absorbed the off-line direction.)

### Session 011 (2026-06-03): Connes "Letter to Riemann" (arXiv:2602.04022)

- [x] **Assessment of Connes 2602.04022** (docs/03_research/connes_2602_letter_to_riemann.md): multi-agent assessment of the survey's research kernel (constructive Weil positivity: minimal eigenvector eta_x of the truncated Weil form, Theorem 6.1 forces eta_x-hat's zeros onto the line, conjectured convergence eta_x-hat -> Xi gives RH). Verdict: Theorem 6.1 manufactures on-line zeros for ANY admissible even-kernel form (zeta-blind); the entire RH content is the unproven convergence, which is the project's (N_off, N_off) obstruction / R3.5 no-shortcut wall restated. Confirms and sharpens the marginal-positivity thesis (Connes' epsilon(lambda) near-radical = the project's stealth window). Lands C-longshot in the accident dossier; trace-without-polarization (sharper realization half) in the spec_z landscape.
- [x] **Arch 3S: Connes eta_x as a live D-H classifier** (experiments/positivity/e3s_connes_eta.py, LEARNINGS #50). Builds the truncated Weil form on a grid (reusing e3m arch kernel + von Mangoldt). (A) MIRACLE: minimal eigenvector recovers the first 8 zeta zeros to ~1e-2 from primes <= 25 (117/119 roots on |z|=1). (B) ZETA-BLIND: the IDENTICAL machine recovers D-H's on-line zeros even better (5.0946 vs 5.0942, ...); Caratheodory-Fejer gives all-roots-on-circle (1.000) for zeta-, D-H-, AND random-derived symbols. (C) OFF-LINE OBSTRUCTION: near D-H's off-line height 85.7 the machine emits only real frequencies and structurally cannot represent the off-line zero 0.8085+85.699i (Hurwitz => eta_DH-hat does not converge to Xi_DH). Discrimination lives only in the unproven, RH-equivalent convergence.
- [x] **M2.6 follow-up / Arch 3Y: the stealth window is a huge-cancellation residue** (experiments/positivity/e3y_stealth_cancellation.py, LEARNINGS #56). Supervised. The three input-side blocks have norms 55 (A_arch), 69 (P_fin), 123 (B_pole) but sum to norm 0.33: positivity for zeta is a ~370x cancellation residue, NOT cushion+perturbation. A_arch is INDEFINITE (min -55), correcting the false e3m docstring claim (fixed in e3m too). Stealth window quantified: off-line obstruction (abs ~0.05) < reconstruction residual (abs ~0.19), ratio 0.25 < 1 => invisible to any input-side certificate at reachable K; the clean detector is the answer-side Schur (e3j). Input-side shadow of the e3v e^{-4 pi x} wall.
- [x] **Overnight C4 / Arch 3Z: Bost-Connes multiplicativity obstruction** (experiments/positivity/e3z_multiplicativity_obstruction.py, LEARNINGS #55). Multiplicativity defect d(L) = mean|a_{mn}-a_m a_n|/mean|a_m a_n| over coprime pairs: zeta, chi3 = 0 (Euler => KMS pure product state); D-H = 1.16 (RH-false), Epstein-d47-principal = 5.14 (RH-TRUE), both non-Euler. d>0 for both RH-true and RH-false => detects non-Euler-ness (no product state), not RH-failure: Bost-Connes K2 firewall, necessary-not-sufficient. Sharpens 2A_R1 (D-H has no equilibrium product state). Algebraic twin of e3w (#53).
- [x] **Overnight C1 / Arch 3X: function-field polarization made concrete** (experiments/arithmetic_geometric/e3x_function_field_polarization.py, LEARNINGS #54). NS(E x E) intersection form for 23 curves over F_p: signature exactly (1,3) (Hodge index), primitive part negative-definite, RH bound a^2<=4q with O(q) buffer. The polarization the integer case lacks, where it is a theorem; contrast with the e3v e^{-4 pi x} marginal wall. Capstone: docs/03_research/session_2026_06_03_capstone.md.
- [x] **First-principles construction sweep for the missing positivity** (docs/03_research/building_the_missing_positivity.md, LEARNINGS #53). "Build the missing math": four independent constructions (Rankin-Selberg pole polarization; Bost-Connes Fock modular form; fibered arithmetic surface; prime free-field reflection positivity), each adversarially stress-tested against D-H. ALL collapse to a known object (arithmetic Rosati / Hodge-standard = RH, or de Branges / Conrey-Li = stronger-than-RH-and-false); no genuinely-new foothold. The seam: the Euler product fixes the object's EXISTENCE / block structure (real, D-H-discriminating) but the off-line zeros live in the shared archimedean continuation it does not touch.
- [x] **Arch 3W: the Rankin loglog-coefficient (the one live invariant, executed)** (experiments/positivity/e3w_rankin_loglog.py, LEARNINGS #53). c_F = lim (sum_{p<=X}|a_p|^2/p)/loglog X: zeta 1.10, chi3 0.97 (Euler -> 1); D-H 0.37 (RH-false), Epstein-d47-principal 0.63 (RH-TRUE), both non-Euler < 1. Decisive: c<1 for BOTH RH-true and RH-false non-Euler controls => detects NON-EULER-ness, not RH-failure (the K2 firewall as a single scalar, necessary-not-sufficient; the reformulation trap #20/#27). Closes the only thread the construction sweep left open.
- [x] **Arch 3V: the marginal-positivity wall, quantified (Connes Fig 1 / candidate-proof ST1)** (experiments/positivity/e3v_marginal_wall.py, LEARNINGS #52). Computes the Slepian prolate concentration eigenvalues directly: 1 - lambda_2 ~ exp(-2c), c = 2 pi e^L, so the Weil-form minimal eigenvalue collapses doubly-exponentially eps(x) ~ exp(-4 pi x) (x = prime cutoff). Crosses float64 by x ~ 3, ~1e-71 at Connes' x = 13 -- explains why the e3s/e3u grid eigensolves report a resolution-limited eps ~ 0.1 and why Connes needs high-precision CF/Dirac machinery for 2.6e-55. The collapse RATE is archimedean (shared with D-H); the FLOOR is the discriminator (zeta -> 0 = RH; D-H -> the e3j -78.7% off-line floor). Turns the marginal-positivity thesis into a standing quantitative benchmark.
- [x] **Arch 3U: assume RH and work backwards** (experiments/positivity/e3u_assume_rh_backwards.py, LEARNINGS #51). Assumes RH so the zeros are the spectrum {+-gamma} of a self-adjoint D (Hilbert-Polya). (A) The heat trace Tr exp(-t D^2) matches Connes Theorem 7.3 (the assume-RH expansion) to rel err 3.9e-5; recovers the Riemann-von Mangoldt density + archimedean constant. (B) a_0 = -0.2494 (Connes -1/4). (C) Splitting a zero pair off the line to (beta, 1-beta) at fixed height changes the heat trace by EXACTLY 0 (it depends only on gamma) while the Weil-Gram min-eig drops -22.5% at beta=0.8. The object RH hands you (D) is exactly blind to beta, the coordinate RH is about: Level 3 is categorically insufficient, re-derived from the assume-RH direction (Hilbert-Polya alone cannot close RH; need Level-4 positivity).
- [x] **Arch 3T: prolate ansatz k_lambda, the archimedean/Euler split** (experiments/positivity/e3t_prolate_residual.py, LEARNINGS #50). k_lambda = E(h_lambda) (prolate eigenfunctions of PW_lambda, ZERO primes) recovers the first zeta zero to 14.133-14.135 (Fact 6.4); fed into the full Weil form, Q(k_lambda)/||k||^2 ~ eps_min and is archimedean-dominated (prime coupling 0.9-1.3%). The prolate "miracle" runs on the archimedean half D-H shares; the Euler block is exactly what the ansatz does NOT supply (sharpens #46). Closing the gap (k_lambda = theta_x, infimum -> 0) is the unproven, RH-equivalent step.

### Session 012 (2026-06-04): "where do we go from here" roadmap synthesis + de-accretion

- [x] **Roadmap synthesis** (32-agent map / strategize / adversarial-stress-test / synthesize): the program has converged to ONE irreducible target, M4 organ (a) = the Euler-pole H^2 cup-product polarization on the infinite-dim arithmetic H^1 (2HH/#61). 14 of 19 fresh candidate moves were killed as restatements / soft-detector traps / dead corners; 3 pursue + 2 watch survive.
- [x] **Soft-detector consolidation** (docs/03_research/soft_detector_wall.md): a frozen, non-destructive index of the soft-detector thread with the 5 load-bearing facts (#46 prime-block K2, #52 e^{-4 pi x} wall, #56 370x cancellation, #61 cup-is-polarization, #63 e^gamma resolution cost), each labeled necessary-not-sufficient. FROZEN: a new soft/realization detector is dead on arrival unless it separates zeta from D-H at reachable truncation or is a genuine signature theorem.
- [x] **Finding-number drift fix**: reconciled state_of_candidate_ABF.md (#43-49 -> canonical LEARNINGS #57-63), PrismaticCohomology.lean tags (#46 -> #60, #47 -> #61), and the PHASE_STATE last-finding pointer (#49 -> #66).
- [x] **Citation fix**: arXiv:2301.12392 is Lance Gurney, *Prismatization over Z* (the genuinely global-over-Spec(Z) construction), NOT Drinfeld; Drinfeld's and Bhatt-Lurie's prismatizations are p-adic/local (web-verified; corrected in spec_z_cohomology_landscape.md).
- [x] **Foundational-object adversarial brief** (docs/03_research/foundational_object_dimension_trace_brief.md): the dimension-or-trace dichotomy as the genuine falsification attempt. Conclusion: the object EXISTS but its polarization is OPEN (NOT "cannot exist" -- refuted by Deninger's constructed foliated space, arXiv:1807.06400, dim 3, orbit spectrum {log p}). The risk is the F_q-to-Z transfer (O(sqrt q) buffer collapses to the e^{-4 pi x} marginal wall).
- [x] **2KK WCart finite-model probe** (experiments/arithmetic_geometric/e2kk_wcart_finite_model.py, LEARNINGS #66): the first WCart probe to form the cup pairing as a matrix and read its signature. WRONG POLARITY at rank 2 (the alternating cup form is unconditionally hyperbolic for every Hodge-star phase and every K) => organ (a) cannot be a Hodge-star on the obvious cup form; needs higher-rank primitive cohomology or a Frobenius-weighted trace positivity. ADVERSARY caught 3 code defects (string-match D-H gate, fake Jordan block, silent non-cup-form), all flagged in the .py STATUS banner + .md correction blocks. Retained as a recorded NEGATIVE coordinate (do not re-run).
- [x] **Digamma Mathlib PR body** (lean/upstream/digamma_pr_body.md): submission-ready PR body for the 3 verified-novel identities (reflection, iterated recurrence, duplication); remaining work is GitHub/CLA/rebase mechanics (Owen).

### Session 013 (2026-06-05): Euler-Sen linear-algebra core merged + adversarial K1 audit

- [x] **Merge the Euler-Sen linear-algebra core (PR #5)**: 2LL/#67 plus the Lean substrate (`EulerSenLinearAlgebra.lean`, `FrobeniusAlgebra.lean`). A Tate-centered defective Sen block (`Theta = -1/2 I + N`, `N^2 = 0`) satisfies the exact cup-target equation (`Theta^T Omega + Omega Theta = -Omega`, residual 0) and recovers the function-field Rosati/Hasse-Weil sign via `Q_N(x,y) = Omega(x, N y)`, escaping the 2KK rigid-hyperbolic trap.
- [x] **2MM Euler-Sen K1 audit** (experiments/arithmetic_geometric/e2mm_euler_sen_k1_audit.{py,md}, LEARNINGS #68): the finite Euler-Sen primitive form is a TRANSPORT theorem, `signature(Q_N) = signature(B)` for arbitrary symmetric `B` (0 mismatches across 32 rows, dimensions 1-6), so it manufactures NO positivity. Sharpens the M4 burden without ambiguity: construct `B` geometrically from Euler/Frobenius data plus the archimedean metric and prove `B > 0` on primitive pieces without zero-side or RH input (importing `B` from a Weil form or known zeros is K1-circular). K2 guard upgraded to data-based (`has_euler_product`, which blocks a renamed Davenport-Heilbronn). 2LL survives as a coherent monodromy slot; 2MM prevents over-reading it as a positivity theorem.
- [x] **RH attack launch prompts** (prompts/): the in-repo agent prompt (`attack_prompt.md`), the self-contained no-repo-access twin (`attack_prompt_no_repo.md`), and the condensed copy-paste-ready ChatGPT twin (`attack_prompt_chatgpt.md`).

### Session 014 (2026-06-05): WCart primitive-quotient probe (the Petrov non-semisimplicity question)

- [x] **2LN WCart primitive-quotient probe** (experiments/arithmetic_geometric/e2ln_wcart_primitive_quotient.{py,md}, LEARNINGS #69): the only WCart direct-polarization escape hatch left after 2LL/2MM. Resolves the Petrov question: the defective Sen nilpotent is ROUTABLE but UNHELPFUL on the primitive quotient. (1) The direct Hermitian Rosati/Lyapunov metric `Theta^* H + H Theta = -H, H>0` does NOT exist on the full nonsemisimple block (a nilpotent is not diagonalizable, so not similar to skew-Hermitian). (2) By Jacobson-Morozov the monodromy weight filtration makes N a pure Lefschetz lowering operator, the ZERO endomorphism on each primitive graded quotient, where the metric DOES exist: the nilpotent dies on primitive for free. (3) The positivity gap relocates undiminished to the SIGN of the primitive form `Q_k(x,y)=c_E*Omega(x,N^k y)` (POS for c_E=+1, NEG for c_E=-1, same |defect|), fixed by the archimedean Weil operator C_E not N. (4) A misaligned Lefschetz leaves N alive on the "primitive" subspace, so the missing datum is the geometric Frobenius/Lefschetz operator, not Theta. Robust on blocks (3,),(4,),(2,2); audits A-E pass (sign convention, pure cup symmetry type, nullspace dim = Tate-dual Jordan matching sum min(m_a,m_b), basis invariance, D-H discipline with the injected-D-H 0.617 polarization defect). WCart stays ALIVE; the M4 burden (#68) is unchanged, restated on the primitive quotient.
### Session 015 (2026-06-05): the Euler Weil operator construction probe

- [x] **2LO Euler Weil operator** (experiments/arithmetic_geometric/e2lo_euler_weil_operator.{py,md}, LEARNINGS #70): tries to BUILD the archimedean C_E that 2LN relocated the gap to, via A_E := Omega^{-1} B_E, C_E := A_E (-A_E^2)^{-1/2}. The polar formula WORKS (over F_q, -A_E^2 PD iff t^2 < 4g^2 q, C_E^2 = -I to 7e-15, Q positive iff t^2 < 4g^2 q, 206/206 rows, reduces to negDef_iff_hasseWeil), but K1 fails CONSTRUCTIVELY: the (1,q) bidegree is shared by all curves over F_q while the trace t = q+1-#E is strictly finer (nine traces over F_5 -> nine distinct C_E), so the (1,p) bidegree + archimedean data do NOT determine B_E. The missing piece is the global Frobenius trace = the product-surface / prismatic Poincare-duality assembly. FE/archimedean-only bypass forms for D-H (dead). D-H discipline + 0.617 defect + basis invariance pass. Confirms the 2MM/#68 K1 burden with the precise missing piece named.
### Session 016 (2026-06-05): recent-construction audit for the global signed t-carrying pairing

- [x] **2P+ recent-global-signed-trace-pairing probe** (experiments/arithmetic_geometric/2P_recent_global_signed_trace_pairing_probe.md, LEARNINGS #71): literature-grounded Direction 8 audit (no new finite model, no min-eig panel). After 2LO named the trace t as the missing datum, scores the three strongest recent constructions against the (iii)-polarization column. None supplies the signed t-carrying pairing: Tang prismatic Poincare duality (arXiv:2210.14279, web-confirmed F-gauge perfect duality + trace maps) = PERFECTNESS not the sign; Gurney prismatization over Z (arXiv:2301.12392) = global SUBSTRATE not the cup/sign/t; Connes-Consani 2026 Jacobian (arXiv:2602.15941) = TRACE/realization not a signed product pairing. They are three two-thirds of the SAME construction; the post-2LO gap is unchanged. Landscape doc sharpened with a per-candidate note. D-H discipline as a real-time classifier.
- [ ] **Next (Direction 8, the real gap)**: construct the global Frobenius/Lefschetz signed trace pairing over Spec(Z) that supplies the trace t (= the product surface Spec(Z) x Spec(Z) + Frobenius correspondence Gamma_S). 2LN+2LO+2P+ have reduced the WCart direct-polarization route to exactly this one open datum and confirmed the recent literature stops one third short of it (perfectness / substrate / trace, never the sign carrying t). The local polar C_E formalism is in hand; the global trace pairing is the gap. This is construction work in arithmetic geometry, not a compute task.

### Session 017 (2026-06-05): Lane 2 writeup, Petrov non-semisimplicity branch pruning

- [x] **Lane 2 Petrov non-semisimplicity writeup** (docs/03_research/lane2_petrov_nonsemisimplicity.md): the structural/theory companion to 2LN/2LO/2P+. Petrov non-semisimplicity (arXiv:2302.11389, Annals, web-verified) is NOT fatal to WCart as the substrate, but KILLS the naive Sen-eigenspace polarization branch: a positive HR form from the semisimple part Theta_ss alone forces the nilpotent N=0, and Petrov gives geometric examples with N!=0. The surviving shape is the N-aware monodromy-primitive forms Q_r(x,y)=Q(x,N^r y), built before diagonalization. D-H-aware by TYPE (Petrov's Frobenius-lift obstruction class is undefined for the Euler-product-free D-H), not by sign. M4 branch pruning: either prove N=0 from Euler/Frobenius geometry, or build an N-aware filtered/derived polarization. Cross-linked into 08B task (b), the euler_sen proposal, and the spec_z landscape.

### Session 019 (2026-06-09): first-principles conjecture program

- [x] **Nine-lens conjecture program + adversarial filter** (docs/03_research/first_principles_conjecture_program.md, LEARNINGS #76): nine BUILDER lenses each proposed one precise new RH attack shape; one ADVERSARY pass filtered against the D-H discipline, the four-level framing, K1 non-circularity, and the dead-branch list. Five survive (PURSUE): Lonely Crystal (LCC, singleton positive-crystal cone, priority 8), Eratosthenes Descent (EDC, octave Haynsworth self-reducing certificate, 7), Euler Crystal (ECC, product-formula-derived Berry-Keating boundary, 7), Euler Facet Rigidity (EFR, saturated Weil-cone face = von Mangoldt, 7), Euler Decimation Flow (EDF, zeta as a center fixed point, 7). Two reshapes (Habiro Ladder, Arithmetic Seifert Flow). Unifying read: marginal positivity (#52/#56) = extremality, not obstacle. Two high-value merges noted (LCC+EFR, EDC+EDF).
- [x] **Two kills logged as negative coordinates** (in the dossier + LEARNINGS #76): Semicircular Polarization (free probability) is PROVEN dead by a constant (free-edge / archimedean-curvature ratio ~18 >> 1, so the central inequality is false; salvage collapses to RH itself). Adelic Ferromagnet (Lee-Yang, p-adic Teichmuller spins) is a #42 restatement (a factored product measure cannot reach the continuation Re(s)<1 where the zeros live; Newman's classical no-go is the shadow). Do not re-propose either.
- [x] **Experiment: LCC LP triple** (experiments/positivity/e3x_lonely_crystal_lp.{py,md}, DONE 2026-06-11 with the #82 scope update, LEARNINGS #83): all four runs clean, no kill triggered. (a) blind control PASS every rung (hit fraction 0.999, window masses exactly 2.00, no zero data in the solve); (b) max c_6 = 1.30/1.45/1.31/0.26/0.21 over five rungs: a resolution effect that collapses once tests resolve log 6, NOT a floor (P2a looks LP-visible); (c) D-H INFEASIBLE at every rung, Farkas duals sign-verified plus the continuum-rigorous single-Fejer certificate (source side -> -0.472 toward Omega_DH(0) = -0.6212 < 0): the #76 emptiness lemma with explicit constants, firewall holds; (e) kappa_2 squeezed onto log 2 from both sides ([0.589, 0.776], P2b LP-visible too), while max c_1 plateaus at ~0.03 (P4 is the one LP-invisible obligation, the genuinely archimedean one).
- [x] **Experiment: EDF under-decimation discriminator** (experiments/positivity/e3aa_edf_under_decimation.py, LEARNINGS #77): both sub-tests fired POSITIVE for EDF. Part A (Clause C): zeta matched min-eig stays marginally positive (+0.029/+0.025/+0.020 at t=8/9/10, K=16) while under-decimation (delete primes p>e^{t/2}) collapses it to -46.0/-77.7/-128.7, deepening monotonically, with the negative direction the broadest/lowest-frequency mode (the predicted mechanism: large primes resonate at low frequency near the archimedean trough Omega(0)~-5.4). Part B (companion): the angular-mean-one center-fixed-point identity is exact to ~1e-16 (p=2,3,5); the p=2 dip depth -3.3468 matches exactly; the resonance value -2.98 and a genuine V_8 packet -1.70 both land in [-3.2,-1.0]; D-H has no per-prime block (Euler-specific). HONEST SCOPE: a falsification instrument that came out positive, NOT a proof; D-H also goes negative under decimation (-6.0/-1.8/-2.2), so under-decimation-breaks-positivity is not itself a zeta-vs-D-H separator. EDF stays a live PURSUE direction.
- [ ] **Experiment: ECC spectral-leakage test** (experiments/spectral/e1x_euler_crystal.py): build the truncated prime crystal as a transfer recursion, compute m_X(i) for X = 1e2..1e5. KILLS: convergence to anything other than i*xi'/xi(3/2) ~ 0.0461 i (Montel guarantees convergence to some Herglotz value) is spectral leakage and kills ECC clause E3. Secondary: clarify whether the Krein-de Branges identification runs from the accelerant (K1-clean) or the spectral measure (K1-circular). When it runs, fold in the interval-arithmetic J-contractivity rider (local_compute_workarounds.md idea 5: mpmath.iv enclosure of clause E1 for the first N primes = a machine-checked finite theorem).
- [x] **Workaround idea 4: EDC octave ladder** (experiments/positivity/e3dd_edc_octave_ladder.py, LEARNINGS #78): first-ever test of Eratosthenes Descent. EDC SURVIVED. Clause (i) CONFIRMED (every octave Schur complement PSD for zeta + chi_3 on the non-circular input-side Weil form M=A+P+B, and zeta to 7 octaves on the zero side; the Haynsworth identity In(G_k)=In(G_{k-1})+In(S_k) holds exactly at every octave, cond <= ~90). Clause (ii): NO octave-graded wall (zero-side zeta margins flat in [0.014,0.039] across support b in [1.5,2.8e9], ~1e117 above the e^{-4 pi a} wall; the global marginality +0.007/+0.023 is the telescoped accumulation of healthy per-octave margins, exactly EDC's predicted picture). Caveats: zero-side flatness near-tautological under RH; input side caps at n_oct=5 by the e^{2L} prime-sum ceiling (that ceiling IS the feasibility wall) so clause(ii) there is 4 points; the off-line break (3J octave refinement, #19) did NOT appear at m=3/octave (resolution limit, 3D.3 needed K=100-1000); Epstein untestable input-side (a_1=0). EDC stays a live PURSUE direction. Next: dense-m zero-side run for the break + e2ll FF wind tunnel (idea 1).
- [x] **Workaround idea 1: function-field wind tunnel for composite pinching** (experiments/arithmetic_geometric/e2ll_ff_crystal_cone.py, LEARNINGS #79): tests the program's single named target (the composite-pinching lemma shared by LCC + EFR) where RH is a theorem and everything is exact. PINCHING CONFIRMED on all 7 curves: RH = PSD unitarized-Frobenius-moment Toeplitz (|alpha|=sqrt q to 0); closed-point counts a_d invert exactly to nonnegative integers (Mobius); Prony recovers exactly the rank(R) distinct Frobenius angles and the rank stabilizes (flat extension) => unique representing measure = pinched; supersingular y^2=x^5+x+1/F_5 (rank 2 < 2g=4) is a strengthening witness; anti-curve control fires (off-line modulus => non-PSD). MECHANISM identified: FF pinching IS flat-extension uniqueness of the truncated trig moment problem (Curto-Fialkow); the transfer gap to zeta is the discrete (2g atoms) -> pole-sourced CONTINUOUS archimedean spectrum (outside Curto-Fialkow/Olevskii-Ulanovskii/BRS) = LCC's named open core. No ghost comb over F_q (LCC/EFR cheapest kill ruled out). The named follow-on target is a moment-problem transfer theorem (discrete -> continuous).
- [x] **Zeta-side moment mirror (e2ll/#79 follow-on)** (experiments/positivity/e3gg_zeta_moment_mirror.py, LEARNINGS #80): ran e2ll's flat-extension machinery on zeta's zeros to exhibit the transfer fail mode. F_q: Frobenius-measure Toeplitz goes flat (rank -> 2g, ghost room -> ~1e-16, |c_k| O(1) atomic) => comb pinned. zeta: zero-measure Toeplitz never flat (rank = m+1, ghost room persists 0.48-0.99 ~ 15 orders of magnitude above the F_q floor, |c_k| ~ O(1/sqrt N) continuous C->I) => moments do NOT pin the comb; alpha-robust. D-H identical (mechanism illustrator, NOT a zeta-vs-D-H detector). The composite-pinching transfer needs a moment-uniqueness theorem for a continuous (pole-sourced archimedean) spectrum + Euler structure, not flat extension = LCC/EFR's named open core, now concrete and quantitative.
- [x] **Lateral import: Bost-Connes ghost-room collapse (the #79/#80 transfer technology exists)** (experiments/arithmetic_geometric/e2nn_bc_ghost_collapse.py, LEARNINGS #81, 2026-06-10): the uniqueness theorem shape #80 demanded (positive functional, continuous spectral component, pinned to a singleton by Euler structure) is PROVEN unconditionally in the BC system (KMS simplex at beta <= 1 = a point, type III_1), with engine = the program's organ pair (pole kills atomic ghosts + prime-rotation density forces uniformity). Finite-window quantification, 6/6 checks: organ-A index collapses by m* <= 5 (Linnik-flavored statistic); organ-B diameter has the phase transition AT the pole (stretched-exp below, log-slow u ~ C/log p_m at beta = 1 exactly, plateau above with ratio 0.9984); both amputations leave ghosts alive; Mertens e^{-gamma} (the #63 constant) is the criticality rate (gamma_est 0.5775 vs 0.5772). K1-clean (no zeros anywhere); not a detector (D-H: mechanism constructionally undefined, #55 firewall class). Technology family: group-measure rigidity (scaling quasi-invariance + ergodicity), NOT flat extension. Lateral scan of 10 fields recorded in docs/03_research/lateral_imports_2026_06.md.
- [x] **BUILDER: the LCC/BC transport** (e2nn/#81 follow-on; DONE 2026-06-11, docs/03_research/lcc_bc_transport.md + experiments/positivity/e3hh_flat_comb_ghost.py, LEARNINGS #82): verdict (B)-with-repair, ADVERSARY UPHELD-WITH-REVISIONS. The literal BC quasi-invariance axiom FAILS sharply (Lemma 1: its solution set is the flat ray c_n = c_1 n^{-1/2}, zeta's own comb, not Lambda); the Mobius-twisted repair (cocycle identity B(pn) - B(n) = log p on the integrated comb) is exactly the von Mangoldt crystal (Lemma 2, machine-checked, Lean-ready); and the ADVERSARY extracted two unconditional theorems: every log-crystal has Chebyshev slope exactly 1 (P1 closed), and the flat comb is infeasible for every c_1 > 0 (kill switch A2 defused; the first unconditional ghost-family kill inside the zeta cone, mechanism = pole pins scale + archimedean trough rejects). The "transport conjecture" itself is #76 clause (i) renamed (vacuity verdict accepted); the open core is unchanged and isolated as P2a (b_6 = 0) with one proven rejection-mechanism instance to imitate. New discipline rule: the cocycle/scaling formalism is L-blind, so any T2/pinching proof must consume the pole/Omega data or it is structurally wrong.

### Session 2026-06-11 follow-ons (from #82)

- [x] **e3x LP, reduced scope**: EXECUTED same day (see the LCC LP triple item above, LEARNINGS #83).
- [x] **e3x stress round (ADVERSARY cases A1-A4)** (experiments/positivity/e3x2_stress_round.py + appendix in the e3x dossier, DONE 2026-06-11, LEARNINGS #84): every reported ceiling was a tolerance number (c_6 ~ 3 eps, c_1 ~ eps/2 across a 5x tolerance range and two families). A1: rung-6 collapse STOPS at Q-matched tolerance (0.2162 vs 0.2133). A2: Hann family gives -34% (no family-independent ceiling level) and D-H phase-1 is FEASIBLE under pure signed tests: the firewall lives in the ghat >= 0 one-triangle certificate, NOT the LP (new discipline rule: every LP family must contain positivity-capable tests). A3: ghost hunt fails constructively (best c_6 >= 0.2 crystal eats 93% of slack, dies under rung-6 completion by 5 orders); LCC kill NOT armed. A4: the c_1 plateau was the frozen grid step (P4 demoted to tolerance-bound, undecided). Survivor ranking: P2b strongest (kappa_2 -> [0.659, 0.727] around log 2), P2a qualitatively LP-visible (intercept ~0.01-0.02), P4 undecided. Rung-7 protocol recorded in the appendix (h ~ L^{-3/2}, comb-edge trim, ghat >= 0 gate).
- [ ] **e3x rung 7 (only with the corrected protocol)**: hold Q fixed via h ~ L^{-3/2}, report gain-normalized c/eps + the zero-tolerance intercept, trim the comb edge (log n <= L - 1), shrink the non-Q tolerance terms (~0.005 = the next floor), include a ghat >= 0 subfamily in any alternative test family. Open question it would answer: is the c_6 intercept (~0.01-0.02) zero or a genuine positive floor?
- [x] **P0/P3 literature pass** (docs/03_research/kms_weights_p0_p3_survey.md, DONE 2026-06-11, LEARNINGS #85): P0 CLOSED (Christensen arXiv:2005.01792, weights = scalar x states on unital algebras, + BC 1995; stronger: Neshveyev-Stammeier arXiv:1912.03141 Thm 4.5 gives a ONE-POINT KMS simplex for the ambient T(N^x) at every beta > 0, Prop 3.8 independently re-derives #82 Lemma 1). P3 CLOSED NEGATIVELY: no etale groupoid has the log-crystals as cocycle-quasi-invariant data ((EF) is an inhomogeneous, Fourier-nonlocal SLICE of a homogeneous orbit-local cone); the "find the LCC groupoid" search is killed as a category error. Corrections: Laca-Raeburn T(N x N^x) has NO KMS states at beta < 1; small-beta richness is Laca-Schulz 2025 (arXiv:2503.03033). Net: the C*-layer delivers exactly flat-ray uniqueness (excluded by e3hh); rigidity is wholly an EF-positivity problem.
- [x] **BUILDER B1 (the constructive residue of #85)** (docs/03_research/b1_glog_rigidity.md, DONE same day, LEARNINGS #86): PROVEN in full, five elementary steps (twist e^{beta x}, (QI) becomes invariance, log 2/log 3 irrational gives density, vague continuity upgrades dense to full invariance, Haar uniqueness). The (QI)-solutions form the single non-normalizable ray e^{-beta x} dx: the weights exemplar on a non-compact unit space. Organ accounting: rotation density does ALL the work, the pole does NONE; two primes suffice. Third independent derivation of the flat ray (with #82 Lemma 1 and Neshveyev-Stammeier Thm 4.5). Closes the cone leg; the EF slice (P2a/P2b/P4) is the entire remaining battlefield. New Lean target V4.
- [x] **Lean (VERIFIER): V1-V3** (lean/ZetaRH/CrystalCocycle.lean, DONE 2026-06-12, LEARNINGS #87): all three PROVED sorry-free, build green (3146 jobs), axioms clean ([propext, Classical.choice, Quot.sound], no sorryAx). V1 `increments_eq_log_iff_eq_vonMangoldt` (both directions), V2 `flat_ray_of_quasiInvariance` + prime-generated form, V3 `divisorSum_le_divisorSum_mul_of_nonneg`. Candidate Mathlib upstream: the unbundled divisor-sum determination lemma `eq_of_divisorSum_eq`.
- [ ] **Lean (VERIFIER) remaining: V4 + the unconditional pair**: V4 = the B1 G_log rigidity lemma (feasibility note in CrystalCocycle.lean: dense_or_cyclic + withDensity + Haar uniqueness exist; missing assembly = the vague-continuity upgrade); then the slope-1 lemma and the flat-comb exclusion as the first FiniteCertificates-style EF-positivity theorems.
- [ ] **Workaround slate: remaining six follow-ons** (docs/03_research/local_compute_workarounds.md, 2026-06-09): four workaround axes (exact-arithmetic proof objects; the F_q wind tunnel; integer invariants; certificate mining instead of value mining). Top three in order: (1) e3dd EDC octave ladder (first-ever EDC test; the clause-(ii) regression separates exponential-in-level from exponential-in-support = the #52 wall, deciding the certificate advantage in one run); (2) e2ll FF crystal cone (composite pinching tested over F_q where RH is a theorem and the face structure is exactly enumerable; a ghost comb there kills LCC+EFR rigidity outright); (3) e3bb/e3cc exact-rational c_6 dual + ghost-crystal search (the zeta-side pinching attack whose output is a rational certificate, not a margin). Riders: e3ee decimation bifurcation (quantifies #77 into a (t,theta) curve + tests the 0.617 clause-D exponent), e3ff two-prime exact identity (rung one of the EDF exactness lemma), certificate PSLQ miner, Lean FiniteCertificates export. All freeze-compliant by construction.

### Pre-existing experiments

- [x] **experiments/multifractal/** — E0 benchmarks, E1 zeta MFDFA, E2 FHK max fit, E3 psi MFDFA

## Publications (registry: PUBLICATIONS.md)

Each item is a `P#` discovery from [`PUBLICATIONS.md`](PUBLICATIONS.md). Run the evaluation gate there before promoting a new finding.

- [ ] **P1 + P2 (Mathlib PRs, READY): submit `riemannZeta_conj` and the digamma identities.** Bodies in hand (`lean/upstream/`); only the human GitHub/CLA/fork/rebase steps remain. Submit digamma first per `lean/upstream/README.md`. (Owen-gated; this repo cannot open PRs.)
- [x] **P5 lit-check DONE (2026-06-16): 1D headline pre-empted** (Mossinghoff-Trudgian 1410.3926 / 2212.06867 / Optimal Cosine Polynomials 2411.01385). Reframed to the negative closure of the higher-dimensional/SDP/SOS escape routes (4E.3 line-restriction lemma); dropped to DEVELOPING. Draft skeleton: `publications/P5_zero_free_ceiling.md`.
- [ ] **P5 decision: fold into P4's Architecture-4 section, or develop the standalone note?** Recommendation is fold-into-P4 (the residue is narrow). Owen-gated. Standalone only if an expert confirms the multivariate/SDP/SOS negative closure is genuinely absent from the literature.
- [x] **P3 lit-check DONE (2026-06-16): thesis is canonical Selberg-class folklore** (Bombieri Clay, Conrey Notices AMS 2003). Dropped to DEVELOPING; residue = the quantitative instrument (Schur counting law + Epstein). Recommended home: fold into P4.
- [ ] **Portfolio decision (Owen-gated): the standalone-publishable set is {P1, P2 Mathlib, P6 formalization}; P3/P5/P7/P8 fold into the P4 survey.** Confirm this shape before investing drafting effort.
- [ ] **P8 (analytic note): lit-check the stealth-window quantification vs Connes arXiv:2602.04022** to isolate the genuinely-new delta (exp(-4πx) rate + the D-H-aware defect).
- [x] **P4 outline DONE (2026-06-16): `publications/P4_survey_outline.md`.** Three pillars (all-roads realization / Spec(Z) scorecard / marginal positivity); bundles P3, P5, P7, P8 as evidence. Distinctive thesis = the convergence claim operationalized by the scorecard.
- [x] **P4 survey lit-check DONE (2026-06-16): operationalization is PROVISIONALLY distinctive; convergence claim is folklore** (corrected per adversary HIGH-1). A deep read of Connes 2602.04022 found the scorecard + D-H discipline + convergence-as-organizing-principle absent there, but the bare convergence claim is known folklore (the project's own spine calls it the arithmetic Hodge standard conjecture). P4 held to the same standard as P3: novelty = the operationalization, not the thesis.
- [x] **P4 first prose pass DONE (2026-06-16): `publications/P4_survey_draft.md`** (gate-free sections §1-3, §5-6 + conclusion; §4 scorecard is a stub).
- [x] **P4 §5 numbers verified (2026-06-16)** against `soft_detector_wall.md` (55/69/123→0.33 ~370x; ε~e^{-4πx}, float64 by x≈3, ~1e-71 at x=13; D(γ)=|1-2β|, 0.617 spike at γ≈85.7, e^γ≈1e37). Draft sharpened with the exact figures. A final raw-output check vs e3v/e3y is still prudent pre-submission.
- [ ] **P4 next: an expert reader for the scorecard's (iii) polarization column**, then write §4 + a full revision. Sequence after P1/P2 ship.
- [x] **P6 lit-check DONE (2026-06-16): CONFIRMED viable** (not pre-empted). Hasse-Weil bound is absent from every proof assistant + an explicit Mathlib future goal; FLT project does not target it. The one research/formalization candidate that survived its lit-check intact.
- [x] **P6 path (b) scoped DONE (2026-06-16): `publications/P6_hasse_bound_scope.md`.** Key finding: general Stepanov-Bombieri needs Riemann-Roch (Mathlib lacks it), but the elliptic-curve specialization dodges the wall via explicit Weierstrass $R_n$ ($\dim R_n \ge n$ elementary). Milestone plan M-b1.1..M-b1.5.
- [x] **P6 M-b1.3 probe DONE (2026-06-16, vs Mathlib v4.30 source): no cheap path.** Mathlib has NO curve divisor theory (no Weil/Cartier divisor, degree, Picard, Riemann-Roch; the EC dev avoids divisors). M-b1.3 needs either curve divisor theory (blocked) or an elementary resultant build (weeks). Both P6 paths are multi-month.
- [ ] **P6 decision (Owen-gated): ship the path-(a) conditional reduction now (done, modest), and/or budget an unconditional Hasse bound as a real multi-month project** (route (b1)(ii): the resultant API is present in Mathlib v4.30, so the residual is multiplicity bookkeeping at infinity/branch points, not the resultant; or path (a) coordinated with FLT's Tate-module infrastructure).

### Adversary review follow-ups (publications/ADVERSARY_REVIEW.md, 2026-06-16)

All findings folded into the dossiers/drafts. Remaining repo-hygiene + research actions:

- [x] **(HIGH-2) DONE (2026-06-16): LEARNINGS.md #21 collision disambiguated + zero_free/README refreshed.** Added a numbering banner to the Session-003 cluster (the `**Finding #N**` labels are session-local, collide with the canonical `### N.` headers; cite by experiment ID). Added 4E.8 (SOS) + 4E.9 (Heath-Brown SDP) sections to `experiments/zero_free/README.md` and corrected the stale "4E.8 is open" line to "the LP/SDP/SOS family is fully closed."
- [ ] **(HIGH-1) P4: do the Deninger + one prismatic/THH survey read** before any "confirmed distinctive" language; the convergence framing's obvious unchecked sources.
- [x] **(LOW-8) DONE (2026-06-16): P8 rate-vs-Connes check.** The e^{-4πx} rate is DOUBLY prior: classical prolate asymptotics (Fuchs 1964, Slepian 1965, Widom: 1-λ~e^{-πs}) + Connes' Figure 1 (1-χ₂~e^{-4π e^L}, his "near radical"; prolate operator is Slepian-Pollak-Landau). The scalar defect D(γ)=|1-2β| is trivial (project's own #61/#63 demotion). P8's only residue = the cancellation anatomy (e3y) + D-H-awareness. Verdict: NO standalone novelty, fold into P4 Pillar 3 with citations. Human to pin Connes' exact figure number (arXiv-HTML fetch couldn't re-surface it).
- [ ] **(MED-5) Apply the new K1 circularity gate question retroactively** to any future candidate; P4 and P8 are already flagged circularity-adjacent.

## Open — experimental

- [~] **POST-BREADTH-PROGRAM PIVOT (2026-06-25, LEARNINGS #119-#122):** the Breadth Program ([`docs/03_research/breadth_program.md`](docs/03_research/breadth_program.md)) ran two aimed sweeps and CONVERGED — it produced a near necessary-and-sufficient M4 polarity FINGERPRINT (contingent + complex-root + line-axis + output-indefinite-with-sign-flip + prohibitive-on-a-fixed-locus = a polarization) + a ~13-screen disqualifier battery ([`breadth_corpus.py`](experiments/lemma_db/breadth_corpus.py)), and showed the fixed-indefinite-form space outside arithmetic geometry is mapped and insufficient. The engine's `aim()` now reports CONVERGED. Caveat (#122): the fingerprint is genus-1-faithful (the 2×2 shadow hides the real M4 difficulty). NEXT (two options the engine names): (a) the **M4 construction** at HIGHER RANK — the 09A AHK case where P6 is NOT Hasse-elementary, or the Faltings-Hriljac + Γ_S / lever-B faces; (b) the **`R3_5.lean` VERIFIER target** — a discrete-vs-continuous spectrum predicate separating the Selberg operator-exists case from the zeta scattering-resonance case (from #121, needs the elan/lake toolchain + an adversary check of the de-Branges=#43 link).
- [x] **The higher-rank AHK object built (option (a) first step, 2026-06-25, LEARNINGS #123, `e2xx_higher_rank_rosati.{py,md}`):** built and identified the #122 caveat's named-but-unbuilt object — the primitive intersection form on the Frobenius *powers* IS (up to a positive-diagonal congruence) the trigonometric moment matrix $G_m=[c_{|j-k|}]$, $c_n=(q^n{+}1{-}\#C(\mathbb{F}_{q^n}))/q^{n/2}$, with RH-for-the-curve $\Leftrightarrow$ all $G_m$ PSD (Carathéodory-Toeplitz). $m{=}1$ reproduces e2uu exactly. Adversary-corrected (PASS-WITH-CORRECTIONS, two headlines withdrawn). Sharpens P3 (carry $t \to$ carry $P(T)=t_1..t_g$) and P6 (binary determinant $\to$ $2g$-dim moment-positivity). P6/M4 over $\mathbb{Z}$ UNTOUCHED — the genuine difficulty is *proving* the moment-positivity, non-elementary already at genus 2. 09A Section 6C. **The open kernel now: can an AHK-machinable lattice's degree map carry $P(T)$ while keeping P1+P4? = the higher-rank P3 over $\mathbb{Z}$.**
- [x] **Both faces of P6 probed (2026-06-26, LEARNINGS #124/#125, `e2yy`/`e2zz`, adversary-corrected):** the user's next-steps pick (option 1 + option C). **AHK face (#124, e2yy):** the higher-rank P3 over $\mathbb{Z}$ walls because a matroid Chow ring is purely **even/Tate (no $H^1$)** — its graded-ring endomorphisms are similitudes of the Lorentzian Chow product (roots of unity + real boosts + the scalar at the supersingular boundary), but the interior $\sqrt q$-Frobenius is a rotation preserving the *definite* form, not the Chow product. So "carry $P(T)$" = "supply the $H^1$ the Tate ring lacks." (Genus-1 operator-level reframing of #105+#40+#119 + the scaling-only-boundary wrinkle; "higher-rank" label withdrawn by the adversary.) **Arakelov face (#125, e2zz):** e2xx is the *finite model* of the archimedean Weil/Li positivity; Faltings-Hriljac is unconditionally definite (can't flip) = wrong polarity; and the marginal-positivity wall (#18/#19) **is** the infinite-support analogue of the $\mathbb{F}_q$ boundary kernel (adversary inverted a wrong-axis first pass: grow the test-dimension and the archimedean min eig collapses to 0; truncated spectrum → exact kernel for $K>n$). $\Gamma_S$ = infinite-dim → asymptotic boundary; M4 = the missing certificate. **Both faces wall at the predicted place; reframings, not P6 advances. P6/M4 untouched. NEXT: P6 itself — the $H^1$/odd-cohomology Frobenius the Tate-type lattice lacks (AHK face), or the certificate that the infinite marginal balance is conditional (Arakelov face); plus Faltings-Hriljac + $\Gamma_S$ global assembly, lever B, the R3_5.lean VERIFIER target.**
- [x] **Chase the H^1 (2026-06-26, LEARNINGS #126, `e2ac`, adversary-corrected):** the prismatic/Sen/Deninger world (the home of odd cohomology over $\mathbb{Z}$) carries odd operators — Sen $\Theta$ $\{-n\}$, Frobenius $F$ $\{\log p\}$, the matroid grading — but they are **weight/orbit operators (real lines)**, not the **circle Frobenius** ($|\alpha|=\sqrt q$) the e2xx moment object needs; being non-$q$-symmetric (and the gradings contain 0) they cannot even *enter* the moment object (a category mismatch). Adversary correction: don't flatten #124 and #44 — the matroid has no realization, the prismatic candidate **realizes $\zeta$ and lacks only the signature = the live M4 target**. So the H^1 chase **confirms the live M4 signature gap from the odd side**, not a new place. A synthesis (mostly #44+#119+#42+#30 restated), not a new theorem. M3/#25 untouched.
- [x] **A GENUINE M4 CONSTRUCTION ATTEMPT (2026-06-26, LEARNINGS #128) -- a full-effort attempt at RH itself, three independent fronts, all wall, no proof.** At the user's direction, three BUILDER fronts attacked M4 directly: Weil-form analytic domination, moment-matrix Gram factorization, the Hilbert-Polya operator. **All three wall on the SAME gap** (the Euler product at Re(s)>1 = a trace; the zeros at Re(s)=1/2 = the signature; crossing = the missing Weil cohomology = M4; #42/#25); D-H firewall held throughout. **Three verified residues:** (1) the per-prime Poisson/Herglotz completion (real Euler positivity, wrong-sign + lossy) + the correction that A_arch+B_pole is NOT PSD ("archimedean dominates" is false; positivity is a three-way cancellation = the root of #18/#19); (2) **the Li-Hankel is non-PSD even for zeta** (`e3s_li_hankel_dissolves.py`; the Li sequence is log-concave not log-convex, so the e2xx moment-MATRIX dissolves over Z -- only termwise $\lambda_n\ge0$ survives, the #122 caveat one level up); (3) the PT/$\eta>0$ compression (RH $\iff$ a positive metric $\eta>0$ exists; the Connes operator is self-adjoint with continuous spectrum, zeros = resonances not eigenvalues). **No proof (expected for RH); the value is the 3-front convergence (the gap is one object) + the residues. M4 untouched.** NEXT (still): the R3_5.lean VERIFIER target (now with three concrete predicates from the attempt -- discrete-vs-continuous spectrum, PT/$\eta>0$, the Li-Hankel no-go); or the genuinely open construction (the Euler-side polarization $\eta>0$ / the continuation across Re(s)=1/2 / the signed-pairing assembly).
- [x] **Faltings-Hriljac + $\Gamma_S$ certificate (the Arakelov face, 2026-06-26, LEARNINGS #127, `e2ad`, adversary-corrected):** attempt to make the certificate precise via the e2xx moment lens. Adversary **withdrew** the two distinctive claims — "FH = the $(0,0)$ entry" (type mismatch: FH is an $r\times r$ PD regulator, the analogue of the whole normalized matrix) and "$\Gamma_S$ assembles the FH height" (misattributed: $\Lambda(E,s)$ joins finite+archimedean = the **Frobenius** side; height→$L$ is BSD at $s=1$, not the $\Gamma$-factor). **Survives:** a correct per-prime $a_p$ computation (389a1, Hasse, 11 scales = the #25 bidegree) + one weak-form #125 refinement (in the normalized moment matrix the diagonal is the unconditional **norm** and the RH flip is the **off-diagonal** Frobenius coupling, so FH unconditionally PD = the right shape for a norm, not "the wrong shape"). A re-cataloging of #25/#44/#125/#30; **the weakest of the four probes**. M4/#25 untouched. **Methodological: the 2026-06-26 probes degraded in novelty (wrinkle → inverted → restatement → re-catalog); the genuinely new ground was e2xx (#123) + e2yy's similitude/Tate near-theorem. More all-roads re-cataloging is not the leverage; the leverage is the M4 CONSTRUCTION itself (the signed-pairing assembly / the odd-cohomology Frobenius) or the Lean VERIFIER target.**
- [x] **Milestone M2.5 (done): recompute A_arch from the bilinear Bombieri physical-space integral** (e2v_rosati_balance_M2_5.py, LEARNINGS #33). Ported 3F's validated archimedean integral into the matrix as a cross-b bilinear block; VALIDATED by T_max-convergence (residual vs the zero-side Gram -> 0, so #32's contamination was the zero-side truncation, not the block); corrected the diagnostic to min eig(A_arch + P_fin + B_pole) (the pencil "balance" wrongly presumed A_arch PD). RESULT: the non-circular Weil form for zeta is POSITIVE, min eig = +0.035 (stable +0.03..+0.05 across K=5..8), computed from the Gamma factor + primes with no zeros. Necessary-not-sufficient (finite-K truncation of the target positivity).
- [x] **Milestone M2.6 (done): general-mu archimedean Bombieri kernel fixed + validated** (e2w_rosati_fourway_M2_6.py, LEARNINGS #34). The constant is C_mu = log pi - psi((1+mu)/2) (the M2.5 control bug was psi(1/2+mu), coincides only at mu=0); verified C_0 = log4pi+gamma, C_1 = log pi+gamma; D-H block residual dropped 5.58 -> 0.12 (validated). BUT the four-way verdict FAILS to separate: min eig(M) = +0.035 (zeta, RH, correct) and +0.094 (D-H, RH-fails, WRONG SIGN). D-H's off-line obstruction (gamma ~ 85.7, ~2.6% of the raw spectrum) sits below the reconstruction-residual floor at reachable b -> the truncated non-circular form is blind to it (the stealth window #18/#19/#27, on the Rosati side). The raw min-eig is necessary-not-sufficient AND non-discriminating.
- [~] **Milestone M3 (must be ANALYTIC, per M2.6): prove the archimedean-dominates-prime domination engaging the EXACT off-line structure** (the Euler product / Gamma-factor cancellation that distinguishes zeta from D-H below the marginal-positivity floor). PARTIAL (e3m_analytic_domination.py): the NUMERICAL half is done. Splitting P_fin into prime-power P_pp + composite P_comp and deleting P_comp gives M_euler = A_arch + P_pp + B_pole, which separates all three controls correctly (zeta +0.035, D-H -0.929, Epstein +0.676) and passes the D-H discipline where the full M did not. NOT done: the analytic domination PROOF. For a non-Euler L the deletion of P_comp is imposed by hand and equals the Euler-product assumption; that it is geometrically forced (P_comp = 0 in absolute prismatic cohomology, so M_euler is the canonical trace and M_euler >= 0 <=> RH) is the Direction 8B/8C conjecture, still open.
- [x] **Stress-test the M_euler discriminator** (e3m2_stress_test.py, LEARNINGS #35). PROBE A (false negative) DEFEATED: independent Euler products chi3, chi4 (RH true) stay positive (+0.067, +0.065) with P_comp = 0 to machine precision, so A_arch-dominates-primes is not zeta-specific. PROBE B (false positive) STRUCTURALLY UNAVAILABLE: every Epstein off-line control is non-principal (a >= 2 => a_1 = r_Q(1) = 0 => von-Mangoldt split undefined), so M_euler's off-line discipline still rests on Davenport-Heilbronn ALONE.
- [x] **Build a SECOND integer-supported (a_1 != 0) off-line control** (e3m3_second_offline.py, LEARNINGS #36). Constructed a Davenport-Heilbronn-type function at conductor 7 (order-6 character, kappa pinned numerically, FE verified to ~1e-51, off-line zero verified at rho = 1.0616 + 28.443 i). M_euler = -0.840 < 0 on it (correct). Constructor validated: DHTypeL(5) reproduces the classical DH kappa (conjugate branch). Removes the single-EXAMPLE fragility of #35; both witnesses are still DH-type (single-family), and Epstein remains untestable (a_1 = 0).
- [x] **Stress-test the GEOMETRIC M3 conjecture (P_comp = 0 geometrically)** (LEARNINGS #37). Result: the conjecture is VACUOUS for zeta (P_comp = 0 is the Euler product, a theorem, so M = M_euler trivially) and INAPPLICABLE to D-H (no Euler product => no surface, so M_euler is the trace of no object). "Prove P_comp = 0 geometrically" is struck from the Direction 8 ladder; M3 did not advance the geometric/signature program. The real gap (build Gamma_S / the surface for zeta + prove the cup-product signature) is unchanged from before M3.
- [ ] **Product Surface Intersection Signature**: Geometricize the M3 trace-side positivity bound (M_euler) as a Hodge index signature on the product surface Spec(Z) x Spec(Z), via Bhatt-Lurie's WCart or Connes-Consani's N^2-hat.
  - [x] **First probe (2CC, LEARNINGS #40, ADVERSARY-checked):** the Connes-Consani square's natural mixed-volume form is Lorentzian by Alexandrov-Fenchel (a Hodge-index signature FOR FREE), but is the characteristic-1 SHADOW -- arithmetic-blind, it loses the Frobenius trace t (Delta.Gamma = p-1, not q+1-t), so its (1,3) signature is RH-agnostic. Reconciles with "no signed pairing exists": the convex-geometry shadow exists; the arithmetic pairing carrying t does not. Realizes attack angle 4.A on the specific surface and shows why it is insufficient. (experiments/arithmetic_geometric/e2cc_tropical_shadow.{py,md})
  - [ ] **The arithmetic q-lift (the sharpened gap):** deform the mixed-volume form (or build a Weil cohomology of the C-C square) whose Delta.Gamma is the point count q+1-t, restoring the trace. The (1,p) bidegree (2Q) is the locus where the lift must inject t.
    - [x] **Probe (b): Id_eps self-intersection vs von Mangoldt (2CC.2, LEARNINGS #41).** The C-C self-composition Psi(lambda)oPsi(lambda^-1)=Id_eps (Thm 7.7) carries -zeta'/zeta: its tangent is the number/scaling operator = the Deninger-Hesselholt flow generator Theta, and -d/ds log det_inf(s-Theta) = -zeta'/zeta. Refines 2CC (diagonal self-intersection trace-rich vs off-diagonal frozen). BUT the trace, not the signature; q-lift gap unchanged. Connects C-C <-> 2R <-> Deninger/Hesselholt. (experiments/arithmetic_geometric/e2cc2_ideps_vonmangoldt.{py,md})
    - [~] **Probe (a) ATTEMPTED (2CC.3, LEARNINGS #42): the q-lift decomposes; formal + per-prime crossed, global = the gap.** Un-idempotent-ization is formal (soft-max restores subtraction; 2CC idempotency obstruction removable). Per-prime lift = the known finite-q FF Hodge index (2G, carries t). The GLOBAL half is the gap: the zeros live in the analytic continuation Re(s)<1, invisible to the local Euler/orbit data (converges only Re(s)>1; on the critical line the partial Euler product does not converge). Realizing the continuation as a signed pairing on the H^1 = the missing Weil cohomology (M3/#25), unchanged. NOT crossed -- localized. (experiments/arithmetic_geometric/e2cc3_q_lift_attempt.{py,md})
    - [x] **2DB.1: chart the de Branges / Conrey-Li per-zero cross-term Q(ρ) = -Re{ξ'(ρ)ξ(1+ρ)} — a NEGATIVE coordinate, the exact CONVERSE of probe (a)/2CC.3 (LEARNINGS #43).** de Branges already BUILT a signed pairing realizing the continuation (the H(E) reproducing kernel, structure function E(z)=ξ(1-iz), HB symmetry = the functional equation), and it DOES see the global zeros — but Conrey-Li (arXiv:math/9812166) proved its positivity is strictly-stronger-than-RH and FAILS for zeta. Reproduced their anchor Q(ρ_34) = -5.389e-69 to 12 sig figs; among the first 50 zeta zeros only k=34 is negative; double-archimedean suppression slope -(π/2)/ln10 = -0.682 (sharper than #38's -(π/4)). K2-null / RH-agnostic (D-H on-line zeros also give sporadic Q<0; the off-line obstruction is double-suppressed to |Q|~1e-56, below #38's 1e-29 floor) = the THIRD soft detector after #38/#39. Sharpens the target below: the M3 signed pairing must be RH-EQUIVALENT (a global sum like λ_n), NOT the pointwise Hermite-Biehler cross-term. Does NOT advance the M3 gap itself. (experiments/arithmetic_geometric/e2db_debranges_crossterm.{py,md})
    - [x] **2PR.1: locate the archimedean carrier on the leading prismatic candidate (Bhatt-Lurie WCart) — a structural coordinate (LEARNINGS #44).** WCart carries TWO operators: the Frobenius F (spectrum {log p}) → the finite Euler factors (#26/#41), and the Sen Θ (Hodge-Tate weights {−n}) → the archimedean DIVISOR √(2π)/Γ(s) via Lerch (verified ratio 1.0). So prismatic cohomology supplies BOTH trace-halves of completed zeta as regularized determinants. Real-but-only-heuristic (spectral coincidence; Deninger 1992 / Serre 1970), supplies the trivial-zero divisor NOT the analytic Γ_R / π-conductor, RH-agnostic (D-H shares the Γ-factor; K2 lives on F not Θ), and BLIND to the non-trivial zeros (|det_∞(ρ−Θ_Sen)|=4.39e9≠0). Answers the "archimedean is a separate gluing problem" caveat only WEAKLY (the divisor is a trace; the polarization is not built). Sharpens "all roads to the signature" from the WCart side; M3 gap unchanged. (experiments/arithmetic_geometric/e2pr_sen_archimedean.{py,md})
    - [ ] **The remaining live route (post-probe-a): the local-to-global Weil cohomology.** Construct (or obstruct) the signed pairing that realizes zeta's analytic continuation (the functional equation / archimedean place, A_arch #34) as a Hodge-index form on the global H^1 of the C-C square -- the object that turns the local prime/orbit data (Re(s)>1, the trace #41) into the global zeros (Re(s)=1/2, the signature). This is M3/#25 = Direction 8's central open step. **Sharpened by 2DB.1/#43: the pairing must be RH-EQUIVALENT (a sum like λ_n), not the pointwise de Branges/Hermite-Biehler cross-term (which is strictly-stronger-than-RH and fails for zeta).**
- [x] **Sub-target 12.4 (Direction 12): the Rodgers-Tao ↔ Weil bridge — RESOLVED, bridge FALSE (LEARNINGS #39).** Equation-level survey (Rodgers-Tao arXiv:1801.05914 + Weil-form decomposition + existing-bridge search): Rodgers-Tao's Λ≥0 functional is the log-gas/Dyson Hamiltonian H=∑log(1/|x_j−x_k|) on the zeros (dH/dt=−4E≤0), RH-AGNOSTIC in sign (E≥0 always), a Level-3 statistical object — NOT the Weil quadratic form. Its arithmetic input (Montgomery pair correlation + CGGGHB small gaps) is consumed as Level-3 statistics, not as Weil positivity. So Λ≥0 = Level-3 half, RH=(Λ≤0) = Level-4 signature; the flow does not short-circuit to the signature. Confirms the four-level framing; re-isolates Direction 8 (the product surface) as the irreducible target.
- [ ] **Sub-target 12.4′ (refined BUILDER residue, low prior): the Gaussian-smoothed explicit formula route.** Rewrite the heat-flow zero-sum H_t as arch(f_t)+prime(f_t) via Balanzario-Cardenas (arXiv:2312.00108), compute dH/dt as an explicit-formula bilinear form, and D-H-test whether it reproduces a *discriminating* sub-block of Bombieri's W(f). Predicted FALSE (the log-gas energy is archimedean-suppressed on the off-line obstruction to ~exp(−(π/4)dγ), #38). The only part of Direction 12 not pre-empted; no "d/dt W(f_t) monotonicity" exists in the literature.
- [ ] **The sharpened AHK BUILDER target (Direction 9A, the AHK-side face of M4; LEARNINGS #97, [09A_ahk_arithmetic_lattice.md](docs/03_research/research_directions/09A_ahk_arithmetic_lattice.md)).** Build a finite graded prime-lattice whose stars are AHK-product-structure products (P1), whose degree map yields q+1−t over Z (P3), with a t-carrying strictly-submodular Lefschetz element (P4) and an indefinite (1,n−1) primitive form (P5); positivity (P6) = M4, left open. Strictly sharper than "build the surface": it asks for the lattice + Lefschetz element, not the variety. Milestones 9A.1-9A.5; the K3 function-field shadow (9A.3) is the same-day kill. Produced by the 2026-06-14 breadth-over-proof-engines sweep (five from-below engines killed, AHK the one retained K1-clean positive coordinate; CKS killed as transport).
  - [x] **Apparatus: record the Boucksom-Jonsson NA-Monge-Ampere / K-stability kill** in experiments/lemma_db/transfer_search.py CORPUS (role='killed', score 1, + guard test test_killed_node_demoted; test_transfer_search.py 6/6) so future sweeps neither lose it nor re-propose it as live.
- [x] **Optimize RH for an AI to attack: the value-signal program (LEARNINGS #98, [optimizing_rh_for_ai.md](docs/03_research/optimizing_rh_for_ai.md)).** Binding constraint = no gradient at M4; optimize by manufacturing gradients + shrinking the kernel.
  - [x] **(A) the proven-case shadow battery** ([experiments/lemma_db/shadow_battery.py](experiments/lemma_db/shadow_battery.py) + test_shadow_battery.py 6/6): five proven-case checkpoints (CP-fq/CP-hodge/CP-ahk/CP-fh/CP-af) scored by coverage with the D-H firewall; the positive mirror of oracle.py. "No gradient" -> "k of 5 + firewall"; discriminates genuine/convex-only/arithmetic-blind/too-strong/forgery.
  - [x] **(C) the regime-two frame-audit** (11-agent workflow): fifth all-roads convergence; P5 isolated; M4 property graph = two cheap edges around the Euler-product vertex P4.
  - [~] **(B) Lean as the value function (SORRY-FREE conditional; #106):** [lean/ZetaRH/FunctionFieldRH.lean](lean/ZetaRH/FunctionFieldRH.lean) is now FULLY SORRY-FREE -- the eigenvalue-extraction link + the assembly are proved (wiring keystone #2G-1), and the geometric Castelnuovo-Severi step is carried as the explicit EllipticFrobeniusData.hodge_index field (fixing a latent false-vacuous sorry). A sorry-free CONDITIONAL theorem: given Castelnuovo-Severi for a genus-1 curve, |alpha|=sqrt q. ROADMAP to make it unconditional (discharge #FF-geom via elementary Hasse, deg quadratic form): [lever_b_function_field_plan.md](docs/03_research/lever_b_function_field_plan.md), milestones M-0..M-4. Prerequisite for any verifier-trained generate-loop (lever 5).
- [x] **The P3-P4 von Neumann probe (#99, e2oo): the live frame-audit edge is CLOSED, and it spawned the modular polarization carrier program.** A faithful trace gives only a positive-DEFINITE, D-H-blind form, and the arithmetic algebra is type III_1 (no faithful trace), so P3-P4 stays CORRELATED; the polarization is steered onto the type III MODULAR structure. The program: [modular_polarization_carrier.md](docs/03_research/modular_polarization_carrier.md).
  - [x] **MC.1 (#100, e2pp):** the modular structure supplies the weight ladder log Delta + the FE duality J Delta J = Delta^{-1} a trace lacks.
  - [x] **MC.2 (#101, e2qq):** it FACTORS the gap (C_E and the monodromy N are extra data beyond log Delta; the discriminator is carries-t, not definite-vs-indefinite).
  - [x] **MC.3 (#102, e2rr):** the finite-prime modular (Bost-Connes) structure is euler-gated (K2 by construction; zeta's comb >=0, D-H's negative at n=3).
  - [ ] **MC.4 (#103, e2ss) = M4:** smallest-case attempt is K1-CIRCULAR (the BC/KMS carrier lives at Re(s)>1, blind to the off-line obstruction in 1/2<Re(s)<1). OPEN residual = the per-block COUPLING (the global Frobenius/Lefschetz signed-trace pairing = the product-surface / Poincare assembly) that forces |t_p|<2 sqrt p from the modular flow, sourced not propagated (#97). Construction-grade = M4.
- [x] **The coupling's four faces (#104, e2tt):** AT-1 closed -- the coupling cannot be sourced from the t-blind modular carrier; it is ONE object with FOUR open faces, all = M4 (modular C_E-cup / FF cup 2G-theorem / AHK 09A / Faltings-Hriljac+Gamma_S). The modular/AHK/product-surface threads are four windows on the same missing coupling.
- [x] **The AHK lattice 9A.1-9A.3 (#105, e2uu):** the bare combinatorial lattice (Boolean on {2,3}) gives P1/P2/P4/P5 (P5 free+t-blind, #48); the SINGLE gap is P3 (the t-carrying degree map). BUILDER target narrows 6->1; 09A sharpened (P5 demoted, P3 elevated).
- [x] **M-0 (lever B roadmap) DONE:** the discriminant bridge lemma proved sorry-free in lean/ZetaRH/FunctionFieldRH.lean -- `disc_nonpos_of_posSemidef` (t^2 <= 4q) and `disc_neg_of_posDef` (strict t^2 < 4q), the mirror of keystone #2G-1; the bridge from "deg >= 0 for isogenies" to the Hasse bound. Build green.
  - [x] **M-1 algebraic core DONE (#107, #FF-M1):** lean/ZetaRH/IsogenyDegree.lean -- `degForm` + the Hasse bridge `disc_nonpos_of_int_nonneg` (deg >= 0 on the lattice => t^2 <= 4q, integer->rational by homogeneity, rational->real by density), strict prime boundary `hasse_strict_of_prime` (via `four_mul_prime_not_isSquare`). Wired by `EllipticFrobeniusData.ofDegreeNonneg` + endpoint `functionfield_RH_elliptic_of_degree` (prime field: deg >= 0 => |alpha|^2 = p). Sorry-free, axiom-clean. **Absorbs M-3 (density glue) and M-4 (prime boundary).**
  - [x] **M-1.5 DONE (#107, #FF-M1):** the QuadraticForm contract in lean/ZetaRH/IsogenyDegree.lean -- `quadratic_eq_basis` (a `QuadraticForm ℤ (ℤ×ℤ)` is FORCED to `m²·Q(1,0)+t·mn+n²·Q(0,1)`) + `hasse_of_quadratic` (nonneg quadratic form with Q(1,0)=1 => t²≤4q). Reduces `hdeg` to "deg is a nonneg quadratic form with two pinned values". Sorry-free, axiom-clean.
  - [x] **O3 wiring DONE (#FF-O3):** lean/ZetaRH/LocalFactor.lean -- `localFactor_root_normSq` (RH for the local factor `1-aT+qT²`: a²<4q => roots have |·|²=1/q, via reciprocal α=β⁻¹), `localPolynomial_eq_of_goodReduction` (unfolds Mathlib's `WeierstrassCurve.localPolynomial` to `1-C a·X+C q·X²`), `localPolynomial_root_normSq` (roots of Mathlib's localPolynomial have |·|²=1/q given Hasse). Trace/count now Mathlib-native. Sorry-free, axiom-clean.
  - [x] **Phase A DONE (#FF-M1):** the deg=det route-agnostic bridge in lean/ZetaRH/IsogenyDegree.lean -- `det_smul_one_add_smul` (2×2 mixed-det identity `det(m·1+n·A)=m²+tr(A)·m·n+det(A)·n²`) + `hasse_of_matrix` (`A : Matrix (Fin 2)(Fin 2) ℤ`, `det(m·1+n·A) ≥ 0` => `(tr A)²≤4·det A`). "deg is a quadratic form" = "det of a 2×2 is a quadratic form" (route B's O2, free). Sorry-free, axiom-clean. SHARPENS the residual to "construct the rank-2 integer Frobenius rep A".
  - [ ] **M-1 residual / O1+O2 (scheme-theoretic, the critical path, ~months):** construct the rank-2 integer Frobenius representation A (faithful `End(E) → Matrix (Fin 2)(Fin 2) ℤ` with `det(m·1+n·A) ≥ 0`, det A = q, trace A = a); feed `hasse_of_matrix`. Route B (Tate module: deg=det on T_ℓ, needs E[n]/Weil pairing/T_ℓ) or A′ (dual isogeny: additivity via theorem of the cube, needs isogeny/End/Pic⁰), kept open. Plan: docs/03_research/lever_b_function_field_plan.md. Older framing: -- construct `End(E)` + a degree `QuadraticForm` (nonneg, deg 1 = 1, deg phi_Frob = q). Obligation O2 (deg is a quadratic form = additivity of the dual) is the crux; route A′ (dual isogeny, theorem of the cube) or route B (deg=det on the Tate module, makes O2 free, FLT-aligned). O3 (trace+count) is already in Mathlib's `WeierstrassCurve.localPolynomial`. Strategy + task list in docs/03_research/lever_b_function_field_plan.md. Upstreamable endpoint: Hasse bound for `localPolynomial`. General prime-power M-4 boundary (t²=4q supersingular) is the one remaining wire. **O1 now precisely NAMED in Lean (2026-06-27, #FF-O1):** `FrobeniusMatrixExists p` (the existence of A as a Prop) + `functionfield_RH_of_frobeniusMatrixExists` reduce RH-for-the-curve to it (reduction sorry-free, axiom-clean); the scheme-theoretic construction of A remains the open residual.

- [x] Arch 3B-extension (rigorous): rigorous Li negativity for D-H at n ~ 4 x 10^5 with >=100-digit precision and explicit error/tail bounds (3B.3, experiments/positivity/e3b3_rigorous.py; first rigorously negative at n=336,000). Extended in 3B.4 to a five-L-function discrimination with a second off-line witness (Epstein).
- [x] Arch 3D.3 extension: test prediction "neg count = # off-line pairs" by going to T_max = 500. **CONFIRMED**: D-H has 9 off-line gammas in UHP at T_max=500, n_neg = 9 (MATCH). Rel min eig = -2.597e-02 (stable at ~-2.6% across all tested T_max). Four data points total: T_max ∈ {200, 300, 350, 500}, off-gammas {4, 5, 7, 9}, all match n_neg exactly. Two non-trivial double-increments (+2 at T_max=350 and T_max=500). See experiments/positivity/e3d4_T_max_scaling.* updated.
- [x] Arch 3E: quantify the Li / de Bruijn-Newman relationship (literature). See experiments/positivity/3e_li_de_bruijn_newman.md. Both are positivity reformulations of RH: Li discrete (sequence), dBN continuous ($\Lambda = 0$ ⟺ RH per Rodgers-Tao 2018 + Newman 1976). Current dBN bounds: $0 \le \Lambda \le 0.22$. Both confirm the project's "marginal positivity" picture (LEARNINGS #7): RH is true only at the margin, which points the proof toward the exact structure of zeta rather than soft positivity.
- [x] Arch 1D: Connes adèle-class space literature review (see experiments/spectral/1d_connes_adele_literature.md). Complements 2A_connes_dossier.md from the Arch 1 angle. A rigorous numerical experiment requires research-grade work and the central spectral identification is conjectural.
- [x] Arch 4A + 4C: Vinogradov-Korobov and conditional-improvement landscape (unified literature dossier; see experiments/zero_free/4a_4c_vinogradov_korobov.md). The $2/3$ exponent is a structural ceiling within the V-K recipe: aux ineq saturated by 4B-4E.7, V-MVT at sharp form via Bourgain-Demeter-Guth 2016, explicit formula is identity. No named conditional (density hypothesis, LH, no-Siegel-zero, Heath-Brown, Pintz, Ford, BDG) pushes the exponent. **Arch 4 is a constraint-mapping architecture, not a route to RH**; pushing the exponent requires a fundamentally new input class (Arch 2 arithmetic-geometric exponential-sum machinery á la Deligne, or Arch 1 Connes spectral identification).
- [x] Arch 4E.6: constrained-domain LP — impose P ≥ 0 only on a hypothetical-off-line-zero submanifold (see experiments/zero_free/e4e6_constrained_lp.py + .md). Tested four formulations (K-point, arc-removal, zero-constrained, trick-at-off-line-height). All collapse to Fejér / full-non-negativity ceiling at well-resolved parameters; apparent gains are LP-unbounded artifacts or sparse-sampling artifacts. **4E.3's lemma is robust under naive domain relaxation.** Genuine escape requires Heath-Brown multi-zero coupling (4E.7), Bombieri variational SOS, or polynomial-ideal SOS (4E.8) — qualitatively different machinery beyond LP-over-a-subset.
- [x] Arch 4E.7: multi-zero MT bookkeeping — Heath-Brown-style coupling (see experiments/zero_free/e4e7_multi_zero_lp.py + .md). LP escape from 4E.3 is real at the shape-factor level (λ_{1,1} is 55-137× larger than λ_1² for N ∈ {2,3,4}), but the naive max c_{1,1} objective produces rank-1 (tensor-product) LP optima. A non-trivial multi-zero MT improvement for RH on zeta would require combining this with higher-harmonic structure (4E.2's +25% rank-2 gain) AND explicit Heath-Brown bookkeeping — research-grade.
- [x] Arch 4E.8: polynomial-ideal sum-of-squares (Putinar/Schmüdgen) for the MT figure of merit (experiments/zero_free/e4e8_sos_sdp.py) — SDP saturates Fejér but does not exceed it; closes the LP/SDP family escape. Extended in 4E.9 (Heath-Brown multi-zero MT, Direction 7): multi-zero shape factor also capped by Fejér (ratio <= 1, rank-2).
- [x] Arch 2C: survey state of F_1 / Arakelov-cohomology programs as of 2025 (commit fd020fc; 280-line landscape survey)
- [x] Arch 2D: identify the smallest open conjecture in Deninger's program worth targeting (commit 10c9f05; M3 prismatic foliation hypothesis, now Direction 4)
- [x] Arch 2A R1: sharpen D-H exclusion (constraint xvii) for each of the six scored candidates (see experiments/arithmetic_geometric/2A_R1_DH_exclusion.md — all six pass K2 by construction; K2 safety conditional on Selberg conjecture)
- [x] Arch 2A R2: compute Spec(Z) x_F1 Spec(Z) in Borger / Lorscheid (see experiments/arithmetic_geometric/2A_R2_fiber_product.md — Borger: Spec(W(Z)) via big Witt ring; Lorscheid: blueprint (Z × Z, doubled relations); both 🟡 on (ii), intersection theory still open)
- [x] Arch 2A R2.5: sketch the Λ-blueprint hybrid framework as a research proposal (see experiments/arithmetic_geometric/2A_R2_5_lambda_blueprints.md). Predicted scorecard 8 ✅ / 4 🟡 / 5 ⏳ if developed. Still needs rigorous definition, categorical verification, fiber product computation — thesis-level project.
- [x] Arch 2A R3: Connes-Consani K1 analysis (see experiments/arithmetic_geometric/2A_R3_connes_positivity.md). Three positivity conjectures analyzed; C1 and C2 are RH-equivalent (K1 fail); C3 is K1-uncertain. Connes / Connes-Consani (xi)-(xiii) moves from ❌ to 🟡 in the scorecard.
- [x] Arch 2A R4: explored Borger Frobenius + Connes trace formula hybrid (see experiments/arithmetic_geometric/2A_R4_borger_connes_hybrid.md) — predicted scorecard ~8 ✅ / 5 🟡 / 3 ❌; fails K1 per R3.5; value is infrastructure for the geometric route
- [x] Arch 2A R5: prismatic cohomology analyzed as candidate cohomology theory for Spec(W(ℤ)) (see experiments/arithmetic_geometric/2A_R5_prismatic_cohomology.md). δ-rings ↔ Λ-rings at one prime; predicted to close constraints (iv)-(vii). Still K1-blocked on positivity per R3.5.
- [ ] Arch 2A R3.6.3 (surfaced by R3.6): investigate whether Connes-Consani machinery (arithmetic site, hyperrings, characteristic-one geometry) can serve as INFRASTRUCTURE for the geometric route — even though its positivity formulations fail K1, the topos-theoretic / sheaf-theoretic tools might support intersection theory on a constructed surface
- [x] Arch 2E (numerical probe): Adams-operation spectrum vs zeta zeros — does Borger's psi_p have zeta-zero-like spectrum on concrete Lambda-rings? (see experiments/arithmetic_geometric/2E_adams_spectrum_probe.md — NO: spectra are roots of unity, {0}, or powers of p; confirms R5 framing that cohomology must do the lifting, bare Lambda-structure is too small)

### Session (2026-06-27): the RH toy sandbox + four deferred Lean targets + doc recovery

- [x] **RH toy sandbox** (experiments/toy/, `python -m experiments.toy.play`, tests 6/6): the function-field world (RH = a theorem) as a checkable training ground for the M4 move. Grades a candidate construction on four checks (reproduce-Weil / reject-fakes / K1-clean / D-H-immune); the reference move (the e2xx moment matrix) scores ALL GREEN, two soft candidates correctly FAIL reject-fakes, D-H is unbuildable by type. Selberg / Hilbert-Polya spectral toy (selberg.py): self-adjoint => zeros on the line; break it => off-line. Honest caveat in code + README: the moment-matrix move that is green here DISSOLVES over Z (#128 Front 2), so it trains the MOVE, not the OBSTRUCTION. Manufactures the value signal the M4 blind spot lacks (optimizing_rh_for_ai.md).
- [x] **Lean: VT-NP1** (lean/ZetaRH/VT_NP1.lean, #VT-NP1, sorry-free + axiom-clean): finite Euler-product zero-freeness on Re(s)>0 -- `localFactor_ne_zero` (1-p^{-s} != 0 since ||p^{-s}||=p^{-Re s}<1), `finiteEulerProductInv_ne_zero`, `np1_reduction` (the off-line obstruction lives only in the infinite-product limit; NP-1=NO, e2vv/#128). Analytic companion of #2G-1.
- [x] **Lean: R3.5 GeometricPositivity tightened** (lean/ZetaRH/R3_5.lean, #119, sorry-free + axiom-clean): split into `RealizesSignatureInteger` (the free signature INTEGER, RH-BLIND -- `realizesSignatureInteger_rhBlind` holds for zeta AND D-H) vs `PrimitivePartDefinite` (RH-equivalent); `geometricPositivity_iff_RH` routes through the latter, no theorem sends the integer realization to RH. Blocks the "realizes the integer => proves definiteness" conflation.
- [x] **Lean: toy Lean face** (lean/ZetaRH/ToyModel.lean, #TOY-1, sorry-free + axiom-clean): genus-1 grader correctness BOTH directions -- `toy_reproduce_weil` (t^2<=4q => |alpha|^2=q) and the NEW off-line witness `toy_reject_fake` (4q<t^2 => a Frobenius root off the circle, via the explicit root (t+sqrt(t^2-4q))/2), packaged in `toy_grader_genus1_correct`.
- [x] **Lean: O1 named** (lean/ZetaRH/FunctionFieldRH.lean, #FF-O1): the lever-B existence residual as a precise Prop `FrobeniusMatrixExists p` + `functionfield_RH_of_frobeniusMatrixExists` reducing RH-for-the-curve to it (reduction sorry-free + axiom-clean; the existence left OPEN and named, not an axiom). Full library build green (3744 jobs); all nine new theorems `#print axioms` = `[propext, Classical.choice, Quot.sound]`.
- [x] **Docs:** the lost #128 three-front working reports reconstructed into a tracked dossier (docs/03_research/m4_construction_attempt.md); the #119 four-area exploration sweep folded into docs/03_research/all_roads_to_the_signature.md; lean/README.md VERIFIER target table + Status updated.

### Session (2026-06-27, cont.): the Arakelov-face pivot (#131/#132) + VT1/VT2 in Lean

Executes #129's recorded recommendation (pivot to the Arakelov face after the AHK route closed into R1, #129/#130).

- [x] **Arakelov-face PROBE** (experiments/arithmetic_geometric/2L_arakelov_face_probe.md, #131; 5-dimension, 11-agent adversarially-verified workflow): the Arakelov face is the MIRROR of the AHK failure, not an escape. AHK has the base but cannot source the carrier (matroid Chow rings are Tate) = dies at R1; the Arakelov face HAS a carrier AND a proven per-surface polarization (single-surface Faltings-Hriljac/Yuan-Zhang = per-motive M4, a theorem) but dies at the BASE (no Spec(Z) x Spec(Z) + Gamma_S = PROP-global); M4 is NOT what it lacks (sharpens #130). BSD severance keeps FH off zeta's zeros (height-to-L = L'(E,1), #113). 2026 lit frontier unmoved (Yuan-Zhang/Moriwaki/CGHX fixed-scheme; Bost theta wrong signature class; Connes-Consani Jacobian trace-side). Corpus nodes (Bost theta, Moriwaki, CGHX) added to spec_z_cohomology_landscape; 2K Morishita-2025 attribution fixed; 2P bracket pointer added.
- [x] **e2ae: the deferred omega-bar^2 = 12 h_Fal computation** (experiments/arithmetic_geometric/e2ae_petersson_faltings.py): 2J's flagged-but-uncomputed Arakelov entry, the archimedean self-intersection via the Petersson norm of the weight-12 discriminant, for 37a1/389a1/5077a1. SL2(Z)-invariant to ~1e-50 (with teeth: two wrong normalizations break off-axis); normalization pinned to +6 log(2pi) by two independent routes; LMFDB Faltings-height cross-check on all three. Adversarially re-derived via an independent code path. K1-clean; a self-intersection DIAGONAL, not M4.
- [x] **Arakelov-side M4 CONSTRUCTION ATTEMPT** (experiments/arithmetic_geometric/2M_arakelov_construction_attempt.md, #132; 3 BUILDER fronts, each adversarially attacked): carrier-spectrum-severance (e2ag) / sharp-vs-generic (2M_generic_vs_sharp_localization.md) / (1,p)-bidegree-assembly (e2af), ALL WALL at the SAME R1+M4+PROP-global gap = the first construction-side three-front convergence onto the Arakelov base (parallels #128's analytic convergence). Genuinely-new: Front 1's carrier-class invariance + Front 3's e2af assembly computation. THREE adversary-withdrawn overclaims recorded (Front 1 divergent-ratio = category mismatch; Front 2 "generic-vs-sharp contains the marginal wall" = vacuous; Front 3 "single adelic scalar = P_fin" = mis-identified + "genus-2 carrier" = relabeled g=1 Gram). D-H held by non-mimicry (9/9). A successful dead branch; structural conclusion consolidates the known verdict, no new leverage.
- [x] **Lean: VT1/VT2 formalized** (lean/ZetaRH/ArakelovAssembly.lean, #2M-VT, sorry-free + axiom-clean, full build green 3745 jobs, all #print axioms = [propext, Classical.choice, Quot.sound]): VT1 = `perPrime_negDef_iff_hasse` / `perPrime_negDef_of_hasse` (per-prime Hasse sign, a specialization of `negDef_iff_hasseWeil` at q=p, honestly not new); VT2 (new) = `conj_lowerRight` (the D_s-conjugated lower-right entry = -2g*p*s^2, grounding the diagonal-equalizing condition p*s^2=1), `no_single_normalizing_scale` (no single scale for distinct primes = the #25 (1,p)-bidegree obstruction as an assembly failure), `forced_scale_asymmetry_unbounded`, packaged in `assembly_obstruction`. Does NOT close M4 (the adelic-scalar sign is the open kernel).
- [ ] **Next (do NOT run a fourth Arakelov front or survey):** spend the loop on isolable RH-independent sub-pieces and watch for an external Gamma_S/polarization theorem (maintain spec_z_cohomology_landscape as the watch-list; the 2026 Connes-Consani Jacobian is the closest trace-side datapoint, still no pairing). The open kernel is the SIGN of the single regularized adelic scalar = M4.

## Open — docs and visualizations

- [ ] Convert PDFs to Markdown — all 4 source PDFs (text conversions exist in sources/, refine)
- [ ] **docs/00_intuitive/** — write intuitive-level explanation
- [ ] **docs/01_undergraduate/** — write undergrad-level explanation
- [ ] **docs/implications/** — why RH matters (primes, physics, crypto)
- [ ] manim scene: ZetaSeriesIntro — partial sums of zeta(s), s real
- [ ] manim scene: ComplexPlane — plotting zeta on the complex plane
- [ ] manim scene: AnalyticContinuation — extending beyond Re(s) > 1
- [ ] manim scene: CriticalStrip — the critical strip 0 < Re(s) < 1
- [ ] manim scene: ZerosOnCriticalLine — known non-trivial zeros at Re(s) = 1/2
- [ ] manim scene: PrimeConnectionExplainer — zeta and prime counting function

## ML / formalization backlog

- [ ] Set up zero data pipeline (Odlyzko tables, LMFDB)
- [ ] Build ML experiment framework for zero pattern analysis
- [ ] Implement operator discovery pipeline (parameterized operators + spectral matching)
- [ ] Implement Weil positivity numerical explorer (largely subsumed by Arch 3C, extend with neural-net basis)
- [ ] Li criterion sequence analysis and prediction
- [ ] Robin's inequality adversarial search (colossally abundant numbers)
- [ ] Formalize RH equivalences in Lean 4
- [ ] Add glossary of terms
- [x] Add bibliography / further reading (references/README.md — 25-PDF library indexed by direction; PDFs gitignored)
- [x] Read-through of the reference library: notes for ALL 26 sources + a synthesis index in docs/03_research/reading_notes/. Top findings: Bhatt-Lurie's Cartier-Witt stack WCart ("de Rham relative to F_1" + global Frobenius F + Sen operator Theta) is the strongest candidate for 2K's F_1 base / Gamma_S / Deninger generator; Connes-Consani 2015 already builds the square N^2-hat with Frobenius correspondences Psi(lambda) but idempotent ops => no signed pairing (the Direction-8 gap isolated); Adiprasito-Huh-Katz give a Hodge-index signature with NO underlying variety (attack 4.A); Deninger/Connes confirm the signature (not the trace formula) is the load-bearing step (K1 wall). The literature independently confirms the 2Q/2R reduction and locates the gap identically to 2K. Notes are full-depth (section-by-section technical content; ~6,200 lines total).
- [ ] Link visualizations to corresponding doc sections
- [ ] Add Jupyter notebooks for interactive exploration
- [ ] Explore L-functions generalization
